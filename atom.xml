<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>学海无涯</title>
  
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://yoursite.com/"/>
  <updated>2021-07-14T10:19:10.110Z</updated>
  <id>http://yoursite.com/</id>
  
  <author>
    <name>WSQ</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>MySQL/MySQL日志</title>
    <link href="http://yoursite.com/2021/07/14/MySQL/MySQL%E6%97%A5%E5%BF%97/"/>
    <id>http://yoursite.com/2021/07/14/MySQL/MySQL%E6%97%A5%E5%BF%97/</id>
    <published>2021-07-14T10:15:06.605Z</published>
    <updated>2021-07-14T10:19:10.110Z</updated>
    
    <content type="html"><![CDATA[<h1 id="日志及分类"><a href="#日志及分类" class="headerlink" title="日志及分类"></a>日志及分类</h1><p>日志是数据库的重要组成部分，主要用来记录数据库的运行情况、日常操作和错误信息。</p><p>在 MySQL 中，日志可以分为二进制日志、错误日志、通用查询日志和慢查询日志。对于 MySQL 的管理工作而言，这些日志文件是不可缺少的。分析这些日志，可以帮助我们了解 MySQL 数据库的运行情况、日常操作、错误信息和哪些地方需要进行优化。</p><p>下面简单介绍 MySQL 中 4 种日志文件的作用。<br>二进制日志：该日志文件会以二进制的形式记录数据库的各种操作，但不记录查询语句。<br>错误日志：该日志文件会记录 MySQL 服务器的启动、关闭和运行错误等信息。<br>通用查询日志：该日志记录 MySQL 服务器的启动和关闭信息、客户端的连接信息、更新、查询数据记录的 SQL 语句等。<br>慢查询日志：记录执行事件超过指定时间的操作，通过工具分析慢查询日志可以定位 MySQL 服务器性能瓶颈所在。</p><p>为了维护 MySQL 数据库，经常需要在 MySQL 中进行日志操作，包含日志文件的启动、查看、停止和删除等，这些操作都是数据库管理中最基本、最重要的操作。</p><p>例如，当用户 root 登录到 MySQL 服务器后，就会在日志文件里记录该用户的登录事件、执行操作等信息。当 MySQL 服务器运行时出错，出错信息就会被记录到日志文件里。</p><p>日志操作是数据库维护中最重要的手段之一。如果 MySQL 数据库系统意外停止服务，我们可以通过错误日志查看出现错误的原因。还可以通过二进制日志文件来查看用户分别执行了哪些操作、对数据库文件做了哪些修改。然后，还可以根据二进制日志中的记录来修复数据库。</p><p>在 MySQL 所支持的日志文件里，除了二进制日志文件外，其它日志文件都是文本文件。默认情况下，MySQL 只会启动错误日志文件，而其它日志则需要手动启动。</p><p>使用日志有优点也有缺点。启动日志后，虽然可以对 MySQL 服务器性能进行维护，但是会降低 MySQL 的执行速度。例如，一个查询操作比较频繁的 MySQL 中，记录通用查询日志和慢查询日志要花费很多的时间。</p><p>日志文件还会占用大量的硬盘空间。对于用户量非常大、操作非常频繁的数据库，日志文件需要的存储空间甚至比数据库文件需要的存储空间还要大。因此，是否启动日志，启动什么类型的日志要根据具体的应用来决定。</p><h1 id="错误日志"><a href="#错误日志" class="headerlink" title="错误日志"></a>错误日志</h1><p>错误日志（Error Log）是 MySQL 中最常用的一种日志，主要记录 MySQL 服务器启动和停止过程中的信息、服务器在运行过程中发生的故障和异常情况等。</p><p>作为初学者，要学会利用错误日志来定位问题。下面介绍如何操作查看错误日志。<br>启动和设置错误日志<br>在 MySQL 数据库中，默认开启错误日志功能。一般情况下，错误日志存储在 MySQL 数据库的数据文件夹下，通常名称为 hostname.err。其中，hostname 表示 MySQL 服务器的主机名。</p><p>在 MySQL 配置文件中，错误日志所记录的信息可以通过 log-error 和 log-warnings 来定义，其中，log-err 定义是否启用错误日志功能和错误日志的存储位置，log-warnings 定义是否将警告信息也记录到错误日志中。</p><p>将 log_error 选项加入到 MySQL 配置文件的 [mysqld] 组中，形式如下：<br>[mysqld]<br>log-error=dir/{filename}</p><p>其中，dir 参数指定错误日志的存储路径；filename 参数指定错误日志的文件名；省略参数时文件名默认为主机名，存放在 Data 目录中。</p><p>重启 MySQL 服务后，参数开始生效，可以在指定路径下看到 filename.err 的文件，如果没有指定 filename，那么错误日志将直接默认为 hostname.err。<br>注意：错误日志中记录的并非全是错误信息，例如 MySQL 如何启动 InnoDB 的表空间文件、如何初始化自己的存储引擎等，这些也记录在错误日志文件中。</p><p>查看错误日志<br>错误日志中记录着开启和关闭 MySQL 服务的时间，以及服务运行过程中出现哪些异常等信息。如果 MySQL 服务出现异常，可以到错误日志中查找原因。</p><p>在 MySQL 中，通过 SHOW 命令可以查看错误日志文件所在的目录及文件名信息。<br>mysql&gt; SHOW VARIABLES LIKE ‘log_error’;<br>+—————+—————————————————————-+<br>| Variable_name | Value                                                          |<br>+—————+—————————————————————-+<br>| log_error     | C:\ProgramData\MySQL\MySQL Server 5.7\Data\LAPTOP-UHQ6V8KP.err |<br>+—————+—————————————————————-+<br>1 row in set, 1 warning (0.04 sec)<br>错误日志以文本文件的形式存储，直接使用普通文本工具就可以查看。这里通过记事本打开，从上面可以知道错误日志的文件名。该文件在默认的数据路径“C:\ProgramData\MySQL\MySQL Server 5.7\Data\”下，打开 LAPTOP-UHQ6V8KP.err 文件，部分内容如下：<br>190906 22:06:45 InnoDB: Completed initialization of buffer pool<br>190906 22:06:45 InnoDB: highest supported file format is Barracuda.<br>190906 22:06:45  InnoDB: Waiting for the background threads to start<br>190906 22:06:46 InnoDB: 5.7.29 started; log sequence number 1605345<br>190906 22:06:47 [Note] Server hostname (bind-address): ‘0.0.0.0’; port: 3306<br>190906 22:06:47 [Note]   - ‘0.0.0.0’ resolves to ‘0.0.0.0’;<br>190906 22:06:47 [Note] Server socket created on IP: ‘0.0.0.0’.<br>190906 22:06:47 [Note] Event Scheduler: Loaded 0 events<br>190906 22:06:47 [Note] /usr/sbin/mysqld: ready for connections.<br>Version: ‘5.7.29-log’  socket: ‘/var/lib/mysql/mysql.sock’  port: 3306  MySQL Community Server (GPL)</p><p>以上是错误日志文件的一部分，主要记载了系统的一些运行错误。<br>删除错误日志<br>在 MySQL 中，可以使用 mysqladmin 命令来开启新的错误日志，以保证 MySQL 服务器上的硬盘空间。mysqladmin 命令的语法如下：<br>mysqladmin -uroot -p flush-logs</p><p>执行该命令后，MySQL 服务器首先会自动创建一个新的错误日志，然后将旧的错误日志更名为 filename.err-old。</p><p>MySQL 服务器发生异常时，管理员可以在错误日志中找到发生异常的时间、原因，然后根据这些信息来解决异常。对于很久之前的错误日志，查看的可能性不大，可以直接将这些错误日志删除。</p><h1 id="二进制日志"><a href="#二进制日志" class="headerlink" title="二进制日志"></a>二进制日志</h1><p>二进制日志（Binary Log）也可叫作变更日志（Update Log），是 MySQL 中非常重要的日志。主要用于记录数据库的变化情况，即 SQL 语句的 DDL 和 DML 语句，不包含数据记录查询操作。</p><p>如果 MySQL 数据库意外停止，可以通过二进制日志文件来查看用户执行了哪些操作，对数据库服务器文件做了哪些修改，然后根据二进制日志文件中的记录来恢复数据库服务器。</p><p>默认情况下，二进制日志功能是关闭的。可以通过以下命令查看二进制日志是否开启，命令如下：<br>mysql&gt; SHOW VARIABLES LIKE ‘log_bin’;<br>+—————+——-+<br>| Variable_name | Value |<br>+—————+——-+<br>| log_bin       | OFF   |<br>+—————+——-+<br>1 row in set, 1 warning (0.02 sec)<br>从结果可以看出，二进制日志是关闭的。<br>启动和设置二进制日志<br>在 MySQL 中，可以通过在配置文件中添加 log-bin 选项来开启二进制日志，格式如下：<br>[mysqld]<br>log-bin=dir/[filename]</p><p>其中，dir 参数指定二进制文件的存储路径；filename 参数指定二进制文件的文件名，其形式为 filename.number，number 的形式为 000001、000002 等。</p><p>每次重启 MySQL 服务后，都会生成一个新的二进制日志文件，这些日志文件的文件名中 filename 部分不会改变，number 会不断递增。</p><p>如果没有 dir 和 filename 参数，二进制日志将默认存储在数据库的数据目录下，默认的文件名为 hostname-bin.number，其中 hostname 表示主机名。</p><p>下面在 my.ini 文件的 [mysqld] 组中添加以下语句：<br>log-bin</p><p>重启 MySQL 服务器后，可以在 MySQL 数据库的数据目录下看到 LAPTOP-UHQ6V8KP-bin.000001 这个文件，同时还生成了 LAPTOP-UHQ6V8KP-bin.index 文件。此处，MySQL 服务器的主机名为 LAPTOP-UHQ6V8KP。</p><p>还可以在 my.ini 文件的 [mysqld] 组中进行如下修改。语句如下：<br>log-bin=C:log\mylog</p><p>重启 MySQL 服务后，可以在 C:log 文件夹下看到 mylog.000001 文件和 mylog.index 文件。<br>查看二进制日志</p><ol><li>查看二进制日志文件列表<br>可以使用如下命令查看 MySQL 中有哪些二进制日志文件：<br>mysql&gt; SHOW binary logs;</li></ol><p>+—————————-+———–+<br>| Log_name                   | File_size |<br>+—————————-+———–+<br>| LAPTOP-UHQ6V8KP-bin.000001 |       177 |<br>| LAPTOP-UHQ6V8KP-bin.000002 |       154 |<br>+—————————-+———–+<br>2 rows in set (0.00 sec)<br>2. 查看当前正在写入的二进制日志文件<br>可以使用以下命令查看当前 MySQL 中正在写入的二进制日志文件。<br>mysql&gt; SHOW master status;<br>+—————————-+———-+————–+——————+——————-+<br>| File                       | Position | Binlog_Do_DB | Binlog_Ignore_DB | Executed_Gtid_Set |<br>+—————————-+———-+————–+——————+——————-+<br>| LAPTOP-UHQ6V8KP-bin.000002 |      154 |              |                  |                   |<br>+—————————-+———-+————–+——————+——————-+<br>1 row in set (0.00 sec)<br>3. 查看二进制日志文件内容<br>二进制日志使用二进制格式存储，不能直接打开查看。如果需要查看二进制日志，必须使用 mysqlbinlog 命令。</p><p>mysqlbinlog 命令的语法形式如下：<br>mysqlbinlog filename.number</p><p>mysqlbinlog 命令只在当前文件夹下查找指定的二进制日志，因此需要在二进制日志所在的目录下运行该命令，否则将会找不到指定的二进制日志文件。<br>例 1<br>下面使用 mysqlbinlog 命令，来查看 C:\log 目录下的 mylog.000001 文件，代码执行如下：<br>C:\Users\11645&gt;cd C:\log<br>C:\log&gt;mysqlbinlog mylog.000001<br>/<em>!50530 SET @@SESSION.PSEUDO_SLAVE_MODE=1</em>/;<br>/<em>!50003 SET @OLD_COMPLETION_TYPE=@@COMPLETION_TYPE,COMPLETION_TYPE=0</em>/;<br>DELIMITER /<em>!</em>/;</p><h1 id="at-4"><a href="#at-4" class="headerlink" title="at 4"></a>at 4</h1><p>#200527  9:33:37 server id 1  end_log_pos 123 CRC32 0x69738cfd  Start: binlog v 4, server v 5.7.29-log created 200527  9:33:37 at startup<br>……<br>由于日志内容较长，这里只展示了 mylog 中的部分内容。</p><p>使用 mysqlbinlog 命令时，可以指定二进制文件的存储路径。这样可以确保 mysqlbinlog 命令可以找到二进制文件。上面例子中的命令可以变为如下形式：<br>mysqlbinlog C:\log\mylog.000001</p><p>这样，mysqlbinlog 命令就会到 C:\log 目录下去查找 mylog.000001 文件。如果不指定路径，mysqlbinlog 命令将在当前目录下查找 mylog.000001 文件。</p><p>除了 filename.number 文件，MySQL 还会生成一个名为 filename.index 的文件，这个文件存储着所有二进制日志文件的列表，可以用记事本打开该文件。<br>小技巧：实际工作中，二进制日志文件与数据库的数据文件不放在同一块硬盘上，这样即使数据文件所在的硬盘被破坏，也可以使用另一块硬盘上的二进制日志来恢复数据库文件。两块硬盘同时坏了的可能性要小得多，这样可以保证数据库中数据的安全。</p><p>删除二进制日志<br>二进制日志中记录着大量的信息，如果很长时间不清理二进制日志，将会浪费很多的磁盘空间。删除二进制日志的方法很多，下面介绍几种删除二进制日志的方法。</p><ol><li>删除所有二进制日志<br>使用 RESET MASTER 语句可以删除的所有二进制日志，该语句的形式如下：<br>RESET MASTER;</li></ol><p>登录 MySQL 数据库后，可以执行该语句来删除所有二进制日志。删除所有二进制日志后，MySQL 将会重新创建新的二进制日志，新二进制日志的编号从 000001 开始。<br>2. 根据编号删除二进制日志<br>每个二进制日志文件后面有一个 6 位数的编号，如 000001。使用 PURGE MASTER LOGS TO 语句，可以删除指定二进制日志的编号之前的日志。该语句的基本语法形式如下：<br>PURGE MASTER LOGS TO ‘filename.number’;</p><p>该语句将删除编号小于 filename.number 的所有二进制日志。<br>例 2<br>下面删除 mylog.000004 之前的二进制日志，代码如下：<br>PURGE MASTER LOGS TO ‘mylog.000004’;</p><p>代码执行完后，编号为 000001、000002 和 000003 的二进制日志将被删除。<br>3. 根据创建时间删除二进制日志<br>使用 PURGE MASTER LOGS TO 语句，可以删除指定时间之前创建的二进制日志，该语句的基本语法格式如下：<br>PURGE MASTER LOGS TO ‘yyyy-mm-dd hh:MM:ss’;</p><p>其中，“hh”为 24 制的小时。该语句将删除在指定时间之前创建的所有二进制日志。<br>例 3<br>下面删除 2019-12-20 15:00:00 之前创建的二进制日志，代码如下：<br>PURGE MASTER LOGS TO ‘2019-12-20 15:00:00”;</p><p>代码执行完后，2019-12-20 15:00:00 之前创建的所有二进制日志将被删除。<br>暂时停止二进制日志<br>在配置文件中设置了 log_bin 选项之后，MySQL 服务器将会一直开启二进制日志功能。删除该选项后就可以停止二进制日志功能，如果需要再次启动这个功能，需要重新添加 log_bin 选项。由于这样比较麻烦，所以 MySQL 提供了暂时停止二进制日志功能的语句。</p><p>如果用户不希望自己执行的某些 SQL 语句记录在二进制日志中，可以在执行这些 SQL 语句之前暂停二进制日志功能。</p><p>使用 SET 语句来暂停/开启二进制日志功能，命令如下：<br>SET SQL_LOG_BIN=0/1;</p><p>以上命令中，0 表示暂停二进制日志功能，1 表示开启二进制功能。<br>拓展<br>my.ini 中的 [mysqld] 组下面有几个设置参数是关于二进制日志的：<br>expire_logs_days = 10<br>max_binlog_size = 1​00M</p><p>expire_logs_day 定义了 MySQL 清除过期日志的时间、二进制日志自动删除的天数。默认值为 0，表示“没有自动删除”。当 MySQL 启动或刷新二进制日志时可能删除。<br>max_binlog_size 定义了单个文件的大小限制，如果二进制日志写入的内容大小超出给定值，日志就会发生滚动（关闭当前文件，重新打开一个新的日志文件）。不能将该变量设置为大于 1GB 或小于 4096B（字节），其默认值是 1GB。</p><h1 id="使用二进制日志还原数据库"><a href="#使用二进制日志还原数据库" class="headerlink" title="使用二进制日志还原数据库"></a>使用二进制日志还原数据库</h1><p>二进制日志中记录了用户对数据库更改的所有操作，如 INSERT 语句、UPDATE 语句、CREATE 语句等。如果数据库因为操作不当或其它原因丢失了数据，可以通过二进制日志来查看在一定时间段内用户的操作，结合数据库备份来还原数据库。</p><p>下面将为读者详细介绍使用二进制日志还原数据库的方法。</p><p>数据库遭到意外损坏时，应该先使用最近的备份文件来还原数据库。另外备份之后，数据库可能进行了一些更新，这时可以使用二进制日志来还原。因为二进制日志中存储了更新数据库的语句，如 UPDATE 语句、INSERT 语句等。</p><p>二进制日志还原数据库的命令如下：<br>mysqlbinlog filename.number | mysql -u root -p</p><p>以上命令可以理解成，先使用 mysqlbinlog 命令来读取 filename.number 中的内容，再使用 mysql 命令将这些内容还原到数据库中。</p><p>技巧：二进制日志虽然可以用来还原 MySQL 数据库，但是其占用的磁盘空间也是非常大的。因此，在备份 MySQL 数据库之后，应该删除备份之前的二进制日志。如果备份之后发生异常，造成数据库的数据损失，可以通过备份之后的二进制日志进行还原。</p><p>使用 mysqlbinlog 命令进行还原操作时，必须是编号（number）小的先还原。例如，mylog.000001 必须在 mylog.000002 之前还原。<br>例 1<br>下面使用二进制日志来还原数据库，代码如下：<br>mysqlbinlog mylog.000001 | mysql -u root -p<br>mysqlbinlog mylog.000002 | mysql -u root -p<br>mysqlbinlog mylog.000003 | mysql -u root -p<br>mysqlbinlog mylog.000004 | mysql -u root -p</p><h1 id="通用查询日志"><a href="#通用查询日志" class="headerlink" title="通用查询日志"></a>通用查询日志</h1><p>通用查询日志（General Query Log）用来记录用户的所有操作，包括启动和关闭 MySQL 服务、更新语句和查询语句等。</p><p>默认情况下，通用查询日志功能是关闭的。可以通过以下命令查看通用查询日志是否开启，命令如下：<br>mysql&gt; SHOW VARIABLES LIKE ‘%general%’;<br>+——————+—————————————————————-+<br>| Variable_name    | Value                                                          |<br>+——————+—————————————————————-+<br>| general_log      | OFF                                                            |<br>| general_log_file | C:\ProgramData\MySQL\MySQL Server 5.7\Data\LAPTOP-UHQ6V8KP.log |<br>+——————+—————————————————————-+<br>2 rows in set, 1 warning (0.01 sec)<br>从结果可以看出，通用查询日志是关闭的，general_log_file 变量指定了通用查询日志文件所在的位置。<br>启动和设置通用查询日志<br>在 MySQL 中，可以通过在 MySQL 配置文件添加 log 选项来开启通用查询日志，格式如下：<br>[mysqld]<br>log=dir/filename</p><p>其中，dir 参数指定通用查询日志的存储路径；filename 参数指定日志的文件名。如果不指定存储路径，通用查询日志将默认存储到 MySQL 数据库的数据文件夹下。如果不指定文件名，默认文件名为 hostname.log，其中 hostname 表示主机名。<br>查看通用查询日志<br>如果希望了解用户最近的操作，可以查看通用查询日志。通用查询日志以文本文件的形式存储，可以使用普通文本文件查看该类型日志内容。<br>例 1<br>首先我们查看通用查询日志功能是否是开启状态，然后查询 tb_student 表的记录，SQL 命令和执行过程如下：<br>mysql&gt; SHOW VARIABLES LIKE ‘%general%’;<br>+——————+—————————————————————-+<br>| Variable_name    | Value                                                          |<br>+——————+—————————————————————-+<br>| general_log      | ON                                                             |<br>| general_log_file | C:\ProgramData\MySQL\MySQL Server 5.7\Data\LAPTOP-UHQ6V8KP.log |<br>+——————+—————————————————————-+<br>2 rows in set, 1 warning (0.02 sec)</p><p>mysql&gt; use test;<br>Database changed<br>mysql&gt; SELECT * FROM tb_student;<br>+—-+——–+<br>| id | name   |<br>+—-+——–+<br>|  1 | Java   |<br>|  2 | MySQL  |<br>|  3 | Python |<br>+—-+——–+<br>3 rows in set (0.06 sec)<br>执行成功后，打开通用查询日志，这里日志名称为 LAPTOP-UHQ6V8KP.log，下面是通用查询日志中的部分内容。<br>C:\Program Files\MySQL\MySQL Server 5.7\bin\mysqld.exe, Version: 5.7.29-log (MySQL Community Server (GPL)). started with:<br>TCP Port: 3306, Named Pipe: MySQL<br>Time                 Id Command    Argument<br>2020-05-29T06:43:44.382878Z     7 Quit<br>2020-05-29T06:44:10.001382Z     8 Connect root@localhost on  using SSL/TLS<br>2020-05-29T06:44:10.007532Z     8 Query select @@version_comment limit 1<br>2020-05-29T06:44:11.748179Z     8 Query SHOW VARIABLES LIKE ‘%general%’<br>2020-05-29T06:44:25.487472Z     8 Query SELECT DATABASE()<br>2020-05-29T06:44:25.487748Z     8 Init DB test<br>2020-05-29T06:44:35.390523Z     8 Query SELECT * FROM tb_student</p><p>可以看出，该日志非常清晰地记录了客户端的所有行为。<br>停止通用查询日志<br>通用查询日志启动后，可以通过两种方法停止该日志。一种是将 MySQL 配置文件中的相关配置注释掉，然后重启服务器，来停止通用查询日志。具体内容如下：<br>[mysqld]<br>#log=dir\filename</p><p>上述方法需要重启 MySQL 服务器，这在某些场景，比如有业务量访问的情况下是不允许的，这时可以通过另一种方法来动态地控制通用查询日志的开启和关闭。</p><p>设置 MySQL 的环境变量 general_log 为关闭状态可以停止该日志，示例如下：<br>mysql&gt; SET GLOBAL general_log=off;<br>Query OK, 0 rows affected (0.00 sec)</p><p>mysql&gt; SHOW VARIABLES LIKE ‘%general_log%’ \G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>Variable_name: general_log<br>        Value: OFF<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 2. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>Variable_name: general_log_file<br>        Value: C:\ProgramData\MySQL\MySQL Server 5.7\Data\LAPTOP-UHQ6V8KP.log<br>2 rows in set, 1 warning (0.01 sec)<br>删除通用查询日志<br>在 MySQL 中，可以使用 mysqladmin 命令来开启新的通用查询日志。新的通用查询日志会直接覆盖旧的查询日志，不需要再手动删除了。</p><p>mysqladmin 命令的语法如下：<br>mysqladmin -uroot -p flush-logs</p><p>需要注意的是，如果希望备份旧的通用查询日志，必须先将旧的日志文件拷贝出来或者改名。然后，再执行 mysqladmin 命令。</p><p>除了上述方法之外，还可以手工删除通用查询日志。删除之后需要重新启动 MySQL 服务。重启之后就会生成新的通用查询日志。如果希望备份旧的日志文件，可以将旧的日志文件改名，然后重启 MySQL 服务。</p><p>由于通用查询日志会记录用户的所有操作，如果数据库的使用非常频繁，通用查询日志将会占用非常大的磁盘空间，对系统性能影响较大。一般情况下，数据管理员可以删除很长时间之前的通用查询日志或关闭此日志，以保证 MySQL 服务器上的硬盘空间。</p><h1 id="慢查询日志"><a href="#慢查询日志" class="headerlink" title="慢查询日志"></a>慢查询日志</h1><p>慢查询日志用来记录在 MySQL 中执行时间超过指定时间的查询语句。通过慢查询日志，可以查找出哪些查询语句的执行效率低，以便进行优化。</p><p>通俗的说，MySQL 慢查询日志是排查问题的 SQL 语句，以及检查当前 MySQL 性能的一个重要功能。如果不是调优需要，一般不建议启动该参数，因为开启慢查询日志会或多或少带来一定的性能影响。</p><p>默认情况下，慢查询日志功能是关闭的。可以通过以下命令查看是否开启慢查询日志功能。命令和执行过程如下：<br>mysql&gt; SHOW VARIABLES LIKE ‘slow_query%’;<br>+———————+———————————————————————+<br>| Variable_name       | Value                                                               |<br>+———————+———————————————————————+<br>| slow_query_log      | OFF                                                                 |<br>| slow_query_log_file | C:\ProgramData\MySQL\MySQL Server 5.7\Data\LAPTOP-UHQ6V8KP-slow.log |<br>+———————+———————————————————————+<br>2 rows in set, 1 warning (0.02 sec)</p><p>mysql&gt; SHOW VARIABLES LIKE ‘long_query_time’;<br>+—————–+———–+<br>| Variable_name   | Value     |<br>+—————–+———–+<br>| long_query_time | 10.000000 |<br>+—————–+———–+<br>1 row in set, 1 warning (0.01 sec)<br>参数说明如下：<br>slow_query_log：慢查询开启状态<br>slow_query_log_file：慢查询日志存放的位置（一般设置为 MySQL 的数据存放目录）<br>long_query_time：查询超过多少秒才记录<br>启动和设置慢查询日志<br>可以通过 log-slow-queries 选项开启慢查询日志。通过 long_query_time 选项来设置时间值，时间以秒为单位。如果查询时间超过了这个时间值，这个查询语句将被记录到慢查询日志。</p><p>将 log_slow_queries 选项和 long_query_time 选项加入到配置文件的 [mysqld] 组中。格式如下：<br>[mysqld]<br>log-slow-queries=dir\filename<br>long_query_time=n</p><p>其中：<br>dir 参数指定慢查询日志的存储路径，如果不指定存储路径，慢查询日志将默认存储到 MySQL 数据库的数据文件夹下。<br>filename 参数指定日志的文件名，生成日志文件的完整名称为 filename-slow.log。 如果不指定文件名，默认文件名为 hostname-slow.log，hostname 是 MySQL 服务器的主机名。<br>“n”参数是设定的时间值，该值的单位是秒。如果不设置 long_query_time 选项，默认时间为 10 秒。</p><p>还可以通过以下命令启动慢查询日志、设置指定时间：<br>SET GLOBAL slow_query_log=ON/OFF;<br>SET GLOBAL long_query_time=n;</p><p>查看慢查询日志<br>如果你想查看哪些查询语句的执行效率低，可以从慢查询日志中获得信息。和错误日志、查询日志一样，慢查询日志也是以文本文件的形式存储的，可以使用普通的文本文件查看工具来查看。<br>例 1<br>开启 MySQL 慢查询日志功能，并设置时间，命令和执行过程如下：<br>mysql&gt; SET GLOBAL slow_query_log=ON;<br>Query OK, 0 rows affected (0.05 sec)</p><p>mysql&gt; SET GLOBAL long_query_time=0.001;<br>Query OK, 0 rows affected (0.00 sec)<br>由于需要演示这里我们将时间设置为了 0.001 秒，执行时间超过 0.001 秒的 SQL 语句将被记录到日志中。</p><p>查询 tb_student 表中的数据，SQL 语句和执行过程如下：<br>mysql&gt; USE test;<br>Database changed<br>mysql&gt; SELECT * FROM tb_student;<br>+—-+——–+<br>| id | name   |<br>+—-+——–+<br>|  1 | Java   |<br>|  2 | MySQL  |<br>|  3 | Python |<br>+—-+——–+<br>3 rows in set (0.08 sec)<br>相应的，慢查询日志的部分内容如下：</p><h1 id="Time-2020-06-01T01-59-18-368780Z"><a href="#Time-2020-06-01T01-59-18-368780Z" class="headerlink" title="Time: 2020-06-01T01:59:18.368780Z"></a>Time: 2020-06-01T01:59:18.368780Z</h1><h1 id="User-Host-root-root-localhost-1-Id-3"><a href="#User-Host-root-root-localhost-1-Id-3" class="headerlink" title="User@Host: root[root] @ localhost [::1]  Id:     3"></a>User@Host: root[root] @ localhost [::1]  Id:     3</h1><h1 id="Query-time-0-006281-Lock-time-0-000755-Rows-sent-2-Rows-examined-1034"><a href="#Query-time-0-006281-Lock-time-0-000755-Rows-sent-2-Rows-examined-1034" class="headerlink" title="Query_time: 0.006281  Lock_time: 0.000755 Rows_sent: 2  Rows_examined: 1034"></a>Query_time: 0.006281  Lock_time: 0.000755 Rows_sent: 2  Rows_examined: 1034</h1><p>use test;<br>SET timestamp=1590976758;<br>SHOW VARIABLES LIKE ‘slow_query%’;<br>删除慢查询日志<br>慢查询日志的删除方法与通用日志的删除方法是一样的。可以使用 mysqladmin 命令来删除。也可以使用手工方式来删除。mysqladmin 命令的语法如下：<br>mysqladmin -uroot -p flush-logs</p><p>执行该命令后，命令行会提示输入密码。输入正确密码后，将执行删除操作。新的慢查询日志会直接覆盖旧的查询日志，不需要再手动删除。</p><p>数据库管理员也可以手工删除慢查询日志，删除之后需要重新启动 MySQL 服务。<br>注意：通用查询日志和慢查询日志都是使用这个命令，使用时一定要注意，一旦执行这个命令，通用查询日志和慢查询日志都只存在新的日志文件中。如果需要备份旧的慢查询日志文件，必须先将旧的日志改名，然后重启 MySQL 服务或执行 mysqladmin 命令。</p><h1 id="设置日志输出方式"><a href="#设置日志输出方式" class="headerlink" title="设置日志输出方式"></a>设置日志输出方式</h1><p>MySQL 的查询日志支持写入到文件或写入数据表两种输出形式。启用了普通查询日志或慢查询日志功能后，可以选择让服务器把日志写入到日志文件、mysql 数据库中的日志表、或者同时写到这两个地方。</p><p>可以通过以下命令查看日志输出类型：<br>mysql&gt; SHOW VARIABLES LIKE ‘%log_out%’;<br>+—————+——-+<br>| Variable_name | Value |<br>+—————+——-+<br>| log_output    | FILE  |<br>+—————+——-+<br>1 row in set, 1 warning (0.08 sec)<br>结果显示，日志输出类型为 FILE。</p><p>要想在运行时更改日志输出目标，可以在启动服务器时，设置全局系统变量 log_output 的值，格式如下：<br>SET GLOBAL log_output=’value’;</p><p>value 的值可以是：<br>FILE：表示把日志写入到文件。如果未指定 log_output 的值，默认为 FILE。<br>TABLE：表示把日志写入到 mysql 数据库的 slow_log 或 general_log 表中。<br>MySQL 可以同时支持 2 种日志存储方式，配置的时候以逗号隔开，即 log_output=’FILE,TABLE’。</p><p>需要注意的是，系统变量 log_output 只确定了日志使用什么输出目标，并不会启用日志功能。</p><p>相对于写入到文件，日志写入到数据表中要耗费更多的系统资源。因此，对于需要启用查询日志，又需要获得更高的系统性能，建议优先选择将日志写入到文件。</p><p>日志表（slow_log 或 general_log）中的内容只允许查看，不允许修改，除非服务器自己进行更改。因此，你只能对日志表使用 SELECT 语句，不能使用 INSERT、DELETE 或 UPDATE 语句。不过，可以使用 TRUNCATE TABLE 语句来清空日志表。<br>例 1<br>首先设置日志写入到日志表，然后查询 test 数据库中 tb_student 数据表的记录，并查看 mysql 数据库中的 slow_log 表中的记录。SQL 语句和运行结果如下：<br>mysql&gt; SET GLOBAL log_output=’TABLE’;<br>Query OK, 0 rows affected (0.00 sec)</p><p>mysql&gt; SHOW VARIABLES LIKE ‘%log_out%’;<br>+—————+——-+<br>| Variable_name | Value |<br>+—————+——-+<br>| log_output    | TABLE |<br>+—————+——-+<br>1 row in set, 1 warning (0.01 sec)</p><p>mysql&gt; use test;<br>Database changed<br>mysql&gt; SELECT * FROM tb_student;<br>+—-+——–+<br>| id | name   |<br>+—-+——–+<br>|  1 | Java   |<br>|  2 | MySQL  |<br>|  3 | Python |<br>+—-+——–+<br>3 rows in set (0.00 sec)</p><p>mysql&gt; SELECT * FROM mysql.slow_log \G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>    start_time: 2020-06-04 15:25:40.030420<br>     user_host: root[root] @ localhost [::1]<br>    query_time: 00:00:00.058887<br>     lock_time: 00:00:00.000000<br>     rows_sent: 0<br>rows_examined: 0<br>            db: test<br>last_insert_id: 0<br>     insert_id: 0<br>     server_id: 1<br>      sql_text: TRUNCATE TABLE mysql.slow_log<br>     thread_id: 11<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 2. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>    start_time: 2020-06-04 15:25:52.229014<br>     user_host: root[root] @ localhost [::1]<br>    query_time: 00:00:00.000339<br>     lock_time: 00:00:00.000000<br>     rows_sent: 1<br>rows_examined: 0<br>            db: test<br>last_insert_id: 0<br>     insert_id: 0<br>     server_id: 1<br>      sql_text: Init DB<br>     thread_id: 11<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 3. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>    start_time: 2020-06-04 15:26:00.867649<br>     user_host: root[root] @ localhost [::1]<br>    query_time: 00:00:00.000379<br>     lock_time: 00:00:00.000115<br>     rows_sent: 7<br>rows_examined: 7<br>            db: test<br>last_insert_id: 0<br>     insert_id: 0<br>     server_id: 1<br>      sql_text: SELECT * FROM tb_student<br>     thread_id: 11<br>3 rows in set (0.00 sec)<br>结果显示，超过慢查询日志指定时间的 SQL 语句都写入到了 mysql 数据库的 slow_log 表中</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;日志及分类&quot;&gt;&lt;a href=&quot;#日志及分类&quot; class=&quot;headerlink&quot; title=&quot;日志及分类&quot;&gt;&lt;/a&gt;日志及分类&lt;/h1&gt;&lt;p&gt;日志是数据库的重要组成部分，主要用来记录数据库的运行情况、日常操作和错误信息。&lt;/p&gt;
&lt;p&gt;在 MySQL 中，日
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>MySQL/MySQL存储过程</title>
    <link href="http://yoursite.com/2021/07/14/MySQL/MySQL%E5%AD%98%E5%82%A8%E8%BF%87%E7%A8%8B/"/>
    <id>http://yoursite.com/2021/07/14/MySQL/MySQL%E5%AD%98%E5%82%A8%E8%BF%87%E7%A8%8B/</id>
    <published>2021-07-14T06:58:51.468Z</published>
    <updated>2021-07-14T07:25:29.453Z</updated>
    
    <content type="html"><![CDATA[<h1 id="存储过程是什么？"><a href="#存储过程是什么？" class="headerlink" title="存储过程是什么？"></a>存储过程是什么？</h1><p>我们前面所学习的 MySQL 语句都是针对一个表或几个表的单条 SQL 语句，但是在数据库的实际操作中，经常会有需要多条 SQL 语句处理多个表才能完成的操作。</p><p>例如，为了确认学生能否毕业，需要同时查询学生档案表、成绩表和综合表，此时就需要使用多条 SQL 语句来针对这几个数据表完成处理要求。</p><p>存储过程是一组为了完成特定功能的 SQL 语句集合。使用存储过程的目的是将常用或复杂的工作预先用 SQL 语句写好并用一个指定名称存储起来，这个过程经编译和优化后存储在数据库服务器中，因此称为存储过程。当以后需要数据库提供与已定义好的存储过程的功能相同的服务时，只需调用“CALL存储过程名字”即可自动完成。</p><p>常用操作数据库的 SQL 语句在执行的时候需要先编译，然后执行。存储过程则采用另一种方式来执行 SQL 语句。</p><p>一个存储过程是一个可编程的函数，它在数据库中创建并保存，一般由 SQL 语句和一些特殊的控制结构组成。当希望在不同的应用程序或平台上执行相同的特定功能时，存储过程尤为合适。</p><p>MySQL 5.0 版本以前并不支持存储过程，这使 MySQL 在应用上大打折扣。MySQL 从 5.0 版本开始支持存储过程，既提高了数据库的处理速度，同时也提高了数据库编程的灵活性</p><p>存储过程是数据库中的一个重要功能，存储过程可以用来转换数据、数据迁移、制作报表，它类似于编程语言，一次执行成功，就可以随时被调用，完成指定的功能操作。</p><p>使用存储过程不仅可以提高数据库的访问效率，同时也可以提高数据库使用的安全性。</p><p>对于调用者来说，存储过程封装了 SQL 语句，调用者无需考虑逻辑功能的具体实现过程。只是简单调用即可，它可以被 Java 和 C# 等编程语言调用。</p><p>编写存储过程对开发者要求稍微高一些，但这并不影响存储过程的普遍使用，因为存储过程有如下优点：</p><p>1) 封装性<br>通常完成一个逻辑功能需要多条 SQL 语句，而且各个语句之间很可能传递参数，所以，编写逻辑功能相对来说稍微复杂些，而存储过程可以把这些 SQL 语句包含到一个独立的单元中，使外界看不到复杂的 SQL 语句，只需要简单调用即可达到目的。并且数据库专业人员可以随时对存储过程进行修改，而不会影响到调用它的应用程序源代码。<br>2) 可增强 SQL 语句的功能和灵活性<br>存储过程可以用流程控制语句编写，有很强的灵活性，可以完成复杂的判断和较复杂的运算。<br>3) 可减少网络流量<br>由于存储过程是在服务器端运行的，且执行速度快，因此当客户计算机上调用该存储过程时，网络中传送的只是该调用语句，从而可降低网络负载。<br>4) 高性能<br>当存储过程被成功编译后，就存储在数据库服务器里了，以后客户端可以直接调用，这样所有的 SQL 语句将从服务器执行，从而提高性能。但需要说明的是，存储过程不是越多越好，过多的使用存储过程反而影响系统性能。<br>5) 提高数据库的安全性和数据的完整性<br>存储过程提高安全性的一个方案就是把它作为中间组件，存储过程里可以对某些表做相关操作，然后存储过程作为接口提供给外部程序。这样，外部程序无法直接操作数据库表，只能通过存储过程来操作对应的表，因此在一定程度上，安全性是可以得到提高的。<br>6) 使数据独立<br>数据的独立可以达到解耦的效果，也就是说，程序可以调用存储过程，来替代执行多条的 SQL 语句。这种情况下，存储过程把数据同用户隔离开来，优点就是当数据表的结构改变时，调用表不用修改程序，只需要数据库管理者重新编写存储过程即可。</p><h1 id="创建存储过程"><a href="#创建存储过程" class="headerlink" title="创建存储过程"></a>创建存储过程</h1><p>MySQL 存储过程是一些 SQL 语句的集合，比如有时候我们可能需要一大串的 SQL 语句，或者说在编写 SQL 语句的过程中需要设置一些变量的值，这个时候我们就完全有必要编写一个存储过程。</p><p>编写存储过程并不是件简单的事情，但是使用存储过程可以简化操作，且减少冗余的操作步骤，同时，还可以减少操作过程中的失误，提高效率，因此应该尽可能的学会使用存储过程。</p><p>下面主要介绍如何创建存储过程。</p><p>可以使用 CREATE PROCEDURE 语句创建存储过程，语法格式如下：<br>CREATE PROCEDURE &lt;过程名&gt; ( [过程参数[,…] ] ) &lt;过程体&gt;<br>[过程参数[,…] ] 格式<br>[ IN | OUT | INOUT ] &lt;参数名&gt; &lt;类型&gt;</p><p>语法说明如下：</p><p>1) 过程名<br>存储过程的名称，默认在当前数据库中创建。若需要在特定数据库中创建存储过程，则要在名称前面加上数据库的名称，即 db_name.sp_name。</p><p>需要注意的是，名称应当尽量避免选取与 MySQL 内置函数相同的名称，否则会发生错误。<br>2) 过程参数<br>存储过程的参数列表。其中，&lt;参数名&gt;为参数名，&lt;类型&gt;为参数的类型（可以是任何有效的 MySQL 数据类型）。当有多个参数时，参数列表中彼此间用逗号分隔。存储过程可以没有参数（此时存储过程的名称后仍需加上一对括号），也可以有 1 个或多个参数。</p><p>MySQL 存储过程支持三种类型的参数，即输入参数、输出参数和输入/输出参数，分别用 IN、OUT 和 INOUT 三个关键字标识。其中，输入参数可以传递给一个存储过程，输出参数用于存储过程需要返回一个操作结果的情形，而输入/输出参数既可以充当输入参数也可以充当输出参数。</p><p>需要注意的是，参数的取名不要与数据表的列名相同，否则尽管不会返回出错信息，但是存储过程的 SQL 语句会将参数名看作列名，从而引发不可预知的结果。<br>3) 过程体<br>存储过程的主体部分，也称为存储过程体，包含在过程调用的时候必须执行的 SQL 语句。这个部分以关键字 BEGIN 开始，以关键字 END 结束。若存储过程体中只有一条 SQL 语句，则可以省略 BEGIN-END 标志。</p><p>在存储过程的创建中，经常会用到一个十分重要的 MySQL 命令，即 DELIMITER 命令，特别是对于通过命令行的方式来操作 MySQL 数据库的使用者，更是要学会使用该命令。</p><p>在 MySQL 中，服务器处理 SQL 语句默认是以分号作为语句结束标志的。然而，在创建存储过程时，存储过程体可能包含有多条 SQL 语句，这些 SQL 语句如果仍以分号作为语句结束符，那么 MySQL 服务器在处理时会以遇到的第一条 SQL 语句结尾处的分号作为整个程序的结束符，而不再去处理存储过程体中后面的 SQL 语句，这样显然不行。</p><p>为解决以上问题，通常使用 DELIMITER 命令将结束命令修改为其他字符。语法格式如下：<br>DELIMITER $$</p><p>语法说明如下：<br>$$ 是用户定义的结束符，通常这个符号可以是一些特殊的符号，如两个“?”或两个“￥”等。<br>当使用 DELIMITER 命令时，应该避免使用反斜杠“\”字符，因为它是 MySQL 的转义字符。</p><p>在 MySQL 命令行客户端输入如下 SQL 语句。<br>mysql &gt; DELIMITER ??</p><p>成功执行这条 SQL 语句后，任何命令、语句或程序的结束标志就换为两个问号“??”了。</p><p>若希望换回默认的分号“;”作为结束标志，则在 MySQL 命令行客户端输入下列语句即可：<br>mysql &gt; DELIMITER ;</p><p>注意：DELIMITER 和分号“;”之间一定要有一个空格。在创建存储过程时，必须具有 CREATE ROUTINE 权限。<br>例 1<br>创建名称为 ShowStuScore 的存储过程，存储过程的作用是从学生成绩信息表中查询学生的成绩信息，输入的 SQL 语句和执行过程如下所示。<br>mysql&gt; DELIMITER //<br>mysql&gt; CREATE PROCEDURE ShowStuScore()<br>    -&gt; BEGIN<br>    -&gt; SELECT * FROM tb_students_score;<br>    -&gt; END //<br>Query OK， 0 rows affected (0.09 sec)<br>结果显示 ShowStuScore 存储过程已经创建成功。<br>例 2<br>创建名称为 GetScoreByStu 的存储过程，输入参数是学生姓名。存储过程的作用是通过输入的学生姓名从学生成绩信息表中查询指定学生的成绩信息，输入的 SQL 语句和执行过程如下所示。<br>mysql&gt; DELIMITER //<br>mysql&gt; CREATE PROCEDURE GetScoreByStu<br>    -&gt; (IN name VARCHAR(30))<br>    -&gt; BEGIN<br>    -&gt; SELECT student_score FROM tb_students_score<br>    -&gt; WHERE student_name=name;<br>    -&gt; END //<br>Query OK, 0 rows affected (0.01 sec)</p><h1 id="查看存储过程"><a href="#查看存储过程" class="headerlink" title="查看存储过程"></a>查看存储过程</h1><p>创建好存储过程后，用户可以通过 SHOW ATATUS 语句来查看存储过程的状态，也可以通过 SHOW CREATE 语句来查看存储过程的定义。本节主要讲解查看存储过程的状态和定义的方法。<br>查看存储过程的状态<br>MySQL 中可以通过 SHOW STATUS 语句查看存储过程的状态，其基本语法形式如下：<br>SHOW PROCEDURE STATUS LIKE 存储过程名;</p><p>LIKE 存储过程名用来匹配存储过程的名称，LIKE 不能省略。<br>实例 1<br>创建数据表 studentinfo，SQL 语句如下：<br>CREATE TABLE <code>studentinfo</code> (<br>    <code>ID</code> int(11) NOT NULL,<br>    <code>NAME</code> varchar(20) DEFAULT NULL,<br>    <code>SCORE</code> decimal(4,2) DEFAULT NULL,<br>    <code>SUBJECT</code> varchar(20) DEFAULT NULL,<br>    <code>TEACHER</code> varchar(20) DEFAULT NULL,<br>    PRIMARY KEY (<code>ID</code>)<br>);</p><p>向数据表 studentinfo 中插入数据，SQL 语句和执行结果如下：<br>mysql&gt; INSERT INTO studentinfo(id,name,score) VALUES(1,”zhangsan”,80),(2,”lisi”,”70”);<br>Query OK, 2 rows affected (0.01 sec)<br>Records: 2  Duplicates: 0  Warnings: 0</p><p>创建存储过程 showstuscore，SQL 语句和运行结果如下：<br>mysql&gt; DELIMITER //<br>mysql&gt; CREATE PROCEDURE showstuscore()<br>    -&gt; BEGIN<br>    -&gt; SELECT id,name,score FROM studentinfo;<br>    -&gt; END //<br>Query OK, 0 rows affected (0.07 sec)</p><p>下面查询名为 showstuscore 的存储过程的状态，SQL 语句和运行结果如下：<br>mysql&gt; SHOW PROCEDURE STATUS LIKE ‘showstuscore’ \G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>                  Db: test<br>                Name: showstuscore<br>                Type: PROCEDURE<br>             Definer: root@localhost<br>            Modified: 2020-02-20 13:34:50<br>             Created: 2020-02-20 13:34:50<br>       Security_type: DEFINER<br>             Comment:<br>character_set_client: gbk<br>collation_connection: gbk_chinese_ci<br>  Database Collation: latin1_swedish_ci<br>1 row in set (0.01 sec)</p><p>mysql&gt; SHOW PROCEDURE STATUS LIKE ‘show%’ \G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>                  Db: test<br>                Name: showstuscore<br>                Type: PROCEDURE<br>             Definer: root@localhost<br>            Modified: 2020-02-21 09:34:50<br>             Created: 2020-02-21 09:34:50<br>       Security_type: DEFINER<br>             Comment:<br>character_set_client: gbk<br>collation_connection: gbk_chinese_ci<br>  Database Collation: latin1_swedish_ci<br>1 row in set (0.00 sec)<br>查询结果显示了存储过程的创建时间、修改时间和字符集等信息。<br>查看存储过程的定义<br>MySQL 中可以通过 SHOW CREATE 语句查看存储过程的状态，语法格式如下：<br>SHOW CREATE PROCEDURE 存储过程名;</p><p>实例 2<br>下面使用 SHOW CREATE 查询名为 showstuscore 的存储过程的状态，SQL 语句和运行结果如下：<br>mysql&gt; SHOW CREATE PROCEDURE showstuscore \G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>           Procedure: showstuscore<br>            sql_mode: STRICT_TRANS_TABLES,NO_AUTO_CREATE_USER,NO_ENGINE_SUBSTITUTION<br>    Create Procedure: CREATE DEFINER=<code>root</code>@<code>localhost</code> PROCEDURE <code>showstuscore</code>()<br>BEGIN<br>SELECT id,name,score FROM studentinfo;<br>END<br>character_set_client: gbk<br>collation_connection: gbk_chinese_ci<br>  Database Collation: latin1_swedish_ci<br>1 row in set (0.01 sec)<br>查询结果显示了存储过程的定义和字符集信息等。</p><p>SHOW STATUS 语句只能查看存储过程是操作的哪一个数据库、存储过程的名称、类型、谁定义的、创建和修改时间、字符编码等信息。但是，这个语句不能查询存储过程的集体定义，如果需要查看详细定义，需要使用 SHOW CREATE 语句。<br>拓展阅读<br>存储过程的信息都存储在 information_schema 数据库下的 Routines 表中，可以通过查询该表的记录来查询存储过程的信息，SQL 语句如下：<br>SELECT * FROM information_schema.Routines WHERE ROUTINE_NAME=存储过程名;</p><p>在 information_schema 数据库下的 routines 表中，存储着所有存储过程的定义。所以，使用 SELECT 语句查询 routines 表中的存储过程和函数的定义时，一定要使用 routine_name 字段指定存储过程的名称，否则，将查询出所有的存储过程的定义。</p><h1 id="修改存储过程"><a href="#修改存储过程" class="headerlink" title="修改存储过程"></a>修改存储过程</h1><p>在实际开发过程中，业务需求修改的情况时有发生，所以修改 MySQL 中的存储过程是不可避免的。</p><p>MySQL 中通过 ALTER PROCEDURE 语句来修改存储过程。本节将详细讲解修改存储过程的方法。</p><p>MySQL 中修改存储过程的语法格式如下：<br>ALTER PROCEDURE 存储过程名 [ 特征 … ]</p><p>特征指定了存储过程的特性，可能的取值有：<br>CONTAINS SQL 表示子程序包含 SQL 语句，但不包含读或写数据的语句。<br>NO SQL 表示子程序中不包含 SQL 语句。<br>READS SQL DATA 表示子程序中包含读数据的语句。<br>MODIFIES SQL DATA 表示子程序中包含写数据的语句。<br>SQL SECURITY { DEFINER |INVOKER } 指明谁有权限来执行。<br>DEFINER 表示只有定义者自己才能够执行。<br>INVOKER 表示调用者可以执行。<br>COMMENT ‘string’ 表示注释信息。<br>实例 1<br>下面修改存储过程 showstuscore 的定义，将读写权限改为 MODIFIES SQL DATA，并指明调用者可以执行，代码如下：<br>mysql&gt; ALTER PROCEDURE showstuscore MODIFIES SQL DATA SQL SECURITY INVOKER;<br>Query OK, 0 rows affected (0.01 sec)<br>执行代码，并查看修改后的信息，运行结果如下：<br>mysql&gt; SHOW CREATE PROCEDURE showstuscore \G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>           Procedure: showstuscore<br>            sql_mode: STRICT_TRANS_TABLES,NO_AUTO_CREATE_USER,NO_ENGINE_SUBSTITUTION<br>    Create Procedure: CREATE DEFINER=<code>root</code>@<code>localhost</code> PROCEDURE <code>showstuscore</code>()<br>    MODIFIES SQL DATA<br>    SQL SECURITY INVOKER<br>BEGIN<br>SELECT id,name,score FROM studentinfo;<br>END<br>character_set_client: gbk<br>collation_connection: gbk_chinese_ci<br>  Database Collation: latin1_swedish_ci<br>1 row in set (0.00 sec)<br>结果显示，存储过程修改成功。从运行结果可以看到，访问数据的权限已经变成了 MODIFIES SQL DATA，安全类型也变成了 INVOKE。</p><p>提示：ALTER PROCEDURE 语句用于修改存储过程的某些特征。如果要修改存储过程的内容，可以先删除原存储过程，再以相同的命名创建新的存储过程；如果要修改存储过程的名称，可以先删除原存储过程，再以不同的命名创建新的存储过程。</p><h1 id="删除存储过程"><a href="#删除存储过程" class="headerlink" title="删除存储过程"></a>删除存储过程</h1><p>存储过程被创建后，就会一直保存在数据库服务器上，直至被删除。当 MySQL 数据库中存在废弃的存储过程时，我们需要将它从数据库中删除。</p><p>MySQL 中使用 DROP PROCEDURE 语句来删除数据库中已经存在的存储过程。语法格式如下：<br>DROP PROCEDURE [ IF EXISTS ] &lt;过程名&gt;</p><p>语法说明如下：<br>过程名：指定要删除的存储过程的名称。<br>IF EXISTS：指定这个关键字，用于防止因删除不存在的存储过程而引发的错误。</p><p>注意：存储过程名称后面没有参数列表，也没有括号，在删除之前，必须确认该存储过程没有任何依赖关系，否则会导致其他与之关联的存储过程无法运行。<br>实例 1<br>下面删除存储过程 ShowStuScore，SQL 语句和运行结果如下：<br>mysql&gt; DROP PROCEDURE ShowStuScore;<br>Query OK, 0 rows affected (0.08 sec)<br>删除后，可以通过查询 information_schema 数据库下的 routines 表来确认上面的删除是否成功。SQL 语句和运行结果如下：<br>mysql&gt; SELECT * FROM information_schema.routines WHERE routine_name=’ShowStuScore’;<br>Empty set (0.03 sec)<br>结果显示，没有查询出任何记录，说明存储过程 ShowStuScore 已经被删除了。</p><h1 id="存储函数"><a href="#存储函数" class="headerlink" title="存储函数"></a>存储函数</h1>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;存储过程是什么？&quot;&gt;&lt;a href=&quot;#存储过程是什么？&quot; class=&quot;headerlink&quot; title=&quot;存储过程是什么？&quot;&gt;&lt;/a&gt;存储过程是什么？&lt;/h1&gt;&lt;p&gt;我们前面所学习的 MySQL 语句都是针对一个表或几个表的单条 SQL 语句，但是在数据库的
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>MySQL/MySQL数据库备份与恢复</title>
    <link href="http://yoursite.com/2021/07/14/MySQL/MySQL%E6%95%B0%E6%8D%AE%E5%BA%93%E5%A4%87%E4%BB%BD%E4%B8%8E%E6%81%A2%E5%A4%8D/"/>
    <id>http://yoursite.com/2021/07/14/MySQL/MySQL%E6%95%B0%E6%8D%AE%E5%BA%93%E5%A4%87%E4%BB%BD%E4%B8%8E%E6%81%A2%E5%A4%8D/</id>
    <published>2021-07-14T06:51:13.260Z</published>
    <updated>2021-07-14T07:25:30.545Z</updated>
    
    <content type="html"><![CDATA[<h1 id="备份类型"><a href="#备份类型" class="headerlink" title="备份类型"></a>备份类型</h1><p>备份是以防万一的一种必要手段，在出现硬件损坏或非人为的因素而导致数据丢失时，可以使用备份恢复数据，以将损失降低到最小程度，因此备份是必须的。备份可以分为以下几个类型。</p><p>根据备份的方法（是否需要数据库离线）可以将备份分为：<br>热备（Hot Backup）<br>冷备（Cold Backup）<br>温备（Warm Backup）</p><p>热备份可以在数据库运行中直接备份，对正在运行的数据库操作没有任何的影响，数据库的读写操作可以正常执行。这种方式在 MySQL 官方手册中称为 Online Backup（在线备份）。</p><p>冷备份必须在数据库停止的情况下进行备份，数据库的读写操作不能执行。这种备份最为简单，一般只需要复制相关的数据库物理文件即可。这种方式在 MySQL 官方手册中称为 Offline Backup（离线备份）。</p><p>温备份同样是在数据库运行中进行的，但是会对当前数据库的操作有所影响，备份时仅支持读操作，不支持写操作。</p><p>按照备份后文件的内容，热备份又可以分为：<br>逻辑备份<br>裸文件备份</p><p>在 MySQL 数据库中，逻辑备份是指备份出的文件内容是可读的，一般是文本内容。内容一般是由一条条 SQL 语句，或者是表内实际数据组成。如 mysqldump 和 SELECT * INTO OUTFILE 的方法。这类方法的好处是可以观察导出文件的内容，一般适用于数据库的升级、迁移等工作。但其缺点是恢复的时间较长。</p><p>裸文件备份是指复制数据库的物理文件，既可以在数据库运行中进行复制（如 ibbackup、xtrabackup 这类工具），也可以在数据库停止运行时直接复制数据文件。这类备份的恢复时间往往比逻辑备份短很多。</p><p>按照备份数据库的内容来分，备份又可以分为：<br>完全备份<br>部分备份</p><p>完全备份是指对数据库进行一个完整的备份，即备份整个数据库，如果数据较多会占用较大的时间和空间。</p><p>部分备份是指备份部分数据库（例如，只备份一个表）。</p><p>部分备份又分为：<br>增量备份<br>差异备份</p><p>增量备份需要使用专业的备份工具。指的是在上次完全备份的基础上，对更改的数据进行备份。也就是说每次备份只会备份自上次备份之后到备份时间之内产生的数据。因此每次备份都比差异备份节约空间，但是恢复数据麻烦。</p><p>差异备份指的是自上一次完全备份以来变化的数据。和增量备份相比，浪费空间，但恢复数据比增量备份简单。</p><p>MySQL 中进行不同方式的备份还要考虑存储引擎是否支持，如 MyISAM 不支持热备，支持温备和冷备。而 InnoDB 支持热备、温备和冷备。</p><p>一般情况下，我们需要备份的数据分为以下几种：<br>表数据<br>二进制日志、InnoDB 事务日志<br>代码（存储过程、存储函数、触发器、事件调度器）<br>服务器配置文件</p><p>下面是几种常用的备份工具：<br>mysqldump：逻辑备份工具，适用于所有的存储引擎，支持温备、完全备份、部分备份、对于 InnoDB 存储引擎支持热备。<br>cp、tar 等归档复制工具：物理备份工具，适用于所有的存储引擎、冷备、完全备份、部分备份。<br>lvm2 snapshot：借助文件系统管理工具进行备份。<br>mysqlhotcopy：名不副实的一个工具，仅支持 MyISAM 存储引擎。<br>xtrabackup：一款由 percona 提供的非常强大的 InnoDB/XtraDB 热备工具，支持完全备份、增量备份。</p><h1 id="冷备份"><a href="#冷备份" class="headerlink" title="冷备份"></a>冷备份</h1><p>在《MySQL备份类型》一节介绍过冷备份，冷备份可以称为 Offline Backup（离线备份）。这种备份最为简单，一般只需要复制相关的数据库物理文件到另外的位置即可。</p><p>由于 MySQL 服务器中的数据文件是基于磁盘的文本文件，所以最简单、最直接的备份操作，就是将数据文件直接复制出来。但是由于 MySQL 服务器的数据文件在运行时期，总是处于打开和使用状态，因此备份文件不一定有效。为了解决该问题，在复制数据文件时，需要先停止 MySQL 服务器。</p><p>这样做的好处是可以保证数据库的完整性，备份过程简单且恢复速度相对快一些，但是关闭数据库会影响现有业务的进行。服务器停止运行期间，用户不能再继续访问网站。例如，一些电商网站店庆促销时，如果为了备份要停库，那么带来的代价损失将不可估量。所以冷备一般用于不是很重要、非核心的业务上面。</p><p>冷备份的优点如下：<br>备份简单、快速，只要复制相关文件即可。<br>备份文件易于在不同操作系统，不同 MySQL 版本上进行恢复。<br>恢复相当简单，只需要把文件恢复到指定位置即可。<br>恢复速度快，不需要执行任何 SQL 语句，也不需要重建索引。<br>低度维护，高度安全。</p><p>冷备份的缺点如下：<br>备份过程中，数据库不能做其它的工作，且必须是关闭状态。<br>InnoDB 存储引擎冷备的文件通常比逻辑文件大很多，因为表空间存放着很多其它的数据，如 undo 段，插入缓冲等信息。<br>若磁盘空间有限，只能拷贝到磁带等其它外部存储设备上，速度会很慢。<br>冷备也不总是可以轻易的跨平台。操作系统、MySQL 的版本、文件大小写敏感和浮点数格式都会成为问题。</p><p>数据库的物理文件主要由数据库的数据文件、日志文件以及配置文件等组成。MySQL 系统有一些共有的日志文件和系统表的数据文件。每种存储引擎的物理文件也不一样，不了解的可阅读学习《不同存储引擎的数据表在磁盘里是如何表示的》一节。</p><p>冷备的备份与恢复过程也很简单。仅仅需要如下几步：</p><p>1）为了保证所备份数据的完整性，在停止 MySQL 数据库服务器之前，需要先执行 FLUSH TABLES 语句将所有数据写入到数据文件的文本文件里。</p><p>2）停掉 MySQL 服务，命令（2种方式）如下：<br>mysqladmin -uroot -proot shutdown<br>NET START mysql</p><p>3）备份过程就是复制整个数据目录到远程备份机或者本地磁盘上，Linux 和 Windows 命令如下：<br>Scp -r /data/mysql/ root@远程备份机ip:/新的目录<br>Copy -r /data/mysql/ 本地新目录</p><p>备份到本次磁盘也可以手动复制上述相关目录里的数据文件。</p><p>4）恢复过程就更简单了，仅仅需要把已备份的数据目录替换原有的目录就可以了，最后重启 MySQL 服务。<br>恢复数据是数据库维护中最常用的操作，利用备份文件可以将 MySQL 数据库服务器恢复到备份时的状态，这样就可以将管理员的非常操作和计算机的故障造成的相关损失降到最小。</p><p>上面我们介绍了如何通过数据文件实现数据备份和恢复。需要注意的是，通过复制数据文件这种方式实现数据恢复时，必须保证两个 MySQL 数据库的主版本号一致。只有两个 MySQL 数据库主版本号相同时，才能保证它们的数据文件类型是相同的。<br>关于 MySQL 数据库服务器的版本号<br>第一个数字表示主版本号，例如 MySQL 5.5.21 和 MySQL 5.5.01 这两个版本的主版本号都是 5，所以这两个数据库拥有相同类型的数据文件。</p><h1 id="冷备份所需物理文件"><a href="#冷备份所需物理文件" class="headerlink" title="冷备份所需物理文件"></a>冷备份所需物理文件</h1><p>在《MySQL冷备份及恢复》一节我们介绍了冷备份主要通过复制相关数据文件来实现，下面主要介绍 MyISAM 和 InnoDB 存储引擎需要备份什么物理文件。<br>MyISAM存储引擎<br>MyISAM 存储引擎的所有数据默认存放在 C:/ProgramData/MySQL/MySQL Server 5.7/Data 路径下，即配置文件（my.ini或my.cnf）中 datadir 参数的值。</p><p>实际上不管我们使用的是 MyISAM 存储引擎还是其他存储引擎，每一个数据库都会在“datadir”目录下有一个文件夹（包括系统信息的数据库 mysql 也是一样）。</p><p>在各个数据库中每一个 MyISAM 存储引擎的表都会有 3 个文件存在，即记录表结构元数据的“.frm”文件，存储表数据的“.MYD”文件，存储索引数据的“.MYI”文件。</p><p>MyISAM 属于非事务性存储引擎，它没有自己的日志文件。所以 MyISAM 存储引擎的物理备份除了需要备份 MySQL 系统共有的物理文件之外，还需要备份上面的 3 种文件。</p><p>Innodb 存储引擎<br>Innodb 存储引擎属于事务性存储引擎，存放数据的位置也可能与 MyISAM 存储引擎有所不同，这主要取决于 Innodb 的相关配置。</p><p>指定 Innodb 存放数据和日志文件的位置参数为 innodb_data_home_dir、innodb_data_file_path 和 innodb_log_group_home_dir。以及决定 Innodb 的表空间存储方式参数 innodb_file_per_table，它决定 Innodb 是以共享表空间存放数据还是以独享表空间方式存储数据。</p><p>以上几个参数的相关使用说明我们在《MySQL InnoDB存储引擎》一节中做了相应的解释，在 MySQL 的官方手册中也有较为详细的说明，这里就不再赘述了。</p><p>如果使用的是共享表空间的存储方式，那么需要备份 innodb_data_home_dir 和 innodb_data_file_path 参数设定的所有数据文件，以及 datadir 中相应数据库目录下的所有 Innodb 存储引擎表的 .frm 文件。</p><p>而如果使用的是独享表空间，那么除了需要备份上面共享表空间方式所需要备份的所有文件之外，我们还需要备份 datadir 中相应数据库目录下的所有 .idb 文件，该文件中存放的才是独享表空间方式下 Innodb 存储引擎表的数据。</p><p>那么既然是使用独享表空间，为什么还要备份共享表空间“才使用到”的数据文件呢？其实这是很多人的一个共性误区，以为使用独享表空间的时候 Innodb 的所有信息就都存放在“datadir”所设定数据库目录下的“.ibd”文件中。实际上并不是这样的，“.ibd”文件中所存放的仅仅只是我们的表数据而已。</p><p>大家都很清楚，Innodb 是事务性存储引擎，它需要 undo 和 redo 信息，而不管 Innodb 使用的是共享还是独享表空间的方式来存储数据。与事务相关的 undo 信息以及其他的一些元数据信息，都是存放在 innodb_data_home_dir 和 innodb_data_file_path 这两个参数所设定的数据文件中的。所以要想 Innodb 的物理备份有效，就必须备份 innodb_data_home_dir 和 innodb_data_file_path 参数所设定的数据文件。</p><p>此外，除了上面所说的数据文件之外，Innodb 还有存放自己的 redo 信息和相关事务信息的日志文件在 innodb_log_group_home_dir 参数所设定的位置。所以要想 Innodb 物理备份能够有效使用，我们还必须要备份 innodb_log_group_home_dir 参数所设定的位置的所有日志文件。</p><h1 id="热备份及恢复"><a href="#热备份及恢复" class="headerlink" title="热备份及恢复"></a>热备份及恢复</h1><p>与冷备份正好相反，热备份是在数据库处于运行状态时直接备份，不影响现有业务的正常进行。热备份又细分为逻辑备份和裸文件备份，下面我们介绍逻辑备份和冷备份以及它们常用的备份和恢复的方法。<br>逻辑备份<br>逻辑备份的最大优点就是对于各种存储引擎，都可以用同样的方法来备份。而冷备份则不同，不同的存储引擎的备份方法也各不相同。因此，对于不同存储引擎混合的数据库，用逻辑备份会更简单一些。</p><p>逻辑备份可以说是最简单，也是目前中小型系统最常用的备份方法。逻辑备份主要有以下几种方法：<br>1）mysqldump<br>mysqldump 是 MySQL 自带的逻辑备份工具。它的备份原理是通过协议连接到 MySQL 数据库，将需要备份的数据查询出来，然后将查询出的数据转换成对应的 INSERT 语句。当我们需要还原恢复这些数据时，只要执行这些 INSERT 语句，就能将对应的数据还原。所以有的资料也将这种备份方式称为 INSERT 备份。</p><p>恢复数据时可以使用 mysql -uroot -p &lt;backup.sql 直接调用备份文件执行所有命令，将数据完全恢复到备份时候的状态。如果已经连接上了 MySQL 服务器，那么可以通过 source /path/backup.sql 来进行恢复。<br>我们在《MySQL mysqldump备份数据库》和《MySQL恢复数据库》中详细介绍了上面所讲的备份和恢复数据的方法。</p><p>2）SELECT INTO…OUTFILE<br>SELECT INTO…OUTFILE 语句可以把表数据导出到一个文本文件中，且能将数据库中的表数据以特定分隔符进行分隔后记录在文本文件中，以达到逻辑备份的效果。</p><p>这种备份方式与 mysqldump 方法相比，使用的存储空间更小，数据格式更加清晰明确，编辑方便。但是这种方法只能导出或导入数据的内容，不包括表的结构，如果表的结构文件损坏，则必须先恢复原来的表的结构。而且这种方法不能在同一个备份文件中存在多个表的备份数据，增加了文件维护和恢复的成本。</p><p>这种备份方法恢复起来会稍微麻烦一点，需要一个表一个表通过相关命令来进行恢复。当然如果是通过脚本来实现自动多表恢复也是比较方便的。恢复方法有 2 个，一个是通过 MySQL 的 LOAD DATA INFILE 命令来恢复数据，另一种方法就是通过 MySQL 提供的使用工具 mysqlimport 来进行恢复。<br>我们在《MySQL导出表数据》和《MySQL数据库恢复（LOAD DATA）》中详细介绍了上面所讲的备份和恢复数据的方法。</p><p>3）mydumper<br>mydumper 是针对 MySQL 数据库备份的一个轻量级第三方的开源工具，备份方式为逻辑备份。它支持多线程，备份速度远高于原生态的 mysqldump 以及其它众多优异特性。与其配套的相应恢复数据为 myloader 工具。DBA 称 mydumper 和 myloader 为备份界的“小钢炮”。</p><p>我们可以看出所谓的逻辑备份就是备份 SQL 语句，然后恢复数据时执行备份 SQL，从而实现数据库数据的重现。逻辑备份完成后所形成的文件都可以直接编辑。</p><p>逻辑备份的作用如下：<br>通过逻辑备份，我们可以仅仅恢复备份中的部分数据而不需要全部恢复。不会影响不相关的数据；<br>通过全库的逻辑备份，我们可以在新的 MySQL 环境下完全重建出一个与备份时完全一样的数据库，并且不受平台类型限制；<br>通过特定条件的逻辑备份，我们可以将某些特定数据轻松迁移（或者同步）到其它的 MySQL 或另外的数据库环境。<br>裸文件备份<br>裸文件备份主要在底层复制数据文件，所以备份速度要比逻辑备份更快。</p><p>我们利用 Percona 公司发布的一个 XtraBackup 热备份工具来完成裸文件备份，它是 Percona 公司的开源项目，据官方介绍它是世界上唯一 一款开源的能够对 InnoDB 和 XtraDB 数据库进行热备的工具。</p><p>它的优点就是备份与恢复过程的速度很快，安全可靠，而且在备份过程中不会缩表，不影响现有业务。但它目前还是不能对表结构文件和其它非事务类型的表进行备份。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;备份类型&quot;&gt;&lt;a href=&quot;#备份类型&quot; class=&quot;headerlink&quot; title=&quot;备份类型&quot;&gt;&lt;/a&gt;备份类型&lt;/h1&gt;&lt;p&gt;备份是以防万一的一种必要手段，在出现硬件损坏或非人为的因素而导致数据丢失时，可以使用备份恢复数据，以将损失降低到最小程度，因
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>MySQL/MySQL字符集和校对规则</title>
    <link href="http://yoursite.com/2021/07/14/MySQL/MySQL%E5%AD%97%E7%AC%A6%E9%9B%86%E5%92%8C%E6%A0%A1%E5%AF%B9%E8%A7%84%E5%88%99/"/>
    <id>http://yoursite.com/2021/07/14/MySQL/MySQL%E5%AD%97%E7%AC%A6%E9%9B%86%E5%92%8C%E6%A0%A1%E5%AF%B9%E8%A7%84%E5%88%99/</id>
    <published>2021-07-14T06:30:23.447Z</published>
    <updated>2021-07-15T05:50:02.644Z</updated>
    
    <content type="html"><![CDATA[<p>字符（Character）是计算机中字母、数字、符号的统称，一个字符可以是一个中文汉字、一个英文字母、一个阿拉伯数字、一个标点符号等。</p><p>计算机是以二进制的形式来存储数据的。平时我们在显示器上看到的数字、英文、标点符号、汉字等字符都是二进制数转换之后的结果。</p><p>字符集（Character set）定义了字符和二进制的对应关系，为字符分配了唯一的编号。常见的字符集有 ASCII、GBK、IOS-8859-1 等。</p><p>字符编码（Character encoding）也可以称为字集码，规定了如何将字符的编号存储到计算机中。<br>大部分字符集都只对应一种字符编码，例如：ASCII、IOS-8859-1、GB2312、GBK，都是既表示了字符集又表示了对应的字符编码。所以一般情况下，可以将两者视为同义词。Unicode 字符集除外，Unicode 有三种编码方案，即 UTF-8、UTF-16 和 UTF-32。最为常用的是 UTF-8 编码。</p><p>校对规则（Collation）也可以称为排序规则，是指在同一个字符集内字符之间的比较规则。字符集和校对规则是一对多的关系，每个字符集都有一个默认的校对规则。字符集和校对规则相辅相成，相互依赖关联。</p><p>简单来说，字符集用来定义 MySQL 存储字符串的方式，校对规则用来定义 MySQL 比较字符串的方式。</p><p>有些数据库并没有清晰的区分开字符集和校对规则。例如，在 SQL Server 中创建数据库时，选择字符集就相当于选定了字符集和校对规则。</p><p>而在 MySQL 中，字符集和校对规则是区分开的，必须设置字符集和校对规则。一般情况下，没有特殊需求，只设置其一即可。只设置字符集时，MySQL 会将校对规则设置为字符集中对应的默认校对规则。</p><p>可以通过SHOW VARIABLES LIKE ‘character%’;命令查看当前 MySQL 使用的字符集，命令和运行结果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; SHOW VARIABLES LIKE &#39;character%&#39;;</span><br><span class="line">+--------------------------+---------------------------------------------------------+</span><br><span class="line">| Variable_name            | Value                                                   |</span><br><span class="line">+--------------------------+---------------------------------------------------------+</span><br><span class="line">| character_set_client     | gbk                                                     |</span><br><span class="line">| character_set_connection | gbk                                                     |</span><br><span class="line">| character_set_database   | latin1                                                  |</span><br><span class="line">| character_set_filesystem | binary                                                  |</span><br><span class="line">| character_set_results    | gbk                                                     |</span><br><span class="line">| character_set_server     | latin1                                                  |</span><br><span class="line">| character_set_system     | utf8                                                    |</span><br><span class="line">| character_sets_dir       | C:\Program Files\MySQL\MySQL Server 5.7\share\charsets\ |</span><br><span class="line">+--------------------------+---------------------------------------------------------+</span><br></pre></td></tr></table></figure><p>上述运行结果说明如下表所示：</p><p>名称    说明<br>character_set_client    MySQL 客户端使用的字符集<br>character_set_connection    连接数据库时使用的字符集<br>character_set_database    创建数据库使用的字符集<br>character_set_filesystem    MySQL 服务器文件系统使用的字符集，默认值为 binary，不做任何转换<br>character_set_results    数据库给客户端返回数据时使用的字符集<br>character_set_server    MySQL 服务器使用的字符集，建议由系统自己管理，不要人为定义<br>character_set_system    数据库系统使用的字符集，默认值为 utf8，不需要设置<br>character_sets_dir    字符集的安装目录<br>乱码时，不需要关心 character_set_filesystem、character_set_system 和 character_sets_dir 这 3 个系统变量，它们不会影响乱码 。</p><p>可以通过SHOW VARIABLES LIKE ‘collation_%’;命令查看当前 MySQL 使用的校对规则，命令和运行结果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; SHOW VARIABLES LIKE &#39;collation\_%&#39;;</span><br><span class="line">+----------------------+-------------------+</span><br><span class="line">| Variable_name        | Value             |</span><br><span class="line">+----------------------+-------------------+</span><br><span class="line">| collation_connection | gbk_chinese_ci    |</span><br><span class="line">| collation_database   | latin1_swedish_ci |</span><br><span class="line">| collation_server     | latin1_swedish_ci |</span><br><span class="line">+----------------------+-------------------+</span><br></pre></td></tr></table></figure><p>对上述运行结果说明如下：<br>collation_connection：连接数据库时使用的校对规则<br>collation_database：创建数据库时使用的校对规则<br>collation_server：MySQL 服务器使用的校对规则</p><p>校对规则命令约定如下：<br>以校对规则所对应的字符集名开头<br>以国家名居中（或以 general 居中）<br>以 ci、cs 或 bin 结尾，ci 表示大小写不敏感，cs 表示大小写敏感，bin 表示按二进制编码值比较。<br>MySQL字符集的转换过程<br>MySQL 中字符集的转换过程如下：</p><p>1）在命令提示符窗口（cmd 命令行）中执行 MySQL 命令或 sql 语句时，这些命令或语句从“命令提示符窗口字符集”转换为“character_set_client”定义的字符集。</p><p>2）使用命令提示符窗口成功连接 MySQL 服务器后，就建立了一条“数据通信链路”，MySQL 命令或 sql 语句沿着“数据链路”传向 MySQL 服务器，由 character_set_client 定义的字符集转换为 character_set_connection 定义的字符集。</p><p>3）MySQL 服务实例收到数据通信链路中的 MySQL 命令或 sql 语句后，将 MySQL 命令或 sql 语句从 character_set_connection 定义的字符集转换为 character_set_server 定义的字符集。</p><p>4）若 MySQL 命令或 sql 语句针对于某个数据库进行操作，此时将 MySQL 命令或 sql 语句从 character_set_server 定义的字符集转换为 character_set_database 定义的字符集。</p><p>5）MySQL 命令或 sql 语句执行结束后，将执行结果设置为 character_set_results 定义的字符集。</p><p>6）执行结果沿着打开的数据通信链路原路返回，将执行结果从 character_set_results 定义的字符集转换为 character_set_client 定义的字符集，最终转换为命令提示符窗口字符集，显示到命令提示符窗口中。</p><h1 id="查看字符集和校对规则"><a href="#查看字符集和校对规则" class="headerlink" title="查看字符集和校对规则"></a>查看字符集和校对规则</h1><p>在 MySQL 中，查看可用字符集的命令和执行过程如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; SHOW CHARACTER set;</span><br><span class="line">+----------+---------------------------------+---------------------+--------+</span><br><span class="line">| Charset  | Description                     | Default collation   | Maxlen |</span><br><span class="line">+----------+---------------------------------+---------------------+--------+</span><br><span class="line">| big5     | Big5 Traditional Chinese        | big5_chinese_ci     |      2 |</span><br><span class="line">| dec8     | DEC West European               | dec8_swedish_ci     |      1 |</span><br><span class="line">| ascii    | US ASCII                        | ascii_general_ci    |      1 |</span><br><span class="line">| gb2312   | GB2312 Simplified Chinese       | gb2312_chinese_ci   |      2 |</span><br><span class="line">| greek    | ISO 8859-7 Greek                | greek_general_ci    |      1 |</span><br><span class="line">| cp1250   | Windows Central European        | cp1250_general_ci   |      1 |</span><br><span class="line">| gbk      | GBK Simplified Chinese          | gbk_chinese_ci      |      2 |</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>其中：<br>第一列（Charset）为字符集名称；<br>第二列（Description）为字符集描述；<br>第三列（Default collation）为字符集的默认校对规则；<br>第四列（Maxlen）表示字符集中一个字符占用的最大字节数。</p><p>常用的字符集如下：</p><ul><li><code>gbk</code>支持中文简体字符。</li><li><code>big5</code>支持中文繁体字符。</li><li><code>utf8</code>几乎支持所有国家的字符。</li></ul><p>也可以通过查询<code>information_schema.character_set</code>表中的记录，来查看 MySQL 支持的字符集。SQL 语句和执行过程如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; SELECT * FROM information_schema.character_sets;</span><br><span class="line">+--------------------+----------------------+---------------------------------+--------+</span><br><span class="line">| CHARACTER_SET_NAME | DEFAULT_COLLATE_NAME | DESCRIPTION                     | MAXLEN |</span><br><span class="line">+--------------------+----------------------+---------------------------------+--------+</span><br><span class="line">| big5               | big5_chinese_ci      | Big5 Traditional Chinese        |      2 |</span><br><span class="line">| dec8               | dec8_swedish_ci      | DEC West European               |      1 |</span><br><span class="line">| cp850              | cp850_general_ci     | DOS West European               |      1 |</span><br><span class="line">| hp8                | hp8_english_ci       | HP West European                |      1 |</span><br><span class="line">......</span><br></pre></td></tr></table></figure><p>可以使用<code>SHOW COLLATION LIKE &#39;***&#39;;</code>命令来查看相关字符集的校对规则。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; SHOW COLLATION LIKE &#39;gbk%&#39;;</span><br><span class="line">+----------------+---------+----+---------+----------+---------+</span><br><span class="line">| Collation      | Charset | Id | Default | Compiled | Sortlen |</span><br><span class="line">+----------------+---------+----+---------+----------+---------+</span><br><span class="line">| gbk_chinese_ci | gbk     | 28 | Yes     | Yes      |       1 |</span><br><span class="line">| gbk_bin        | gbk     | 87 |         | Yes      |       1 |</span><br><span class="line">+----------------+---------+----+---------+----------+---------+</span><br></pre></td></tr></table></figure><p>上面运行结果为 GBK 字符集所对应的校对规则，其中 gbk_chinese_ci 是默认的校对规则，对大小写不敏感。而 gbk_bin 按照二进制编码的值进行比较，对大小写敏感。</p><p>也可以通过查询<code>information_schema.COLLATIONS</code>表中的记录，来查看 MySQL 中可用的校对规则。SQL 语句和执行过程如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; SELECT * FROM information_schema.COLLATIONS;</span><br><span class="line">+--------------------------+--------------------+-----+------------+-------------+---------+</span><br><span class="line">| COLLATION_NAME           | CHARACTER_SET_NAME | ID  | IS_DEFAULT | IS_COMPILED | SORTLEN |</span><br><span class="line">+--------------------------+--------------------+-----+------------+-------------+---------+</span><br><span class="line">| big5_chinese_ci          | big5               |   1 | Yes        | Yes         |       1 |</span><br><span class="line">| big5_bin                 | big5               |  84 |            | Yes         |       1 |</span><br><span class="line">| dec8_swedish_ci          | dec8               |   3 | Yes        | Yes         |       1 |</span><br><span class="line">| dec8_bin                 | dec8               |  69 |            | Yes         |       1 |</span><br><span class="line">| cp850_general_ci         | cp850              |   4 | Yes        | Yes         |       1 |</span><br><span class="line">| cp850_bin                | cp850              |  80 |            | Yes         |       1 |</span><br><span class="line">......</span><br></pre></td></tr></table></figure><p>分别指定“A”和“a”按照 gbk_chinese_ci 和 gbk_bin 校对规则进行比较。SQL 语句和运行结果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; SELECT CASE WHEN &#39;A&#39; COLLATE gbk_chinese_ci &#x3D; &#39;a&#39; COLLATE gbk_chinese_ci then 1</span><br><span class="line">    -&gt; else 0 end;</span><br><span class="line">+-------------------------------------------------------------------------------------+</span><br><span class="line">| CASE WHEN &#39;A&#39; COLLATE gbk_chinese_ci &#x3D; &#39;a&#39; COLLATE gbk_chinese_ci then 1</span><br><span class="line">else 0 end |</span><br><span class="line">+-------------------------------------------------------------------------------------+</span><br><span class="line">|                                                                                   1 |</span><br><span class="line">+-------------------------------------------------------------------------------------+</span><br><span class="line">1 row in set (0.02 sec)</span><br><span class="line"></span><br><span class="line">mysql&gt; SELECT CASE WHEN &#39;A&#39; COLLATE gbk_bin &#x3D; &#39;a&#39; COLLATE gbk_bin then 1 else 0 end;</span><br><span class="line">+-----------------------------------------------------------------------+</span><br><span class="line">| CASE WHEN &#39;A&#39; COLLATE gbk_bin &#x3D; &#39;a&#39; COLLATE gbk_bin then 1</span><br><span class="line">else 0 end |</span><br><span class="line">+-----------------------------------------------------------------------+</span><br><span class="line">|                                                                     0 |</span><br><span class="line">+-----------------------------------------------------------------------+</span><br></pre></td></tr></table></figure><p>由于 gbk_chinese_ci 校对规则忽略大小写，所以认为两个“A“和“a”是相同的。 gbk_bin 校对规则不忽略大小写，则认为两个字符是不同的。</p><p>在实际应用中，我们应事先确认应用需要按照什么样方式排序，是否区分大小写，然后选择相应的校对规则。</p><h1 id="设置默认字符集和校对规则"><a href="#设置默认字符集和校对规则" class="headerlink" title="设置默认字符集和校对规则"></a>设置默认字符集和校对规则</h1><p>MySQL 服务器可以支持多种字符集，在同一台服务器、同一个数据库甚至同一个表的不同字段中，都可以使用不同的字符集。Oracle 等其它数据库管理系统都只能使用相同的字符集，相比之下，MySQL 明显存在更大的灵活性。</p><p>MySQL 的字符集和校对规则有 4 个级别的默认设置，即服务器级、数据库级、表级和字段级。它们分别在不同的地方设置，作用也不相同。<br>服务器字符集和校对规则<br>修改服务器默认字符集和校对规则的方法如下。</p><p>1）可以在 my.ini 配置文件中设置服务器字符集和校对规则，添加内容如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">[mysqld]</span><br><span class="line">character-set-server&#x3D;字符集名称</span><br></pre></td></tr></table></figure><p>2）连接 MySQL 服务器时指定字符集：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysql --default-character-set&#x3D;字符集名称 -h 主机IP地址 -u 用户名 -p 密码</span><br></pre></td></tr></table></figure><p>如果没有指定服务器字符集，MySQL 会默认使用 latin1 作为服务器字符集。如果只指定了字符集，没有指定校对规则，MySQL 会使用该字符集对应的默认校对规则。如果要使用字符集的非默认校对规则，需要在指定字符集的同时指定校对规则。</p><p>可以用 SHOW VARIABLES LIKE ‘character_set_server’ 和 SHOW VARIABLES LIKE ‘collation_server’ 命令查询当前服务器的字符集和校对规则。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; SHOW VARIABLES LIKE &#39;character_set_server&#39;;</span><br><span class="line">+----------------------+--------+</span><br><span class="line">| Variable_name        | Value  |</span><br><span class="line">+----------------------+--------+</span><br><span class="line">| character_set_server | gbk    |</span><br><span class="line">+----------------------+--------+</span><br><span class="line">1 row in set, 1 warning (0.01 sec)</span><br><span class="line"></span><br><span class="line">mysql&gt; SHOW VARIABLES LIKE &#39;collation_server&#39;;</span><br><span class="line">+------------------+-------------------+</span><br><span class="line">| Variable_name    | Value             |</span><br><span class="line">+------------------+-------------------+</span><br><span class="line">| collation_server | gbk_chinese_ci    |</span><br><span class="line">+------------------+-------------------+</span><br></pre></td></tr></table></figure><p>数据库字符集和校对规则<br>数据库的字符集和校对规则在创建数据库时指定，也可以在创建完数据库后通过 ALTER DATABASE 命令进行修改，具体操作可阅读学习《MySQL修改数据库》一节。</p><p>需要注意的是，如果数据库里已经存在数据，修改字符集后，已有的数据不会按照新的字符集重新存放，所以不能通过修改数据库的字符集来修改数据的内容。在《MySQL修改字符集步骤详解》一节我们介绍了如何修改已存在数据字符集的方法。</p><p>设置数据库字符集的规则如下：<br>如果指定了字符集和校对规则，则使用指定的字符集和校对规则；<br>如果指定了字符集没有指定校对规则，则使用指定字符集的默认校对规则；<br>如果指定了校对规则但未指定字符集，则字符集使用与该校对规则关联的字符集；<br>如果没有指定字符集和校对规则，则使用服务器字符集和校对规则作为数据库的字符集和校对规则。<br>为了避免受到默认值的影响，推荐在创建数据库时指定字符集和校对规则。</p><p>可以使用 SHOW VARIABLES LIKE ‘character_set_database’ 和 SHOW VARIABLES LIKE ‘collation_database’ 命令查看当前数据库的字符集和校对规则。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; SHOW VARIABLES LIKE &#39;character_set_database&#39;;</span><br><span class="line">+------------------------+--------+</span><br><span class="line">| Variable_name          | Value  |</span><br><span class="line">+------------------------+--------+</span><br><span class="line">| character_set_database | latin1 |</span><br><span class="line">+------------------------+--------+</span><br><span class="line">1 row in set, 1 warning (0.00 sec)</span><br><span class="line"></span><br><span class="line">mysql&gt; SHOW VARIABLES LIKE &#39;collation_database&#39;;</span><br><span class="line">+--------------------+-------------------+</span><br><span class="line">| Variable_name      | Value             |</span><br><span class="line">+--------------------+-------------------+</span><br><span class="line">| collation_database | latin1_swedish_ci |</span><br><span class="line">+--------------------+-------------------+</span><br></pre></td></tr></table></figure><p>表字符集和校对规则<br>表的字符集和校对规则在创建表的时候指定，也可以在创建完表后通过 ALTER TABLE 命令进行修改，具体操作可阅读学习《MySQL修改数据表》一节。</p><p>同样，如果表中已有记录，修改字符集后，原有的记录不会按照新的字符集重新存放。表的字段仍然使用原来的字符集。</p><p>设置表的字符集规则和设置数据库字符集的规则基本类似：<br>如果指定了字符集和校对规则，使用指定的字符集和校对规则；<br>如果指定了字符集没有指定校对规则，使用指定字符集的默认校对规则；<br>如果指定了校对规则但未指定字符集，则字符集使用与该校对规则关联的字符集；<br>如果没有指定字符集和校对规则，使用数据库字符集和校对规则作为表的字符集和校对规则。<br>为了避免受到默认值的影响，推荐在创建表的时候指定字符集和校对规则。</p><p>可以使用 SHOW CREATE TABLE 命令查看当前表的字符集和校对规则，SQL 语句和运行结果如下：<br>mysql&gt; SHOW CREATE TABLE tb_students_info \G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>       Table: tb_students_info<br>Create Table: CREATE TABLE <code>tb_students_info</code> (<br>  <code>id</code> int(11) NOT NULL AUTO_INCREMENT,<br>  <code>name</code> varchar(10) DEFAULT NULL,<br>  <code>age</code> int(11) DEFAULT NULL,<br>  <code>sex</code> char(1) DEFAULT NULL,<br>  <code>height</code> float DEFAULT NULL,<br>  <code>course_id</code> int(11) DEFAULT NULL,<br>  PRIMARY KEY (<code>id</code>)<br>) ENGINE=InnoDB AUTO_INCREMENT=12 DEFAULT CHARSET=utf8<br>1 row in set (0.00 sec)<br>列字符集和校对规则<br>MySQL 可以定义列级别的字符集和校对规则，主要是针对相同表的不同字段需要使用不同字符集的情况。一般遇到这种情况的几率比较小，这只是 MySQL 提供给我们一个灵活设置的手段。</p><p>列字符集和校对规则的定义可以在创建表时指定，或者在修改表时调整。语法格式如下：<br>ALTER TABLE 表名 MODIFY 列名  数据类型 CHARACTER SET 字符集名;</p><p>例 1<br>修改 tb_students_info 表中 name 列的字符集，并查看。SQL 语句和运行结果如下：<br>mysql&gt; ALTER TABLE tb_students_info MODIFY name VARCHAR(10) CHARACTER SET gbk;<br>Query OK, 11 rows affected (0.11 sec)<br>Records: 11  Duplicates: 0  Warnings: 0</p><p>mysql&gt;  SHOW CREATE TABLE tb_students_info \G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>       Table: tb_students_info<br>Create Table: CREATE TABLE <code>tb_students_info</code> (<br>  <code>id</code> int(11) NOT NULL AUTO_INCREMENT,<br>  <code>name</code> varchar(10) CHARACTER SET gbk DEFAULT NULL,<br>  <code>age</code> int(11) DEFAULT NULL,<br>  <code>sex</code> char(1) DEFAULT NULL,<br>  <code>height</code> float DEFAULT NULL,<br>  <code>course_id</code> int(11) DEFAULT NULL,<br>  PRIMARY KEY (<code>id</code>)<br>) ENGINE=InnoDB AUTO_INCREMENT=12 DEFAULT CHARSET=utf8<br>1 row in set (0.00 sec)<br>结果显示，name 列字符集修改成功。<br>如果在创建列的时候没有特别指定字符集和校对规则，默认使用表的字符集和校对规则。</p><p>连接字符集和校对规则<br>上面所讲的 4 种设置方式，确定的都是数据保存的字符集和校对规则。实际应用中，还需要设置客户端和服务器之间交互的字符集和校对规则。</p><p>对于客户端和服务器的交互操作，MySQL 提供了 3 个不同的参数：character_set_client、character_set_connection 和 character_set_results，分别代表客户端、连接和返回结果的字符集。通常情况下，这 3 个字符集是相同的，这样可以确保正确读出用户写入的数据，尤其是中文字符。字符集不同时，容易导致写入的记录不能正确读出。</p><p>设置客户端和服务器连接的字符集和校对规则有以下几种方法：</p><p>1）在 my.ini 配置文件中，设置以下语句：<br>[mysql]<br>default-character-set=gbk</p><p>这样服务器启动后，所有连接默认使用 GBK 字符集进行连接。</p><p>2）可以通过以下命令来设置连接的字符集和校对规则，这个命令可以同时修改以上 3 个参数（character_set_client、character_set_connection 和 character_set_results）的值。<br>SET NAMES gbk;</p><p>使用这个方法可以“临时一次性地”修改客户端和服务器连接时的字符集为 gbk。</p><p>3）MySQL 还提供了下列 MySQL 命令“临时地”修改 MySQL“当前会话的”字符集和校对规则。<br>set character_set_client = gbk;<br>set character_set_connection = gbk;<br>set character_set_database = gbk;<br>set character_set_results = gbk;<br>set character_set_server = gbk;<br>set collation_connection = gbk_chinese_ci;<br>set collation_database = gbk_chinese_ci;<br>set collation_server = gbk_chinese_ci;</p><h1 id="修改字符集步骤"><a href="#修改字符集步骤" class="headerlink" title="修改字符集步骤"></a>修改字符集步骤</h1><p>在实际应用中，如果一开始没有正确的设置字符集，在运行一段时间以后，才发现当前字符集不能满足要求，需要进行调整，但又不想丢弃这段时间的数据，这个时候就需要修改字符集。</p><p>在《MySQL设置默认字符集和校对规则》一节我们讲到，ALTER DATABASE 或 ALTER TABLE 命令对已经存在的数据没有作用，只对新创建的表或记录生效。如果想修改已存在数据的字符集，需要先将数据导出，经过适当的调整后，再重新导入。<br>例 1<br>以下模拟的是将 gb2312 字符集的数据库修改成 gbk 字符集的数据库的过程。</p><p>1）创建 testset 数据库，设置其字符集为 gb2312，并添加数据。<br>mysql&gt; CREATE TABLE test.testset(<br>    -&gt; id INT(11) DEFAULT NULL,<br>    -&gt; name VARCHAR(25) DEFAULT NULL<br>    -&gt; )CHARSET=gb2312;<br>Query OK, 0 rows affected (0.10 sec)</p><p>mysql&gt; INSERT INTO test.testset VALUES (1,’C语言’);<br>Query OK, 1 row affected (0.01 sec)</p><p>mysql&gt; INSERT INTO test.testset VALUES (2,’Java语言’);<br>Query OK, 1 row affected (0.01 sec)</p><p>mysql&gt; INSERT INTO test.testset VALUES (3,’Python语言’);<br>Query OK, 1 row affected (0.01 sec)<br>2）导出 testset 表结构，命令如下：<br>mysqldump -uroot -p –default-character-set=gbk -d test testset&gt; D:\testset.sql</p><p>其中，–default-character-set=gbk 表示以什么字符集连接；-d 表示只导出表结构，不导出数据。</p><p>3）打开 testset.sql 文件，修改表结构定义中的字符集为新的字符集，如下图所示。</p><p>修改表结构的字符集</p><p>4）确保表中的记录不再更新，导出所有记录。<br>mysqldump -uroot -p –quick –no-create-info –extended-insert –default-character-set=gb2312 test testset&gt; D:\testdata.sql</p><p>–quick：该选项用于存储记录多的表。它强制 mysqldump 从服务器一次一行地查询表中的行，而不是查询所有行，并在输出前将它缓存到内存中。<br>–extended-insert：使用 INSERT 插入多行数据语法。可以使文件更小，导入文件时加速插入。<br>–no-create-info：不导出表的 CREATE TABLE 语句。<br>–default-character-set=gb2312：按照原有的字符集导出所有数据。这样导出的文件中，所有中文都是可见的，不会保存成乱码。</p><p>5）打开 testdata.sql，将 SET NAMES gb2312 修改成 SET NAMES gbk，如下图所示。</p><p>6）使用新的字符集创建新的数据库。<br>CREATE DATABASE test2 DEFAULT CHARSET gbk;</p><p>7）创建表，执行 testset.sql。<br>mysql -uroot -p test2 &lt; D:\testset.sql</p><p>8）导入数据，执行 testdata.sql。<br>mysql -uroot -p test2 &lt; D:\testdata.sql</p><p>9）查看 testset 表结构是否修改了字符集，以及表内数据是否丢失或乱码，SQL 语句和运行结果如下：<br>mysql&gt; SHOW CREATE TABLE test2.testset \G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>       Table: testset<br>Create Table: CREATE TABLE <code>testset</code> (<br>  <code>id</code> int(11) DEFAULT NULL,<br>  <code>name</code> varchar(25) DEFAULT NULL<br>) ENGINE=InnoDB DEFAULT CHARSET=gbk<br>1 row in set (0.00 sec)</p><p>mysql&gt; SELECT * FROM test2.testset;<br>+——+————+<br>| id   | name       |<br>+——+————+<br>|    1 | C语言      |<br>|    2 | Java语言   |<br>|    3 | Python语言 |<br>+——+————+<br>3 rows in set (0.00 sec)<br>注意：选择目标字符集的时候，要注意最好的是原字符集的超集，或者确定比原字符集的字库更大，否则如果目标字符集的字库小于原字符集的字库，那么目标字符集中不支持的字符导入后会变成乱码，丢失一部分数据。</p><h1 id="字符集的选择"><a href="#字符集的选择" class="headerlink" title="字符集的选择"></a>字符集的选择</h1><p>由于数据库中存储的数据大部分都是各种文字，所以字符集对数据库的存储、处理性能，以及日后系统的移植、推广都会有影响。对数据库来说，字符集非常重要。不论是在 MySQL 数据库还是其它数据库，都存在字符集的选择问题。</p><p>如果在创建数据库时没有正确选择字符集，在后期就可能需要更换字符集，而更换字符集是代价比较高的操作，也存在一定的风险。所以推荐在应用开始阶段，就按照实际需求，正确的选择合适的字符集，避免后期不必要的调整。</p><p>在《MySQL查看字符集和校对规则》一节中，我们了解到目前 MySQL 5.7 支持几十种字符集，包括 UCS-2、UTF-16、UTF-16LE、UTF-32、 UTF-8 和 utf8mb4 等 Unicode 字符集。那么面对众多的字符集，我们该如何选择呢？</p><p>在选择数据库字符集时，可以根据应用的需求，结合字符集的特点来权衡，主要考虑以下几方面的因素。</p><p>1）满足应用支持语言的需求。如果应用要处理各种各样的文字，或者将发布到使用不同语言的国家或地区，就应该选择 Unicode 字符集。对 MySQL 来说，目前就是 UTF-8。</p><p>2）如果应用中涉及已有数据的导入，就要充分考虑数据库字符集对已有数据的兼容性。假如已有数据的字符集是 GBK，如果选择 GB 2312-80 为数据库字符集，就很可能出现某些文字无法正确导入。</p><p>3）如果数据库只需要支持一般中文，数据量很大，性能要求也很高，那就应该选择双字节定长编码的中文字符集，比如 GBK。</p><p>因为，相对于 UTF-8 而言，GBK 比较“小”，每个汉字只占 2 个字节，而 UTF-8 汉字编码需要 3 个字节，这样可以减少磁盘 I/O、数据库 Cache 以及网络传输的时间，从而提高性能。相反，如果应用主要处理英文字符，仅有少量汉字数据，那么选择 UTF-8 更好，因为 GBK、UCS-2、UTF-16 的西文字符编码都是 2 个字节，会造成很多不必要的开销。</p><p>4）如果数据库需要做大量的字符运算，如比较、排序等，那么选择定长字符集可能更好，因为定长字符集的处理速度要比变长字符集的处理速度快。</p><p>5）如果所有客户端程序都支持相同的字符集，则应该优先选择该字符集作为数据库字符集。这样可以避免因字符集转换带来的性能开销和数据损失。</p><p>6）在多种字符集都能够满足应用的前提下，应尽量使用小的字符集。因为更小的字符集意味着能够节省空间、减少网络传输字节数，同时由于存储空间的较小间接的提高了系统的性能。<br>拓展<br>有很多字符集都可以保存汉字，比如 UTF-8、GB2312、GBK、Latin1 等等。但是常用的是 GB2312 和 GBK。因为 GB2312 字库比 GBK 字库小，有些偏僻字（例如：洺）不能保存，因此在选择字符集的时候一定要权衡这些偏僻字出现的几率，一般情况下，最好选用 GBK。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;字符（Character）是计算机中字母、数字、符号的统称，一个字符可以是一个中文汉字、一个英文字母、一个阿拉伯数字、一个标点符号等。&lt;/p&gt;
&lt;p&gt;计算机是以二进制的形式来存储数据的。平时我们在显示器上看到的数字、英文、标点符号、汉字等字符都是二进制数转换之后的结果。&lt;/
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>MySQL/MySQL锁机制</title>
    <link href="http://yoursite.com/2021/07/14/MySQL/MySQL%E9%94%81%E6%9C%BA%E5%88%B6/"/>
    <id>http://yoursite.com/2021/07/14/MySQL/MySQL%E9%94%81%E6%9C%BA%E5%88%B6/</id>
    <published>2021-07-14T06:20:51.947Z</published>
    <updated>2021-07-14T06:20:51.947Z</updated>
    
    <summary type="html">
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>MySQL/MySQL事务</title>
    <link href="http://yoursite.com/2021/07/14/MySQL/MySQL%E4%BA%8B%E5%8A%A1/"/>
    <id>http://yoursite.com/2021/07/14/MySQL/MySQL%E4%BA%8B%E5%8A%A1/</id>
    <published>2021-07-14T06:13:34.389Z</published>
    <updated>2021-07-14T07:25:37.354Z</updated>
    
    <content type="html"><![CDATA[<h1 id="为什么需要事务"><a href="#为什么需要事务" class="headerlink" title="为什么需要事务"></a>为什么需要事务</h1><p>在银行业务中，有一条记账原则，即有借有贷，借贷相等。为了保证这种原则，每发生一笔银行业务，就必须确保会计账目上借方科目和贷方科目至少各记一笔，并且这两笔账要么同时成功，要么同时失败。如果出现只记录了借方科目，或者只记录了贷方科目的情况，就违反了记账原则。会出现记错账的情况。</p><p>在银行的日常业务中，只要是同一银行（如都是中国农业银行，简称农行），一般都支持账户间的直接转账。因此，银行转账操作往往会涉及两个或两个以上的账户。在转出账户的存款减少一定金额的同时，转入账户的存款就要增加相应的金额。</p><p>下面，在 MySQL 数据库中模拟一下上述提及的转账问题。</p><p>假如要从张三的账户直接转账 500 元到李四的账户。首先需要创建账户表，存放用户张三和李四的账户信息。创建账户表和插入数据的 SQL 语句和运行结果如下所示：<br>mysql&gt; CREATE DATABASE mybank;<br>Query OK, 1 row affected (0.02 sec)<br>mysql&gt; USE mybank;<br>Database changed<br>mysql&gt; CREATE TABLE bank(<br>    -&gt; customerName VARCHAR(20),   #用户名<br>    -&gt; currentMoney DECIMAL(10,2)    #当前余额<br>    -&gt; )ENGINE=InnoDB DEFAULT CHARSET=utf8;<br>Query OK, 0 rows affected (0.26 sec)</p><p>mysql&gt; INSERT INTO bank (customerName,currentMoney) VALUES(‘张三’,1000);;<br>Query OK, 1 row affected (0.07 sec)</p><p>mysql&gt; INSERT INTO bank (customerName,currentMoney) VALUES(‘李四’,1);<br>Query OK, 1 row affected (0.08 sec)<br>查询 bank 数据表的 SQL 语句和运行结果如下：<br>mysql&gt; SELECT * FROM bank;<br>+————–+————–+<br>| customerName | currentMoney |<br>+————–+————–+<br>| 张三         |      1000.00 |<br>| 李四         |         1.00 |<br>+————–+————–+<br>2 rows in set (0.02 sec)<br>结果显示，张三和李四两个账户的余额总和为 1000+1=1001 元。</p><p>下面开始模拟实现转账功能。从张三的账户直接转账 500 元到李四的账户，可以使用 UPDATE 语句分别修改张三的账户和李四的账户。张三的账户减少 500 元，李四的账户增加 500 元， SQL 语句如下所示：<br>/<em>转账测试：张三转账给李四 500 元</em>/<br>#张三的账户少 500 元，李四的账户多 500 元<br>UPDATE bank SET currentMoney = currentMoney-500 WHERE customerName = ‘张三’;<br>UPDATE bank SET currentMoney = currentMoney+500 WHERE customerName = ‘李四’;<br>正常情况下，执行以上的转账操作后，余额总和应保持不变，仍为 1001 元。但是，如果在这个过程的其中一个环节出现差错，如在张三的账户减少 500 元之后，这时发生了服务器故障，李四的账户没有立即增加 500 元，此时，第三方读取到两个账户的余额总和变为 500+1=501 元，即账户总额间少了 500 元。</p><p>MySQL 为了解决此类问题，提供了事务。事务可以将一系列的数据操作捆绑成一个整体进行统一管理，如果某一事务执行成功，则在该事务中进行的所有数据更改均会提交，成为数据库中的永久组成部分。如果事务执行时遇到错误，则就必须取消或回滚。取消或回滚后，数据将全部恢复到操作前的状态，所有数据的更改均被清除。</p><p>MySQL 通过事务保证了数据的一致性。上述提到的转账过程就是一个事务，它需要两条 UPDATE 语句来完成。这两条语句是一个整体，如果其中任何一个环节出现问题，则整个转账业务也应取消，两个账户中的余额应恢复为原来的数据，从而确保转账前和转账后的余额总和不变，即都是 1001 元。</p><h1 id="数据库事务的概念和特性"><a href="#数据库事务的概念和特性" class="headerlink" title="数据库事务的概念和特性"></a>数据库事务的概念和特性</h1><p>数据库的事务（Transaction）是一种机制、一个操作序列，包含了一组数据库操作命令。事务把所有的命令作为一个整体一起向系统提交或撤销操作请求，即这一组数据库命令要么都执行，要么都不执行，因此事务是一个不可分割的工作逻辑单元。</p><p>在数据库系统上执行并发操作时，事务是作为最小的控制单元来使用的，特别适用于多用户同时操作的数据库系统。例如，航空公司的订票系统、银行、保险公司以及证券交易系统等。</p><p>事务具有 4 个特性，即原子性（Atomicity）、一致性（Consistency）、隔离性（Isolation）和持久性（Durability），这 4 个特性通常简称为 ACID。</p><ol><li>原子性<br>事务是一个完整的操作。事务的各元素是不可分的（原子的）。事务中的所有元素必须作为一个整体提交或回滚。如果事务中的任何元素失败，则整个事务将失败。</li></ol><p>以银行转账事务为例，如果该事务提交了，则这两个账户的数据将会更新。如果由于某种原因，事务在成功更新这两个账户之前终止了，则不会更新这两个账户的余额，并且会撤销对任何账户余额的修改，事务不能部分提交。<br>2. 一致性<br>当事务完成时，数据必须处于一致状态。也就是说，在事务开始之前，数据库中存储的数据处于一致状态。在正在进行的事务中. 数据可能处于不一致的状态，如数据可能有部分被修改。然而，当事务成功完成时，数据必须再次回到已知的一致状态。通过事务对数据所做的修改不能损坏数据，或者说事务不能使数据存储处于不稳定的状态。</p><p>以银行转账事务事务为例。在事务开始之前，所有账户余额的总额处于一致状态。在事务进行的过程中，一个账户余额减少了，而另一个账户余额尚未修改。因此，所有账户余额的总额处于不一致状态。事务完成以后，账户余额的总额再次恢复到一致状态。<br>3. 隔离性<br>对数据进行修改的所有并发事务是彼此隔离的，这表明事务必须是独立的，它不应以任何方式依赖于或影响其他事务。修改数据的事务可以在另一个使用相同数据的事务开始之前访问这些数据，或者在另一个使用相同数据的事务结束之后访问这些数据。</p><p>另外，当事务修改数据时，如果任何其他进程正在同时使用相同的数据，则直到该事务成功提交之后，对数据的修改才能生效。张三和李四之间的转账与王五和赵二之间的转账，永远是相互独立的。<br>4. 持久性<br>事务的持久性指不管系统是否发生了故障，事务处理的结果都是永久的。</p><p>一个事务成功完成之后，它对数据库所作的改变是永久性的，即使系统出现故障也是如此。也就是说，一旦事务被提交，事务对数据所做的任何变动都会被永久地保留在数据库中。</p><p>事务的 ACID 原则保证了一个事务或者成功提交，或者失败回滚，二者必居其一。因此，它对事务的修改具有可恢复性。即当事务失败时，它对数据的修改都会恢复到该事务执行前的状态。</p><h1 id="执行事务的语法和流程"><a href="#执行事务的语法和流程" class="headerlink" title="执行事务的语法和流程"></a>执行事务的语法和流程</h1><p>MySQL 提供了多种存储引擎来支持事务。支持事务的存储引擎有 InnoDB 和 BDB，其中，InnoDB 存储引擎事务主要通过 UNDO 日志和 REDO 日志实现，MyISAM 存储引擎不支持事务。</p><p>拓展：任何一种数据库，都会拥有各种各样的日志，用来记录数据库的运行情况、日常操作、错误信息等，MySQL 也不例外。例如，当用户 root 登录到 MySQL 服务器，就会在日志文件里记录该用户的登录时间、执行操作等。</p><p>为了维护 MySQL 服务器，经常需要在 MySQL 数据库中进行日志操作：<br>UNDO 日志：复制事务执行前的数据，用于在事务发生异常时回滚数据。<br>REDO 日志：记录在事务执行中，每条对数据进行更新的操作，当事务提交时，该内容将被刷新到磁盘。</p><p>默认设置下，每条 SQL 语句就是一个事务，即执行 SQL 语句后自动提交。为了达到将几个操作做为一个整体的目的，需要使用 BEGIN 或 START TRANSACTION 开启一个事务，或者禁止当前会话的自动提交。<br>关于事务的自动提交可阅读学习《MySQL设置事务自动提交》一节。</p><p>执行事务的语法和流程<br>SQL 使用下列语句来管理事务。</p><p>1) 开始事务<br>BEGIN;</p><p>或<br>START TRANSACTION;</p><p>这个语句显式地标记一个事务的起始点。<br>2) 提交事务<br>MySQL 使用下面的语句来提交事务：<br>COMMIT;</p><p>COMMIT 表示提交事务，即提交事务的所有操作，具体地说，就是将事务中所有对数据库的更新都写到磁盘上的物理数据库中，事务正常结束。</p><p>提交事务，意味着将事务开始以来所执行的所有数据都修改成为数据库的永久部分，因此也标志着一个事务的结束。一旦执行了该命令，将不能回滚事务。只有在所有修改都准备好提交给数据库时，才执行这一操作。<br>3) 回滚（撤销）事务<br>MySQL 使用以下语句回滚事务：<br>ROLLBACK;</p><p>ROLLBACK 表示撤销事务，即在事务运行的过程中发生了某种故障，事务不能继续执行，系统将事务中对数据库的所有已完成的操作全部撤销，回滚到事务开始时的状态。这里的操作指对数据库的更新操作。</p><p>当事务执行过程中遇到错误时，使用 ROLLBACK 语句使事务回滚到起点或指定的保持点处。同时，系统将清除自事务起点或到某个保存点所做的所有的数据修改，并且释放由事务控制的资源。因此，这条语句也标志着事务的结束。<br>总结<br>BEGIN 或 START TRANSACTION 语句后面的 SQL 语句对数据库数据的更新操作都将记录在事务日志中，直至遇到 ROLLBACK 语句或 COMMIT 语句。如果事务中某一操作失败且执行了 ROLLBACK 语句，那么在开启事务语句之后所有更新的数据都能回滚到事务开始前的状态。如果事务中的所有操作都全部正确完成，并且使用了 COMMIT 语句向数据库提交更新数据，则此时的数据又处在新的一致状态。<br>实例演示<br>下面通过两个例子来演示一下 MySQL 事务的具体用法。<br>示例 1<br>下面模拟在张三的账户减少 500 元后，李四的账户还未增加 500 时，有其他会话访问数据表的场景。由于代码需要在两个窗口中执行，为了方便阅读，这里我们称为 A 窗口和 B 窗口。</p><p>1) 在 A 窗口中开启一个事务，并更新 mybank 数据库中 bank 表的数据，SQL 语句和运行结果如下：<br>mysql&gt; USE mybank;<br>Database changed<br>mysql&gt; BEGIN;<br>Query OK, 0 rows affected (0.00 sec)<br>mysql&gt; UPDATE bank SET currentMoney = currentMoney-500<br>    -&gt; WHERE customerName=’张三’;<br>Query OK, 1 row affected (0.05 sec)<br>Rows matched: 1  Changed: 1  Warnings: 0</p><p>2) 在 B 窗口中查询 bank 数据表中的数据，SQL 语句和运行结果如下：<br>mysql&gt; SELECT * FROM mybank.bank;<br>+————–+————–+<br>| customerName | currentMoney |<br>+————–+————–+<br>| 张三         |      1000.00 |<br>| 李四         |         1.00 |<br>+————–+————–+<br>2 rows in set (0.00 sec)<br>从结果可以看出，虽然 A 窗口中的事务已经更改了 bank 表中的数据，但没有立即更新数据，这时其他会话读取到的仍然是更新前的数据。</p><p>3) 在 A 窗口中继续执行事务并提交事务，SQL 语句和运行结果如下：<br>mysql&gt; UPDATE bank SET currentMoney = currentMoney+500<br>    -&gt; WHERE customerName=’李四’;<br>Query OK, 1 row affected (0.05 sec)<br>Rows matched: 1  Changed: 1  Warnings: 0<br>mysql&gt; COMMIT;<br>Query OK, 0 rows affected (0.07 sec)</p><p>4) 在 B 窗口中再次查询 bank 数据表的数据，SQL 语句和运行结果如下：<br>mysql&gt; SELECT * FROM mybank.bank;<br>+————–+————–+<br>| customerName | currentMoney |<br>+————–+————–+<br>| 张三         |       500.00 |<br>| 李四         |       501.00 |<br>+————–+————–+<br>2 rows in set (0.00 sec)<br>在 A 窗口中执行 COMMIT 提交事务后，对数据所做的更新将一起提交，其他会话读取到的是更新后的数据。从结果可以看出张三和李四的总账户余额和转账前保持一致，这样数据从一个一致性状态更新到另一个一致性状态。</p><p>前面提到，当事务在执行中出现问题，也就是不能按正常的流程执行一个完整的事务时，可以使用 ROLLBACK 语句进行回滚，使用数据恢复到初始状态。</p><p>在例 1 中，张三的账户余额已经减少到 500 元，如果再转出 1000 元，将会出现余额为负数，因此需要回滚到原始状态。如例 2 所示。<br>示例 2<br>将张三的账户余额减少 1000 元，并让事务回滚，SQL 语句和运行结果如下所示：<br>mysql&gt; BEGIN;<br>Query OK, 0 rows affected (0.00 sec)</p><p>mysql&gt; UPDATE bank SET currentMoney = currentMoney-1000 WHERE customerName=’张三’;<br>Query OK, 1 row affected (0.04 sec)<br>Rows matched: 1  Changed: 1  Warnings: 0</p><p>mysql&gt; ROLLBACK;<br>Query OK, 0 rows affected (0.07 sec)</p><p>mysql&gt; SELECT * FROM mybank.bank;<br>+————–+————–+<br>| customerName | currentMoney |<br>+————–+————–+<br>| 张三         |       500.00 |<br>| 李四         |       501.00 |<br>+————–+————–+<br>2 rows in set (0.00 sec)<br>从结果可以看出，执行事务回滚后，账户数据恢复到初始状态，即该事务执行之前的状态。<br>拓展<br>在数据库操作中，为了有效保证并发读取数据的正确性，提出了事务的隔离级别。在例 1 和例 2 的演示中，事务的隔离级别为默认隔离级别。在 MySQL 中，事务的默认隔离级别是 REPEATABLE-READ （可重读）隔离级别，即事务未结束时（未执行 COMMIT 或 ROLLBACK），其它会话只能读取到未提交数据。</p><p>请猛击《MySQL事务隔离级别》了解更多内容。<br>注意事项<br>MySQL 事务是一项非常消耗资源的功能，大家在使用过程中要注意以下几点。</p><p>1) 事务尽可能简短<br>事务的开启到结束会在数据库管理系统中保留大量资源，以保证事务的原子性、一致性、隔离性和持久性。如果在多用户系统中，较大的事务将会占用系统的大量资源，使得系统不堪重负，会影响软件的运行性能，甚至导致系统崩溃。<br>2) 事务中访问的数据量尽量最少<br>当并发执行事务处理时，事务操作的数据量越少，事务之间对相同数据的操作就越少。<br>3) 查询数据时尽量不要使用事务<br>对数据进行浏览查询操作并不会更新数据库的数据，因此应尽量不使用事务查询数据，避免占用过量的系统资源。<br>4) 在事务处理过程中尽量不要出现等待用户输入的操作<br>在处理事务的过程中，如果需要等待用户输入数据，那么事务会长时间地占用资源，有可能造成系统阻塞。</p><h1 id="设置事务自动提交（开启和关闭）"><a href="#设置事务自动提交（开启和关闭）" class="headerlink" title="设置事务自动提交（开启和关闭）"></a>设置事务自动提交（开启和关闭）</h1><p>MySQL 默认开启事务自动提交模式，即除非显式的开启事务（BEGIN 或 START TRANSACTION），否则每条 SOL 语句都会被当做一个单独的事务自动执行。但有些情况下，我们需要关闭事务自动提交来保证数据的一致性。下面主要介绍如何设置事务自动提交模式。</p><p>在 MySQL 中，可以通过  SHOW VARIABLES 语句查看当前事务自动提交模式，如下所示：<br>mysql&gt; SHOW VARIABLES LIKE ‘autocommit’;<br>+—————+——-+<br>| Variable_name | Value |<br>+—————+——-+<br>| autocommit    | ON    |<br>+—————+——-+<br>1 row in set, 1 warning (0.04 sec)<br>结果显示，autocommit 的值是 ON，表示系统开启自动提交模式。</p><p>在 MySQL 中，可以使用 SET autocommit 语句设置事务的自动提交模式，语法格式如下：<br>SET autocommit = 0|1|ON|OFF;</p><p>对取值的说明：<br>值为 0 和值为 OFF：关闭事务自动提交。如果关闭自动提交，用户将会一直处于某个事务中，只有提交或回滚后才会结束当前事务，重新开始一个新事务。<br>值为 1 和值为 ON：开启事务自动提交。如果开启自动提交，则每执行一条 SQL 语句，事务都会提交一次。<br>示例<br>下面我们关闭事务自动提交，模拟银行转账。</p><p>使用 SET autocommit 语句关闭事务自动提交，且张三转给李四 500 元，SQL 语句和运行结果如下：<br>mysql&gt; SET autocommit = 0;                                                      ;<br>Query OK, 0 rows affected (0.00 sec)<br>mysql&gt; SELECT * FROM mybank.bank;<br>+————–+————–+<br>| customerName | currentMoney |<br>+————–+————–+<br>| 张三         |      1000.00 |<br>| 李四         |         1.00 |<br>+————–+————–+<br>2 rows in set (0.00 sec)<br>mysql&gt; UPDATE bank SET currentMoney = currentMoney-500 WHERE customerName=’张三’ ;<br>Query OK, 1 row affected (0.02 sec)<br>Rows matched: 1  Changed: 1  Warnings: 0<br>mysql&gt; UPDATE bank SET currentMoney = currentMoney+500 WHERE customerName=’李四’;<br>Query OK, 1 row affected (0.00 sec)<br>Rows matched: 1  Changed: 1  Warnings: 0</p><p>这时重新打开一个 cmd 窗口，查看 bank 数据表中张三和李四的余额，SQL 语句和运行结果如下所示：<br>mysql&gt; SELECT * FROM mybank.bank;<br>+————–+————–+<br>| customerName | currentMoney |<br>+————–+————–+<br>| 张三         |      1000.00 |<br>| 李四         |         1.00 |<br>+————–+————–+<br>2 rows in set (0.00 sec)<br>结果显示，张三和李四的余额是事务执行前的数据。</p><p>下面在之前的窗口中使用 COMMIT 语句提交事务，并查询 bank 数据表的数据，如下所示：<br>mysql&gt; COMMIT;<br>Query OK, 0 rows affected (0.07 sec)<br>mysql&gt; SELECT * FROM mybank.bank;<br>+————–+————–+<br>| customerName | currentMoney |<br>+————–+————–+<br>| 张三         |       500.00 |<br>| 李四         |       501.00 |<br>+————–+————–+<br>2 rows in set (0.00 sec)<br>结果显示，bank 数据表的数据更新成功。</p><p>在本例中，关闭自动提交后，该位置会作为一个事务起点，直到执行 COMMIT 语句和 ROLLBACK 语句后，该事务才结束。结束之后，这就是下一个事务的起点。</p><p>关闭自动提交功能后，只用当执行 COMMIT 命令后，MySQL 才将数据表中的资料提交到数据库中。如果执行 ROLLBACK 命令，数据将会被回滚。如果不提交事务，而终止 MySQL 会话，数据库将会自动执行回滚操作。</p><p>使用 BEGIN 或 START TRANSACTION 开启一个事务之后，自动提交将保持禁用状态，直到使用 COMMIT 或 ROLLBACK 结束事务。之后，自动提交模式会恢复到之前的状态，即如果 BEGIN 前 autocommit = 1，则完成本次事务后 autocommit 还是 1。如果 BEGIN 前 autocommit = 0，则完成本次事务后 autocommit 还是 0。</p><h1 id="事务隔离级别"><a href="#事务隔离级别" class="headerlink" title="事务隔离级别"></a>事务隔离级别</h1><p>在《数据库事务》一节中介绍了 MySQL 事务的四大特性，其中事务的隔离性就是指当多个事务同时运行时，各事务之间相互隔离，不可互相干扰。如果事务没有隔离性，就容易出现脏读、不可重复读和幻读等情况。<br>为了保证并发时操作数据的正确性，数据库都会有事务隔离级别的概念。</p><p>1) 脏读<br>脏读是指一个事务正在访问数据，并且对数据进行了修改，但是这种修改还没有提交到数据库中，这时，另外一个事务也访问这个数据，然后使用了这个数据。<br>2) 不可重复读<br>不可重复读是指在一个事务内，多次读取同一个数据。</p><p>在这个事务还没有结束时，另外一个事务也访问了该同一数据。那么，在第一个事务中的两次读数据之间，由于第二个事务的修改，那么第一个事务两次读到的的数据可能是不一样的。这样在一个事务内两次读到的数据是不一样的，因此称为是不可重复读。<br>3) 幻读<br>幻读是指当事务不是独立执行时发生的一种现象，例如第一个事务对一个表中的数据进行了修改，这种修改涉及到表中的全部数据行。同时，第二个事务也修改这个表中的数据，这种修改是向表中插入一行新数据。那么，以后就会发生操作第一个事务的用户发现表中还有没有修改的数据行，就好象发生了幻觉一样。</p><p>为了解决以上这些问题，标准 SQL 定义了 4 类事务隔离级别，用来指定事务中的哪些数据改变是可见的，哪些数据改变是不可见的。</p><p>MySQL 包括的事务隔离级别如下：<br>读未提交（READ UNCOMITTED）<br>读提交（READ COMMITTED）<br>可重复读（REPEATABLE READ）<br>串行化（SERIALIZABLE）</p><p>MySQL 事务隔离级别可能产生的问题如下表所示：</p><p>隔离级别    脏读    不可重复读    幻读<br>READ UNCOMITTED    √    √    √<br>READ COMMITTED    ×    √    √<br>REPEATABLE READ    ×    ×    √<br>SERIALIZABLE    ×    ×    ×<br>MySQL 的事务的隔离级别由低到高分别为 READ UNCOMITTED、READ COMMITTED、REPEATABLE READ、SERIALIZABLE。低级别的隔离级别可以支持更高的并发处理，同时占用的系统资源更少。</p><p>下面根据实例来一一阐述它们的概念和联系。</p><ol><li>读未提交（READ UNCOMITTED，RU）<br>顾名思义，读未提交就是可以读到未提交的内容。</li></ol><p>如果一个事务读取到了另一个未提交事务修改过的数据，那么这种隔离级别就称之为读未提交。</p><p>在该隔离级别下，所有事务都可以看到其它未提交事务的执行结果。因为它的性能与其他隔离级别相比没有高多少，所以一般情况下，该隔离级别在实际应用中很少使用。</p><p>例 1 主要演示了在读未提交隔离级别中产生的脏读现象。<br>示例 1</p><p>1) 先在 test 数据库中创建 testnum 数据表，并插入数据。SQL 语句和执行结果如下：<br>mysql&gt; CREATE TABLE testnum(<br>    -&gt; num INT(4));<br>Query OK, 0 rows affected (0.57 sec)<br>mysql&gt; INSERT INTO test.testnum (num) VALUES(1),(2),(3),(4),(5);<br>Query OK, 5 rows affected (0.09 sec)</p><p>2) 下面的语句需要在两个命令行窗口中执行。为了方便理解，我们分别称之为 A 窗口和 B 窗口。</p><p>在 A 窗口中修改事务隔离级别，因为 A 窗口和 B 窗口的事务隔离级别需要保持一致，所以我们使用 SET GLOBAL TRANSACTION 修改全局变量。SQL 语句如下：<br>mysql&gt; SET GLOBAL TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;<br>Query OK, 0 rows affected (0.04 sec)<br>flush privileges;<br>Query OK, 0 rows affected (0.04 sec)</p><p>查询事务隔离级别，SQL 语句和运行结果如下：<br>mysql&gt; show variables like ‘%tx_isolation%’\G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>Variable_name: tx_isolation<br>        Value: READ-UNCOMMITTED<br>1 row in set, 1 warning (0.00 sec)<br>结果显示，现在 MySQL 的事务隔离级别为 READ-UNCOMMITTED。</p><p>3) 在 A 窗口中开启一个事务，并查询 testnum 数据表，SQL 语句和运行结果如下：<br>mysql&gt; BEGIN;<br>Query OK, 0 rows affected (0.00 sec)<br>mysql&gt; SELECT * FROM testnum;<br>+——+<br>| num  |<br>+——+<br>|    1 |<br>|    2 |<br>|    3 |<br>|    4 |<br>|    5 |<br>+——+<br>5 rows in set (0.00 sec)</p><p>4) 打开 B 窗口，查看当前 MySQL 的事务隔离级别，SQL 语句如下：<br>mysql&gt; show variables like ‘%tx_isolation%’\G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>Variable_name: tx_isolation<br>        Value: READ-UNCOMMITTED<br>1 row in set, 1 warning (0.00 sec)</p><p>确定事务隔离级别是 READ-UNCOMMITTED 后，开启一个事务，并使用 UPDATE 语句更新 testnum 数据表，SQL 语句和运行结果如下：<br>mysql&gt; BEGIN;<br>Query OK, 0 rows affected (0.00 sec)<br>mysql&gt; UPDATE test.testnum SET num=num*2 WHERE num=2;<br>Query OK, 1 row affected (0.02 sec)<br>Rows matched: 1  Changed: 1  Warnings: 0</p><p>5) 现在返回 A 窗口，再次查询 testnum 数据表，SQL 语句和运行结果如下：<br>mysql&gt; SELECT * FROM testnum;<br>+——+<br>| num  |<br>+——+<br>|    1 |<br>|    4 |<br>|    3 |<br>|    4 |<br>|    5 |<br>+——+<br>5 rows in set (0.02 sec)<br>由结果可以看出，A 窗口中的事务读取到了更新后的数据。</p><p>6) 下面在 B 窗口中回滚事务，SQL 语句和运行结果如下：<br>mysql&gt; ROLLBACK;<br>Query OK, 0 rows affected (0.09 sec)</p><p>7) 在 A 窗口中查询 testnum 数据表，SQL 语句和运行结果如下：<br>mysql&gt; SELECT * FROM testnum;<br>+——+<br>| num  |<br>+——+<br>|    1 |<br>|    2 |<br>|    3 |<br>|    4 |<br>|    5 |<br>+——+<br>5 rows in set (0.00 sec)</p><p>当 MySQL 的事务隔离级别为 READ UNCOMITTED 时，首先分别在 A 窗口和 B 窗口中开启事务，在 B 窗口中的事务更新但未提交之前， A 窗口中的事务就已经读取到了更新后的数据。但由于 B 窗口中的事务回滚了，所以 A 事务出现了脏读现象。</p><p>使用读提交隔离级别可以解决实例中产生的脏读问题。<br>2. 读提交（READ COMMITTED，RC）<br>顾名思义，读提交就是只能读到已经提交了的内容。</p><p>如果一个事务只能读取到另一个已提交事务修改过的数据，并且其它事务每对该数据进行一次修改并提交后，该事务都能查询得到最新值，那么这种隔离级别就称之为读提交。</p><p>该隔离级别满足了隔离的简单定义：一个事务从开始到提交前所做的任何改变都是不可见的，事务只能读取到已经提交的事务所做的改变。</p><p>这是大多数数据库系统的默认事务隔离级别（例如 Oracle、SQL Server），但不是 MySQL 默认的。</p><p>例 2 演示了在读提交隔离级别中产生的不可重复读问题。<br>示例 2</p><p>1) 使用 SET 语句将 MySQL 事务隔离级别修改为 READ COMMITTED，并查看。SQL 语句和运行结果如下：<br>mysql&gt; SET GLOBAL TRANSACTION ISOLATION LEVEL READ COMMITTED;<br>Query OK, 0 rows affected (0.00 sec)<br>mysql&gt; show variables like ‘%tx_isolation%’\G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>Variable_name: tx_isolation<br>        Value: READ-COMMITTED<br>1 row in set, 1 warning (0.00 sec)</p><p>2) 确定当前事务隔离级别为 READ COMMITTED 后，开启一个事务，SQL 语句和运行结果如下：<br>mysql&gt; BEGIN;<br>Query OK, 0 rows affected (0.00 sec)</p><p>3) 在 B 窗口中开启事务，并使用 UPDATE 语句更新 testnum 数据表，SQL 语句和运行结果如下：<br>mysql&gt; BEGIN;<br>Query OK, 0 rows affected (0.00 sec)</p><p>mysql&gt;  UPDATE test.testnum SET num=num*2 WHERE num=2;<br>Query OK, 1 row affected (0.07 sec)<br>Rows matched: 1  Changed: 1  Warnings: 0</p><p>4) 在 A 窗口中查询 testnum 数据表，SQL 语句和运行结果如下：<br>mysql&gt; SELECT * from test.testnum;<br>+——+<br>| num  |<br>+——+<br>|    1 |<br>|    2 |<br>|    3 |<br>|    4 |<br>|    5 |<br>+——+<br>5 rows in set (0.00 sec)</p><p>5) 提交 B 窗口中的事务，SQL 语句和运行结果如下：<br>mysql&gt; COMMIT;<br>Query OK, 0 rows affected (0.07 sec)</p><p>6) 在 A 窗口中查询 testnum 数据表，SQL 语句和运行结果如下：<br>mysql&gt; SELECT * from test.testnum;<br>+——+<br>| num  |<br>+——+<br>|    1 |<br>|    4 |<br>|    3 |<br>|    4 |<br>|    5 |<br>+——+<br>5 rows in set (0.00 sec)</p><p>当 MySQL 的事务隔离级别为 READ COMMITTED 时，首先分别在 A 窗口和 B 窗口中开启事务，在 B 窗口中的事务更新并提交后，A 窗口中的事务读取到了更新后的数据。在该过程中，A 窗口中的事务必须要等待 B 窗口中的事务提交后才能读取到更新后的数据，这样就解决了脏读问题。而处于 A 窗口中的事务出现了不同的查询结果，即不可重复读现象。</p><p>使用可重复读隔离级别可以解决实例中产生的不可重复读问题。<br>3. 可重复读（REPEATABLE READ，RR）<br>顾名思义，可重复读是专门针对不可重复读这种情况而制定的隔离级别，可以有效的避免不可重复读。</p><p>在一些场景中，一个事务只能读取到另一个已提交事务修改过的数据，但是第一次读过某条记录后，即使其它事务修改了该记录的值并且提交，之后该事务再读该条记录时，读到的仍是第一次读到的值，而不是每次都读到不同的数据。那么这种隔离级别就称之为可重复读。</p><p>可重复读是 MySQL 的默认事务隔离级别，它能确保同一事务的多个实例在并发读取数据时，会看到同样的数据行。在该隔离级别下，如果有事务正在读取数据，就不允许有其它事务进行修改操作，这样就解决了可重复读问题。</p><p>例 3 演示了在可重复读隔离级别中产生的幻读问题。<br>示例 3</p><p>1) 在 test 数据库中创建 testuser 数据表，SQL 语句和执行结果如下：<br>mysql&gt; CREATE TABLE testuser(<br>    -&gt; id INT (4) PRIMARY KEY,<br>    -&gt; name VARCHAR(20));<br>Query OK, 0 rows affected (0.29 sec)</p><p>2) 使用 SET 语句修改事务隔离级别，SQL 语句如下：<br>mysql&gt; SET GLOBAL TRANSACTION ISOLATION LEVEL REPEATABLE READ;<br>Query OK, 0 rows affected (0.00 sec)</p><p>3) 在 A 窗口中开启事务，并查询 testuser 数据表，SQL 语句和运行结果如下：<br>mysql&gt; BEGIN;<br>Query OK, 0 rows affected (0.00 sec)</p><p>mysql&gt; SELECT * FROM test.testuser where id=1;<br>Empty set (0.04 sec)</p><p>4) 在 B 窗口中开启一个事务，并向 testuser 表中插入一条数据，SQL 语句和运行结果如下：<br>mysql&gt; BEGIN;<br>Query OK, 0 rows affected (0.00 sec)<br>mysql&gt;  INSERT INTO test.testuser VALUES(1,’zhangsan’);<br>Query OK, 1 row affected (0.04 sec)<br>mysql&gt; COMMIT;<br>Query OK, 0 rows affected (0.06 sec)</p><p>5) 现在返回 A 窗口，向 testnum 数据表中插入数据，SQL 语句和运行结果如下：<br>mysql&gt; INSERT INTO test.testuser VALUES(1,’lisi’);<br>ERROR 1062 (23000): Duplicate entry ‘1’ for key ‘PRIMARY’<br>mysql&gt;  SELECT * FROM test.testuser where id=1;<br>Empty set (0.00 sec)</p><p>使用串行化隔离级别可以解决实例中产生的幻读问题。<br>4. 串行化（SERIALIZABLE）<br>如果一个事务先根据某些条件查询出一些记录，之后另一个事务又向表中插入了符合这些条件的记录，原先的事务再次按照该条件查询时，能把另一个事务插入的记录也读出来。那么这种隔离级别就称之为串行化。</p><p>SERIALIZABLE 是最高的事务隔离级别，主要通过强制事务排序来解决幻读问题。简单来说，就是在每个读取的数据行上加上共享锁实现，这样就避免了脏读、不可重复读和幻读等问题。但是该事务隔离级别执行效率低下，且性能开销也最大，所以一般情况下不推荐使用。</p><h1 id="查看和修改事务隔离级别"><a href="#查看和修改事务隔离级别" class="headerlink" title="查看和修改事务隔离级别"></a>查看和修改事务隔离级别</h1><p>查看事务隔离级别<br>在 MySQL 中，可以通过show variables like ‘%tx_isolation%’或select @@tx_isolation;语句来查看当前事务隔离级别。</p><p>查看当前事务隔离级别的 SQL 语句和运行结果如下：<br>mysql&gt; show variables like ‘%tx_isolation%’;<br>+—————+—————–+<br>| Variable_name | Value           |<br>+—————+—————–+<br>| tx_isolation  | REPEATABLE-READ |<br>+—————+—————–+<br>1 row in set, 1 warning (0.17 sec）<br>mysql&gt; select @@tx_isolation;<br>+—————–+<br>| @@tx_isolation  |<br>+—————–+<br>| REPEATABLE-READ |<br>+—————–+<br>1 row in set, 1 warning (0.00 sec)<br>结果显示，目前 MySQL 的事务隔离级别是 REPEATABLE-READ。</p><p>另外，还可以使用下列语句分别查询全局和会话的事务隔离级别：<br>SELECT @@global.tx_isolation;<br>SELECT @@session.tx_isolation;<br>提示：在MySQL 8.0.3 中，tx_isolation 变量被 transaction_isolation 变量替换了。在 MySQL 8.0.3 版本中查询事务隔离级别，只要把上述查询语句中的 tx_isolation 变量替换成 transaction_isolation 变量即可。<br>修改事务隔离级别<br>MySQL 提供了 SET TRANSACTION 语句，该语句可以改变单个会话或全局的事务隔离级别。语法格式如下：<br>SET [SESSION | GLOBAL] TRANSACTION ISOLATION LEVEL {READ UNCOMMITTED | READ COMMITTED | REPEATABLE READ | SERIALIZABLE}</p><p>其中，SESSION 和 GLOBAL 关键字用来指定修改的事务隔离级别的范围：<br>SESSION：表示修改的事务隔离级别将应用于当前 session（当前 cmd 窗口）内的所有事务；<br>GLOBAL：表示修改的事务隔离级别将应用于所有 session（全局）中的所有事务，且当前已经存在的 session 不受影响；<br>如果省略 SESSION 和 GLOBAL，表示修改的事务隔离级别将应用于当前 session 内的下一个还未开始的事务。</p><p>任何用户都能改变会话的事务隔离级别，但是只有拥有 SUPER 权限的用户才能改变全局的事务隔离级别。</p><p>如果使用普通用户修改全局事务隔离级别，就会提示需要超级权限才能执行此操作的错误信息，SQL 语句和运行结果如下：<br>C:\Users\leovo&gt;mysql -utestuser -p<br>Enter password: <strong>**</strong><br>Welcome to the MySQL monitor.  Commands end with ; or \g.<br>Your MySQL connection id is 41<br>Server version: 5.7.29-log MySQL Community Server (GPL)</p><p>Copyright (c) 2000, 2020, Oracle and/or its affiliates. All rights reserved.</p><p>Oracle is a registered trademark of Oracle Corporation and/or its<br>affiliates. Other names may be trademarks of their respective<br>owners.</p><p>Type ‘help;’ or ‘\h’ for help. Type ‘\c’ to clear the current input statement.</p><p>mysql&gt; SET GLOBAL TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;<br>ERROR 1227 (42000): Access denied; you need (at least one of) the SUPER privilege(s) for this operation<br>mysql&gt; SET SESSION TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;<br>Query OK, 0 rows affected (0.00 sec)<br>示例 1<br>使用 SET TRANSACTION 语句分别修改 session 和全局的事务隔离级别SQL 语句和运行结果如下：<br>mysql&gt;  select @@session.tx_isolation;<br>+————————+<br>| @@session.tx_isolation |<br>+————————+<br>| SERIALIZABLE           |<br>+————————+<br>1 row in set, 1 warning (0.00 sec)</p><p>mysql&gt; SET GLOBAL TRANSACTION ISOLATION LEVEL REPEATABLE READ;<br>Query OK, 0 rows affected (0.00 sec)</p><p>mysql&gt;  select @@global.tx_isolation;<br>+———————–+<br>| @@global.tx_isolation |<br>+———————–+<br>| REPEATABLE-READ       |<br>+———————–+<br>1 row in set, 1 warning (0.00 sec)</p><p>还可以使用 set tx_isolation 命令直接修改当前 session 的事务隔离级别，SQL 语句和运行结果如下：<br>mysql&gt; set tx_isolation=’READ-COMMITTED’;<br>Query OK, 0 rows affected, 1 warning (0.00 sec)</p><p>mysql&gt; select @@session.tx_isolation;<br>+————————+<br>| @@session.tx_isolation |<br>+————————+<br>| READ-COMMITTED         |<br>+————————+<br>1 row in set, 1 warning (0.00 sec)</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;为什么需要事务&quot;&gt;&lt;a href=&quot;#为什么需要事务&quot; class=&quot;headerlink&quot; title=&quot;为什么需要事务&quot;&gt;&lt;/a&gt;为什么需要事务&lt;/h1&gt;&lt;p&gt;在银行业务中，有一条记账原则，即有借有贷，借贷相等。为了保证这种原则，每发生一笔银行业务，就必须确保
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>MySQL/MySQL索引</title>
    <link href="http://yoursite.com/2021/07/13/MySQL/MySQL%E7%B4%A2%E5%BC%95/"/>
    <id>http://yoursite.com/2021/07/13/MySQL/MySQL%E7%B4%A2%E5%BC%95/</id>
    <published>2021-07-13T06:45:55.809Z</published>
    <updated>2021-07-13T07:55:34.631Z</updated>
    
    <content type="html"><![CDATA[<h1 id="索引（Index）是什么"><a href="#索引（Index）是什么" class="headerlink" title="索引（Index）是什么"></a>索引（Index）是什么</h1><p>索引是一种特殊的数据库结构，由数据表中的一列或多列组合而成，可以用来快速查询数据表中有某一特定值的记录。本节将详细讲解索引的含义、作用和优缺点。</p><p>通过索引，查询数据时不用读完记录的所有信息，而只是查询索引列。否则，数据库系统将读取每条记录的所有信息进行匹配。</p><p>可以把索引比作新华字典的音序表。例如，要查“库”字，如果不使用音序，就需要从字典的 400 页中逐页来找。但是，如果提取拼音出来，构成音序表，就只需要从 10 多页的音序表中直接查找。这样就可以大大节省时间。</p><p>因此，使用索引可以很大程度上提高数据库的查询速度，还有效的提高了数据库系统的性能。<br>为什么要使用索引<br>索引就是根据表中的一列或若干列按照一定顺序建立的列值与记录行之间的对应关系表，实质上是一张描述索引列的列值与原表中记录行之间一 一对应关系的有序表。</p><p>索引是 MySQL 中十分重要的数据库对象，是数据库性能调优技术的基础，常用于实现数据的快速检索。</p><p>在 MySQL 中，通常有以下两种方式访问数据库表的行数据：</p><p>1) 顺序访问<br>顺序访问是在表中实行全表扫描，从头到尾逐行遍历，直到在无序的行数据中找到符合条件的目标数据。</p><p>顺序访问实现比较简单，但是当表中有大量数据的时候，效率非常低下。例如，在几千万条数据中查找少量的数据时，使用顺序访问方式将会遍历所有的数据，花费大量的时间，显然会影响数据库的处理性能。<br>2) 索引访问<br>索引访问是通过遍历索引来直接访问表中记录行的方式。</p><p>使用这种方式的前提是对表建立一个索引，在列上创建了索引之后，查找数据时可以直接根据该列上的索引找到对应记录行的位置，从而快捷地查找到数据。索引存储了指定列数据值的指针，根据指定的排序顺序对这些指针排序。</p><p>例如，在学生基本信息表 tb_students 中，如果基于 student_id 建立了索引，系统就建立了一张索引列到实际记录的映射表。当用户需要查找 student_id 为 12022 的数据的时候，系统先在 student_id 索引上找到该记录，然后通过映射表直接找到数据行，并且返回该行数据。因为扫描索引的速度一般远远大于扫描实际数据行的速度，所以采用索引的方式可以大大提高数据库的工作效率。</p><p>简而言之，不使用索引，MySQL 就必须从第一条记录开始读完整个表，直到找出相关的行。表越大，查询数据所花费的时间就越多。如果表中查询的列有一个索引，MySQL 就能快速到达一个位置去搜索数据文件，而不必查看所有数据，这样将会节省很大一部分时间。<br>索引的优缺点<br>索引有其明显的优势，也有其不可避免的缺点。<br>优点<br>索引的优点如下：<br>通过创建唯一索引可以保证数据库表中每一行数据的唯一性。<br>可以给所有的 MySQL 列类型设置索引。<br>可以大大加快数据的查询速度，这是使用索引最主要的原因。<br>在实现数据的参考完整性方面可以加速表与表之间的连接。<br>在使用分组和排序子句进行数据查询时也可以显著减少查询中分组和排序的时间<br>缺点<br>增加索引也有许多不利的方面，主要如下：<br>创建和维护索引组要耗费时间，并且随着数据量的增加所耗费的时间也会增加。<br>索引需要占磁盘空间，除了数据表占数据空间以外，每一个索引还要占一定的物理空间。如果有大量的索引，索引文件可能比数据文件更快达到最大文件尺寸。<br>当对表中的数据进行增加、删除和修改的时候，索引也要动态维护，这样就降低了数据的维护速度。</p><p>使用索引时，需要综合考虑索引的优点和缺点。</p><p>索引可以提高查询速度，但是会影响插入记录的速度。因为，向有索引的表中插入记录时，数据库系统会按照索引进行排序，这样就降低了插入记录的速度，插入大量记录时的速度影响会更加明显。这种情况下，最好的办法是先删除表中的索引，然后插入数据，插入完成后，再创建索引。</p><h1 id="索引类型"><a href="#索引类型" class="headerlink" title="索引类型"></a>索引类型</h1><p>索引的类型和存储引擎有关，每种存储引擎所支持的索引类型不一定完全相同。MySQL 索引可以从存储方式、逻辑角度和实际使用的角度来进行分类。<br>存储方式区分<br>根据存储方式的不同，MySQL 中常用的索引在物理上分为  B-树索引和 HASH 索引两类，两种不同类型的索引各有其不同的适用范围。</p><p>1) B-树索引<br>B-树索引又称为 BTREE 索引，目前大部分的索引都是采用 B-树索引来存储的。</p><p>B-树索引是一个典型的数据结构，其包含的组件主要有以下几个：<br>叶子节点：包含的条目直接指向表里的数据行。叶子节点之间彼此相连，一个叶子节点有一个指向下一个叶子节点的指针。<br>分支节点：包含的条目指向索引里其他的分支节点或者叶子节点。<br>根节点：一个 B-树索引只有一个根节点，实际上就是位于树的最顶端的分支节点。</p><p>基于这种树形数据结构，表中的每一行都会在索引上有一个对应值。因此，在表中进行数据查询时，可以根据索引值一步一步定位到数据所在的行。</p><p>B-树索引可以进行全键值、键值范围和键值前缀查询，也可以对查询结果进行 ORDER BY 排序。但 B-树索引必须遵循左边前缀原则，要考虑以下几点约束：<br>查询必须从索引的最左边的列开始。<br>查询不能跳过某一索引列，必须按照从左到右的顺序进行匹配。<br>存储引擎不能使用索引中范围条件右边的列。<br>2) 哈希索引<br>哈希（Hash）一般翻译为“散列”，也有直接音译成“哈希”的，就是把任意长度的输入（又叫作预映射，pre-image）通过散列算法变换成固定长度的输出，该输出就是散列值。</p><p>哈希索引也称为散列索引或 HASH 索引。MySQL 目前仅有 MEMORY 存储引擎和 HEAP 存储引擎支持这类索引。其中，MEMORY 存储引擎可以支持 B-树索引和 HASH 索引，且将 HASH 当成默认索引。</p><p>HASH 索引不是基于树形的数据结构查找数据，而是根据索引列对应的哈希值的方法获取表的记录行。哈希索引的最大特点是访问速度快，但也存在下面的一些缺点：<br>MySQL 需要读取表中索引列的值来参与散列计算，散列计算是一个比较耗时的操作。也就是说，相对于 B-树索引来说，建立哈希索引会耗费更多的时间。<br>不能使用 HASH 索引排序。<br>HASH 索引只支持等值比较，如“=”“IN()”或“&lt;=&gt;”。<br>HASH 索引不支持键的部分匹配，因为在计算 HASH 值的时候是通过整个索引值来计算的。<br>逻辑区分<br>根据索引的具体用途，MySQL 中的索引在逻辑上分为以下 5 类：</p><p>1) 普通索引<br>普通索引是 MySQL 中最基本的索引类型，它没有任何限制，唯一任务就是加快系统对数据的访问速度。</p><p>普通索引允许在定义索引的列中插入重复值和空值。</p><p>创建普通索引时，通常使用的关键字是 INDEX 或 KEY。<br>例 1<br>下面在 tb_student 表中的 id 字段上建立名为 index_id 的索引。<br>CREATE INDEX index_id ON tb_student(id);</p><p>2) 唯一索引<br>唯一索引与普通索引类似，不同的是创建唯一性索引的目的不是为了提高访问速度，而是为了避免数据出现重复。</p><p>唯一索引列的值必须唯一，允许有空值。如果是组合索引，则列值的组合必须唯一。</p><p>创建唯一索引通常使用 UNIQUE 关键字。<br>例 2<br>下面在 tb_student 表中的 id 字段上建立名为 index_id 的索引，SQL 语句如下：<br>CREATE UNIQUE INDEX index_id ON tb_student(id);</p><p>其中，id 字段可以有唯一性约束，也可以没有。<br>3) 主键索引<br>顾名思义，主键索引就是专门为主键字段创建的索引，也属于索引的一种。</p><p>主键索引是一种特殊的唯一索引，不允许值重复或者值为空。</p><p>创建主键索引通常使用 PRIMARY KEY 关键字。不能使用 CREATE INDEX 语句创建主键索引。<br>4) 空间索引<br>空间索引是对空间数据类型的字段建立的索引，使用 SPATIAL 关键字进行扩展。</p><p>创建空间索引的列必须将其声明为 NOT NULL，空间索引只能在存储引擎为 MyISAM 的表中创建。</p><p>空间索引主要用于地理空间数据类型 GEOMETRY。对于初学者来说，这类索引很少会用到。<br>例 3<br>下面在 tb_student 表中的 line 字段上建立名为 index_line 的索引，SQL 语句如下：<br>CREATE SPATIAL INDEX index_line ON tb_student(line);</p><p>其中，tb_student 表的存储引擎必须是 MyISAM，line 字段必须为空间数据类型，而且是非空的。<br>5) 全文索引<br>全文索引主要用来查找文本中的关键字，只能在 CHAR、VARCHAR 或 TEXT 类型的列上创建。在 MySQL 中只有 MyISAM 存储引擎支持全文索引。</p><p>全文索引允许在索引列中插入重复值和空值。</p><p>不过对于大容量的数据表，生成全文索引非常消耗时间和硬盘空间。</p><p>创建全文索引使用 FULLTEXT 关键字。<br>例 4<br>在 tb_student 表中的 info 字段上建立名为 index_info 的全文索引，SQL 语句如下：<br>CREATE FULLTEXT INDEX index_info ON tb_student(info);</p><p>其中，index_info 的存储引擎必须是 MyISAM，info 字段必须是 CHAR、VARCHAR 和 TEXT 等类型。<br>实际使用区分<br>索引在逻辑上分为以上 5 类，但在实际使用中，索引通常被创建成单列索引和组合索引。<br>1）单列索引<br>单列索引就是索引只包含原表的一个列。在表中的单个字段上创建索引，单列索引只根据该字段进行索引。</p><p>单列索引可以是普通索引，也可以是唯一性索引，还可以是全文索引。只要保证该索引只对应一个字段即可。<br>例 5<br>下面在 tb_student 表中的 address 字段上建立名为 index_addr 的单列索引，address 字段的数据类型为 VARCHAR(20)，索引的数据类型为 CHAR(4)。SQL 语句如下：<br>CREATE INDEX index_addr ON tb_student(address(4));</p><p>这样，查询时可以只查询 address 字段的前 4 个字符，而不需要全部查询。<br>2）多列索引<br>组合索引也称为复合索引或多列索引。相对于单列索引来说，组合索引是将原表的多个列共同组成一个索引。多列索引是在表的多个字段上创建一个索引。该索引指向创建时对应的多个字段，可以通过这几个字段进行查询。但是，只有查询条件中使用了这些字段中第一个字段时，索引才会被使用。</p><p>例如，在表中的 id、name 和 sex 字段上建立一个多列索引，那么，只有查询条件使用了 id 字段时，该索引才会被使用。<br>例 6<br>下面在 tb_student 表中的 name 和 address 字段上建立名为 index_na 的索引，SQL 语句如下：<br>CREATE INDEX index_na ON tb_student(name,address);</p><p>该索引创建好了以后，查询条件中必须有 name 字段才能使用索引。<br>提示：一个表可以有多个单列索引，但这些索引不是组合索引。一个组合索引实质上为表的查询提供了多个索引，以此来加快查询速度。比如，在一个表中创建了一个组合索引(c1，c2，c3)，在实际查询中，系统用来实际加速的索引有三个：单个索引(c1)、双列索引(c1，c2)和多列索引(c1，c2，c3)。</p><h1 id="创建索引"><a href="#创建索引" class="headerlink" title="创建索引"></a>创建索引</h1><p>创建索引是指在某个表的一列或多列上建立一个索引，可以提高对表的访问速度。创建索引对 MySQL 数据库的高效运行来说是很重要的。<br>基本语法<br>MySQL 提供了三种创建索引的方法：</p><p>1) 使用 CREATE INDEX 语句<br>可以使用专门用于创建索引的 CREATE INDEX 语句在一个已有的表上创建索引，但该语句不能创建主键。</p><p>语法格式：<br>CREATE &lt;索引名&gt; ON &lt;表名&gt; (&lt;列名&gt; [&lt;长度&gt;] [ ASC | DESC])</p><p>语法说明如下：<br>&lt;索引名&gt;：指定索引名。一个表可以创建多个索引，但每个索引在该表中的名称是唯一的。<br>&lt;表名&gt;：指定要创建索引的表名。<br>&lt;列名&gt;：指定要创建索引的列名。通常可以考虑将查询语句中在 JOIN 子句和 WHERE 子句里经常出现的列作为索引列。<br>&lt;长度&gt;：可选项。指定使用列前的 length 个字符来创建索引。使用列的一部分创建索引有利于减小索引文件的大小，节省索引列所占的空间。在某些情况下，只能对列的前缀进行索引。索引列的长度有一个最大上限 255 个字节（MyISAM 和 InnoDB 表的最大上限为 1000 个字节），如果索引列的长度超过了这个上限，就只能用列的前缀进行索引。另外，BLOB 或 TEXT 类型的列也必须使用前缀索引。<br>ASC|DESC：可选项。ASC指定索引按照升序来排列，DESC指定索引按照降序来排列，默认为ASC。<br>2) 使用 CREATE TABLE 语句<br>索引也可以在创建表（CREATE TABLE）的同时创建。在 CREATE TABLE 语句中添加以下语句。语法格式：<br>CONSTRAINT PRIMARY KEY [索引类型] (&lt;列名&gt;,…)</p><p>在 CREATE TABLE 语句中添加此语句，表示在创建新表的同时创建该表的主键。</p><p>语法格式：<br>KEY | INDEX [&lt;索引名&gt;] [&lt;索引类型&gt;] (&lt;列名&gt;,…)</p><p>在 CREATE TABLE 语句中添加此语句，表示在创建新表的同时创建该表的索引。</p><p>语法格式：<br>UNIQUE [ INDEX | KEY] [&lt;索引名&gt;] [&lt;索引类型&gt;] (&lt;列名&gt;,…)</p><p>在 CREATE TABLE 语句中添加此语句，表示在创建新表的同时创建该表的唯一性索引。</p><p>语法格式：<br>FOREIGN KEY &lt;索引名&gt; &lt;列名&gt;</p><p>在 CREATE TABLE 语句中添加此语句，表示在创建新表的同时创建该表的外键。</p><p>在使用 CREATE TABLE 语句定义列选项的时候，可以通过直接在某个列定义后面添加 PRIMARY KEY 的方式创建主键。而当主键是由多个列组成的多列索引时，则不能使用这种方法，只能用在语句的最后加上一个 PRIMARY KRY(&lt;列名&gt;，…) 子句的方式来实现。<br>3) 使用 ALTER TABLE 语句<br>CREATE INDEX 语句可以在一个已有的表上创建索引，ALTER TABLE 语句也可以在一个已有的表上创建索引。在使用 ALTER TABLE 语句修改表的同时，可以向已有的表添加索引。具体的做法是在 ALTER TABLE 语句中添加以下语法成分的某一项或几项。</p><p>语法格式：<br>ADD INDEX [&lt;索引名&gt;] [&lt;索引类型&gt;] (&lt;列名&gt;,…)</p><p>在 ALTER TABLE 语句中添加此语法成分，表示在修改表的同时为该表添加索引。</p><p>语法格式：<br>ADD PRIMARY KEY [&lt;索引类型&gt;] (&lt;列名&gt;,…)</p><p>在 ALTER TABLE 语句中添加此语法成分，表示在修改表的同时为该表添加主键。</p><p>语法格式：<br>ADD UNIQUE [ INDEX | KEY] [&lt;索引名&gt;] [&lt;索引类型&gt;] (&lt;列名&gt;,…)</p><p>在 ALTER TABLE 语句中添加此语法成分，表示在修改表的同时为该表添加唯一性索引。</p><p>语法格式：<br>ADD FOREIGN KEY [&lt;索引名&gt;] (&lt;列名&gt;,…)</p><p>在 ALTER TABLE 语句中添加此语法成分，表示在修改表的同时为该表添加外键。<br>创建普通索引<br>创建普通索引时，通常使用 INDEX 关键字。<br>例 1<br>创建一个表 tb_stu_info，在该表的 height 字段创建普通索引。输入的 SQL 语句和执行过程如下所示。<br>mysql&gt; CREATE TABLE tb_stu_info<br>    -&gt; (<br>    -&gt; id INT NOT NULL,<br>    -&gt; name CHAR(45) DEFAULT NULL,<br>    -&gt; dept_id INT DEFAULT NULL,<br>    -&gt; age INT DEFAULT NULL,<br>    -&gt; height INT DEFAULT NULL,<br>    -&gt; INDEX(height)<br>    -&gt; );<br>Query OK，0 rows affected (0.40 sec)<br>mysql&gt; SHOW CREATE TABLE tb_stu_info\G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>       Table: tb_stu_info<br>Create Table: CREATE TABLE <code>tb_stu_info</code> (<br>  <code>id</code> int(11) NOT NULL,<br>  <code>name</code> char(45) DEFAULT NULL,<br>  <code>dept_id</code> int(11) DEFAULT NULL,<br>  <code>age</code> int(11) DEFAULT NULL,<br>  <code>height</code> int(11) DEFAULT NULL,<br>  KEY <code>height</code> (<code>height</code>)<br>) ENGINE=InnoDB DEFAULT CHARSET=gb2312<br>1 row in set (0.01 sec)<br>创建唯一索引<br>创建唯一索引，通常使用 UNIQUE 参数。<br>例 2<br>创建一个表 tb_stu_info2，在该表的 id 字段上使用 UNIQUE 关键字创建唯一索引。输入的 SQL 语句和执行过程如下所示。<br>mysql&gt; CREATE TABLE tb_stu_info2<br>    -&gt; (<br>    -&gt; id INT NOT NULL,<br>    -&gt; name CHAR(45) DEFAULT NULL,<br>    -&gt; dept_id INT DEFAULT NULL,<br>    -&gt; age INT DEFAULT NULL,<br>    -&gt; height INT DEFAULT NULL,<br>    -&gt; UNIQUE INDEX(height)<br>    -&gt; );<br>Query OK，0 rows affected (0.40 sec)<br>mysql&gt; SHOW CREATE TABLE tb_stu_info2\G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>       Table: tb_stu_info2<br>Create Table: CREATE TABLE <code>tb_stu_info2</code> (<br>  <code>id</code> int(11) NOT NULL,<br>  <code>name</code> char(45) DEFAULT NULL,<br>  <code>dept_id</code> int(11) DEFAULT NULL,<br>  <code>age</code> int(11) DEFAULT NULL,<br>  <code>height</code> int(11) DEFAULT NULL,<br>  UNIQUE KEY <code>height</code> (<code>height</code>)<br>) ENGINE=InnoDB DEFAULT CHARSET=gb2312<br>1 row in set (0.00 sec)</p><h1 id="查看索引"><a href="#查看索引" class="headerlink" title="查看索引"></a>查看索引</h1><p>索引创建完成后，可以利用 SQL 语句查看已经存在的索引。在 MySQL 中，可以使用 SHOW INDEX 语句查看表中创建的索引。</p><p>查看索引的语法格式如下：<br>SHOW INDEX FROM &lt;表名&gt; [ FROM &lt;数据库名&gt;]</p><p>语法说明如下：<br>&lt;表名&gt;：指定需要查看索引的数据表名。<br>&lt;数据库名&gt;：指定需要查看索引的数据表所在的数据库，可省略。比如，SHOW INDEX FROM student FROM test; 语句表示查看 test 数据库中 student 数据表的索引。<br>示例<br>使用 SHOW INDEX 语句查看《MySQL创建索引》一节中 tb_stu_info2 数据表的索引信息，SQL 语句和运行结果如下所示。<br>mysql&gt; SHOW INDEX FROM tb_stu_info2\G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>        Table: tb_stu_info2<br>   Non_unique: 0<br>     Key_name: height<br> Seq_in_index: 1<br>  Column_name: height<br>    Collation: A<br>  Cardinality: 0<br>     Sub_part: NULL<br>       Packed: NULL<br>         Null: YES<br>   Index_type: BTREE<br>      Comment:<br>Index_comment:<br>1 row in set (0.03 sec)<br>其中各主要参数说明如下：<br>参数    说明<br>Table    表示创建索引的数据表名，这里是 tb_stu_info2 数据表。<br>Non_unique    表示该索引是否是唯一索引。若不是唯一索引，则该列的值为 1；若是唯一索引，则该列的值为 0。<br>Key_name    表示索引的名称。<br>Seq_in_index    表示该列在索引中的位置，如果索引是单列的，则该列的值为 1；如果索引是组合索引，则该列的值为每列在索引定义中的顺序。<br>Column_name    表示定义索引的列字段。<br>Collation    表示列以何种顺序存储在索引中。在 MySQL 中，升序显示值“A”（升序），若显示为 NULL，则表示无分类。<br>Cardinality    索引中唯一值数目的估计值。基数根据被存储为整数的统计数据计数，所以即使对于小型表，该值也没有必要是精确的。基数越大，当进行联合时，MySQL 使用该索引的机会就越大。<br>Sub_part    表示列中被编入索引的字符的数量。若列只是部分被编入索引，则该列的值为被编入索引的字符的数目；若整列被编入索引，则该列的值为 NULL。<br>Packed    指示关键字如何被压缩。若没有被压缩，值为 NULL。<br>Null    用于显示索引列中是否包含 NULL。若列含有 NULL，该列的值为 YES。若没有，则该列的值为 NO。<br>Index_type    显示索引使用的类型和方法（BTREE、FULLTEXT、HASH、RTREE）。<br>Comment    显示评注。</p><h1 id="修改和删除索引"><a href="#修改和删除索引" class="headerlink" title="修改和删除索引"></a>修改和删除索引</h1><p>删除索引是指将表中已经存在的索引删除掉。不用的索引建议进行删除，因为它们会降低表的更新速度，影响数据库的性能。对于这样的索引，应该将其删除。</p><p>在 MySQL 中修改索引可以通过删除原索引，再根据需要创建一个同名的索引，从而实现修改索引的操作。<br>基本语法<br>当不再需要索引时，可以使用 DROP INDEX 语句或 ALTER TABLE 语句来对索引进行删除。</p><p>1) 使用 DROP INDEX 语句<br>语法格式：<br>DROP INDEX &lt;索引名&gt; ON &lt;表名&gt;</p><p>语法说明如下：<br>&lt;索引名&gt;：要删除的索引名。<br>&lt;表名&gt;：指定该索引所在的表名。<br>2) 使用 ALTER TABLE 语句<br>根据 ALTER TABLE 语句的语法可知，该语句也可以用于删除索引。具体使用方法是将 ALTER TABLE 语句的语法中部分指定为以下子句中的某一项。<br>DROP PRIMARY KEY：表示删除表中的主键。一个表只有一个主键，主键也是一个索引。<br>DROP INDEX index_name：表示删除名称为 index_name 的索引。<br>DROP FOREIGN KEY fk_symbol：表示删除外键。<br>注意：如果删除的列是索引的组成部分，那么在删除该列时，也会将该列从索引中删除；如果组成索引的所有列都被删除，那么整个索引将被删除。</p><p>删除索引<br>【实例 1】删除表 tb_stu_info 中的索引，输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; DROP INDEX height<br>    -&gt; ON tb_stu_info;<br>Query OK, 0 rows affected (0.27 sec)<br>Records: 0  Duplicates: 0  Warnings: 0<br>mysql&gt; SHOW CREATE TABLE tb_stu_info\G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>       Table: tb_stu_info<br>Create Table: CREATE TABLE <code>tb_stu_info</code> (<br>  <code>id</code> int(11) NOT NULL,<br>  <code>name</code> char(45) DEFAULT NULL,<br>  <code>dept_id</code> int(11) DEFAULT NULL,<br>  <code>age</code> int(11) DEFAULT NULL,<br>  <code>height</code> int(11) DEFAULT NULL<br>) ENGINE=InnoDB DEFAULT CHARSET=gb2312<br>1 row in set (0.00 sec)<br>【实例 2】删除表 tb_stu_info2 中名称为 id 的索引，输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; ALTER TABLE tb_stu_info2<br>    -&gt; DROP INDEX height;<br>Query OK, 0 rows affected (0.13 sec)<br>Records: 0  Duplicates: 0  Warnings: 0<br>mysql&gt; SHOW CREATE TABLE tb_stu_info2\G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>       Table: tb_stu_info2<br>Create Table: CREATE TABLE <code>tb_stu_info2</code> (<br>  <code>id</code> int(11) NOT NULL,<br>  <code>name</code> char(45) DEFAULT NULL,<br>  <code>dept_id</code> int(11) DEFAULT NULL,<br>  <code>age</code> int(11) DEFAULT NULL,<br>  <code>height</code> int(11) DEFAULT NULL<br>) ENGINE=InnoDB DEFAULT CHARSET=gb2312<br>1 row in set (0.00 sec)</p><h1 id="索引在什么情况下不会被使用"><a href="#索引在什么情况下不会被使用" class="headerlink" title="索引在什么情况下不会被使用"></a>索引在什么情况下不会被使用</h1><p>索引可以提高查询的速度，但并不是使用带有索引的字段查询时，索引都会起作用。使用索引有几种特殊情况，在这些情况下，有可能使用带有索引的字段查询时，索引并没有起作用，下面重点介绍这几种特殊情况。</p><ol><li>查询语句中使用LIKE关键字<br>在查询语句中使用 LIKE 关键字进行查询时，如果匹配字符串的第一个字符为“%”，索引不会被使用。如果“%”不是在第一个位置，索引就会被使用。<br>例 1<br>为了便于理解，我们先查询 tb_student 表中的数据，SQL 语句和运行结果如下：<br>mysql&gt; SELECT * FROM tb_student;</li></ol><p>+—-+——+——+——+<br>| id | name | age  | sex  |<br>+—-+——+——+——+<br>|  1 | 张三 |   12 | 男   |<br>|  2 | 李四 |   12 | 男   |<br>|  3 | 王五 |   13 | 女   |<br>|  4 | 张四 |   13 | 女   |<br>|  5 | 王四 |   15 | 男   |<br>|  6 | 赵六 |   12 | 女   |<br>+—-+——+——+——+<br>6 rows in set (0.03 sec)<br>下面在查询语句中使用 LIKE 关键字，且匹配的字符串中含有“%”符号，使用 EXPLAIN 分析查询情况，SQL 语句和运行结果如下：<br>mysql&gt;  EXPLAIN SELECT * FROM tb_student WHERE name LIKE ‘%四’\G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>           id: 1<br>  select_type: SIMPLE<br>        table: tb_student<br>   partitions: NULL<br>         type: ALL<br>possible_keys: NULL<br>          key: NULL<br>      key_len: NULL<br>          ref: NULL<br>         rows: 6<br>     filtered: 16.67<br>        Extra: Using where<br>1 row in set, 1 warning (0.01 sec)</p><p>mysql&gt; CREATE INDEX index_name ON tb_student(name);<br>Query OK, 6 rows affected (0.13 sec)</p><p>mysql&gt;  EXPLAIN SELECT * FROM tb_student WHERE name LIKE ‘李%’\G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>           id: 1<br>  select_type: SIMPLE<br>        table: tb_student<br>   partitions: NULL<br>         type: range<br>possible_keys: index_name<br>          key: index_name<br>      key_len: 77<br>          ref: NULL<br>         rows: 1<br>     filtered: 100.00<br>        Extra: Using index condition<br>1 row in set, 1 warning (0.00 sec)<br>第一个查询语句执行后，rows 参数的值为 6，表示这次查询过程中查询了 6 条记录；第二个查询语句执行后，rows 参数的值为 1，表示这次查询过程只查询 1 条记录。同样是使用 name 字段进行查询，因为第一个查询语句的 LIKE 关键字后的字符串是以“%”开头的，所以第一个查询语句没有使用索引，而第二个查询语句使用了索引 index_name。<br>2. 查询语句中使用多列索引<br>多列索引是在表的多个字段上创建一个索引，只有查询条件中使用了这些字段中的第一个字段，索引才会被使用。<br>例 2<br>在 name 和 age 两个字段上创建多列索引，并验证多列索引的使用情况，SQL 语句和运行结果如下：<br>mysql&gt; CREATE INDEX index_name_age ON tb_student(name,age);<br>Query OK, 6 rows affected (0.11 sec)</p><p>mysql&gt; EXPLAIN SELECT * FROM tb_student WHERE name LIKE ‘李%’\G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>           id: 1<br>  select_type: SIMPLE<br>        table: tb_student<br>   partitions: NULL<br>         type: range<br>possible_keys: index_name_age<br>          key: index_name_age<br>      key_len: 77<br>          ref: NULL<br>         rows: 1<br>     filtered: 100.00<br>        Extra: Using index condition<br>1 row in set, 1 warning (0.05 sec)</p><p>mysql&gt; EXPLAIN SELECT * FROM tb_student WHERE age LIKE ‘12’\G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>           id: 1<br>  select_type: SIMPLE<br>        table: tb_student<br>   partitions: NULL<br>         type: ALL<br>possible_keys: NULL<br>          key: NULL<br>      key_len: NULL<br>          ref: NULL<br>         rows: 6<br>     filtered: 16.67<br>        Extra: Using where<br>1 row in set, 1 warning (0.00 sec)<br>第一条查询语句的查询条件使用了 name 字段，分析结果显示 rows 参数的值为 1，且查询过程中使用了 index_name_age 索引。第二条查询语句的查询条件使用了 age 字段，结果显示 rows 参数的值为 6，且 key 参数的值为 NULL，这说明第二个查询语句没有使用索引。</p><p>因为 name 字段是多列索引的第一个字段，所以只有查询条件中使用了 name 字段才会使 index_name_age 索引起作用。<br>3. 查询语句中使用OR关键字<br>查询语句只有 OR 关键字时，如果 OR 前后的两个条件的列都是索引，那么查询中将使用索引。如果 OR 前后有一个条件的列不是索引，那么查询中将不使用索引。<br>例 3<br>下面演示 OR 关键字的使用。<br>mysql&gt; EXPLAIN SELECT * FROM tb_student WHERE name=’张三’ or sex=’男’\G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>           id: 1<br>  select_type: SIMPLE<br>        table: tb_student<br>   partitions: NULL<br>         type: ALL<br>possible_keys: index_name,index_name_age<br>          key: NULL<br>      key_len: NULL<br>          ref: NULL<br>         rows: 6<br>     filtered: 30.56<br>        Extra: Using where<br>1 row in set, 1 warning (0.06 sec)<br>mysql&gt; EXPLAIN SELECT * FROM tb_student WHERE name=’张三’ or id=’12’\G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>           id: 1<br>  select_type: SIMPLE<br>        table: tb_student<br>   partitions: NULL<br>         type: index_merge<br>possible_keys: PRIMARY,index_name,index_name_age<br>          key: index_name,PRIMARY<br>      key_len: 77,4<br>          ref: NULL<br>         rows: 2<br>     filtered: 100.00<br>        Extra: Using union(index_name,PRIMARY); Using where<br>1 row in set, 1 warning (0.01 sec)<br>由于 sex 字段没有索引，所以第一条查询语句没有使用索引；name 字段和 id 字段都有索引，所以第二条查询语句使用了 index_name 和 PRIMARY 索引 。<br>总结<br>使用索引查询记录时，一定要注意索引的使用情况。例如，LIKE 关键字配置的字符串不能以“%”开头；使用多列索引时，查询条件必须要使用这个索引的第一个字段；使用 OR 关键字时，OR 关键字连接的所有条件都必须使用索引。</p><h1 id="索引的设计原则"><a href="#索引的设计原则" class="headerlink" title="索引的设计原则"></a>索引的设计原则</h1><p>索引的设计可以遵循一些已有的原则，创建索引的时候应尽量考虑符合这些原则，便于提升索引的使用效率，更高效的使用索引。本节将介绍一些索引的设计原则。</p><ol><li><p>选择唯一性索引<br>唯一性索引的值是唯一的，可以更快速的通过该索引来确定某条记录。例如，学生表中学号是具有唯一性的字段。为该字段建立唯一性索引可以很快的确定某个学生的信息。如果使用姓名的话，可能存在同名现象，从而降低查询速度。</p></li><li><p>为经常需要排序、分组和联合操作的字段建立索引<br>经常需要 ORDER BY、GROUP BY、DISTINCT 和 UNION 等操作的字段，排序操作会浪费很多时间。如果为其建立索引，可以有效地避免排序操作。</p></li><li><p>为常作为查询条件的字段建立索引<br>如果某个字段经常用来做查询条件，那么该字段的查询速度会影响整个表的查询速度。因此，为这样的字段建立索引，可以提高整个表的查询速度。<br>注意：常查询条件的字段不一定是所要选择的列，换句话说，最适合索引的列是出现在 WHERE 子句中的列，或连接子句中指定的列，而不是出现在 SELECT 关键字后的选择列表中的列。</p></li><li><p>限制索引的数目<br>索引的数目不是“越多越好”。每个索引都需要占用磁盘空间，索引越多，需要的磁盘空间就越大。在修改表的内容时，索引必须进行更新，有时还可能需要重构。因此，索引越多，更新表的时间就越长。</p></li></ol><p>如果有一个索引很少利用或从不使用，那么会不必要地减缓表的修改速度。此外，MySQL 在生成一个执行计划时，要考虑各个索引，这也要花费时间。创建多余的索引给查询优化带来了更多的工作。索引太多，也可能会使 MySQL 选择不到所要使用的最佳索引。<br>5. 尽量使用数据量少的索引<br>如果索引的值很长，那么查询的速度会受到影响。例如，对一个 CHAR(100) 类型的字段进行全文检索需要的时间肯定要比对 CHAR(10) 类型的字段需要的时间要多。<br>6. 数据量小的表最好不要使用索引<br>由于数据较小，查询花费的时间可能比遍历索引的时间还要短，索引可能不会产生优化效果。<br>7. 尽量使用前缀来索引<br>如果索引字段的值很长，最好使用值的前缀来索引。例如，TEXT 和 BLOG 类型的字段，进行全文检索会很浪费时间。如果只检索字段的前面的若干个字符，这样可以提高检索速度。<br>8. 删除不再使用或者很少使用的索引<br>表中的数据被大量更新，或者数据的使用方式被改变后，原有的一些索引可能不再需要。应该定期找出这些索引，将它们删除，从而减少索引对更新操作的影响。<br>总结<br>选择索引的最终目的是为了使查询的速度变快，上面给出的原则是最基本的准则，但不能只拘泥于上面的准则。应该在学习和工作中不断的实践，根据应用的实际情况进行分析和判断，选择最合适的索引方式。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;索引（Index）是什么&quot;&gt;&lt;a href=&quot;#索引（Index）是什么&quot; class=&quot;headerlink&quot; title=&quot;索引（Index）是什么&quot;&gt;&lt;/a&gt;索引（Index）是什么&lt;/h1&gt;&lt;p&gt;索引是一种特殊的数据库结构，由数据表中的一列或多列组合而成，
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>MySQL/MySQL视图</title>
    <link href="http://yoursite.com/2021/07/13/MySQL/MySQL%E8%A7%86%E5%9B%BE/"/>
    <id>http://yoursite.com/2021/07/13/MySQL/MySQL%E8%A7%86%E5%9B%BE/</id>
    <published>2021-07-13T06:03:23.409Z</published>
    <updated>2021-07-13T07:55:36.273Z</updated>
    
    <content type="html"><![CDATA[<h1 id="视图是什么"><a href="#视图是什么" class="headerlink" title="视图是什么"></a>视图是什么</h1><p>MySQL 视图（View）是一种虚拟存在的表，同真实表一样，视图也由列和行构成，但视图并不实际存在于数据库中。行和列的数据来自于定义视图的查询中所使用的表，并且还是在使用视图时动态生成的。</p><p>数据库中只存放了视图的定义，并没有存放视图中的数据，这些数据都存放在定义视图查询所引用的真实表中。使用视图查询数据时，数据库会从真实表中取出对应的数据。因此，视图中的数据是依赖于真实表中的数据的。一旦真实表中的数据发生改变，显示在视图中的数据也会发生改变。</p><p>视图可以从原有的表上选取对用户有用的信息，那些对用户没用，或者用户没有权限了解的信息，都可以直接屏蔽掉，作用类似于筛选。这样做既使应用简单化，也保证了系统的安全。</p><p>例如，下面的数据库中有一张公司部门表 department。department 表的结构如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; DESC department;</span><br><span class="line">+----------+-------------+------+-----+---------+-------+</span><br><span class="line">| Field    | Type        | Null | Key | Default | Extra |</span><br><span class="line">+----------+-------------+------+-----+---------+-------+</span><br><span class="line">| d_id     | int(4)      | NO   | PRI | NULL    |       |</span><br><span class="line">| d_name   | varchar(20) | NO   | UNI    | NULL    |       |</span><br><span class="line">| function | varchar(50) | YES  |     | NULL    |       |</span><br><span class="line">| address  | varchar(50) | YES  |     | NULL    |       |</span><br><span class="line">+----------+-------------+------+-----+---------+-------+</span><br><span class="line">4 rows in set (0.02 sec)</span><br></pre></td></tr></table></figure><p>还有一张员工表 worker。worker 表的结构如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; DESC worker;</span><br><span class="line">+-------------+-------------+------+-----+---------+-------+</span><br><span class="line">| Field       | Type        | Null | Key | Default | Extra |</span><br><span class="line">+-------------+-------------+------+-----+---------+-------+</span><br><span class="line">| num         | int(10)     | NO   | PRI | NULL    |       |</span><br><span class="line">| d_id        | int(4)      | YES  |MUL     | NULL    |       |</span><br><span class="line">| name        | varchar(20) | NO   |     | NULL    |       |</span><br><span class="line">| sex         | varchar(4)  | NO   |     | NULL    |       |</span><br><span class="line">| birthday    | datetime    | YES  |     | NULL    |       |</span><br><span class="line">| homeaddress | varchar(50) | YES  |     | NULL    |       |</span><br><span class="line">+-------------+-------------+------+-----+---------+-------+</span><br><span class="line">6 rows in set (0.01 sec)</span><br></pre></td></tr></table></figure><p>由于各部门领导的权力范围不同，因此，各部门的领导只能看到该部门的员工信息；而且，领导可能不关心员工的生日和家庭住址。为了达到这个目的，可以为各部门的领导建立一个视图，通过该视图，领导只能看到本部门员工的指定信息。</p><p>例如，为生产部门建立一个名为 product <em>view 的视图。通过视图 product</em> view，生产部门的领导只能看到生产部门员工的工作号、姓名和性别等信息。这些 department 表的信息和 worker 表的信息依然存在于各自的表中，而视图 product_view 中不保存任何数据信息。当 department 表和 worker 表的信息发生改变时，视图 product_view 显示的信息也会发生相应的变化。<br>技巧：如果经常需要从多个表查询指定字段的数据，可以在这些表上建立一个视图，通过这个视图显示这些字段的数据。</p><p>MySQL 的视图不支持输入参数的功能，因此交互性上还有欠缺。但对于变化不是很大的操作，使用视图可以很大程度上简化用户的操作。</p><p>视图并不同于数据表，它们的区别在于以下几点：<br>视图不是数据库中真实的表，而是一张虚拟表，其结构和数据是建立在对数据中真实表的查询基础上的。<br>存储在数据库中的查询操作 SQL 语句定义了视图的内容，列数据和行数据来自于视图查询所引用的实际表，引用视图时动态生成这些数据。<br>视图没有实际的物理记录，不是以数据集的形式存储在数据库中的，它所对应的数据实际上是存储在视图所引用的真实表中的。<br>视图是数据的窗口，而表是内容。表是实际数据的存放单位，而视图只是以不同的显示方式展示数据，其数据来源还是实际表。<br>视图是查看数据表的一种方法，可以查询数据表中某些字段构成的数据，只是一些 SQL 语句的集合。从安全的角度来看，视图的数据安全性更高，使用视图的用户不接触数据表，不知道表结构。<br>视图的建立和删除只影响视图本身，不影响对应的基本表。</p><h2 id="视图的优点"><a href="#视图的优点" class="headerlink" title="视图的优点"></a>视图的优点</h2><p>视图与表在本质上虽然不相同，但视图经过定义以后，结构形式和表一样，可以进行查询、修改、更新和删除等操作。同时，视图具有如下优点：</p><p>1) 定制用户数据，聚焦特定的数据<br>在实际的应用过程中，不同的用户可能对不同的数据有不同的要求。</p><p>例如，当数据库同时存在时，如学生基本信息表、课程表和教师信息表等多种表同时存在时，可以根据需求让不同的用户使用各自的数据。学生查看修改自己基本信息的视图，安排课程人员查看修改课程表和教师信息的视图，教师查看学生信息和课程信息表的视图。<br>2) 简化数据操作<br>在使用查询时，很多时候要使用聚合函数，同时还要显示其他字段的信息，可能还需要关联到其他表，语句可能会很长，如果这个动作频繁发生的话，可以创建视图来简化操作。<br>3) 提高数据的安全性<br>视图是虚拟的，物理上是不存在的。可以只授予用户视图的权限，而不具体指定使用表的权限，来保护基础数据的安全。<br>4) 共享所需数据<br>通过使用视图，每个用户不必都定义和存储自己所需的数据，可以共享数据库中的数据，同样的数据只需要存储一次。<br>5) 更改数据格式<br>通过使用视图，可以重新格式化检索出的数据，并组织输出到其他应用程序中。<br>6) 重用 SQL 语句<br>视图提供的是对查询操作的封装，本身不包含数据，所呈现的数据是根据视图定义从基础表中检索出来的，如果基础表的数据新增或删除，视图呈现的也是更新后的数据。视图定义后，编写完所需的查询，可以方便地重用该视图。</p><p>要注意区别视图和数据表的本质，即视图是基于真实表的一张虚拟的表，其数据来源均建立在真实表的基础上。</p><p>使用视图的时候，还应该注意以下几点：</p><ul><li>创建视图需要足够的访问权限。</li><li>创建视图的数目没有限制。</li><li>视图可以嵌套，即从其他视图中检索数据的查询来创建视图。</li><li>视图不能索引，也不能有关联的触发器、默认值或规则。</li><li>视图可以和表一起使用。</li><li>视图不包含数据，所以每次使用视图时，都必须执行查询中所需的任何一个检索操作。如果用多个连接和过滤条件创建了复杂的视图或嵌套了视图，可能会发现系统运行性能下降得十分严重。因此，在部署大量视图应用时，应该进行系统测试。</li></ul><p>提示：<code>ORDER BY</code>子句可以用在视图中，但若该视图检索数据的<code>SELECT</code>语句中也含有<code>ORDER BY</code>子句，则该视图中的<code>ORDER BY</code>子句将被覆盖。</p><h1 id="创建视图"><a href="#创建视图" class="headerlink" title="创建视图"></a>创建视图</h1><p>创建视图是指在已经存在的 MySQL 数据库表上建立视图。视图可以建立在一张表中，也可以建立在多张表中。<br>基本语法<br>可以使用 CREATE VIEW 语句来创建视图。</p><p>语法格式如下：<br>CREATE VIEW &lt;视图名&gt; AS &lt;SELECT语句&gt;</p><p>语法说明如下。<br>&lt;视图名&gt;：指定视图的名称。该名称在数据库中必须是唯一的，不能与其他表或视图同名。<br>&lt;SELECT语句&gt;：指定创建视图的 SELECT 语句，可用于查询多个基础表或源视图。</p><p>对于创建视图中的 SELECT 语句的指定存在以下限制：<br>用户除了拥有 CREATE VIEW 权限外，还具有操作中涉及的基础表和其他视图的相关权限。<br>SELECT 语句不能引用系统或用户变量。<br>SELECT 语句不能包含 FROM 子句中的子查询。<br>SELECT 语句不能引用预处理语句参数。</p><p>视图定义中引用的表或视图必须存在。但是，创建完视图后，可以删除定义引用的表或视图。可使用 CHECK TABLE 语句检查视图定义是否存在这类问题。</p><p>视图定义中允许使用 ORDER BY 语句，但是若从特定视图进行选择，而该视图使用了自己的 ORDER BY 语句，则视图定义中的 ORDER BY 将被忽略。</p><p>视图定义中不能引用 TEMPORARY 表（临时表），不能创建 TEMPORARY 视图。</p><p>WITH CHECK OPTION 的意思是，修改视图时，检查插入的数据是否符合 WHERE 设置的条件。<br>创建基于单表的视图<br>MySQL 可以在单个数据表上创建视图。</p><p>查看 test_db 数据库中的 tb_students_info 表的数据，如下所示。<br>mysql&gt; SELECT * FROM tb_students_info;<br>+—-+——–+———+——+——+——–+————+<br>| id | name   | dept_id | age  | sex  | height | login_date |<br>+—-+——–+———+——+——+——–+————+<br>|  1 | Dany   |       1 |   25 | F    |    160 | 2015-09-10 |<br>|  2 | Green  |       3 |   23 | F    |    158 | 2016-10-22 |<br>|  3 | Henry  |       2 |   23 | M    |    185 | 2015-05-31 |<br>|  4 | Jane   |       1 |   22 | F    |    162 | 2016-12-20 |<br>|  5 | Jim    |       1 |   24 | M    |    175 | 2016-01-15 |<br>|  6 | John   |       2 |   21 | M    |    172 | 2015-11-11 |<br>|  7 | Lily   |       6 |   22 | F    |    165 | 2016-02-26 |<br>|  8 | Susan  |       4 |   23 | F    |    170 | 2015-10-01 |<br>|  9 | Thomas |       3 |   22 | M    |    178 | 2016-06-07 |<br>| 10 | Tom    |       4 |   23 | M    |    165 | 2016-08-05 |<br>+—-+——–+———+——+——+——–+————+<br>10 rows in set (0.00 sec)<br>【实例 1】在 tb_students_info 表上创建一个名为 view_students_info 的视图，输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; CREATE VIEW view_students_info<br>    -&gt; AS SELECT * FROM tb_students_info;<br>Query OK, 0 rows affected (0.00 sec)<br>mysql&gt; SELECT * FROM view_students_info;<br>+—-+——–+———+——+——+——–+————+<br>| id | name   | dept_id | age  | sex  | height | login_date |<br>+—-+——–+———+——+——+——–+————+<br>|  1 | Dany   |       1 |   25 | F    |    160 | 2015-09-10 |<br>|  2 | Green  |       3 |   23 | F    |    158 | 2016-10-22 |<br>|  3 | Henry  |       2 |   23 | M    |    185 | 2015-05-31 |<br>|  4 | Jane   |       1 |   22 | F    |    162 | 2016-12-20 |<br>|  5 | Jim    |       1 |   24 | M    |    175 | 2016-01-15 |<br>|  6 | John   |       2 |   21 | M    |    172 | 2015-11-11 |<br>|  7 | Lily   |       6 |   22 | F    |    165 | 2016-02-26 |<br>|  8 | Susan  |       4 |   23 | F    |    170 | 2015-10-01 |<br>|  9 | Thomas |       3 |   22 | M    |    178 | 2016-06-07 |<br>| 10 | Tom    |       4 |   23 | M    |    165 | 2016-08-05 |<br>+—-+——–+———+——+——+——–+————+<br>10 rows in set (0.04 sec)<br>默认情况下，创建的视图和基本表的字段是一样的，也可以通过指定视图字段的名称来创建视图。</p><p>【实例 2】在 tb_students_info 表上创建一个名为 v_students_info 的视图，输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; CREATE VIEW v_students_info<br>    -&gt; (s_id,s_name,d_id,s_age,s_sex,s_height,s_date)<br>    -&gt; AS SELECT id,name,dept_id,age,sex,height,login_date<br>    -&gt; FROM tb_students_info;<br>Query OK, 0 rows affected (0.06 sec)<br>mysql&gt; SELECT * FROM v_students_info;<br>+——+——–+——+——-+——-+———-+————+<br>| s_id | s_name | d_id | s_age | s_sex | s_height | s_date     |<br>+——+——–+——+——-+——-+———-+————+<br>|    1 | Dany   |    1 |    24 | F     |      160 | 2015-09-10 |<br>|    2 | Green  |    3 |    23 | F     |      158 | 2016-10-22 |<br>|    3 | Henry  |    2 |    23 | M     |      185 | 2015-05-31 |<br>|    4 | Jane   |    1 |    22 | F     |      162 | 2016-12-20 |<br>|    5 | Jim    |    1 |    24 | M     |      175 | 2016-01-15 |<br>|    6 | John   |    2 |    21 | M     |      172 | 2015-11-11 |<br>|    7 | Lily   |    6 |    22 | F     |      165 | 2016-02-26 |<br>|    8 | Susan  |    4 |    23 | F     |      170 | 2015-10-01 |<br>|    9 | Thomas |    3 |    22 | M     |      178 | 2016-06-07 |<br>|   10 | Tom    |    4 |    23 | M     |      165 | 2016-08-05 |<br>+——+——–+——+——-+——-+———-+————+<br>10 rows in set (0.01 sec)<br>可以看到，view_students_info 和 v_students_info 两个视图中的字段名称不同，但是数据却相同。因此，在使用视图时，可能用户不需要了解基本表的结构，更接触不到实际表中的数据，从而保证了数据库的安全。<br>创建基于多表的视图<br>MySQL 中也可以在两个以上的表中创建视图，使用 CREATE VIEW 语句创建。</p><p>【实例 3】在表 tb_student_info 和表 tb_departments 上创建视图 v_students_info，输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; CREATE VIEW v_students_info<br>    -&gt; (s_id,s_name,d_id,s_age,s_sex,s_height,s_date)<br>    -&gt; AS SELECT id,name,dept_id,age,sex,height,login_date<br>    -&gt; FROM tb_students_info;<br>Query OK, 0 rows affected (0.06 sec)<br>mysql&gt; SELECT * FROM v_students_info;<br>+——+——–+——+——-+——-+———-+————+<br>| s_id | s_name | d_id | s_age | s_sex | s_height | s_date     |<br>+——+——–+——+——-+——-+———-+————+<br>|    1 | Dany   |    1 |    24 | F     |      160 | 2015-09-10 |<br>|    2 | Green  |    3 |    23 | F     |      158 | 2016-10-22 |<br>|    3 | Henry  |    2 |    23 | M     |      185 | 2015-05-31 |<br>|    4 | Jane   |    1 |    22 | F     |      162 | 2016-12-20 |<br>|    5 | Jim    |    1 |    24 | M     |      175 | 2016-01-15 |<br>|    6 | John   |    2 |    21 | M     |      172 | 2015-11-11 |<br>|    7 | Lily   |    6 |    22 | F     |      165 | 2016-02-26 |<br>|    8 | Susan  |    4 |    23 | F     |      170 | 2015-10-01 |<br>|    9 | Thomas |    3 |    22 | M     |      178 | 2016-06-07 |<br>|   10 | Tom    |    4 |    23 | M     |      165 | 2016-08-05 |<br>+——+——–+——+——-+——-+———-+————+<br>10 rows in set (0.01 sec)<br>通过这个视图可以很好地保护基本表中的数据。视图中包含 s_id、s_name 和 dept_name，s_id 字段对应 tb_students_info 表中的 id 字段，s_name 字段对应 tb_students_info 表中的 name 字段，dept_name 字段对应 tb_departments 表中的 dept_name 字段。<br>查询视图<br>视图一经定义之后，就可以如同查询数据表一样，使用 SELECT 语句查询视图中的数据，语法和查询基础表的数据一样。</p><p>视图用于查询主要应用在以下几个方面：<br>使用视图重新格式化检索出的数据。<br>使用视图简化复杂的表连接。<br>使用视图过滤数据。</p><p>DESCRIBE 可以用来查看视图，语法如下：<br>DESCRIBE 视图名；</p><p>【实例 4】通过 DESCRIBE 语句查看视图 v_students_info 的定义，输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; DESCRIBE v_students_info;<br>+———-+—————+——+—–+————+——-+<br>| Field    | Type          | Null | Key | Default    | Extra |<br>+———-+—————+——+—–+————+——-+<br>| s_id     | int(11)       | NO   |     | 0          |       |<br>| s_name   | varchar(45)   | YES  |     | NULL       |       |<br>| d_id     | int(11)       | YES  |     | NULL       |       |<br>| s_age    | int(11)       | YES  |     | NULL       |       |<br>| s_sex    | enum(‘M’,’F’) | YES  |     | NULL       |       |<br>| s_height | int(11)       | YES  |     | NULL       |       |<br>| s_date   | date          | YES  |     | 2016-10-22 |       |<br>+———-+—————+——+—–+————+——-+<br>7 rows in set (0.04 sec)<br>注意：DESCRIBE 一般情况下可以简写成 DESC，输入这个命令的执行结果和输入 DESCRIBE 是一样的。</p><h1 id="查看视图"><a href="#查看视图" class="headerlink" title="查看视图"></a>查看视图</h1><p>创建好视图后，可以通过查看视图的语句来查看视图的字段信息以及详细信息。本节主要讲解如何使用 SQL 语句来查看视图的字段信息以及详细信息。<br>查看视图的字段信息<br>查看视图的字段信息与查看数据表的字段信息一样，都是使用 DESCRIBE 关键字来查看的。具体语法如下：<br>DESCRIBE 视图名;</p><p>或简写成：<br>DESC 视图名;</p><p>示例 1<br>下面创建学生信息表 studentinfo 的一个视图，用于查询学生姓名和考试分数。</p><p>创建学生信息表 studentinfo 的 SQL 语句和运行结果如下：<br>mysql&gt; CREATE TABLE studentinfo(<br>    -&gt; ID INT(11) PRIMARY KEY,<br>    -&gt; NAME VARCHAR(20),<br>    -&gt; SCORE DECIMAL(4,2),<br>    -&gt; SUBJECT VARCHAR(20),<br>    -&gt; TEACHER VARCHAR(20));<br>Query OK, 0 rows affected (0.10 sec)</p><p>创建查询学生姓名和分数的视图语句如下：<br>mysql&gt; CREATE VIEW v_studentinfo AS SELECT name,score FROM studentinfo;<br>Query OK, 0 rows affected (0.04 sec)</p><p>通过 DESCRIBE 语句查看视图 v_studentsinfo 中的字段信息，SQL 语句和运行结果如下所示。<br>mysql&gt; DESCRIBE v_studentinfo;<br>+——-+————–+——+—–+———+——-+<br>| Field | Type         | Null | Key | Default | Extra |<br>+——-+————–+——+—–+———+——-+<br>| name  | varchar(20)  | YES  |     | NULL    |       |<br>| score | decimal(4,2) | YES  |     | NULL    |       |<br>+——-+————–+——+—–+———+——-+<br>2 rows in set (0.01 sec)<br>注意：使用 DESC 的执行结果和使用 DESCRIBE 是一样的。</p><p>由运行结果可以看出，查看视图的字段内容与查看表的字段内容显示的格式是相同的。因此，更能说明视图实际上也是一张数据表了，不同的是，视图中的数据都来自于数据库中已经存在的表。<br>查看视图的详细信息<br>在 MySQL 中，SHOW CREATE VIEW 语句可以查看视图的详细定义。其语法如下所示：<br>SHOW CREATE VIEW 视图名;</p><p>通过上面的语句，还可以查看创建视图的语句。创建视图的语句可以作为修改或者重新创建视图的参考，方便用户操作。<br>示例 2<br>使用 SHOW CREATE VIEW 查看视图，SQL 语句和运行结果如下所示：<br>mysql&gt;  SHOW CREATE VIEW v_studentinfo \G<br><strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong> 1. row <strong><strong><strong><strong><strong><strong>***</strong></strong></strong></strong></strong></strong><br>                View: v_studentinfo<br>         Create View: CREATE ALGORITHM=UNDEFINED DEFINER=<code>root</code>@<code>localhost</code> SQL SECURITY DEFINER VIEW <code>v_studentinfo</code> AS select <code>studentinfo</code>.<code>NAME</code> AS <code>name</code>,<code>studentinfo</code>.<code>SCORE</code> AS <code>score</code> from <code>studentinfo</code><br>character_set_client: gbk<br>collation_connection: gbk_chinese_ci<br>1 row in set (0.00 sec)<br>上述 SQL 语句以\G结尾，这样能使显示结果格式化。如果不使用\G，显示的结果会比较混乱，如下所示：<br>mysql&gt; DESCRIBE v_studentinfo;<br>+——-+————–+——+—–+———+——-+<br>| Field | Type         | Null | Key | Default | Extra |<br>+——-+————–+——+—–+———+——-+<br>| name  | varchar(20)  | YES  |     | NULL    |       |<br>| score | decimal(4,2) | YES  |     | NULL    |       |<br>+——-+————–+——+—–+———+——-+<br>2 rows in set (0.01 sec)</p><p>mysql&gt;  SHOW CREATE VIEW v_studentinfo;<br>+—————+———————————————————————————————————————————————————————————————-+———————-+———————-+<br>| View          | Create View                                                                                                                                                                                  | character_set_client | collation_connection |<br>+—————+———————————————————————————————————————————————————————————————-+———————-+———————-+<br>| v_studentinfo | CREATE ALGORITHM=UNDEFINED DEFINER=<code>root</code>@<code>localhost</code> SQL SECURITY DEFINER VIEW <code>v_studentinfo</code> AS select <code>studentinfo</code>.<code>NAME</code> AS <code>name</code>,<code>studentinfo</code>.<code>SCORE</code> AS <code>score</code> from <code>studentinfo</code> | gbk                  | gbk_chinese_ci       |<br>+—————+———————————————————————————————————————————————————————————————-+———————-+———————-+<br>1 row in set (0.01 sec)<br>拓展阅读<br>所有视图的定义都是存储在 information_schema 数据库下的 views 表中，也可以在这个表中查看所有视图的详细信息，SQL 语句如下：<br>SELECT * FROM information_schema.views;</p><p>不过，通常情况下都是使用 SHOW CREATE VIEW 语句。</p><h1 id="修改视图"><a href="#修改视图" class="headerlink" title="修改视图"></a>修改视图</h1><p>修改视图是指修改 MySQL 数据库中存在的视图，当基本表的某些字段发生变化时，可以通过修改视图来保持与基本表的一致性。<br>基本语法<br>可以使用 ALTER VIEW 语句来对已有的视图进行修改。</p><p>语法格式如下：<br>ALTER VIEW &lt;视图名&gt; AS &lt;SELECT语句&gt;</p><p>语法说明如下：<br>&lt;视图名&gt;：指定视图的名称。该名称在数据库中必须是唯一的，不能与其他表或视图同名。<br>&lt;SELECT 语句&gt;：指定创建视图的 SELECT 语句，可用于查询多个基础表或源视图。</p><p>需要注意的是，对于 ALTER VIEW 语句的使用，需要用户具有针对视图的 CREATE VIEW 和 DROP 权限，以及由 SELECT 语句选择的每一列上的某些权限。</p><p>修改视图的定义，除了可以通过 ALTER VIEW 外，也可以使用 DROP VIEW 语句先删除视图，再使用 CREATE VIEW 语句来实现。<br>修改视图内容<br>视图是一个虚拟表，实际的数据来自于基本表，所以通过插入、修改和删除操作更新视图中的数据，实质上是在更新视图所引用的基本表的数据。<br>注意：对视图的修改就是对基本表的修改，因此在修改时，要满足基本表的数据定义。</p><p>某些视图是可更新的。也就是说，可以使用 UPDATE、DELETE 或 INSERT 等语句更新基本表的内容。对于可更新的视图，视图中的行和基本表的行之间必须具有一对一的关系。</p><p>还有一些特定的其他结构，这些结构会使得视图不可更新。更具体地讲，如果视图包含以下结构中的任何一种，它就是不可更新的：<br>聚合函数 SUM()、MIN()、MAX()、COUNT() 等。<br>DISTINCT 关键字。<br>GROUP BY 子句。<br>HAVING 子句。<br>UNION 或 UNION ALL 运算符。<br>位于选择列表中的子查询。<br>FROM 子句中的不可更新视图或包含多个表。<br>WHERE 子句中的子查询，引用 FROM 子句中的表。<br>ALGORITHM 选项为 TEMPTABLE（使用临时表总会使视图成为不可更新的）的时候。</p><p>【实例 1】使用 ALTER 语句修改视图 view_students_info，输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; ALTER VIEW view_students_info<br>    -&gt; AS SELECT id,name,age<br>    -&gt; FROM tb_students_info;<br>Query OK, 0 rows affected (0.07 sec)<br>mysql&gt; DESC view_students_info;<br>+——-+————-+——+—–+———+——-+<br>| Field | Type        | Null | Key | Default | Extra |<br>+——-+————-+——+—–+———+——-+<br>| id    | int(11)     | NO   |     | 0       |       |<br>| name  | varchar(45) | YES  |     | NULL    |       |<br>| age   | int(11)     | YES  |     | NULL    |       |<br>+——-+————-+——+—–+———+——-+<br>3 rows in set (0.03 sec)<br>用户可以通过视图来插入、更新、删除表中的数据，因为视图是一个虚拟的表，没有数据。通过视图更新时转到基本表上进行更新，如果对视图增加或删除记录，实际上是对基本表增加或删除记录。</p><p>查看视图 view_students_info 的数据内容，如下所示。<br>mysql&gt; SELECT * FROM view_students_info;<br>+—-+——–+——+<br>| id | name   | age  |<br>+—-+——–+——+<br>|  1 | Dany   |   24 |<br>|  2 | Green  |   23 |<br>|  3 | Henry  |   23 |<br>|  4 | Jane   |   22 |<br>|  5 | Jim    |   24 |<br>|  6 | John   |   21 |<br>|  7 | Lily   |   22 |<br>|  8 | Susan  |   23 |<br>|  9 | Thomas |   22 |<br>| 10 | Tom    |   23 |<br>+—-+——–+——+<br>10 rows in set (0.00 sec)<br>【实例 2】使用 UPDATE 语句更新视图 view_students_info，输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; UPDATE view_students_info<br>    -&gt; SET age=25 WHERE id=1;<br>Query OK, 0 rows affected (0.24 sec)<br>Rows matched: 1  Changed: 0  Warnings: 0<br>mysql&gt; SELECT * FROM view_students_info;<br>+—-+——–+——+<br>| id | name   | age  |<br>+—-+——–+——+<br>|  1 | Dany   |   25 |<br>|  2 | Green  |   23 |<br>|  3 | Henry  |   23 |<br>|  4 | Jane   |   22 |<br>|  5 | Jim    |   24 |<br>|  6 | John   |   21 |<br>|  7 | Lily   |   22 |<br>|  8 | Susan  |   23 |<br>|  9 | Thomas |   22 |<br>| 10 | Tom    |   23 |<br>+—-+——–+——+<br>10 rows in set (0.00 sec)<br>查看基本表 tb_students_info 和视图 v_students_info 的内容，如下所示。<br>mysql&gt; SELECT * FROM tb_students_info;<br>+—-+——–+———+——+——+——–+————+<br>| id | name   | dept_id | age  | sex  | height | login_date |<br>+—-+——–+———+——+——+——–+————+<br>|  1 | Dany   |       1 |   25 | F    |    160 | 2015-09-10 |<br>|  2 | Green  |       3 |   23 | F    |    158 | 2016-10-22 |<br>|  3 | Henry  |       2 |   23 | M    |    185 | 2015-05-31 |<br>|  4 | Jane   |       1 |   22 | F    |    162 | 2016-12-20 |<br>|  5 | Jim    |       1 |   24 | M    |    175 | 2016-01-15 |<br>|  6 | John   |       2 |   21 | M    |    172 | 2015-11-11 |<br>|  7 | Lily   |       6 |   22 | F    |    165 | 2016-02-26 |<br>|  8 | Susan  |       4 |   23 | F    |    170 | 2015-10-01 |<br>|  9 | Thomas |       3 |   22 | M    |    178 | 2016-06-07 |<br>| 10 | Tom    |       4 |   23 | M    |    165 | 2016-08-05 |<br>+—-+——–+———+——+——+——–+————+<br>10 rows in set (0.00 sec)</p><p>mysql&gt; SELECT * FROM v_students_info;<br>+——+——–+——+——-+——-+———-+————+<br>| s_id | s_name | d_id | s_age | s_sex | s_height | s_date     |<br>+——+——–+——+——-+——-+———-+————+<br>|    1 | Dany   |    1 |    25 | F     |      160 | 2015-09-10 |<br>|    2 | Green  |    3 |    23 | F     |      158 | 2016-10-22 |<br>|    3 | Henry  |    2 |    23 | M     |      185 | 2015-05-31 |<br>|    4 | Jane   |    1 |    22 | F     |      162 | 2016-12-20 |<br>|    5 | Jim    |    1 |    24 | M     |      175 | 2016-01-15 |<br>|    6 | John   |    2 |    21 | M     |      172 | 2015-11-11 |<br>|    7 | Lily   |    6 |    22 | F     |      165 | 2016-02-26 |<br>|    8 | Susan  |    4 |    23 | F     |      170 | 2015-10-01 |<br>|    9 | Thomas |    3 |    22 | M     |      178 | 2016-06-07 |<br>|   10 | Tom    |    4 |    23 | M     |      165 | 2016-08-05 |<br>+——+——–+——+——-+——-+———-+————+<br>10 rows in set (0.00 sec)<br>修改视图名称<br>修改视图的名称可以先将视图删除，然后按照相同的定义语句进行视图的创建，并命名为新的视图名称。</p><h1 id="删除视图"><a href="#删除视图" class="headerlink" title="删除视图"></a>删除视图</h1><p>删除视图是指删除 MySQL 数据库中已存在的视图。删除视图时，只能删除视图的定义，不会删除数据。<br>基本语法<br>可以使用 DROP VIEW 语句来删除视图。</p><p>语法格式如下：<br>DROP VIEW &lt;视图名1&gt; [ , &lt;视图名2&gt; …]</p><p>其中：&lt;视图名&gt;指定要删除的视图名。DROP VIEW 语句可以一次删除多个视图，但是必须在每个视图上拥有 DROP 权限。<br>删除视图<br>【实例】删除 v_students_info 视图，输入的 SQL 语句和执行过程如下所示。<br>mysql&gt; DROP VIEW IF EXISTS v_students_info;<br>Query OK, 0 rows affected (0.00 sec)<br>mysql&gt; SHOW CREATE VIEW v_students_info;<br>ERROR 1146 (42S02): Table ‘test_db.v_students_info’ doesn’t exist<br>可以看到，v_students_info 视图已不存在，将其成功删除。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;视图是什么&quot;&gt;&lt;a href=&quot;#视图是什么&quot; class=&quot;headerlink&quot; title=&quot;视图是什么&quot;&gt;&lt;/a&gt;视图是什么&lt;/h1&gt;&lt;p&gt;MySQL 视图（View）是一种虚拟存在的表，同真实表一样，视图也由列和行构成，但视图并不实际存在于数据库中。行和
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>MySQL/MySQL用户管理</title>
    <link href="http://yoursite.com/2021/07/12/MySQL/MySQL%E7%94%A8%E6%88%B7%E7%AE%A1%E7%90%86/"/>
    <id>http://yoursite.com/2021/07/12/MySQL/MySQL%E7%94%A8%E6%88%B7%E7%AE%A1%E7%90%86/</id>
    <published>2021-07-12T10:04:51.672Z</published>
    <updated>2021-07-15T03:21:45.957Z</updated>
    
    <content type="html"><![CDATA[<p>MySQL 是一个多用户数据库，具有功能强大的访问控制系统，可以为不同用户指定不同权限。</p><h1 id="user权限表"><a href="#user权限表" class="headerlink" title="user权限表"></a>user权限表</h1><p>MySQL 在安装时会自动创建一个名为<code>mysql</code>的数据库，<code>mysql</code>数据库中存储的都是用户权限表。用户登录以后，MySQL 会根据这些权限表的内容为每个用户赋予相应的权限。</p><p><code>user</code>表是 MySQL 中最重要的一个权限表，用来记录允许连接到服务器的账号信息。需要注意的是，在<code>user</code>表里启用的所有权限都是全局级的，适用于所有数据库。</p><p>user 表中的字段大致可以分为 4 类，分别是用户列、权限列、安全列和资源控制列。</p><h2 id="用户列"><a href="#用户列" class="headerlink" title="用户列"></a>用户列</h2><p>用户列存储了用户连接 MySQL 数据库时需要输入的信息。需要注意的是 MySQL 5.7 版本不再使用<code>Password</code>来作为密码的字段，而改成了<code>authentication_string</code>。</p><p>MySQL 5.7 版本的用户列：</p><table><thead><tr><th align="center">字段名</th><th align="center">字段类型</th><th align="center">是否为空</th><th align="center">默认值</th><th align="center">说明</th></tr></thead><tbody><tr><td align="center">Host</td><td align="center">char(60)</td><td align="center">NO</td><td align="center">无</td><td align="center">主机名</td></tr><tr><td align="center">User</td><td align="center">char(32)</td><td align="center">NO</td><td align="center">无</td><td align="center">用户名</td></tr><tr><td align="center">authentication_string</td><td align="center">text</td><td align="center">YES</td><td align="center">无</td><td align="center">密码</td></tr></tbody></table><p>用户登录时，如果这 3 个字段同时匹配，MySQL 数据库系统才会允许其登录。创建新用户时，也是设置这 3 个字段的值。修改用户密码时，实际就是修改<code>user</code>表的<code>authentication_string</code>字段的值。因此，这 3 个字段决定了用户能否登录。</p><h2 id="权限列"><a href="#权限列" class="headerlink" title="权限列"></a>权限列</h2><p>权限列的字段决定了用户的权限，用来描述在全局范围内允许对数据和数据库进行的操作。</p><p>权限大致分为两大类，分别是高级管理权限和普通权限：</p><ul><li>高级管理权限主要对数据库进行管理，例如关闭服务的权限、超级权限和加载用户等；</li><li>普通权限主要操作数据库，例如查询权限、修改权限等。</li></ul><p><code>user</code>表的权限列包括<code>Select_priv、Insert_ priv</code>等以<code>priv</code>结尾的字段，这些字段值的数据类型为<code>ENUM</code>，可取的值只有<code>Y</code>和<code>N</code>：<code>Y</code>表示该用户有对应的权限，<code>N</code>表示该用户没有对应的权限。从安全角度考虑，这些字段的默认值都为<code>N</code>。</p><p>| 字段名 | 字段类型 | 是否为空 | 默认值 | 说明<br>| Select_priv | enum(‘N’,’Y’) | NO | N | 是否可以通过SELECT 命令查询数据<br>| Insert_priv | enum(‘N’,’Y’) | NO | N | 是否可以通过 INSERT 命令插入数据<br>| Update_priv | enum(‘N’,’Y’) | NO | N | 是否可以通过UPDATE 命令修改现有数据<br>| Delete_priv | enum(‘N’,’Y’) | NO | N | 是否可以通过DELETE 命令删除现有数据<br>| Create_priv | enum(‘N’,’Y’) | NO | N | 是否可以创建新的数据库和表<br>| Drop_priv | enum(‘N’,’Y’) | NO | N | 是否可以删除现有数据库和表<br>| Reload_priv | enum(‘N’,’Y’) | NO | N | 是否可以执行刷新和重新加载MySQL所用的各种内部缓存的特定命令，包括日志、权限、主机、查询和表<br>| Shutdown_priv | enum(‘N’,’Y’) | NO | N | 是否可以关闭MySQL服务器。将此权限提供给root账户之外的任何用户时，都应当非常谨慎<br>| Process_priv | enum(‘N’,’Y’) | NO | N | 是否可以通过SHOW PROCESSLIST命令查看其他用户的进程<br>| File_priv | enum(‘N’,’Y’) | NO | N | 是否可以执行SELECT INTO OUTFILE和LOAD DATA INFILE命令<br>| Grant_priv | enum(‘N’,’Y’) | NO | N | 是否可以将自己的权限再授予其他用户<br>| References_priv | enum(‘N’,’Y’) | NO | N | 是否可以创建外键约束<br>| Index_priv | enum(‘N’,’Y’) | NO | N | 是否可以对索引进行增删查<br>| Alter_priv | enum(‘N’,’Y’) | NO | N | 是否可以重命名和修改表结构<br>| Show_db_priv | enum(‘N’,’Y’) | NO | N | 是否可以查看服务器上所有数据库的名字，包括用户拥有足够访问权限的数据库<br>| Super_priv | enum(‘N’,’Y’) | NO | N | 是否可以执行某些强大的管理功能，例如通过KILL命令删除用户进程；使用SET GLOBAL命令修改全局MySQL变量，执行关于复制和日志的各种命令。（超级权限）<br>| Create_tmp_table_priv | enum(‘N’,’Y’) | NO    N    是否可以创建临时表<br>| Lock_tables_priv | enum(‘N’,’Y’) | NO | N    是否可以使用LOCK TABLES命令阻止对表的访问/修改<br>| Execute_priv | enum(‘N’,’Y’) | NO | N    是否可以执行存储过程<br>| Repl_slave_priv | enum(‘N’,’Y’) | NO | N    是否可以读取用于维护复制数据库环境的二进制日志文件<br>| Repl_client_priv | enum(‘N’,’Y’) | NO    N    是否可以确定复制从服务器和主服务器的位置<br>| Create_view_priv | enum(‘N’,’Y’) | NO    N    是否可以创建视图<br>| Show_view_priv | enum(‘N’,’Y’) | NO    N    是否可以查看视图<br>| Create_routine_priv | enum(‘N’,’Y’) | NO    N    是否可以更改或放弃存储过程和函数<br>| Alter_routine_priv | enum(‘N’,’Y’) | NO    N    是否可以修改或删除存储函数及函数<br>| Create_user_priv | enum(‘N’,’Y’) | NO    N    是否可以执行CREATE USER命令，这个命令用于创建新的MySQL账户<br>| Event_priv | enum(‘N’,’Y’) | NO | N | 是否可以创建、修改和删除事件<br>| Trigger_priv | enum(‘N’,’Y’) | NO | N | 是否可以创建和删除触发器<br>| Create_tablespace_priv | enum(‘N’,’Y’) | NO | N    是否可以创建表空间 </p><p>如果要修改权限，可以使用 GRANT 语句为用户赋予一些权限，也可以通过 UPDATE 语句更新 user 表的方式来设置权限。<br>安全列<br>安全列主要用来判断用户是否能够登录成功，user 表中的安全列如表 3 所示：</p><p>表 3：user 表的安全列<br>字段名    字段类型    是否为空    默认值    说明<br>ssl_type    enum(‘’,’ANY’,’X509’,’SPECIFIED’)    NO         支持ssl标准加密安全字段<br>ssl_cipher    blob    NO         支持ssl标准加密安全字段<br>x509_issuer    blob    NO         支持x509标准字段<br>x509_subject    blob    NO         支持x509标准字段<br>plugin    char(64)    NO    mysql_native_password    引入plugins以进行用户连接时的密码验证，plugin创建外部/代理用户<br>password_expired    enum(‘N’,’Y’)    NO    N    密码是否过期 (N 未过期，y 已过期)<br>password_last_changed    timestamp    YES         记录密码最近修改的时间<br>password_lifetime    smallint(5) unsigned    YES         设置密码的有效时间，单位为天数<br>account_locked    enum(‘N’,’Y’)    NO    N    用户是否被锁定（Y 锁定，N 未锁定）<br>注意：即使 password_expired 为“Y”，用户也可以使用密码登录 MySQL，但是不允许做任何操作。</p><p>通常标准的发行版不支持 ssl，读者可以使用 SHOW VARIABLES LIKE “have_openssl” 语句来查看是否具有 ssl 功能。如果 have_openssl 的值为 DISABLED，那么则不支持 ssl 加密功能。<br>资源控制列<br>资源控制列的字段用来限制用户使用的资源，user 表中的资源控制列如表 4 所示。</p><p>表 4：user 表的资源控制列<br>字段名    字段类型    是否为空    默认值    说明<br>max_questions    int(11) unsigned    NO    0    规定每小时允许执行查询的操作次数<br>max_updates    int(11) unsigned    NO    0    规定每小时允许执行更新的操作次数<br>max_connections    int(11) unsigned    NO    0    规定每小时允许执行的连接操作次数<br>max_user_connections    int(11) unsigned    NO    0    规定允许同时建立的连接次数<br>以上字段的默认值为 0，表示没有限制。一个小时内用户查询或者连接数量超过资源控制限制，用户将被锁定，直到下一个小时才可以在此执行对应的操作。可以使用 GRANT 语句更新这些字段的值。</p><h1 id="其它权限表"><a href="#其它权限表" class="headerlink" title="其它权限表"></a>其它权限表</h1><p>在 MySQL 数据库中，权限表除了<code>user</code>表外，还有<code>db</code>表、<code>tables_priv</code>表、<code>columns_priv</code>表和<code>procs_priv</code>表。</p><h2 id="db表"><a href="#db表" class="headerlink" title="db表"></a>db表</h2><p><code>db</code>表中存储了用户对某个数据库的操作权限。表中的字段大致可以分为两类，分别是用户列和权限列。</p><h3 id="用户列-1"><a href="#用户列-1" class="headerlink" title="用户列"></a>用户列</h3><p><code>db</code>表用户列有 3 个字段，分别是<code>Host、User、Db</code>，标识从某个主机连接某个用户对某个数据库的操作权限，这 3 个字段的组合构成了 db 表的主键。</p><table><thead><tr><th align="center">字段名</th><th align="center">字段类型</th><th align="center">是否为空</th><th align="center">默认值</th><th align="center">说明</th></tr></thead><tbody><tr><td align="center">Host</td><td align="center">char(60)</td><td align="center">NO</td><td align="center">无</td><td align="center">主机名</td></tr><tr><td align="center">Db</td><td align="center">char(64)</td><td align="center">NO</td><td align="center">无</td><td align="center">数据库名</td></tr><tr><td align="center">User</td><td align="center">char(32)</td><td align="center">NO</td><td align="center">无</td><td align="center">用户名</td></tr></tbody></table><h3 id="权限列-1"><a href="#权限列-1" class="headerlink" title="权限列"></a>权限列</h3><p><code>db</code>表中的权限列和 user 表中的权限列大致相同，只是user 表中的权限是针对所有数据库的，而 db 表中的权限只针对指定的数据库。如果希望用户只对某个数据库有操作权限，可以先将 user 表中对应的权限设置为 N，然后在 db 表中设置对应数据库的操作权限。</p><h2 id="tables-priv表和columns-priv表"><a href="#tables-priv表和columns-priv表" class="headerlink" title="tables_priv表和columns_priv表"></a>tables_priv表和columns_priv表</h2><p>tables_priv 表用来对单个表进行权限设置，columns_priv 表用来对单个数据列进行权限设置。tables_priv 表结构如下表所示：</p><p>字段名    字段类型    是否为空    默认值    说明<br>Host    char(60)    NO    无    主机<br>Db    char(64)    NO    无    数据库名<br>User    char(32)    NO    无    用户名<br>Table_name    char(64)    NO    无    表名<br>Grantor    char(93)    NO    无    修改该记录的用户<br>Timestamp    timestamp    NO    CURRENT_TIMESTAMP    修改该记录的时间<br>Table_priv    set(‘Select’,’Insert’,’Update’,’Delete’,’<br>Create’,’Drop’,’Grant’,’References’,<br>‘Index’,’Alter’,’Create View’,’Show view’,’Trigger’)    NO    无    表示对表的操作权限，包括 Select、Insert、Update、Delete、Create、Drop、Grant、References、Index 和 Alter 等<br>Column_priv    set(‘Select’,’Insert’,’Update’,’References’)    NO    无    表示对表中的列的操作权限，包括 Select、Insert、Update 和 References<br>columns_priv 表结构如下表所示：</p><p>字段名    字段类型    是否为空    默认值    说明<br>Host    char(60)    NO    无    主机<br>Db    char(64)    NO    无    数据库名<br>User    char(32)    NO    无    用户名<br>Table_name    char(64)    NO    无    表名<br>Column_name    char(64)    NO    无    数据列名称，用来指定对哪些数据列具有操作权限<br>Timestamp    timestamp    NO    CURRENT_TIMESTAMP    修改该记录的时间<br>Column_priv    set(‘Select’,’Insert’,’Update’,’References’)    NO    无    表示对表中的列的操作权限，包括 Select、Insert、Update 和 References</p><h2 id="procs-priv表"><a href="#procs-priv表" class="headerlink" title="procs_priv表"></a>procs_priv表</h2><p>procs_priv 表可以对存储过程和存储函数进行权限设置，procs_priv 的表结构如表所示：</p><p>字段名    字段类型    是否为空    默认值    说明<br>Host    char(60)    NO    无    主机名<br>Db    char(64)    NO    无    数据库名<br>User    char(32)    NO    无    用户名<br>Routine_name    char(64)    NO    无    表示存储过程或函数的名称<br>Routine_type    enum(‘FUNCTION’,’PROCEDURE’)    NO    无    表示存储过程或函数的类型，Routine_type 字段有两个值，分别是 FUNCTION 和 PROCEDURE。FUNCTION 表示这是一个函数；PROCEDURE 表示这是一个<br>存储过程。<br>Grantor    char(93)    NO    无    插入或修改该记录的用户<br>Proc_priv    set(‘Execute’,’Alter Routine’,’Grant’)    NO    无    表示拥有的权限，包括 Execute、Alter Routine、Grant 3种<br>Timestamp    timestamp    NO    CURRENT_TIMESTAMP    表示记录更新时间</p><h1 id="创建用户"><a href="#创建用户" class="headerlink" title="创建用户"></a>创建用户</h1><p>MySQL 在安装时，会默认创建一个名为 root 的用户，该用户拥有超级权限，可以控制整个 MySQL 服务器。</p><p>在对 MySQL 的日常管理和操作中，为了避免有人恶意使用 root 用户控制数据库，我们通常创建一些具有适当权限的用户，尽可能地不用或少用 root 用户登录系统，以此来确保数据的安全访问。</p><p>MySQL 提供了以下 3 种方法创建用户：</p><ul><li>使用<code>CREATE USER</code>语句创建用户</li><li>在<code>mysql.user</code>表中添加用户</li><li>使用<code>GRANT</code>语句创建用户</li></ul><h2 id="使用CREATE-USER语句创建用户"><a href="#使用CREATE-USER语句创建用户" class="headerlink" title="使用CREATE USER语句创建用户"></a>使用CREATE USER语句创建用户</h2><p>可以使用<code>CREATE USER</code>语句来创建 MySQL 用户，并设置相应的密码。其基本语法格式如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CREATE USER &lt;用户&gt; [ IDENTIFIED BY [ PASSWORD ] &#39;password&#39; ] [ ,用户 [ IDENTIFIED BY [ PASSWORD ] &#39;password&#39; ]]</span><br></pre></td></tr></table></figure><p>参数说明如下：</p><ol><li>用户<br>指定创建用户账号，格式为<code>user_name&#39;@&#39;host_name</code>。这里的<code>user_name</code>是用户名，<code>host_name</code>为主机名，即用户连接 MySQL 时所用主机的名字。如果在创建的过程中，只给出了用户名，而没指定主机名，那么主机名默认为“%”，表示一组主机，即对所有主机开放权限。</li><li>IDENTIFIED BY子句<br>用于指定用户密码。新用户可以没有初始密码，若该用户不设密码，可省略此子句。</li><li>PASSWORD ‘password’<br>PASSWORD 表示使用哈希值设置密码，该参数可选。如果密码是一个普通的字符串，则不需要使用 PASSWORD 关键字。’password’ 表示用户登录时使用的密码，需要用单引号括起来。</li></ol><p>使用<code>CREATE USER</code>语句时应注意以下几点：</p><ul><li>CREATE USER 语句可以不指定初始密码。但是从安全的角度来说，不推荐这种做法。</li><li>使用 CREATE USER 语句必须拥有 mysql 数据库的 INSERT 权限或全局 CREATE USER 权限。</li><li>使用 CREATE USER 语句创建一个用户后，MySQL 会在 mysql 数据库的 user 表中添加一条新记录。</li><li>CREATE USER 语句可以同时创建多个用户，多个用户用逗号隔开。</li></ul><p>新创建的用户拥有的权限很少，它们只能执行不需要权限的操作。如登录 MySQL、使用 SHOW 语句查询所有存储引擎和字符集的列表等。如果两个用户的用户名相同，但主机名不同，MySQL 会将它们视为两个用户，并允许为这两个用户分配不同的权限集合。</p><p>使用 CREATE USER 创建一个用户，用户名是 test1，密码是 test1，主机名是 localhost。SQL 语句和执行过程如下。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; CREATE USER &#39;test1&#39;@&#39;localhost&#39; IDENTIFIED BY &#39;test1&#39;;</span><br><span class="line">Query OK, 1 rows affected (0.06 sec)</span><br></pre></td></tr></table></figure><p>结果显示，创建 test1 用户成功。</p><p>在实际应用中，我们应避免明文指定密码，可以通过 PASSWORD 关键字使用密码的哈希值设置密码。</p><p>在 MySQL 中，可以使用 password() 函数获取密码的哈希值，查看 test1 哈希值的 SQL 语句和执行过程如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; SELECT password(&#39;test1&#39;);</span><br><span class="line">+-------------------------------------------+</span><br><span class="line">| password(&#39;test1&#39;)                         |</span><br><span class="line">+-------------------------------------------+</span><br><span class="line">| *06C0BF5B64ECE2F648B5F048A71903906BA08E5C |</span><br><span class="line">+-------------------------------------------+</span><br><span class="line">1 row in set, 1 warning (0.00 sec)</span><br></pre></td></tr></table></figure><p>“*06C0BF5B64ECE2F648B5F048A71903906BA08E5C”就是 test1 的哈希值。下面创建用户 test1，SQL 语句和执行过程如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; CREATE USER &#39;test1&#39;@&#39;localhost&#39;IDENTIFIED BY PASSWORD &#39;*06C0BF5B64ECE2F648B5F048A71903906BA08E5C&#39;;</span><br><span class="line">Query OK, 0 rows affected, 1 warning (0.00 sec)</span><br></pre></td></tr></table></figure><p>执行成功后就可以使用密码“test1”登录了。</p><h2 id="使用-INSERT-语句新建用户"><a href="#使用-INSERT-语句新建用户" class="headerlink" title="使用 INSERT 语句新建用户"></a>使用 INSERT 语句新建用户</h2><p>可以使用<code>INSERT</code>语句将用户的信息添加到<code>mysql.user</code>表中，但必须拥有对<code>mysql.user</code>表的<code>INSERT</code>权限。通常<code>INSERT</code>语句只添加<code>Host、User</code>和<code>authentication_string</code>这 3 个字段的值。</p><p>MySQL 5.7 的<code>user</code>表中的密码字段从<code>Password</code>变成了<code>authentication_string</code>，如果是 MySQL 5.7 之前的版本，将<code>authentication_string</code>字段替换成<code>Password</code>即可。</p><p>使用<code>INSERT</code>语句创建用户的代码如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">INSERT INTO mysql.user(Host, User,  authentication_string, ssl_cipher, x509_issuer, x509_subject) VALUES (&#39;hostname&#39;, &#39;username&#39;, PASSWORD(&#39;password&#39;), &#39;&#39;, &#39;&#39;, &#39;&#39;);</span><br></pre></td></tr></table></figure><p>由于<code>mysql</code>数据库的<code>user</code>表中，<code>ssl_cipher、x509_issuer</code>和<code>x509_subject</code>这 3 个字段没有默认值，所以向<code>user</code>表插入新记录时，一定要设置这 3 个字段的值，否则<code>INSERT</code>语句将不能执行。</p><p>下面使用 INSERT 语句创建名为 test2 的用户，主机名是 localhost，密码也是 test2。SQL 语句和执行过程如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; INSERT INTO mysql.user(Host, User, authentication_string, ssl_cipher, x509_issuer, x509_subject)</span><br><span class="line">       VALUES (&#39;localhost&#39;, &#39;test2&#39;, PASSWORD(&#39;test2&#39;), &#39;&#39;, &#39;&#39;, &#39;&#39;);</span><br><span class="line">Query OK, 1 row affected, 1 warning (0.02 sec)</span><br></pre></td></tr></table></figure><p>结果显示，新建用户成功。但是这时如果通过该账户登录 MySQL 服务器，不会登录成功，因为 test2 用户还没有生效。</p><p>可以使用 FLUSH 命令让用户生效，命令如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">FLUSH PRIVILEGES;</span><br></pre></td></tr></table></figure><p>使用以上命令可以让 MySQL 刷新系统权限相关表。执行 FLUSH 命令需要 RELOAD 权限。</p><p>注意：user 表中的 User 和 Host 字段区分大小写，创建用户时要指定正确的用户名称或主机名。</p><h2 id="使用GRANT语句新建用户"><a href="#使用GRANT语句新建用户" class="headerlink" title="使用GRANT语句新建用户"></a>使用GRANT语句新建用户</h2><p>虽然 CREATE USER 和 INSERT INTO 语句都可以创建普通用户，但是这两种方式不便授予用户权限。于是 MySQL 提供了 GRANT 语句。</p><p>使用 GRANT 语句创建用户的基本语法形式如下:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">GRANT priv_type ON database.table TO user [IDENTIFIED BY [PASSWORD] &#39;password&#39;]</span><br></pre></td></tr></table></figure><p>其中：<br>priv_type 参数表示新用户的权限；<br>database.table 参数表示新用户的权限范围，即只能在指定的数据库和表上使用自己的权限；<br>user 参数指定新用户的账号，由用户名和主机名构成；<br>IDENTIFIED BY 关键字用来设置密码；<br>password 参数表示新用户的密码。<br>例 4<br>下面使用 GRANT 语句创建名为 test3 的用户，主机名为 localhost，密码为 test3。该用户对所有数据库的所有表都有 SELECT 权限。SQL 语句和执行过程如下：<br>mysql&gt; GRANT SELECT ON<em>.</em> TO ‘test3’@localhost IDENTIFIED BY ‘test3’;<br>Query OK, 0 rows affected, 1 warning (0.01 sec)<br>其中，“<em>.</em>” 表示所有数据库下的所有表。结果显示创建用户成功，且 test3 用户对所有表都有查询（SELECT）权限。</p><p>技巧：GRANT 语句是 MySQL 中一个非常重要的语句，它可以用来创建用户、修改用户密码和设置用户权限。教程后面会详细介绍如何使用 GRANT 语句修改密码、更改权限。</p><h1 id="修改用户"><a href="#修改用户" class="headerlink" title="修改用户"></a>修改用户</h1><p>在 MySQL 中，我们可以使用<code>RENAME USER</code>语句修改一个或多个已经存在的用户账号。</p><p>语法格式如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">RENAME USER &lt;旧用户&gt; TO &lt;新用户&gt;</span><br></pre></td></tr></table></figure><p>其中：<br>&lt;旧用户&gt;：系统中已经存在的 MySQL 用户账号。<br>&lt;新用户&gt;：新的 MySQL 用户账号。</p><p>使用 RENAME USER 语句时应注意以下几点：<br>RENAME USER 语句用于对原有的 MySQL 用户进行重命名。<br>若系统中旧账户不存在或者新账户已存在，该语句执行时会出现错误。<br>使用 RENAME USER 语句，必须拥有 mysql 数据库的 UPDATE 权限或全局 CREATE USER 权限。<br>例 1<br>使用 RENAME USER 语句将用户名 test1 修改为 testUser1，主机是 localhost。SQL 语句和执行过程如下。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; RENAME USER &#39;test1&#39;@&#39;localhost&#39;</span><br><span class="line">    -&gt; TO &#39;testUser1&#39;@&#39;localhost&#39;;</span><br><span class="line">Query OK, 0 rows affected (0.03 sec)</span><br></pre></td></tr></table></figure><p>在 cmd 命令行工具中，使用 testUser1 用户登录数据库服务器，如下所示。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">C:\Users\USER&gt;mysql -h localhost -u testUser1 -p</span><br><span class="line">Enter password: *****</span><br><span class="line">Welcome to the MySQL monitor.  Commands end with ; or \g.</span><br><span class="line">Your MySQL connection id is 7</span><br><span class="line">Server version: 5.7.20-log MySQL Community Server (GPL)</span><br><span class="line">Copyright (c) 2000, 2017, Oracle and&#x2F;or its affiliates. All rights reserved.</span><br><span class="line">Oracle is a registered trademark of Oracle Corporation and&#x2F;or its</span><br><span class="line">affiliates. Other names may be trademarks of their respective</span><br><span class="line">owners.</span><br><span class="line">Type &#39;help;&#39; or &#39;\h&#39; for help. Type &#39;\c&#39; to clear the current input statement.</span><br></pre></td></tr></table></figure><h1 id="删除用户"><a href="#删除用户" class="headerlink" title="删除用户"></a>删除用户</h1><p>在 MySQL 数据库中，可以使用 DROP USER 语句删除用户，也可以直接在 mysql.user 表中删除用户以及相关权限。</p><ol><li>使用 DROP USER 语句删除普通用户<br>使用 DROP USER 语句删除用户的语法格式如下：<br>DROP USER &lt;用户1&gt; [ , &lt;用户2&gt; ]…</li></ol><p>其中，用户用来指定需要删除的用户账号。</p><p>使用 DROP USER 语句应注意以下几点：<br>DROP USER 语句可用于删除一个或多个用户，并撤销其权限。<br>使用 DROP USER 语句必须拥有 mysql 数据库的 DELETE 权限或全局 CREATE USER 权限。<br>在 DROP USER 语句的使用中，若没有明确地给出账户的主机名，则该主机名默认为“%”。<br>注意：用户的删除不会影响他们之前所创建的表、索引或其他数据库对象，因为 MySQL 并不会记录是谁创建了这些对象。</p><p>例 1<br>下面使用 DROP USER 语句删除用户‘test1@’localhost’。SQL 语句和执行过程如下。<br>mysql&gt; DROP USER ‘test1’@’localhost’;<br>Query OK, 0 rows affected (0.00 sec)<br>在 cmd 命令行工具中，使用 test1 用户登录数据库服务器，发现登录失败，说明用户已经删除，如下所示。<br>C:\Users\USER&gt;mysql -h localhost -u test1 -p<br>Enter password: ****<br>ERROR 1045 (28000): Access denied for user ‘test’@’localhost’ (using  password: YES)<br>2. 使用DELETE语句删除普通用户<br>可以使用 DELETE 语句直接删除 mysql.user 表中相应的用户信息，但必须拥有 mysql.user 表的 DELETE 权限。其基本语法格式如下：<br>DELETE FROM mysql.user WHERE Host=’hostname’ AND User=’username’;</p><p>Host 和 User 这两个字段都是 mysql.user 表的主键。因此，需要两个字段的值才能确定一条记录。<br>例 2<br>下面使用 DELETE 语句删除用户‘test2’@’localhost’。SQL 语句和执行过程如下所示。<br>DELETE FROM mysql.user WHERE Host=’localhost’AND User=’test2’;<br>Query OK, 1 rows affected (0.00 sec)<br>结果显示删除成功。可以使用 SELETE 语句查询 mysql.user 表，以确定该用户是否已经成功删除。</p><h1 id="查看用户权限"><a href="#查看用户权限" class="headerlink" title="查看用户权限"></a>查看用户权限</h1><p>在 MySQL 中，可以通过查看 mysql.user 表中的数据记录来查看相应的用户权限，也可以使用 SHOW GRANTS 语句查询用户的权限。</p><p>mysql 数据库下的 user 表中存储着用户的基本权限，可以使用 SELECT 语句来查看。SELECT 语句的代码如下：<br>SELECT * FROM mysql.user;</p><p>要执行该语句，必须拥有对 user 表的查询权限。</p><p>注意：新创建的用户只有登录 MySQL 服务器的权限，没有任何其它权限，不能查询 user 表。</p><p>除了使用 SELECT 语句之外，还可以使用 SHOW GRANTS FOR 语句查看权限。其语法格式如下：<br>SHOW GRANTS FOR ‘username’@’hostname’;</p><p>其中，username 表示用户名，hostname 表示主机名或主机 IP。<br>例 1<br>下面创建 testuser1 用户并查询权限，SQL 语句和执行过程如下：<br>mysql&gt; CREATE USER ‘testuser1’@’localhost’;<br>Query OK, 0 rows affected (0.00 sec)</p><p>mysql&gt; SHOW GRANTS FOR ‘testuser1’@’localhost’;<br>+———————————————–+<br>| Grants for testuser1@localhost                |<br>+———————————————–+<br>| GRANT USAGE ON <em>.</em> TO ‘testuser1’@’localhost’ |<br>+———————————————–+<br>1 row in set (0.00 sec)<br>其中，USAGE ON <em>.</em>表示该用户对任何数据库和任何表都没有权限。<br>例 2<br>下面查询 root 用户的权限，代码如下：<br>mysql&gt; SHOW GRANTS FOR ‘root’@’localhost’;<br>+———————————————————————+<br>| Grants for root@localhost                                           |<br>+———————————————————————+<br>| GRANT ALL PRIVILEGES ON <em>.</em> TO ‘root’@’localhost’ WITH GRANT OPTION |<br>| GRANT PROXY ON ‘’@’’ TO ‘root’@’localhost’ WITH GRANT OPTION        |<br>+———————————————————————+<br>2 rows in set (0.00 sec)</p><h1 id="GRANT：用户授权"><a href="#GRANT：用户授权" class="headerlink" title="GRANT：用户授权"></a>GRANT：用户授权</h1><p>授权就是为某个用户赋予某些权限。例如，可以为新建的用户赋予查询所有数据库和表的权限。MySQL 提供了 GRANT 语句来为用户设置权限。</p><p>在 MySQL 中，拥有 GRANT 权限的用户才可以执行 GRANT 语句，其语法格式如下：<br>GRANT priv_type [(column_list)] ON database.table<br>TO user [IDENTIFIED BY [PASSWORD] ‘password’]<br>[, user[IDENTIFIED BY [PASSWORD] ‘password’]] …<br>[WITH with_option [with_option]…]<br>其中：<br>priv_type 参数表示权限类型；<br>columns_list 参数表示权限作用于哪些列上，省略该参数时，表示作用于整个表；<br>database.table 用于指定权限的级别；<br>user 参数表示用户账户，由用户名和主机名构成，格式是“‘username’@’hostname’”；<br>IDENTIFIED BY 参数用来为用户设置密码；<br>password 参数是用户的新密码。</p><p>WITH 关键字后面带有一个或多个 with_option 参数。这个参数有 5 个选项，详细介绍如下：<br>GRANT OPTION：被授权的用户可以将这些权限赋予给别的用户；<br>MAX_QUERIES_PER_HOUR count：设置每个小时可以允许执行 count 次查询；<br>MAX_UPDATES_PER_HOUR count：设置每个小时可以允许执行 count 次更新；<br>MAX_CONNECTIONS_PER_HOUR count：设置每小时可以建立 count 个连接;<br>MAX_USER_CONNECTIONS count：设置单个用户可以同时具有的 count 个连接。</p><p>MySQL 中可以授予的权限有如下几组：<br>列权限，和表中的一个具体列相关。例如，可以使用 UPDATE 语句更新表 students 中 name 列的值的权限。<br>表权限，和一个具体表中的所有数据相关。例如，可以使用 SELECT 语句查询表 students 的所有数据的权限。<br>数据库权限，和一个具体的数据库中的所有表相关。例如，可以在已有的数据库 mytest 中创建新表的权限。<br>用户权限，和 MySQL 中所有的数据库相关。例如，可以删除已有的数据库或者创建一个新的数据库的权限。</p><p>对应地，在 GRANT 语句中可用于指定权限级别的值有以下几类格式：<br><em>：表示当前数据库中的所有表。<br><em>.</em>：表示所有数据库中的所有表。<br>db_name.*：表示某个数据库中的所有表，db_name 指定数据库名。<br>db_name.tbl_name：表示某个数据库中的某个表或视图，db_name 指定数据库名，tbl_name 指定表名或视图名。<br>db_name.routine_name：表示某个数据库中的某个存储过程或函数，routine_name 指定存储过程名或函数名。<br>TO 子句：如果权限被授予给一个不存在的用户，MySQL 会自动执行一条 CREATE USER 语句来创建这个用户，但同时必须为该用户设置密码。<br>权限类型说明<br>下面讲解 GRANT 语句中的权限类型（可参考《MySQL user表详解》一节阅读）。<br>1）授予数据库权限时，&lt;权限类型&gt;可以指定为以下值：<br>权限名称    对应user表中的字段    说明<br>SELECT    Select_priv    表示授予用户可以使用 SELECT 语句访问特定数据库中所有表和视图的权限。<br>INSERT    Insert_priv    表示授予用户可以使用 INSERT 语句向特定数据库中所有表添加数据行的权限。<br>DELETE    Delete_priv    表示授予用户可以使用 DELETE 语句删除特定数据库中所有表的数据行的权限。<br>UPDATE    Update_priv    表示授予用户可以使用 UPDATE 语句更新特定数据库中所有数据表的值的权限。<br>REFERENCES    References_priv    表示授予用户可以创建指向特定的数据库中的表外键的权限。<br>CREATE    Create_priv    表示授权用户可以使用 CREATE TABLE 语句在特定数据库中创建新表的权限。<br>ALTER    Alter_priv     表示授予用户可以使用 ALTER TABLE 语句修改特定数据库中所有数据表的权限。<br>SHOW VIEW    Show_view_priv    表示授予用户可以查看特定数据库中已有视图的视图定义的权限。<br>CREATE ROUTINE    Create_routine_priv    表示授予用户可以为特定的数据库创建存储过程和存储函数的权限。<br>ALTER ROUTINE    Alter_routine_priv    表示授予用户可以更新和删除数据库中已有的存储过程和存储函数的权限。<br>INDEX    Index_priv    表示授予用户可以在特定数据库中的所有数据表上定义和删除索引的权限。<br>DROP    Drop_priv    表示授予用户可以删除特定数据库中所有表和视图的权限。<br>CREATE TEMPORARY TABLES    Create_tmp_table_priv    表示授予用户可以在特定数据库中创建临时表的权限。<br>CREATE VIEW    Create_view_priv    表示授予用户可以在特定数据库中创建新的视图的权限。<br>EXECUTE ROUTINE    Execute_priv    表示授予用户可以调用特定数据库的存储过程和存储函数的权限。<br>LOCK TABLES    Lock_tables_priv    表示授予用户可以锁定特定数据库的已有数据表的权限。<br>ALL 或 ALL PRIVILEGES 或 SUPER    Super_priv    表示以上所有权限/超级权限<br>2) 授予表权限时，&lt;权限类型&gt;可以指定为以下值：<br>权限名称    对应user表中的字段    说明<br>SELECT    Select_priv    授予用户可以使用 SELECT 语句进行访问特定表的权限<br>INSERT    Insert_priv    授予用户可以使用 INSERT 语句向一个特定表中添加数据行的权限<br>DELETE    Delete_priv    授予用户可以使用 DELETE 语句从一个特定表中删除数据行的权限<br>DROP    Drop_priv    授予用户可以删除数据表的权限<br>UPDATE    Update_priv    授予用户可以使用 UPDATE 语句更新特定数据表的权限<br>ALTER    Alter_priv     授予用户可以使用 ALTER TABLE 语句修改数据表的权限<br>REFERENCES    References_priv    授予用户可以创建一个外键来参照特定数据表的权限<br>CREATE    Create_priv    授予用户可以使用特定的名字创建一个数据表的权限<br>INDEX    Index_priv    授予用户可以在表上定义索引的权限<br>ALL 或 ALL PRIVILEGES 或 SUPER    Super_priv    所有的权限名<br>3) 授予列权限时，&lt;权限类型&gt;的值只能指定为 SELECT、INSERT 和 UPDATE，同时权限的后面需要加上列名列表 column-list。<br>4) 最有效率的权限是用户权限。<br>授予用户权限时，&lt;权限类型&gt;除了可以指定为授予数据库权限时的所有值之外，还可以是下面这些值：<br>CREATE USER：表示授予用户可以创建和删除新用户的权限。<br>SHOW DATABASES：表示授予用户可以使用 SHOW DATABASES 语句查看所有已有的数据库的定义的权限。<br>例 1<br>使用 GRANT 语句创建一个新的用户 testUser，密码为 testPwd。用户 testUser 对所有的数据有查询、插入权限，并授予 GRANT 权限。SQL 语句和执行过程如下。<br>mysql&gt; GRANT SELECT,INSERT ON *.</em><br>    -&gt; TO ‘testUser’@’localhost’<br>    -&gt; IDENTIFIED BY ‘testPwd’<br>    -&gt; WITH GRANT OPTION;<br>Query OK, 0 rows affected, 1 warning (0.05 sec)<br>使用 SHOW GRANTS 语句查询用户 testUser 的权限，如下所示。<br>mysql&gt; SHOW GRANTS FOR ‘testUser’@’localhost’;<br>+————————————————————————-+<br>| Grants for testUser@localhost                                           |<br>+————————————————————————-+<br>| GRANT SELECT, INSERT ON <em>.</em> TO ‘testUser’@’localhost’ WITH GRANT OPTION |<br>+————————————————————————-+<br>1 row in set (0.00 sec)<br>结果显示，testUser 对所有数据库的所有表有查询、插入权限，并可以将这些权限赋予给别的用户。<br>拓展<br>数据库管理员给普通用户授权时一定要特别小心，如果授权不当，可能会给数据库带来致命的破坏。一旦发现给用户的权限太多，应该尽快使用 REVOKE 语句将权限收回。此处特别注意，最好不要授予普通用户 SUPER 权限，GRANT 权限。关于用户权限的删除，可阅读学习《MySQL REVOKE：删除用户权限》一节。</p><h1 id="REVOKE：删除用户权限"><a href="#REVOKE：删除用户权限" class="headerlink" title="REVOKE：删除用户权限"></a>REVOKE：删除用户权限</h1><p>在 MySQL 中，可以使用 REVOKE 语句删除某个用户的某些权限（此用户不会被删除），在一定程度上可以保证系统的安全性。例如，如果数据库管理员觉得某个用户不应该拥有 DELETE 权限，那么就可以删除 DELETE 权限。</p><p>使用 REVOKE 语句删除权限的语法格式有两种形式，如下所示：<br>1）第一种<br>删除用户某些特定的权限，语法格式如下：<br>REVOKE priv_type [(column_list)]…<br>ON database.table<br>FROM user [, user]…</p><p>REVOKE 语句中的参数与 GRANT 语句的参数意思相同。其中：<br>priv_type 参数表示权限的类型；<br>column_list 参数表示权限作用于哪些列上，没有该参数时作用于整个表上；<br>user 参数由用户名和主机名构成，格式为“username’@’hostname’”。<br>2）第二种<br>删除特定用户的所有权限，语法格式如下：<br>REVOKE ALL PRIVILEGES, GRANT OPTION FROM user [, user] …</p><p>删除用户权限需要注意以下几点：<br>REVOKE 语法和 GRANT 语句的语法格式相似，但具有相反的效果。<br>要使用 REVOKE 语句，必须拥有 MySQL 数据库的全局 CREATE USER 权限或 UPDATE 权限。<br>例 1<br>使用 REVOKE 语句取消用户 testUser 的插入权限，SQL 语句和执行过程如下。<br>mysql&gt; REVOKE INSERT ON <em>.</em><br>    -&gt; FROM ‘testUser’@’localhost’;<br>Query OK, 0 rows affected (0.01 sec)</p><p>mysql&gt; SHOW GRANTS FOR ‘testUser’@’localhost’;<br>+—————————————————————–+<br>| Grants for testUser@localhost                                   |<br>+—————————————————————–+<br>| GRANT SELECT ON <em>.</em> TO ‘testUser’@’localhost’ WITH GRANT OPTION |<br>+—————————————————————–+<br>1 row in set (0.00 sec)<br>结果显示，删除 testUser 用户的 INSERT 权限成功。</p><h1 id="登录和退出服务器"><a href="#登录和退出服务器" class="headerlink" title="登录和退出服务器"></a>登录和退出服务器</h1><p>在《登录MySQL数据库》一节，我们了解到用户可以通过 mysql 命令来登录 MySQL 服务器。但是前面还有些参数没有介绍到，本节将详细介绍 MySQL 中登录和退出服务器的方法。</p><p>启动 MySQL 服务后，可以使用以下命令来登录。<br>mysql -h hostname|hostlP -p port -u username -p DatabaseName -e “SQL语句”</p><p>对上述参数说明如下：<br>-h：指定连接 MySQL 服务器的地址。可以用两种方式表示，hostname 为主机名，hostIP 为主机 IP 地址。<br>-p：指定连接 MySQL 服务器的端口号，port 为连接的端口号。MySQL 的默认端口号是 3306，因此如果不指定该参数，默认使用 3306 连接 MySQL 服务器。<br>-u：指定连接 MySQL 服务器的用户名，username 为用户名。<br>-p：提示输入密码，即提示 Enter password。<br>DatabaseName：指定连接到 MySQL 服务器后，登录到哪一个数据库中。如果没有指定，默认为 mysql 数据库。<br>-e：指定需要执行的 SQL 语句，登录 MySQL 服务器后执行这个 SQL 语句，然后退出 MySQL 服务器。<br>例 1<br>下面使用 root 用户登录到 test 数据库中，命令和运行过程如下：<br>C:\Users\11645&gt;mysql -h localhost -u root -p test<br>Enter password: ****<br>Welcome to the MySQL monitor.  Commands end with ; or \g.<br>Your MySQL connection id is 2<br>Server version: 5.7.29-log MySQL Community Server (GPL)</p><p>Copyright (c) 2000, 2020, Oracle and/or its affiliates. All rights reserved.</p><p>Oracle is a registered trademark of Oracle Corporation and/or its<br>affiliates. Other names may be trademarks of their respective<br>owners.</p><p>Type ‘help;’ or ‘\h’ for help. Type ‘\c’ to clear the current input statement.<br>上述命令中，通过值 localhost 指定 MySQL 服务器的地址，参数 -u 指定了登录 MySQL 服务器的用户账户，参数 -p 表示会出现输入密码提示信息，最后值“test”指定了登录成功后要使用的数据库。</p><p>由结果可以看到，输入命令后，会出现“Enter password”提示信息，在这条信息之后输入密码，然后按 Enter 键。密码正确后，就成功登录到 MySQL 服务器了。<br>例 2<br>下面使用 root 用户登录到自己计算机的 mysql 数据库，同时查询 student 表的表结构，命令和运行过程如下：<br>C:\Users\11645&gt;mysql -h localhost -u root -p test -e”DESC student”<br>Enter password: ****<br>+——-+————-+——+—–+———+——-+<br>| Field | Type        | Null | Key | Default | Extra |<br>+——-+————-+——+—–+———+——-+<br>| id    | int(4)      | NO   | PRI | NULL    |       |<br>| name  | varchar(20) | YES  |     | NULL    |       |<br>| age   | int(4)      | YES  |     | NULL    |       |<br>| stuno | int(11)     | YES  |     | NULL    |       |<br>+——-+————-+——+—–+———+——-+<br>结果显示，执行命令并输入正确密码后，窗口中就会显示出 student 表的基本结构。</p><p>用户也可以直接在 mysql 命令的 -p 后加上登录密码，登录密码与 -p 之间没有空格。<br>例 3<br>下面使用 root 用户登录到自己计算机的 MySQL 服务器中，密码直接加在 mysql 命令中。命令如下：<br>C:\Users\11645&gt;mysql -h localhost -u root -proot<br>mysql: [Warning] Using a password on the command line interface can be insecure.<br>Welcome to the MySQL monitor.  Commands end with ; or \g.<br>Your MySQL connection id is 4<br>Server version: 5.7.29-log MySQL Community Server (GPL)</p><p>Copyright (c) 2000, 2020, Oracle and/or its affiliates. All rights reserved.</p><p>Oracle is a registered trademark of Oracle Corporation and/or its<br>affiliates. Other names may be trademarks of their respective<br>owners.</p><p>Type ‘help;’ or ‘\h’ for help. Type ‘\c’ to clear the current input statement.<br>上述命令执行后，后面不会提示输入密码。因为 -p 后面有密码，MySQL 会直接使用这个密码。</p><p>退出 MySQL 服务器的方式很简单，只要在命令行输入 EXIT 或 QUIT 即可。“\q”是 QUIT 的缩写，也可以用来退出 MySQL 服务器。退出后就会显示 Bye。如下所示：<br>mysql&gt; QUIT;<br>Bye</p><h1 id="root修改普通用户密码"><a href="#root修改普通用户密码" class="headerlink" title="root修改普通用户密码"></a>root修改普通用户密码</h1><p>在 MySQL 中，root 用户拥有很高的权限，不仅可以修改自己的密码，还可以修改其他用户的密码。本节主要介绍 root 用户修改普通用户密码的几种方法。<br>使用SET语句修改普通用户的密码<br>在 MySQL 中，只有 root 用户可以通过更新 MySQL 数据库来更改密码。使用 root 用户登录到 MySQL 服务器后，可以使用 SET 语句来修改普通用户密码。语法格式如下：<br>SET PASSWORD FOR ‘username’@’hostname’ = PASSWORD (‘newpwd’);</p><p>其中，username 参数是普通用户的用户名，hostname 参数是普通用户的主机名，newpwd 是要更改的新密码。</p><p>注意：新密码必须使用 PASSWORD() 函数来加密，如果不使用 PASSWORD() 加密，也会执行成功，但是用户会无法登录。</p><p>如果是普通用户修改密码，可省略 FOR 子句来更改自己的密码。语法格式如下：<br>SET PASSWORD = PASSWORD(‘newpwd’);</p><p>示例 1<br>首先创建一个没有密码的 testuser 用户，SQL 语句和运行结果如下：<br>mysql&gt; CREATE USER ‘testuser’@’localhost’;<br>Query OK, 0 rows affected (0.14 sec)<br>root 用户登录 MySQL 服务器后，再使用 SET 语句将 testuser 用户的密码修改为“newpwd”，SQL 语句和运行结果如下：<br>mysql&gt; SET PASSWORD FOR ‘testuser’@’localhost’ = PASSWORD(“newpwd”);<br>Query OK, 0 rows affected, 1 warning (0.01 sec)<br>由运行结果可以看出，SET 语句执行成功，testuser 用户的密码被成功设置为“newpwd”。</p><p>下面验证 testuser 用户密码是否修改成功。退出 MySQL 服务器，使用 testuser 用户登录，输入密码“newpwd”，SQL 语句和运行结果如下：<br>C:\Users\leovo&gt;mysql -utestuser -p<br>Enter password: <strong>**</strong><br>Welcome to the MySQL monitor.  Commands end with ; or \g.<br>Your MySQL connection id is 15<br>Server version: 5.7.29-log MySQL Community Server (GPL)</p><p>Copyright (c) 2000, 2020, Oracle and/or its affiliates. All rights reserved.</p><p>Oracle is a registered trademark of Oracle Corporation and/or its<br>affiliates. Other names may be trademarks of their respective<br>owners.</p><p>Type ‘help;’ or ‘\h’ for help. Type ‘\c’ to clear the current input statement.<br>由运行结果可以看出，testuser 用户登录成功，修改密码成功。<br>示例 2<br>使用 testuser 用户登录 MySQL 服务器，再使用 SET 语句将密码更改为“newpwd1”，SQL 语句和运行结果如下所示：<br>mysql&gt; SET PASSWORD = PASSWORD(‘newpwd1’);<br>Query OK, 0 rows affected, 1 warning (0.00 sec)</p><p>由运行结果可以看出，修改密码成功。<br>使用UPDATE语句修改普通用户的密码<br>使用 root 用户登录 MySQL 服务器后，可以使用 UPDATE 语句修改 MySQL 数据库的 user 表的 authentication_string 字段，从而修改普通用户的密码。UPDATA 语句的语法如下：<br>UPDATE MySQL.user SET authentication_string = PASSWORD(“newpwd”) WHERE User = “username” AND Host = “hostname”;</p><p>其中，username 参数是普通用户的用户名，hostname 参数是普通用户的主机名，newpwd 是要更改的新密码。</p><p>注意，执行 UPDATE 语句后，需要执行 FLUSH PRIVILEGES 语句重新加载用户权限。<br>示例 3<br>使用 root 用户登录 MySQL 服务器，再使用 UPDATE 语句将 testuser 用户的密码修改为“newpwd2”的 SQL 语句和运行结果如下：<br>mysql&gt; UPDATE MySQL.user SET authentication_string = PASSWORD (“newpwd2”)<br>    -&gt; WHERE User = “testuser” AND Host = “localhost”;<br>Query OK, 1 row affected, 1 warning (0.07 sec)<br>Rows matched: 1  Changed: 1  Warnings: 1<br>mysql&gt; FLUSH PRIVILEGES;<br>Query OK, 0 rows affected (0.03 sec)<br>由运行结果可以看出，密码修改成功。testuser 的密码被修改成了 newpwd2。使用 FLUSH PRIVILEGES 重新加载权限后，就可以使用新的密码登录 testuser 用户了。<br>使用 GRANT 语句修改普通用户密码<br>除了前面介绍的方法，还可以在全局级别使用 GRANT USAGE 语句指定某个账户的密码而不影响账户当前的权限。需要注意的是，使用 GRANT 语句修改密码，必须拥有 GRANT 权限。一般情况下最好使用该方法来指定或修改密码。语法格式如下：<br>GRANT USAGE ON <em>.</em> TO ‘user’@’hostname’ IDENTIFIED BY ‘newpwd’;</p><p>其中，username 参数是普通用户的用户名，hostname 参数是普通用户的主机名，newpwd 是要更改的新密码。<br>示例 4<br>使用 root 用户登录 MySQL 服务器，再使用 GRANT 语句将 testuser 用户的密码修改为“newpwd3”，SQL 语句和运行结果如下：<br>mysql&gt; GRANT USAGE ON <em>.</em> TO ‘testuser’@’localhost’ IDENTIFIED BY ‘newpwd3’;<br>Query OK, 0 rows affected, 1 warning (0.05 sec)<br>由运行结果可以看出，密码修改成功。</p><h1 id="修改root密码"><a href="#修改root密码" class="headerlink" title="修改root密码"></a>修改root密码</h1><p>在 MySQL 中，root 用户拥有很高的权限，因此必须保证 root 用户密码的安全。修改 root 用户密码的方式有很多种，本节将介绍几种常用的修改 root 用户密码的方法。<br>使用mysqladmin命令在命令行指定新密码<br>root 用户可以使用 mysqladmin 命令来修改密码，mysqladmin 的语法格式如下：<br>mysqladmin -u username -h hostname -p password “newpwd”</p><p>语法参数说明如下：<br>usermame 指需要修改密码的用户名称，在这里指定为 root 用户；<br>hostname 指需要修改密码的用户主机名，该参数可以不写，默认是 localhost；<br>password 为关键字，而不是指旧密码；<br>newpwd 为新设置的密码，必须用双引号括起来。如果使用单引号会引发错误，可能会造成修改后的密码不是你想要的。</p><p>执行完上面的语句，root 用户的密码将被修改为“newpwd”。<br>示例 1<br>下面使用 mysqladmin 将 root 用户的密码修改为“rootpwd”，在 Windows 命令行窗口（cmd）中执行命令和运行结果如下：<br>C:\Users\leovo&gt;mysqladmin -u root -p password “rootpwd”<br>Enter password: ****<br>mysqladmin: [Warning] Using a password on the command line interface can be insecure.<br>Warning: Since password will be sent to server in plain text, use ssl connection to ensure password safety.<br>输入 mysqladmin 命令后，按回车键，然后输入 root 用户原来的密码。执行完毕后，密码修改成功，root 用户登录时将使用新的密码。</p><p>运行结果中，输入密码后会提示在命令行界面上使用密码可能不安全的警告信息，因为在命令行输入密码时，MySQL 服务器就会提示这些安全警告信息。</p><p>下面使用修改后的“rootpwd”密码登录 root 用户，SQL 语句和运行结果如下：<br>C:\Users\leovo&gt;mysql -uroot -p<br>Enter password: <strong>***</strong><br>Welcome to the MySQL monitor.  Commands end with ; or \g.<br>Your MySQL connection id is 23<br>Server version: 5.7.29-log MySQL Community Server (GPL)</p><p>Copyright (c) 2000, 2020, Oracle and/or its affiliates. All rights reserved.</p><p>Oracle is a registered trademark of Oracle Corporation and/or its<br>affiliates. Other names may be trademarks of their respective<br>owners.</p><p>Type ‘help;’ or ‘\h’ for help. Type ‘\c’ to clear the current input statement.<br>结果显示，root 用户登录成功，所以使用 mysqladmin 命令修改 root 用户密码成功。<br>修改MySQL数据库的user表<br>因为所有账户信息都保存在 user 表中，因此可以直接通过修改 user 表来改变 root 用户的密码。</p><p>root 用户登录到 MySQL 服务器后，可以使用 UPDATE 语句修改 MySQL 数据库的 user 表的 authentication_string 字段，从而修改用户的密码。</p><p>使用 UPDATA 语句修改 root 用户密码的语法格式如下：<br>UPDATE mysql.user set authentication_string = PASSWORD (“rootpwd) WHERE User = “root” and Host=”localhost”;</p><p>新密码必须使用 PASSWORD() 函数来加密。执行UPDATE语句后，需要执行FLUSH PRIVILEGES语句重新加载用户权限。<br>示例 2<br>下面使用 UPDATE 语句将 root用户的密码修改为“rootpwd2”。</p><p>使用 root 用户登录到 MySQL 服务器后，SQL 语句和运行结果如下所示：<br>mysql&gt; UPDATE mysql.user set authentication_string = password (“rootpwd2”)<br>    -&gt; WHERE User = “root” and Host = “localhost”;<br>Query OK, 1 row affected, 0 warning (0.00 sec)<br>Rows matched: 1  Changed: 1  Warnings:0<br>mysql&gt; FLUSH PRIVILEGES;<br>Query OK, 0 rows affected (0.06 sec)<br>结果显示，密码修改成功。而且使用了FLUSH PRIVILEGES;语句加载权限。退出后就必须使用新密码来登录了。<br>使用SET语句修改root用户的密码<br>SET PASSWORD 语句可以用来重新设置其他用户的登录密码或者自己使用的账户的密码。使用 SET 语句修改密码的语法结构如下：<br>SET PASSWORD = PASSWORD (“rootpwd”);</p><p>示例 3<br>下面使用 SET 语句将 root 用户的密码修改为“rootpwd3”。</p><p>使用 root 用户登录到 MySQL 服务器后，SQL 语句和运行结果如下所示：<br>MySQL&gt; SET PASSWORD = password (“rootpwd3”);<br>Query OK, 0 rows affected (0.00 sec)<br>结果显示，SET 语句执行成功，root 用户的密码被成功设置为“rootpwd3”。</p><h1 id="忘记root密码后如何重置"><a href="#忘记root密码后如何重置" class="headerlink" title="忘记root密码后如何重置"></a>忘记root密码后如何重置</h1><p>在忘记 MySQL 密码的情况下，可以通过 –skip-grant-tables 关闭服务器的认证，然后重置 root 的密码，具体操作步骤如下。</p><p>步骤 1)：关闭正在运行的 MySQL 服务。打开 cmd 进入 MySQL 的 bin 目录。</p><p>步骤 2)：输入mysqld –console –skip-grant-tables –shared-memory 命令。–skip-grant-tables 会让 MySQL 服务器跳过验证步骤，允许所有用户以匿名的方式，无需做密码验证就可以直接登录 MySQL 服务器，并且拥有所有的操作权限。</p><p>步骤 3)：上一个 DOS 窗口不要关闭，打开一个新的 DOS 窗口，此时仅输入 mysql 命令，不需要用户名和密码，即可连接到 MySQL。</p><p>步骤 4)：输入命令 update mysql.user set authentication_string=password(‘root’) where user=’root’ and Host =’localhost’; 设置新密码。</p><p>注意：MySQL 5.7 版本中的 user 表里已经去掉了 password 字段，改为了 authentication_string。</p><p>步骤 5)：刷新权限（必须步骤），输入flush privileges;命令。</p><p>步骤 6)：因为之前使用 –skip-grant-tables 启动，所以需要重启 MySQL 服务器去掉 –skip-grant-tables。输入无误后输入quit;命令退出 MySQL 服务。</p><p>步骤 7)：重启 MySQL 服务，使用用户名 root 和刚才设置的新密码 root 登录就可以了。</p><h1 id="修改密码的3种方式"><a href="#修改密码的3种方式" class="headerlink" title="修改密码的3种方式"></a>修改密码的3种方式</h1><p>在使用数据库时，我们也许会遇到 MySQL 需要修改密码的情况，比如密码太简单需要修改等。本节主要介绍了 3 种修改 MySQL 数据库密码的方法。</p><ol><li>使用 SET PASSWORD 命令<br>步骤 1)：输入命令mysql -u root -p指定 root 用户登录 MySQL，输入后按回车键输入密码。如果没有配置环境变量，请在 MySQL 的 bin 目录下登录操作。</li></ol><p>步骤 2)：使用 SET PASSWORD 修改密码命令格式为 set password for username @localhost = password(newpwd);<br>，其中 username 为要修改密码的用户名，newpwd 为要修改的新密码。如图所示。</p><p>步骤 3)：输入quit;命令退出 MySQL 重新登录，输入新密码“root”登录就可以了；<br>2. 使用mysqladmin修改密码<br>使用 mysqladmin 命令修改 MySQL 的 root 用户密码格式为 mysqladmin -u用户名 -p旧密码 password 新密码。</p><p>注意：下图修改密码的命令中 -uroot 和 -proot 是整体，不要写成 -u root -p root，-u 和 root 间可以加空格，但是会有警告出现，所以就不要加空格了。</p><ol start="3"><li>UPDATE直接编辑user表<br>步骤 1)：输入命令mysql -u root -p指定 root 用户登录 MySQL，输入后按回车键输入密码。如果没有配置环境变量，请在 MySQL 的 bin 目录下登录操作。</li></ol><p>步骤 2)：输入use mysql;命令连接权限数据库。</p><p>步骤 3)：输入命令update mysql.user set authentication_string=password(‘新密码’) where user=’用户名’ and Host =’localhost’;设置新密码。</p><p>步骤 4)：输入 flush privileges; 命令刷新权限。</p><p>步骤 5)：输入quit;命令退出 MySQL 重新登录，此时密码已经修改为刚才输入的新密码了。</p><p>如果你忘记了 MySQL 的 root 密码，可参考《MySQL忘记root密码解决方案》一节。</p><h1 id="权限控制实现原理"><a href="#权限控制实现原理" class="headerlink" title="权限控制实现原理"></a>权限控制实现原理</h1><p>MySQL 权限表在数据库启动时载入内存，用户通过身份认证后，系统会在内存中进行相应权限的存取。当 MySQL 允许一个用户执行各种操作时，它将首先核实该用户向 MySQL 服务器发送的连接请求，然后确认用户的操作请求是否被允许。</p><p>当用户进行连接时，MySQL 实现权限控制主要有以下两个阶段：<br>1）连接核实阶段<br>登录 MySQL 服务器时，客户端连接请求中会提供用户名称、主机地址和密码，MySQL 服务器会使用 user 表中的 Host、User 和 authentication_string （MySQL 5.7 版本之前是 Password）字段执行身份检查。</p><p>只有客户端请求的主机名和用户名在 user 表中有匹配的记录，并且密码正确时，MySQL 服务器才会通过身份认证，接受连接，否则拒绝连接。<br>MySQL 通过 IP 地址和用户名联合进行身份认证。例如 MySQL 安装后默认创建的用户 root@localhost，表示用户 root 只能从本地（localhost）进行连接时才能通过认证。此用户从其它任何主机对数据库进行连接时都将被拒绝。也就是说，用户名相同，IP 地址不同，MySQL 则将其视为不同的用户。</p><p>服务器接受连接后进入请求核实阶段等待用户请求。如果连接核实没有通过，服务器则完全拒绝访问。<br>2）请求核实阶段<br>建立连接后，服务器进入请求核实阶段，对在此连接上的每个请求，服务器都会检查用户是否有足够的权限来执行它。这正是授权表中的权限列发挥作用的地方。</p><p>权限按照以下权限表的顺序得到数据库权限：user→db→tables_priv→columns_priv→procs_priv。在这几个权限表中，权限范围依次递减，全局权限覆盖局部权限。</p><p>请求核实的过程如下所示：</p><p>1）用户向 MySQL 发出操作请求。</p><p>2）MySQL 首先检查 user 表，匹配 User、Host 字段值，查看请求的全局权限在 user 表中是否被授权。授权则允许操作执行，如果指定的权限在 user 表中没有被授权。MySQL 将检查 db 表。</p><p>3）db 表是下一安全层级，其中的权限限定于数据库层级，在该层级的 SELECT 权限允许用户查看指定数据库的所有表中的数据。</p><p>MySQL 检查 db 权限表中的权限信息，匹配 User、Host 字段值，查看请求的数据库级别的权限在 db 表中是否被授权。授权则允许操作执行，否则 MySQL 继续向下查找。</p><p>4）MySQL 检查 tables_priv 权限表中的权限信息，匹配 User、Host 字段值，查看请求的数据表级别的权限在 tables_priv 表中是否被授权。授权则允许操作执行，否则 MySQL 继续向下查找。</p><p>5）MySQL 检查 columns_priv 权限表中的权限信息，匹配  User、Host 字段值，查看请求的列级别的权限在 columns_priv 表中是否被授权。授权则允许操作执行，否则 MySQL 继续向下查找。</p><p>6）如果所有权限表都检查完毕，还是没有找到允许的权限操作，那么 MySQL 将返回错误信息，即用户请求的操作不能执行，操作失败。<br>提示：上面提到 MySQL 通过向下层级的顺序检查权限表，但并不意味着所有的权限都要执行该过程。例如，一个用户登录到 MySQL 服务器之后只执行对 MySQL 的管理操作，此时只涉及管理权限，因此 MySQL 只检查 user 表。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;MySQL 是一个多用户数据库，具有功能强大的访问控制系统，可以为不同用户指定不同权限。&lt;/p&gt;
&lt;h1 id=&quot;user权限表&quot;&gt;&lt;a href=&quot;#user权限表&quot; class=&quot;headerlink&quot; title=&quot;user权限表&quot;&gt;&lt;/a&gt;user权限表&lt;/h1&gt;&lt;
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>MySQL/MySQL操作表中数据</title>
    <link href="http://yoursite.com/2021/07/12/MySQL/MySQL%E6%93%8D%E4%BD%9C%E8%A1%A8%E4%B8%AD%E6%95%B0%E6%8D%AE/"/>
    <id>http://yoursite.com/2021/07/12/MySQL/MySQL%E6%93%8D%E4%BD%9C%E8%A1%A8%E4%B8%AD%E6%95%B0%E6%8D%AE/</id>
    <published>2021-07-12T08:14:40.577Z</published>
    <updated>2021-07-14T10:14:55.205Z</updated>
    
    <content type="html"><![CDATA[<h1 id="INSERT：插入数据"><a href="#INSERT：插入数据" class="headerlink" title="INSERT：插入数据"></a>INSERT：插入数据</h1><p>数据库与表创建成功以后，需要向数据库的表中插入数据。在 MySQL 中可以使用 INSERT 语句向数据库已有的表中插入一行或者多行元组数据。<br>基本语法<br>INSERT 语句有两种语法形式，分别是 INSERT…VALUES 语句和 INSERT…SET 语句。</p><p>1) INSERT…VALUES语句<br>INSERT VALUES 的语法格式为：<br>INSERT INTO &lt;表名&gt; [ &lt;列名1&gt; [ , … &lt;列名n&gt;] ]<br>VALUES (值1) [… , (值n) ];</p><p>语法说明如下。<br>&lt;表名&gt;：指定被操作的表名。<br>&lt;列名&gt;：指定需要插入数据的列名。若向表中的所有列插入数据，则全部的列名均可以省略，直接采用 INSERT&lt;表名&gt;VALUES(…) 即可。<br>VALUES 或 VALUE 子句：该子句包含要插入的数据清单。数据清单中数据的顺序要和列的顺序相对应。<br>2) INSERT…SET语句<br>语法格式为：<br>INSERT INTO &lt;表名&gt;<br>SET &lt;列名1&gt; = &lt;值1&gt;,<br>        &lt;列名2&gt; = &lt;值2&gt;,<br>        …</p><p>此语句用于直接给表中的某些列指定对应的列值，即要插入的数据的列名在 SET 子句中指定，col_name 为指定的列名，等号后面为指定的数据，而对于未指定的列，列值会指定为该列的默认值。</p><p>由 INSERT 语句的两种形式可以看出：<br>使用 INSERT…VALUES 语句可以向表中插入一行数据，也可以插入多行数据；<br>使用 INSERT…SET 语句可以指定插入行中每列的值，也可以指定部分列的值；<br>INSERT…SELECT 语句向表中插入其他表的数据。<br>采用 INSERT…SET 语句可以向表中插入部分列的值，这种方式更为灵活；<br>INSERT…VALUES 语句可以一次插入多条数据。</p><p>在 MySQL 中，用单条 INSERT 语句处理多个插入要比使用多条 INSERT 语句更快。</p><p>当使用单条 INSERT 语句插入多行数据的时候，只需要将每行数据用圆括号括起来即可。<br>向表中的全部字段添加值<br>在 test_db 数据库中创建一个课程信息表 tb_courses，包含课程编号 course_id、课程名称 course_name、课程学分 course_grade 和课程备注 course_info，输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; CREATE TABLE tb_courses<br>    -&gt; (<br>    -&gt; course_id INT NOT NULL AUTO_INCREMENT,<br>    -&gt; course_name CHAR(40) NOT NULL,<br>    -&gt; course_grade FLOAT NOT NULL,<br>    -&gt; course_info CHAR(100) NULL,<br>    -&gt; PRIMARY KEY(course_id)<br>    -&gt; );<br>Query OK, 0 rows affected (0.00 sec)<br>向表中所有字段插入值的方法有两种：一种是指定所有字段名；另一种是完全不指定字段名。</p><p>【实例 1】在 tb_courses 表中插入一条新记录，course_id 值为 1，course_name 值为“Network”，course_grade 值为 3，info 值为“Computer Network”。</p><p>在执行插入操作之前，查看 tb_courses 表的SQL语句和执行结果如下所示。<br>mysql&gt; SELECT * FROM tb_courses;<br>Empty set (0.00 sec)<br>查询结果显示当前表内容为空，没有数据，接下来执行插入数据的操作，输入的 SQL 语句和执行过程如下所示。<br>mysql&gt; INSERT INTO tb_courses<br>    -&gt; (course_id,course_name,course_grade,course_info)<br>    -&gt; VALUES(1,’Network’,3,’Computer Network’);<br>Query OK, 1 rows affected (0.08 sec)<br>mysql&gt; SELECT * FROM tb_courses;<br>+———–+————-+————–+——————+<br>| course_id | course_name | course_grade | course_info      |<br>+———–+————-+————–+——————+<br>|         1 | Network     |            3 | Computer Network |<br>+———–+————-+————–+——————+<br>1 row in set (0.00 sec)<br>可以看到插入记录成功。在插入数据时，指定了 tb_courses 表的所有字段，因此将为每一个字段插入新的值。</p><p>INSERT 语句后面的列名称顺序可以不是 tb_courses 表定义时的顺序，即插入数据时，不需要按照表定义的顺序插入，只要保证值的顺序与列字段的顺序相同就可以。</p><p>【实例 2】在 tb_courses 表中插入一条新记录，course_id 值为 2，course_name 值为“Database”，course_grade 值为 3，info值为“MySQL”。输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; INSERT INTO tb_courses<br>    -&gt; (course_name,course_info,course_id,course_grade)<br>    -&gt; VALUES(‘Database’,’MySQL’,2,3);<br>Query OK, 1 rows affected (0.08 sec)<br>mysql&gt; SELECT * FROM tb_courses;<br>+———–+————-+————–+——————+<br>| course_id | course_name | course_grade | course_info      |<br>+———–+————-+————–+——————+<br>|         1 | Network     |            3 | Computer Network |<br>|         2 | Database    |            3 | MySQL            |<br>+———–+————-+————–+——————+<br>2 rows in set (0.00 sec)<br>使用 INSERT 插入数据时，允许列名称列表 column_list 为空，此时值列表中需要为表的每一个字段指定值，并且值的顺序必须和数据表中字段定义时的顺序相同。</p><p>【实例 3】在 tb_courses 表中插入一条新记录，course_id 值为 3，course_name 值为“Java”，course_grade 值为 4，info 值为“Jave EE”。输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; INSERT INTO tb_courses<br>    -&gt; VLAUES(3,’Java’,4,’Java EE’);<br>Query OK, 1 rows affected (0.08 sec)<br>mysql&gt; SELECT * FROM tb_courses;<br>+———–+————-+————–+——————+<br>| course_id | course_name | course_grade | course_info      |<br>+———–+————-+————–+——————+<br>|         1 | Network     |            3 | Computer Network |<br>|         2 | Database    |            3 | MySQL            |<br>|         3 | Java        |            4 | Java EE          |<br>+———–+————-+————–+——————+<br>3 rows in set (0.00 sec)<br>INSERT 语句中没有指定插入列表，只有一个值列表。在这种情况下，值列表为每一个字段列指定插入的值，并且这些值的顺序必须和 tb_courses 表中字段定义的顺序相同。<br>注意：虽然使用 INSERT 插入数据时可以忽略插入数据的列名称，若值不包含列名称，则 VALUES 关键字后面的值不仅要求完整，而且顺序必须和表定义时列的顺序相同。如果表的结构被修改，对列进行增加、删除或者位置改变操作，这些操作将使得用这种方式插入数据时的顺序也同时改变。如果指定列名称，就不会受到表结构改变的影响。</p><p>向表中指定字段添加值<br>为表的指定字段插入数据，是在 INSERT 语句中只向部分字段中插入值，而其他字段的值为表定义时的默认值。</p><p>【实例 4】在 tb_courses 表中插入一条新记录，course_name 值为“System”，course_grade 值为 3，course_info 值为“Operating System”，输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; INSERT INTO tb_courses<br>    -&gt; (course_name,course_grade,course_info)<br>    -&gt; VALUES(‘System’,3,’Operation System’);<br>Query OK, 1 rows affected (0.08 sec)<br>mysql&gt; SELECT * FROM tb_courses;<br>+———–+————-+————–+——————+<br>| course_id | course_name | course_grade | course_info      |<br>+———–+————-+————–+——————+<br>|         1 | Network     |            3 | Computer Network |<br>|         2 | Database    |            3 | MySQL            |<br>|         3 | Java        |            4 | Java EE          |<br>|         4 | System      |            3 | Operating System |<br>+———–+————-+————–+——————+<br>4 rows in set (0.00 sec)<br>可以看到插入记录成功。如查询结果显示，这里的 course_id 字段自动添加了一个整数值 4。这时的 course_id 字段为表的主键，不能为空，系统自动为该字段插入自增的序列值。在插入记录时，如果某些字段没有指定插入值，MySQL 将插入该字段定义时的默认值。<br>使用 INSERT INTO…FROM 语句复制表数据<br>INSERT INTO…SELECT…FROM 语句用于快速地从一个或多个表中取出数据，并将这些数据作为行数据插入另一个表中。</p><p>SELECT 子句返回的是一个查询到的结果集，INSERT 语句将这个结果集插入指定表中，结果集中的每行数据的字段数、字段的数据类型都必须与被操作的表完全一致。</p><p>在数据库 test_db 中创建一个与 tb_courses 表结构相同的数据表 tb_courses_new，创建表的 SQL 语句和执行过程如下所示。<br>mysql&gt; CREATE TABLE tb_courses_new<br>    -&gt; (<br>    -&gt; course_id INT NOT NULL AUTO_INCREMENT,<br>    -&gt; course_name CHAR(40) NOT NULL,<br>    -&gt; course_grade FLOAT NOT NULL,<br>    -&gt; course_info CHAR(100) NULL,<br>    -&gt; PRIMARY KEY(course_id)<br>    -&gt; );<br>Query OK, 0 rows affected (0.00 sec)<br>mysql&gt; SELECT * FROM tb_courses_new;<br>Empty set (0.00 sec)<br>【实例 5】从 tb_courses 表中查询所有的记录，并将其插入 tb_courses_new 表中。输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; INSERT INTO tb_courses_new<br>    -&gt; (course_id,course_name,course_grade,course_info)<br>    -&gt; SELECT course_id,course_name,course_grade,course_info<br>    -&gt; FROM tb_courses;<br>Query OK, 4 rows affected (0.17 sec)<br>Records: 4  Duplicates: 0  Warnings: 0<br>mysql&gt; SELECT * FROM tb_courses_new;<br>+———–+————-+————–+——————+<br>| course_id | course_name | course_grade | course_info      |<br>+———–+————-+————–+——————+<br>|         1 | Network     |            3 | Computer Network |<br>|         2 | Database    |            3 | MySQL            |<br>|         3 | Java        |            4 | Java EE          |<br>|         4 | System      |            3 | Operating System |<br>+———–+————-+————–+——————+<br>4 rows in set (0.00 sec)</p><h1 id="UPDATE：修改数据"><a href="#UPDATE：修改数据" class="headerlink" title="UPDATE：修改数据"></a>UPDATE：修改数据</h1><p>在 MySQL 中，可以使用 UPDATE 语句来修改、更新一个或多个表的数据。<br>UPDATE 语句的基本语法<br>使用 UPDATE 语句修改单个表，语法格式为：<br>UPDATE &lt;表名&gt; SET 字段 1=值 1 [,字段 2=值 2… ] [WHERE 子句 ]<br>[ORDER BY 子句] [LIMIT 子句]</p><p>语法说明如下：<br>&lt;表名&gt;：用于指定要更新的表名称。<br>SET 子句：用于指定表中要修改的列名及其列值。其中，每个指定的列值可以是表达式，也可以是该列对应的默认值。如果指定的是默认值，可用关键字 DEFAULT 表示列值。<br>WHERE 子句：可选项。用于限定表中要修改的行。若不指定，则修改表中所有的行。<br>ORDER BY 子句：可选项。用于限定表中的行被修改的次序。<br>LIMIT 子句：可选项。用于限定被修改的行数。<br>注意：修改一行数据的多个列值时，SET 子句的每个值用逗号分开即可。</p><p>修改表中的数据<br>【实例 1】在 tb_courses_new 表中，更新所有行的 course_grade 字段值为 4，输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; UPDATE tb_courses_new<br>    -&gt; SET course_grade=4;<br>Query OK, 3 rows affected (0.11 sec)<br>Rows matched: 4  Changed: 3  Warnings: 0<br>mysql&gt; SELECT * FROM tb_courses_new;<br>+———–+————-+————–+——————+<br>| course_id | course_name | course_grade | course_info      |<br>+———–+————-+————–+——————+<br>|         1 | Network     |            4 | Computer Network |<br>|         2 | Database    |            4 | MySQL            |<br>|         3 | Java        |            4 | Java EE          |<br>|         4 | System      |            4 | Operating System |<br>+———–+————-+————–+——————+<br>4 rows in set (0.00 sec)<br>根据条件修改表中的数据<br>【实例 2】在 tb_courses 表中，更新 course_id 值为 2 的记录，将 course_grade 字段值改为 3.5，将 course_name 字段值改为“DB”，输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; UPDATE tb_courses_new<br>    -&gt; SET course_name=’DB’,course_grade=3.5<br>    -&gt; WHERE course_id=2;<br>Query OK, 1 row affected (0.13 sec)<br>Rows matched: 1  Changed: 1  Warnings: 0<br>mysql&gt; SELECT * FROM tb_courses_new;<br>+———–+————-+————–+——————+<br>| course_id | course_name | course_grade | course_info      |<br>+———–+————-+————–+——————+<br>|         1 | Network     |            4 | Computer Network |<br>|         2 | DB          |          3.5 | MySQL            |<br>|         3 | Java        |            4 | Java EE          |<br>|         4 | System      |            4 | Operating System |<br>+———–+————-+————–+——————+<br>4 rows in set (0.00 sec)<br>注意：保证 UPDATE 以 WHERE 子句结束，通过 WHERE 子句指定被更新的记录所需要满足的条件，如果忽略 WHERE 子句，MySQL 将更新表中所有的行。</p><h1 id="DELETE：删除数据"><a href="#DELETE：删除数据" class="headerlink" title="DELETE：删除数据"></a>DELETE：删除数据</h1><p>在 MySQL 中，可以使用 DELETE 语句来删除表的一行或者多行数据。<br>删除单个表中的数据<br>使用 DELETE 语句从单个表中删除数据，语法格式为：<br>DELETE FROM &lt;表名&gt; [WHERE 子句] [ORDER BY 子句] [LIMIT 子句]</p><p>语法说明如下：<br>&lt;表名&gt;：指定要删除数据的表名。<br>ORDER BY 子句：可选项。表示删除时，表中各行将按照子句中指定的顺序进行删除。<br>WHERE 子句：可选项。表示为删除操作限定删除条件，若省略该子句，则代表删除该表中的所有行。<br>LIMIT 子句：可选项。用于告知服务器在控制命令被返回到客户端前被删除行的最大值。</p><p>注意：在不使用 WHERE 条件的时候，将删除所有数据。<br>删除表中的全部数据<br>【实例 1】删除 tb_courses_new 表中的全部数据，输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; DELETE FROM tb_courses_new;<br>Query OK, 3 rows affected (0.12 sec)<br>mysql&gt; SELECT * FROM tb_courses_new;<br>Empty set (0.00 sec)<br>根据条件删除表中的数据<br>【实例 2】在 tb_courses_new 表中，删除 course_id 为 4 的记录，输入的 SQL 语句和执行结果如下所示。<br>mysql&gt; DELETE FROM tb_courses<br>    -&gt; WHERE course_id=4;<br>Query OK, 1 row affected (0.00 sec)<br>mysql&gt; SELECT * FROM tb_courses;<br>+———–+————-+————–+——————+<br>| course_id | course_name | course_grade | course_info      |<br>+———–+————-+————–+——————+<br>|         1 | Network     |            3 | Computer Network |<br>|         2 | Database    |            3 | MySQL            |<br>|         3 | Java        |            4 | Java EE          |<br>+———–+————-+————–+——————+<br>3 rows in set (0.00 sec)<br>由运行结果可以看出，course_id 为 4 的记录已经被删除。</p><h1 id="TRUNCATE：清空表记录"><a href="#TRUNCATE：清空表记录" class="headerlink" title="TRUNCATE：清空表记录"></a>TRUNCATE：清空表记录</h1>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;INSERT：插入数据&quot;&gt;&lt;a href=&quot;#INSERT：插入数据&quot; class=&quot;headerlink&quot; title=&quot;INSERT：插入数据&quot;&gt;&lt;/a&gt;INSERT：插入数据&lt;/h1&gt;&lt;p&gt;数据库与表创建成功以后，需要向数据库的表中插入数据。在 MySQL 
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>java/MyBatis/MyBatis与Spring整合</title>
    <link href="http://yoursite.com/2021/07/07/java/MyBatis/MyBatis%E4%B8%8ESpring%E6%95%B4%E5%90%88/"/>
    <id>http://yoursite.com/2021/07/07/java/MyBatis/MyBatis%E4%B8%8ESpring%E6%95%B4%E5%90%88/</id>
    <published>2021-07-07T02:03:39.459Z</published>
    <updated>2021-07-07T02:17:02.576Z</updated>
    
    <content type="html"><![CDATA[<h1 id="导入相关JAR包"><a href="#导入相关JAR包" class="headerlink" title="导入相关JAR包"></a>导入相关JAR包</h1><p>实现 MyBatis 与 Spring 的整合需要导入相关 JAR 包，包括 MyBatis、Spring 以及其他 JAR 包。<br>1）MyBatis 框架所需的 JAR 包<br>MyBatis 框架所需的 JAR 包包括它的核心包和依赖包。<br>2）Spring 框架所需的 JAR 包<br>Spring 框架所需的 JAR 包包括它的核心模块 JAR、AOP 开发使用的 JAR、JDBC 和事务的 JAR 包（其中依赖包不需要再导入，因为 MyBatis 已提供），具体如下：<br>aopalliance-1.0.jar<br>aspectjweaver-1.6.9.jar<br>spring-aop-3.2.13.RELEASE.jar<br>spring-aspects-3.2.13.RELEASE.jar<br>spring-beans-3.2.13.RELEASE.jar<br>spring-context-3.2.13.RELEASE.jar<br>spring-core-3.2.13.RELEASE.jar<br>spring-expression-3.2.13.RELEASE.jar<br>spring-jdbc-3.2.13.RELEASE.jar<br>spring-tx-3.2.13.RELEASE.jar<br>3）MyBatis 与 Spring 整合的中间 JAR 包<br>该中间 JAR 包的版本为 mybatis-spring-1.3.1.jar。<br>4）数据库驱动 JAR 包<br>MySQL 数据库驱动包为 mysql-connector-java-5.1.25-bin.jar。<br>5）数据源所需的 JAR 包<br>在整合时使用的是 DBCP 数据源，需要准备 DBCP 和连接池的 JAR 包。</p><p>DBCP 的 JAR 包为 commons-dbcp2-2.2.0.jar。</p><p>连接池的 JAR 包为 commons-pool2-2.5.0.jar。</p><h1 id="在Spring中配置MyBatis工厂"><a href="#在Spring中配置MyBatis工厂" class="headerlink" title="在Spring中配置MyBatis工厂"></a>在Spring中配置MyBatis工厂</h1><p>通过与 Spring 的整合，MyBatis 的 SessionFactory 交由 Spring 来构建，在构建时需要在 Spring 的配置文件中添加如下代码：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!--配置数据源--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"dataSource"</span> <span class="attr">class</span>=<span class="string">"org.apache.commons.dbcp.BasicDataSource"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"driverClassName"</span> <span class="attr">value</span>=<span class="string">"com.mysql.jdbc.Driver"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"url"</span> <span class="attr">value</span>=<span class="string">"jdbc:mysql://127.0.0.1:3306/springtest?seUnicode=true<span class="symbol">&amp;amp;</span>characterEncoding=utf-8"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"username"</span> <span class="attr">value</span>=<span class="string">"root"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"password"</span> <span class="attr">value</span>=<span class="string">"1128"</span> /&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 最大连接数 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"maxTotal"</span> <span class="attr">value</span>=<span class="string">"30"</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 最大空闲连接数 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"maxIdle"</span> <span class="attr">value</span>=<span class="string">"10"</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 初始化连接数 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"initialSize"</span> <span class="attr">value</span>=<span class="string">"5"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 配置SqlSessionFactoryBean --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"sqlSessionFactory"</span> <span class="attr">class</span>=<span class="string">"org.mybatis.spring.SqlSessionFactoryBean"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 引用数据源组件 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dataSource"</span> <span class="attr">ref</span>=<span class="string">"dataSource"</span> /&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 引用MyBatis配置文件中的配置 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"configLocation"</span> <span class="attr">value</span>=<span class="string">"classpath:mybatis-config.xml"</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure><p>使用 Spring 管理 MyBatis 的数据操作接口<br>使用 Spring 管理 MyBatis 数据操作接口的方式有多种，其中最常用、最简洁的一种是基于 MapperScannerConfigurer 的整合。该方式需要在 Spring 的配置文件中加入以下内容：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!-- Mapper代理开发，使用Spring自动扫描MyBatis的接口并装配 （Sprinh将指定包中的所有被@Mapper注解标注的接口自动装配为MyBatis的映射接口） --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.mybatis.spring.mapper.MapperScannerConfigurer"</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- mybatis-spring组件的扫描器，com.dao只需要接口（接口方法与SQL映射文件中的相同） --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"basePackage"</span> <span class="attr">value</span>=<span class="string">"com.dao"</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"sqlSessionFactoryBeanName"</span> <span class="attr">value</span>=<span class="string">"sqlSessionFactory"</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure><h1 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h1><h3 id="1-创建应用并导入相关-JAR-包"><a href="#1-创建应用并导入相关-JAR-包" class="headerlink" title="1.创建应用并导入相关 JAR 包"></a>1.创建应用并导入相关 JAR 包</h3><p>创建一个名为 MyBatis-Spring 的 Web 应用，并将《MyBatis与Spring的整合步骤》教程的 JAR 导入 /WEB-INF/lib 目录下。</p><h3 id="2-创建持久化类"><a href="#2-创建持久化类" class="headerlink" title="2.创建持久化类"></a>2.创建持久化类</h3><p>在 src 目录下创建一个名为 com.po 的包，将《第一个MyBatis程序》教程的持久化类复制到包中。</p><h3 id="3-创建-SQL-映射文件和-MyBatis-核心配置文件"><a href="#3-创建-SQL-映射文件和-MyBatis-核心配置文件" class="headerlink" title="3.创建 SQL 映射文件和 MyBatis 核心配置文件"></a>3.创建 SQL 映射文件和 MyBatis 核心配置文件</h3><p>在 src 目录下创建一个名为 com.mybatis 的包，在该包中创建 MyBatis 核心配置文件 mybatis-config.xml 和 SQL 映射文件 UserMapper.xml。</p><p>UserMapper.xml 的代码如下：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">mapper</span></span></span><br><span class="line"><span class="meta">PUBLIC <span class="meta-string">"-//mybatis.org//DTD Mapper 3.0//EN"</span></span></span><br><span class="line"><span class="meta"><span class="meta-string">"http://mybatis.org/dtd/mybatis-3-mapper.dtd"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">mapper</span> <span class="attr">namespace</span>=<span class="string">"com.dao.UserDao"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--根据uid查询一个用户信息 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"selectUserById"</span> <span class="attr">parameterType</span>=<span class="string">"Integer"</span> <span class="attr">resultType</span>=<span class="string">"com..po.MyUser"</span>&gt;</span></span><br><span class="line">        select * from user where uid = #&#123;uid&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 查询所有用户信息 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"selectAllUser"</span> <span class="attr">resultType</span>=<span class="string">"com.po.MyUser"</span>&gt;</span></span><br><span class="line">        select * from user</span><br><span class="line">    <span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 添加一个用户，#&#123;uname&#125;为 com.mybatis.po.MyUser 的属性值 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">insert</span> <span class="attr">id</span>=<span class="string">"addUser"</span> <span class="attr">parameterType</span>=<span class="string">"com.po.MyUser"</span>&gt;</span></span><br><span class="line">    insert into user (uname,usex) values(#&#123;uname&#125;,#&#123;usex&#125;)</span><br><span class="line">    <span class="tag">&lt;/<span class="name">insert</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--修改一个用户 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">update</span> <span class="attr">id</span>=<span class="string">"updateUser"</span> <span class="attr">parameterType</span>=<span class="string">"com..po.MyUser"</span>&gt;</span></span><br><span class="line">        update user set uname = #&#123;uname&#125;,usex = #&#123;usex&#125; where uid = #&#123;uid&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">update</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 删除一个用户 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">delete</span> <span class="attr">id</span>=<span class="string">"deleteUser"</span> <span class="attr">parameterType</span>=<span class="string">"Integer"</span>&gt;</span></span><br><span class="line">        delete from user where uid = #&#123;uid&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">delete</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mapper</span>&gt;</span></span><br></pre></td></tr></table></figure><p><code>mybatis-config.xml</code>的代码如下：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">configuration</span> <span class="meta-keyword">PUBLIC</span> <span class="meta-string">"-//mybatis.org//DTD Config 3.0//EN"</span></span></span><br><span class="line"><span class="meta"><span class="meta-string">"http://mybatis.org/dtd/mybatis-3-config.dtd"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- 告诉MyBatis到哪里去找映射文件 --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">mappers</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mapper</span> <span class="attr">resource</span>=<span class="string">"com/mybatis/UserMapper.xml"</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">mappers</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="创建数据访问接口"><a href="#创建数据访问接口" class="headerlink" title="创建数据访问接口"></a>创建数据访问接口</h3><p>在<code>src</code>目录下创建一个名为<code>com.dao</code>的包，在该包中创建<code>UserDao</code>接口，并将接口使用<code>@Mapper</code>注解为<code>Mapper</code>，接口中的方法与 SQL 映射文件一致。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> com.dao;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"><span class="keyword">import</span> org.apache.ibatis.annotations.Mapper;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Repository;</span><br><span class="line"><span class="keyword">import</span> com.po.MyUser;</span><br><span class="line"><span class="meta">@Repository</span>(<span class="string">"userDao"</span>)</span><br><span class="line"><span class="meta">@Mapper</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">* 使用Spring自动扫描MyBatis的接口并装配 （Spring将指定包中所有被@Mapper注解标注的接口自动装配为MyBatis的映射接口</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">UserDao</span> </span>&#123;</span><br><span class="line">  <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 接口方法对应的SQL映射文件UserMapper.xml中的id</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> MyUser <span class="title">selectUserById</span><span class="params">(Integer uid)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> List&lt;MyUser&gt; <span class="title">selectAllUser</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">addUser</span><span class="params">(MyUser user)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">updateUser</span><span class="params">(MyUser user)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">deleteUser</span><span class="params">(Integer uid)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="创建日志文件"><a href="#创建日志文件" class="headerlink" title="创建日志文件"></a>创建日志文件</h3><p>在<code>src</code>目录下创建日志文件<code>log4j.properties</code>：</p><figure class="highlight yml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Global logging configuration</span></span><br><span class="line"><span class="string">log4j.rootLogger=ERROR,stdout</span></span><br><span class="line"><span class="comment"># MyBatis logging configuration...</span></span><br><span class="line"><span class="string">log4j.logger.com.mybatis=DEBUG</span></span><br><span class="line"><span class="comment"># Console output...</span></span><br><span class="line"><span class="string">log4j.appender.stdout=org.apache.log4j.ConsoleAppender</span></span><br><span class="line"><span class="string">log4j.appender.stdout.layout=org.apache.log4j.PatternLayout</span></span><br><span class="line"><span class="string">log4j.appender.stdout.layout.ConversionPattern=%5p</span> <span class="string">[%t]</span> <span class="bullet">-</span> <span class="string">%m%n</span></span><br></pre></td></tr></table></figure><h3 id="6-创建控制层"><a href="#6-创建控制层" class="headerlink" title="6.创建控制层"></a>6.创建控制层</h3><p>在<code>src</code>目录下创建一个名为<code>com.controller</code>的包，在包中创建<code>UserController</code>类，在该类中调用数据访问接口中的方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> com.controller;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Controller;</span><br><span class="line"><span class="keyword">import</span> com.dao.UserDao;</span><br><span class="line"><span class="keyword">import</span> com.po.MyUser;</span><br><span class="line"><span class="meta">@Controller</span>(<span class="string">"userController"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UserController</span> </span>&#123;</span><br><span class="line">  <span class="meta">@Autowired</span></span><br><span class="line">  <span class="keyword">private</span> UserDao userDao;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 查询一个用户</span></span><br><span class="line">    MyUser auser = userDao.selectUserById(<span class="number">1</span>);</span><br><span class="line">    System.out.println(auser);</span><br><span class="line">    System.out.println(<span class="string">"============================"</span>);</span><br><span class="line">    <span class="comment">// 添加一个用户</span></span><br><span class="line">    MyUser addmu = <span class="keyword">new</span> MyUser();</span><br><span class="line">    addmu.setUname(<span class="string">"陈恒"</span>);</span><br><span class="line">    addmu.setUsex(<span class="string">"男"</span>);</span><br><span class="line">    <span class="keyword">int</span> add = userDao.addUser(addmu);</span><br><span class="line">    System.out.println(<span class="string">"添加了"</span> + add + <span class="string">"条记录"</span>);</span><br><span class="line">    System.out.println(<span class="string">"============================"</span>);</span><br><span class="line">    <span class="comment">// 修改一个用户</span></span><br><span class="line">    MyUser updatemu = <span class="keyword">new</span> MyUser();</span><br><span class="line">    updatemu.setUid(<span class="number">1</span>);</span><br><span class="line">    updatemu.setUname(<span class="string">"张三"</span>);</span><br><span class="line">    updatemu.setUsex(<span class="string">"女"</span>);</span><br><span class="line">    <span class="keyword">int</span> up = userDao.updateUser(updatemu);</span><br><span class="line">    System.out.println(<span class="string">"修改了"</span> + up + <span class="string">"条记录"</span>);</span><br><span class="line">    System.out.println(<span class="string">"============================"</span>);</span><br><span class="line">    <span class="comment">// 删除一个用户</span></span><br><span class="line">    <span class="keyword">int</span> dl = userDao.deleteUser(<span class="number">9</span>);</span><br><span class="line">    System.out.println(<span class="string">"删除了"</span> + dl + <span class="string">"条记录"</span>);</span><br><span class="line">    System.out.println(<span class="string">"============================"</span>);</span><br><span class="line">    <span class="comment">// 查询所有用户</span></span><br><span class="line">    List&lt;MyUser&gt; list = userDao.selectAllUser();</span><br><span class="line">    <span class="keyword">for</span> (MyUser myUser : list) &#123;</span><br><span class="line">      System.out.println(myUser);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="7-创建-Spring-的配置文件"><a href="#7-创建-Spring-的配置文件" class="headerlink" title="7.创建 Spring 的配置文件"></a>7.创建 Spring 的配置文件</h3><p>在<code>src</code>目录下创建配置文件<code>applicationContext.xml</code>，在配置文件中配置数据源、MyBatis 工厂以及 Mapper 代理开发等信息。</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:context</span>=<span class="string">"http://www.springframework.org/schema/context"</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:tx</span>=<span class="string">"http://www.springframework.org/schema/tx"</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:aop</span>=<span class="string">"http://www.springframework.org/schema/aop"</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans</span></span></span><br><span class="line"><span class="tag"><span class="string">          http://www.springframework.org/schema/beans/spring-beans-2.5.xsd  </span></span></span><br><span class="line"><span class="tag"><span class="string">          http://www.springframework.org/schema/context</span></span></span><br><span class="line"><span class="tag"><span class="string">          http://www.springframework.org/schema/context/spring-context.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">          http://www.springframework.org/schema/tx</span></span></span><br><span class="line"><span class="tag"><span class="string">          http://www.springframework.org/schema/tx/spring-tx-2.5.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">          http://www.springframework.org/schema/aop</span></span></span><br><span class="line"><span class="tag"><span class="string">          http://www.springframework.org/schema/aop/spring-aop-2.5.xsd"</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- 指定需要扫描的包（包括子包），使注解生效 --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"com.dao"</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"com.controller"</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"dataSource"</span> <span class="attr">class</span>=<span class="string">"org.apache.commons.dbcp.BasicDataSource"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"driverClassName"</span> <span class="attr">value</span>=<span class="string">"com.mysql.jdbc.Driver"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"url"</span> <span class="attr">value</span>=<span class="string">"jdbc:mysql://127.0.0.1:3306/smbms?useUnicode=true<span class="symbol">&amp;amp;</span>characterEncoding=utf-8"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"username"</span> <span class="attr">value</span>=<span class="string">"root"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"password"</span> <span class="attr">value</span>=<span class="string">"1128"</span> /&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 最大连接数 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"maxTotal"</span> <span class="attr">value</span>=<span class="string">"30"</span> /&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 最大空闲连接数 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"maxIdle"</span> <span class="attr">value</span>=<span class="string">"10"</span> /&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 初始化连接数 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"initialSize"</span> <span class="attr">value</span>=<span class="string">"5"</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- 添加事务支持 --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"txManager"</span> <span class="attr">class</span>=<span class="string">"org.springframework.jdbc.datasource.DataSourceTransactionManager"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dataSource"</span> <span class="attr">ref</span>=<span class="string">"dataSource"</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- 注册事务管理驱动 --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">tx:annotation-driven</span> <span class="attr">transaction-manager</span>=<span class="string">"txManager"</span> /&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- 配置SqlSessionFactoryBean --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"sqlSessionFactory"</span> <span class="attr">class</span>=<span class="string">"org.mybatis.spring.SqlSessionFactoryBean"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 引用数据源组件 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dataSource"</span> <span class="attr">ref</span>=<span class="string">"dataSource"</span> /&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 引用MyBatis配置文件中的配置 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"configLocation"</span> <span class="attr">value</span>=<span class="string">"classpath:com/mybatis/mybatis-config.xml"</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- Mapper代理开发，使用Spring自动扫描MyBatis的接口并装配 （Sprinh将指定包中的所有被@Mapper注解标注的接口自动装配为MyBatis的映射接口） --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.mybatis.spring.mapper.MapperScannerConfigurer"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- mybatis-spring组件的扫描器，com.dao只需要接口（接口方法与SQL映射文件中的相同） --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"basePackage"</span> <span class="attr">value</span>=<span class="string">"com.dao"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"sqlSessionFactoryBeanName"</span> <span class="attr">value</span>=<span class="string">"sqlSessionFactory"</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="8-创建测试类"><a href="#8-创建测试类" class="headerlink" title="8.创建测试类"></a>8.创建测试类</h3><p>在<code>com.controller</code>包中创建测试类<code>TestController</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> com.controller;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.ApplicationContext;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.support.ClassPathXmlApplicationContext;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestController</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    String xmlPath = <span class="string">"applicationContext.xml"</span>;</span><br><span class="line">    ApplicationContext applicationContext = <span class="keyword">new</span> ClassPathXmlApplicationContext(xmlPath);</span><br><span class="line">    UserController uc = (UserController) applicationContext.getBean(<span class="string">"userController"</span>);</span><br><span class="line">    uc.test();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;导入相关JAR包&quot;&gt;&lt;a href=&quot;#导入相关JAR包&quot; class=&quot;headerlink&quot; title=&quot;导入相关JAR包&quot;&gt;&lt;/a&gt;导入相关JAR包&lt;/h1&gt;&lt;p&gt;实现 MyBatis 与 Spring 的整合需要导入相关 JAR 包，包括 MyBatis
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>java/基础/Java 字符串处理</title>
    <link href="http://yoursite.com/2021/07/06/java/%E5%9F%BA%E7%A1%80/Java%20%E5%AD%97%E7%AC%A6%E4%B8%B2%E5%A4%84%E7%90%86/"/>
    <id>http://yoursite.com/2021/07/06/java/%E5%9F%BA%E7%A1%80/Java%20%E5%AD%97%E7%AC%A6%E4%B8%B2%E5%A4%84%E7%90%86/</id>
    <published>2021-07-06T06:50:18.707Z</published>
    <updated>2021-07-06T07:58:08.677Z</updated>
    
    <content type="html"><![CDATA[<h1 id="定义字符串"><a href="#定义字符串" class="headerlink" title="定义字符串"></a>定义字符串</h1><p>Java 没有内置的字符串类型，而是在标准 Java 类库中提供了一个 String 类来创建和操作字符串。</p><p>在 Java 中定义一个字符串最简单的方法是用双引号把它包围起来。这种用双引号括起来的一串字符实际上都是 String 对象，如字符串“Hello”在编译后即成为 String 对象。因此也可以通过创建 String 类的实例来定义字符串。</p><p>不论使用哪种形式创建字符串，字符串对象一旦被创建，其值是不能改变的，但可以使用其他变量重新赋值的方式进行更改。</p><h2 id="直接定义字符串"><a href="#直接定义字符串" class="headerlink" title="直接定义字符串"></a>直接定义字符串</h2><p>直接定义字符串是指使用双引号表示字符串中的内容。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">String str = <span class="string">"Hello Java"</span>;</span><br></pre></td></tr></table></figure><p>注意：字符串变量必须经过初始化才能使用。</p><h2 id="使用-String-类定义"><a href="#使用-String-类定义" class="headerlink" title="使用 String 类定义"></a>使用 String 类定义</h2><p>在 Java 中每个双引号定义的字符串都是一个<code>String</code>类的对象。因此，可以通过使用<code>String</code>类的构造方法来创建字符串，该类位于<code>java.lang</code>包中。</p><p><code>String</code>类的构造方法有多种重载形式，每种形式都可以定义字符串。</p><ol><li><code>String()</code><br>初始化一个新创建的<code>String</code>对象，表示一个空字符序列。</li><li><code>String(String original)</code><br>初始化一个新创建的<code>String</code>对象，使其表示一个与参数相同的字符序列。换句话说，新创建的字符串是该参数字符串的副本。<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">String str1 = <span class="keyword">new</span> String(<span class="string">"Hello Java"</span>);</span><br><span class="line">String str2 = <span class="keyword">new</span> String(str1);</span><br></pre></td></tr></table></figure>这里<code>str1</code>和<code>str2</code>的值是相等的。</li><li><code>String(char[ ]value)</code><br>分配一个新的字符串，将参数中的字符数组元素全部变为字符串。该字符数组的内容已被复制，后续对字符数组的修改不会影响新创建的字符串。<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">char</span> a[] = &#123;<span class="string">'H'</span>,<span class="string">'e'</span>,<span class="string">'l'</span>,<span class="string">'l'</span>,<span class="string">'0'</span>&#125;;</span><br><span class="line">String sChar = <span class="keyword">new</span> String(a);</span><br><span class="line">a[<span class="number">1</span>] = <span class="string">'s'</span>;</span><br></pre></td></tr></table></figure>上述<code>sChar</code>变量的值是字符串<code>&quot;Hello&quot;</code>。 即使在创建字符串之后，对<code>a</code>数组中的第 2 个元素进行了修改，但未影响<code>sChar</code>的值。</li><li><code>String(char[] value,int offset,int count)</code><br>分配一个新的<code>String</code>，它包含来自该字符数组参数一个子数组的字符。<code>offset</code>参数是子数组第一个字符的索引，<code>count</code>参数指定子数组的长度。该子数组的内容已被赋值，后续对字符数组的修改不会影响新创建的字符串。<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">char</span> a[]=&#123;<span class="string">'H'</span>,<span class="string">'e'</span>,<span class="string">'l'</span>,<span class="string">'l'</span>,<span class="string">'o'</span>&#125;;</span><br><span class="line">String sChar=<span class="keyword">new</span> String(a,<span class="number">1</span>,<span class="number">4</span>);</span><br><span class="line">a[<span class="number">1</span>]=<span class="string">'s'</span>;</span><br></pre></td></tr></table></figure>上述<code>sChar</code>变量的值是字符串<code>&quot;ello&quot;</code>。该构造方法使用字符数组中的部分连续元素来创建字符串对象。<h1 id="String字符串和整型int的相互转换"><a href="#String字符串和整型int的相互转换" class="headerlink" title="String字符串和整型int的相互转换"></a>String字符串和整型int的相互转换</h1><h2 id="String转换为int"><a href="#String转换为int" class="headerlink" title="String转换为int"></a>String转换为int</h2>String 字符串转整型 int 有以下两种方式：</li></ol><ul><li><code>Integer.parseInt(str)</code></li><li><code>Integer.valueOf(str).intValue()</code></li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">  String str = <span class="string">"123"</span>;</span><br><span class="line">  <span class="keyword">int</span> n = <span class="number">0</span>;</span><br><span class="line">  <span class="comment">// 第一种转换方法：Integer.parseInt(str)</span></span><br><span class="line">  n = Integer.parseInt(str);</span><br><span class="line">  System.out.println(<span class="string">"Integer.parseInt(str) : "</span> + n);</span><br><span class="line">  <span class="comment">// 第二种转换方法：Integer.valueOf(str).intValue()</span></span><br><span class="line">  n = <span class="number">0</span>;</span><br><span class="line">  n = Integer.valueOf(str).intValue();</span><br><span class="line">  System.out.println(<span class="string">"Integer.parseInt(str) : "</span> + n);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//输出结果为：</span></span><br><span class="line"><span class="comment">//Integer.parseInt(str) : 123</span></span><br><span class="line"><span class="comment">//Integer.parseInt(str) : 123</span></span><br></pre></td></tr></table></figure><p>在<code>String</code>转换<code>int</code>时，<code>String</code>的值一定是整数，否则会报数字转换异常（<code>java.lang.NumberFormatException</code>）。</p><h2 id="int转换为String"><a href="#int转换为String" class="headerlink" title="int转换为String"></a>int转换为String</h2><p>整型<code>int</code>转<code>String</code>字符串类型有以下 3 种方法：</p><ul><li><code>String s = String.valueOf(i);</code></li><li><code>String s = Integer.toString(i);</code></li><li><code>String s = &quot;&quot; + i;</code></li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> num = <span class="number">10</span>;</span><br><span class="line">  <span class="comment">// 第一种方法：String.valueOf(i);</span></span><br><span class="line">  num = <span class="number">10</span>;</span><br><span class="line">  String str = String.valueOf(num);</span><br><span class="line">  System.out.println(<span class="string">"str:"</span> + str);</span><br><span class="line">  <span class="comment">// 第二种方法：Integer.toString(i);</span></span><br><span class="line">  num = <span class="number">10</span>;</span><br><span class="line">  String str2 = Integer.toString(num);</span><br><span class="line">  System.out.println(<span class="string">"str2:"</span> + str2);</span><br><span class="line">  <span class="comment">// 第三种方法："" + i;</span></span><br><span class="line">  String str3 = num + <span class="string">""</span>;</span><br><span class="line">  System.out.println(<span class="string">"str3:"</span> + str3);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//输出结果为：</span></span><br><span class="line"><span class="comment">//str:10</span></span><br><span class="line"><span class="comment">//str2:10</span></span><br><span class="line"><span class="comment">//str3:10</span></span><br></pre></td></tr></table></figure><p>使用第三种方法相对第一第二种耗时比较大。在使用第一种<code>valueOf()</code>方法时，注意<code>valueOf</code>括号中的值不能为空，否则会报空指针异常（<code>NullPointerException</code>）。</p><h2 id="valueOf-、parse-和toString"><a href="#valueOf-、parse-和toString" class="headerlink" title="valueOf() 、parse()和toString()"></a>valueOf() 、parse()和toString()</h2><h3 id="valueOf-NaN"><a href="#valueOf-NaN" class="headerlink" title="valueOf()"></a>valueOf()</h3><p>valueOf() 方法将数据的内部格式转换为可读的形式。它是一种静态方法，对于所有 Java 内置的类型，在字符串内被重载，以便每一种类型都能被转换成字符串。valueOf() 方法还被类型 Object 重载，所以创建的任何形式类的对象也可被用作一个参数。这里是它的几种形式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> String <span class="title">valueOf</span><span class="params">(<span class="keyword">double</span> num)</span></span></span><br><span class="line"><span class="function"><span class="keyword">static</span> String <span class="title">valueOf</span><span class="params">(<span class="keyword">long</span> num)</span></span></span><br><span class="line"><span class="function"><span class="keyword">static</span> String <span class="title">valueOf</span><span class="params">(Object ob)</span></span></span><br><span class="line"><span class="function"><span class="keyword">static</span> String <span class="title">valueOf</span><span class="params">(<span class="keyword">char</span> chars[])</span></span></span><br></pre></td></tr></table></figure><p>调用 valueOf() 方法可以得到其他类型数据的字符串形式——例如在进行连接操作时。对各种数据类型，可以直接调用这种方法得到合理的字符串形式。所有的简单类型数据转换成相应于它们的普通字符串形式。任何传递给 valueOf() 方法的对象都将返回对象的 toString() 方法调用的结果。事实上，也可以通过直接调用 toString() 方法而得到相同的结果。</p><p>对大多数数组，valueOf() 方法返回一个相当晦涩的字符串，这说明它是一个某种类型的数组。然而对于字符数组，它创建一个包含了字符数组中的字符的字符串对象。valueOf() 方法有一种特定形式允许指定字符数组的一个子集。</p><p>它具有如下的一般形式：<br>static String valueOf(char chars[ ], int startIndex, int numChars)</p><p>这里 chars 是存放字符的数组，startIndex 是字符数组中期望得到的子字符串的首字符下标，numChars 指定子字符串的长度。</p><h3 id="parse"><a href="#parse" class="headerlink" title="parse()"></a>parse()</h3><p>parseXxx(String) 这种形式，是指把字符串转换为数值型，其中 Xxx 对应不同的数据类型，然后转换为 Xxx 指定的类型，如 int 型和 float 型。</p><h3 id="toString-NaN"><a href="#toString-NaN" class="headerlink" title="toString()"></a>toString()</h3><p>toString() 可以把一个引用类型转换为 String 字符串类型，是 sun 公司开发 Java 的时候为了方便所有类的字符串操作而特意加入的一个方法。</p><h1 id="字符串拼接"><a href="#字符串拼接" class="headerlink" title="字符串拼接"></a>字符串拼接</h1><p>String 字符串虽然是不可变字符串，但也可以进行拼接只是会产生一个新的对象。String 字符串拼接可以使用“+”运算符或 String 的 concat(String str) 方法。“+”运算符优势是可以连接任何类型数据拼接成为字符串，而 concat 方法只能拼接 String 类型字符串。</p><h2 id="使用连接运算符“-”"><a href="#使用连接运算符“-”" class="headerlink" title="使用连接运算符“+”"></a>使用连接运算符“+”</h2><p>Java 语言允许使用“+”号连接（拼接）两个字符串。在使用“+”运算符连接字符串和 int 型（或 double 型）数据时，“+”将 int（或 double）型数据自动转换成 String 类型。</p><p>只要“+”运算符的一个操作数是字符串，编译器就会将另一个操作数转换成字符串形式，所以应该谨慎地将其他数据类型与字符串相连，以免出现意想不到的结果。</p><h2 id="使用-concat-方法"><a href="#使用-concat-方法" class="headerlink" title="使用 concat() 方法"></a>使用 concat() 方法</h2><p>在 Java 中，String 类的 concat() 方法实现了将一个字符串连接到另一个字符串的后面。concat() 方法语法格式如下：<br>字符串 1.concat(字符串 2);<br>执行结果是字符串 2 被连接到字符串 1 后面，形成新的字符串。</p><p>concat() 方法一次只能连接两个字符串，如果需要连接多个字符串，需要调用多次 concat() 方法。</p><h1 id="获取字符串长度"><a href="#获取字符串长度" class="headerlink" title="获取字符串长度"></a>获取字符串长度</h1><p>在 Java 中，要获取字符串的长度，可以使用 String 类的 length() 方法，其语法形式如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">字符串名.length();</span><br></pre></td></tr></table></figure><h1 id="字符串大小写转换"><a href="#字符串大小写转换" class="headerlink" title="字符串大小写转换"></a>字符串大小写转换</h1><p>String 类的 toLowerCase() 方法可以将字符串中的所有字符全部转换成小写，而非字母的字符不受影响。语法格式如下：<br>字符串名.toLowerCase()    // 将字符串中的字母全部转换为小写，非字母不受影响<br>toUpperCase() 则将字符串中的所有字符全部转换成大写，而非字母的字符不受影响。语法格式如下：<br>字符串名.toUpperCase()    // 将字符串中的字母全部转换为大写，非字母不受影响</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">String str=<span class="string">"abcdef 我 ghijklmn"</span>;</span><br><span class="line">System.out.println(str.toLowerCase());    <span class="comment">// 输出：abcdef 我 ghijklmn</span></span><br><span class="line">System.out.println(str.toUpperCase());    <span class="comment">// 输出：ABCDEF 我 GHIJKLMN</span></span><br></pre></td></tr></table></figure><h1 id="去除字符串中的空格"><a href="#去除字符串中的空格" class="headerlink" title="去除字符串中的空格"></a>去除字符串中的空格</h1><p>字符串中存在的首尾空格一般情况下都没有任何意义，如字符串“ Hello ”，但是这些空格会影响到字符串的操作，如连接字符串或比较字符串等，所以应该去掉字符串中的首尾空格，这需要使用 String 类提供的 trim() 方法。</p><p>trim() 方法的语法形式如下：<br>字符串名.trim()</p><p>使用 trim() 方法的示例如下：<br>String str = “ hello “;<br>System.out.println(str.length());    // 输出 7<br>System.out.println(str.trim().length());    // 输出 5<br>从该示例中可以看出，字符串中的每个空格占一个位置，直接影响了计算字符串的长度。</p><p>如果不确定要操作的字符串首尾是否有空格，最好在操作之前调用该字符串的 trim() 方法去除首尾空格，然后再对其进行操作。</p><p>注意：trim() 只能去掉字符串中前后的半角空格（英文空格），而无法去掉全角空格（中文空格）。可用以下代码将全角空格替换为半角空格再进行操作，其中替换是 String 类的 replace() 方法，《Java字符串的替换》一节会详细介绍这个方法。<br>str = str.replace((char) 12288, ‘ ‘);    // 将中文空格替换为英文空格<br>str = str.trim();</p><p>其中，12288 是中文全角空格的 unicode 编码。</p><h1 id="截取（提取）子字符串"><a href="#截取（提取）子字符串" class="headerlink" title="截取（提取）子字符串"></a>截取（提取）子字符串</h1><p>在 String 中提供了两个截取字符串的方法，一个是从指定位置截取到字符串结尾，另一个是截取指定范围的内容。</p><h2 id="1-substring-int-beginIndex-形式"><a href="#1-substring-int-beginIndex-形式" class="headerlink" title="1. substring(int beginIndex) 形式"></a>1. substring(int beginIndex) 形式</h2><p>此方式用于提取从索引位置开始至结尾处的字符串部分。调用时，括号中是需要提取字符串的开始位置，方法的返回值是提取的字符串。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">String str = <span class="string">"我爱 Java 编程"</span>;</span><br><span class="line">String result = str.substring(<span class="number">3</span>);</span><br><span class="line">System.out.println(result);    <span class="comment">// 输出：Java 编程</span></span><br></pre></td></tr></table></figure><h2 id="2-substring-int-beginIndex，int-endIndex-形式"><a href="#2-substring-int-beginIndex，int-endIndex-形式" class="headerlink" title="2. substring(int beginIndex，int endIndex) 形式"></a>2. substring(int beginIndex，int endIndex) 形式</h2><p><code>beginIndex</code>表示截取的起始索引，截取的字符串中包括起始索引对应的字符；<code>endIndex</code>表示结束索引，截取的字符串中不包括结束索引对应的字符，如果不指定<code>endIndex</code>，则表示截取到目标字符串末尾。该方法用于提取位置<code>beginIndex</code>和位置<code>endIndex</code>位置之间的字符串部分。</p><p>注意：substring() 方法是按字符截取，而不是按字节截取。</p><h1 id="分割字符串"><a href="#分割字符串" class="headerlink" title="分割字符串"></a>分割字符串</h1><p>String 类的 split() 方法可以按指定的分割符对目标字符串进行分割，分割后的内容存放在字符串数组中。该方法主要有如下两种重载形式：<br>str.split(String sign)<br>str.split(String sign,int limit)<br>其中它们的含义如下：<br>str 为需要分割的目标字符串。<br>sign 为指定的分割符，可以是任意字符串。<br>limit 表示分割后生成的字符串的限制个数，如果不指定，则表示不限制，直到将整个目标字符串完全分割为止。</p><p>使用分隔符注意如下：</p><p>1）“.”和“|”都是转义字符，必须得加“\”。<br>如果用“.”作为分隔的话，必须写成String.split(“\.”)，这样才能正确的分隔开，不能用String.split(“.”)。<br>如果用“|”作为分隔的话，必须写成String.split(“\|”)，这样才能正确的分隔开，不能用String.split(“|”)。</p><p>2）如果在一个字符串中有多个分隔符，可以用“|”作为连字符，比如：“acount=? and uu =? or n=?”，把三个都分隔出来，可以用String.split(“and|or”)。</p><h1 id="字符串的替换"><a href="#字符串的替换" class="headerlink" title="字符串的替换"></a>字符串的替换</h1><p>String 类提供了 3 种字符串替换方法，分别是 replace()、replaceFirst() 和 replaceAll()。<br>replace() 方法<br>replace() 方法用于将目标字符串中的指定字符（串）替换成新的字符（串），其语法格式如下：<br>字符串.replace(String oldChar, String newChar)<br>其中，oldChar 表示被替换的字符串；newChar 表示用于替换的字符串。replace() 方法会将字符串中所有 oldChar 替换成 newChar。<br>replaceFirst() 方法<br>replaceFirst() 方法用于将目标字符串中匹配某正则表达式的第一个子字符串替换成新的字符串，其语法形式如下：<br>字符串.replaceFirst(String regex, String replacement)<br>其中，regex 表示正则表达式；replacement 表示用于替换的字符串。例如：<br>String words = “hello java，hello php”;<br>String newStr = words.replaceFirst(“hello”,”你好 “);<br>System.out.println(newStr);    // 输出：你好 java,hello php<br>replaceAll() 方法<br>replaceAll() 方法用于将目标字符串中匹配某正则表达式的所有子字符串替换成新的字符串，其语法形式如下：<br>字符串.replaceAll(String regex, String replacement)<br>其中，regex 表示正则表达式，replacement 表示用于替换的字符串。例如：<br>String words = “hello java，hello php”;<br>String newStr = words.replaceAll(“hello”,”你好 “);<br>System.out.println(newStr);    // 输出：你好 java,你好 php</p><p>public static void main(String[] args) {<br>    // 定义原始字符串<br>    String intro = “今天时星其天，外面时下雨天。妈米去买菜了，漏网在家写作业。” + “语文作业时”其”写 5 行，数学使第 10 页。”;<br>    // 将文本中的所有”时”和”使”都替换为”是”<br>    String newStrFirst = intro.replaceAll(“[时使]”, “是”);<br>    // 将文本中的所有”妈米”改为”妈妈”<br>    String newStrSecond = newStrFirst.replaceAll(“妈米”, “妈妈”);<br>    // 将文本中的所有”漏网”改为”留我”<br>    String newStrThird = newStrSecond.replaceAll(“漏网”, “留我”);<br>    // 将文本中第一次出现的”其”改为”期”<br>    String newStrFourth = newStrThird.replaceFirst(“[其]”, “期”);<br>    // 输出最终字符串<br>    System.out.println(newStrFourth);<br>}<br>运行该程序，输出的正确字符串内容如下：<br>今天是星期天，外面是下雨天。妈妈去买菜了，留我在家写作业。语文作业是”其”写 5 行，数学是第 10 页。</p><h1 id="字符串比较"><a href="#字符串比较" class="headerlink" title="字符串比较"></a>字符串比较</h1>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;定义字符串&quot;&gt;&lt;a href=&quot;#定义字符串&quot; class=&quot;headerlink&quot; title=&quot;定义字符串&quot;&gt;&lt;/a&gt;定义字符串&lt;/h1&gt;&lt;p&gt;Java 没有内置的字符串类型，而是在标准 Java 类库中提供了一个 String 类来创建和操作字符串。&lt;/p&gt;
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>java/基础/Java 集合</title>
    <link href="http://yoursite.com/2021/07/06/java/%E5%9F%BA%E7%A1%80/Java%20%E9%9B%86%E5%90%88/"/>
    <id>http://yoursite.com/2021/07/06/java/%E5%9F%BA%E7%A1%80/Java%20%E9%9B%86%E5%90%88/</id>
    <published>2021-07-06T05:55:06.556Z</published>
    <updated>2021-07-06T06:44:20.303Z</updated>
    
    <content type="html"><![CDATA[<p>在编程时，可以使用数组来保存多个对象，但数组长度不可变化，一旦在初始化数组时指定了数组长度，这个数组长度就是不可变的。如果需要保存数量变化的数据，数组就有点无能为力了。而且数组无法保存具有映射关系的数据，如成绩表为语文——79，数学——80，这种数据看上去像两个数组，但这两个数组的元素之间有一定的关联关系。</p><p>为了保存数量不确定的数据，以及保存具有映射关系的数据（也被称为关联数组），Java 提供了集合类。集合类主要负责保存、盛装其他数据，因此集合类也被称为容器类。Java 所有的集合类都位于 java.util 包下，提供了一个表示和操作对象集合的统一构架，包含大量集合接口，以及这些接口的实现类和操作它们的算法。</p><p>集合类和数组不一样，数组元素既可以是基本类型的值，也可以是对象（实际上保存的是对象的引用变量），而集合里只能保存对象（实际上只是保存对象的引用变量，但通常习惯上认为集合里保存的是对象）。</p><p>Java 集合类型分为 Collection 和 Map，它们是 Java 集合的根接口，这两个接口又包含了一些子接口或实现类。下图分别为 Collection 和 Map 的子接口及其实现类。黄色块为集合的接口，蓝色块为集合的实现类。</p><img src="/2021/07/06/java/%E5%9F%BA%E7%A1%80/Java%20%E9%9B%86%E5%90%88/1.png" class="" title="Collection接口基本结构"><img src="/2021/07/06/java/%E5%9F%BA%E7%A1%80/Java%20%E9%9B%86%E5%90%88/2.png" class="" title="Map接口基本结构"><p>Java集合接口的作用：</p><table><thead><tr><th align="center">接口名称</th><th align="center">作    用</th></tr></thead><tbody><tr><td align="center">Iterator 接口</td><td align="center">集合的输出接口，主要用于遍历输出（即迭代访问）Collection 集合中的元素，Iterator 对象被称之为迭代器。迭代器接口是集合接口的父接口，实现类实现 Collection 时就必须实现 Iterator 接口。</td></tr><tr><td align="center">Collection 接口</td><td align="center">是 List、Set 和 Queue 的父接口，是存放一组单值的最大接口。所谓的单值是指集合中的每个元素都是一个对象。一般很少直接使用此接口直接操作。</td></tr><tr><td align="center">Queue 接口</td><td align="center">Queue 是 Java 提供的队列实现，有点类似于 List。</td></tr><tr><td align="center">Dueue 接口</td><td align="center">是 Queue 的一个子接口，为双向队列。</td></tr><tr><td align="center">List 接口</td><td align="center">是最常用的接口。是有序集合，允许有相同的元素。使用 List 能够精确地控制每个元素插入的位置，用户能够使用索引（元素在 List 中的位置，类似于数组下标）来访问 List 中的元素，与数组类似。</td></tr><tr><td align="center">Set 接口</td><td align="center">不能包含重复的元素。</td></tr><tr><td align="center">Map 接口</td><td align="center">是存放一对值的最大接口，即接口中的每个元素都是一对，以 key➡value 的形式保存。</td></tr></tbody></table><p>对于 Set、List、Queue 和 Map 这 4 种集合，Java 最常用的实现类分别是 HashSet、TreeSet、ArrayList、ArrayDueue、LinkedList 和 HashMap、TreeMap 等。</p><table><thead><tr><th align="center">类名称</th><th align="center">作    用</th></tr></thead><tbody><tr><td align="center">HashSet</td><td align="center">为优化査询速度而设计的 Set。它是基于 HashMap 实现的，HashSet 底层使用 HashMap 来保存所有元素，实现比较简单</td></tr><tr><td align="center">TreeSet</td><td align="center">实现了 Set 接口，是一个有序的 Set，这样就能从 Set 里面提取一个有序序列</td></tr><tr><td align="center">ArrayList</td><td align="center">一个用数组实现的 List，能进行快速的随机访问，效率高而且实现了可变大小的数组</td></tr><tr><td align="center">ArrayDueue</td><td align="center">是一个基于数组实现的双端队列，按“先进先出”的方式操作集合元素</td></tr><tr><td align="center">LinkedList</td><td align="center">对顺序访问进行了优化，但随机访问的速度相对较慢。此外它还有 addFirst()、addLast()、getFirst()、getLast()、removeFirst() 和 removeLast() 等方法，能把它当成栈（Stack）或队列（Queue）来用</td></tr><tr><td align="center">HsahMap</td><td align="center">按哈希算法来存取键对象</td></tr><tr><td align="center">TreeMap</td><td align="center">可以对键对象进行排序</td></tr></tbody></table><h1 id="Collection接口"><a href="#Collection接口" class="headerlink" title="Collection接口"></a>Collection接口</h1><p>Collection 接口是 List、Set 和 Queue 接口的父接口，通常情况下不被直接使用。Collection 接口定义了一些通用的方法，通过这些方法可以实现对集合的基本操作。定义的方法既可用于操作 Set 集合，也可用于操作 List 和 Queue 集合。</p><p>| 方法名称 | 说明 |<br>| boolean add(E e) | 向集合中添加一个元素，如果集合对象被添加操作改变了，则返回 true。E 是元素的数据类型 |<br>| boolean addAll(Collection c) | 向集合中添加集合 c 中的所有元素，如果集合对象被添加操作改变了，则返回 true。 |<br>| void clear() | 清除集合中的所有元素，将集合长度变为 0。 |<br>| boolean contains(Object o) | 判断集合中是否存在指定元素 |<br>| boolean containsAll(Collection c) | 判断集合中是否包含集合 c 中的所有元素 |<br>| boolean isEmpty() | 判断集合是否为空 |<br>| Iterator<E>iterator() | 返回一个 Iterator 对象，用于遍历集合中的元素 |<br>| boolean remove(Object o) | 从集合中删除一个指定元素，当集合中包含了一个或多个元素 o 时，该方法只删除第一个符合条件的元素，该方法将返回 true。 |<br>| boolean removeAll(Collection c) | 从集合中删除所有在集合 c 中出现的元素（相当于把调用该方法的集合减去集合 c）。如果该操作改变了调用该方法的集合，则该方法返回 true。 |<br>| boolean retainAll(Collection c) | 从集合中删除集合 c 里不包含的元素（相当于把调用该方法的集合变成该集合和集合 c 的交集），如果该操作改变了调用该方法的集合，则该方法返回 true。 |<br>| int size() | 返回集合中元素的个数 |<br>| Object[] toArray() | 把集合转换为一个数组，所有的集合元素变成对应的数组元素。 |</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    ArrayList list1 = <span class="keyword">new</span> ArrayList(); <span class="comment">// 创建集合 list1</span></span><br><span class="line">    ArrayList list2 = <span class="keyword">new</span> ArrayList(); <span class="comment">// 创建集合 list2</span></span><br><span class="line">    list1.add(<span class="string">"one"</span>); <span class="comment">// 向 list1 添加一个元素</span></span><br><span class="line">    list1.add(<span class="string">"two"</span>); <span class="comment">// 向 list1 添加一个元素</span></span><br><span class="line">    list2.addAll(list1); <span class="comment">// 将 list1 的所有元素添加到 list2</span></span><br><span class="line">    list2.add(<span class="string">"three"</span>); <span class="comment">// 向 list2 添加一个元素</span></span><br><span class="line">    System.out.println(<span class="string">"list2 集合中的元素如下："</span>);</span><br><span class="line">    Iterator it1 = list2.iterator();</span><br><span class="line">    <span class="keyword">while</span> (it1.hasNext()) &#123;</span><br><span class="line">        System.out.print(it1.next() + <span class="string">"、"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>由于 Collection 是接口，不能对其实例化，所以上述代码中使用了 Collection 接口的 ArrayList 实现类来调用 Collection 的方法。add() 方法可以向 Collection 中添加一个元素，而调用 addAll() 方法可以将指定 Collection 中的所有元素添加到另一个 Collection 中。</p><p>代码创建了两个集合 list1 和 list2，然后调用 add() 方法向 list1 中添加了两个元素，再调用 addAll() 方法将这两个元素添加到 list2 中。接下来又向 list2 中添加了一个元素，最后输出 list2 集合中的所有元素，结果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">list2 集合中的元素如下：</span><br><span class="line">one、two、three、</span><br></pre></td></tr></table></figure><h1 id="List集合"><a href="#List集合" class="headerlink" title="List集合"></a>List集合</h1><p>List 是一个有序、可重复的集合，集合中每个元素都有其对应的顺序索引。List 集合允许使用重复元素，可以通过索引来访问指定位置的集合元素。List 集合默认按元素的添加顺序设置元素的索引，第一个添加到 List 集合中的元素的索引为 0，第二个为 1，依此类推。</p><p>List 实现了 Collection 接口，它主要有两个常用的实现类：ArrayList 类和 LinkedList 类。</p><h2 id="ArrayList-类"><a href="#ArrayList-类" class="headerlink" title="ArrayList 类"></a>ArrayList 类</h2><p>ArrayList 类实现了可变数组的大小，存储在内的数据称为元素。它还提供了快速基于索引访问元素的方式，对尾部成员的增加和删除支持较好。使用 ArrayList 创建的集合，允许对集合中的元素进行快速的随机访问，不过，向 ArrayList 中插入与删除元素的速度相对较慢。</p><p>ArrayList 类的常用构造方法有如下两种重载形式：<br>ArrayList()：构造一个初始容量为 10 的空列表。<br>ArrayList(Collection&lt;?extends E&gt;c)：构造一个包含指定 Collection 元素的列表，这些元素是按照该 Collection 的迭代器返回它们的顺序排列的。</p><p>ArrayList 类除了包含 Collection 接口中的所有方法之外，还包括 List 接口中提供的如表 1 所示的方法。</p><table><thead><tr><th align="center">方法名称</th><th align="center">说明</th></tr></thead><tbody><tr><td align="center">E get(int index)</td><td align="center">获取此集合中指定索引位置的元素，E 为集合中元素的数据类型</td></tr><tr><td align="center">int index(Object o)</td><td align="center">返回此集合中第一次出现指定元素的索引，如果此集合不包含该元素，则返回 -1</td></tr><tr><td align="center">int lastIndexOf(Object o)</td><td align="center">返回此集合中最后一次出现指定元素的索引，如果此集合不包含该元素，则返回 -1</td></tr><tr><td align="center">E set(int index, Eelement)</td><td align="center">将此集合中指定索引位置的元素修改为 element 参数指定的对象。此方法返回此集合中指定索引位置的原元素</td></tr><tr><td align="center">List<E> subList(int fromlndex, int tolndex)</td><td align="center">返回一个新的集合，新集合中包含 fromlndex 和 tolndex 索引之间的所有元素。包含 fromlndex 处的元素，不包含 tolndex 索引处的元素</td></tr></tbody></table><p>注意：当调用 List 的 set(int index, Object element) 方法来改变 List 集合指定索引处的元素时，指定的索引必须是 List 集合的有效索引。例如集合长度为 4，就不能指定替换索引为 4 处的元素，也就是说这个方法不会改变 List 集合的长度。</p><table><thead><tr><th align="center">方法名称</th><th align="center">说明</th></tr></thead><tbody><tr><td align="center">void addFirst(E e)</td><td align="center">将指定元素添加到此集合的开头</td></tr><tr><td align="center">void addLast(E e)</td><td align="center">将指定元素添加到此集合的末尾</td></tr><tr><td align="center">E getFirst()</td><td align="center">返回此集合的第一个元素</td></tr><tr><td align="center">E getLast()</td><td align="center">返回此集合的最后一个元素</td></tr><tr><td align="center">E removeFirst()</td><td align="center">删除此集合中的第一个元素</td></tr><tr><td align="center">E removeLast()</td><td align="center">删除此集合中的最后一个元素</td></tr></tbody></table><h1 id="ArrayList-类和-LinkedList-类的区别"><a href="#ArrayList-类和-LinkedList-类的区别" class="headerlink" title="ArrayList 类和 LinkedList 类的区别"></a>ArrayList 类和 LinkedList 类的区别</h1><p>ArrayList 与 LinkedList 都是 List 接口的实现类，因此都实现了 List 的所有未实现的方法，只是实现的方式有所不同。</p><p>ArrayList 是基于动态数组数据结构的实现，访问元素速度优于 LinkedList。LinkedList 是基于链表数据结构的实现，占用的内存空间比较大，但在批量插入或删除数据时优于 ArrayList。</p><p>对于快速访问对象的需求，使用 ArrayList 实现执行效率上会比较好。需要频繁向集合中插入和删除元素时，使用 LinkedList 类比 ArrayList 类效果高。</p><p>不同的结构对应于不同的算法，有的考虑节省占用空间，有的考虑提高运行效率，对于程序员而言，它们就像是“熊掌”和“鱼肉”，不可兼得。高运行速度往往是以牺牲空间为代价的，而节省占用空间往往是以牺牲运行速度为代价的。</p><h1 id="Set集合"><a href="#Set集合" class="headerlink" title="Set集合"></a>Set集合</h1><p>Set 集合类似于一个罐子，程序可以依次把多个对象“丢进”Set 集合，而 Set 集合通常不能记住元素的添加顺序。也就是说 Set 集合中的对象不按特定的方式排序，只是简单地把对象加入集合。Set 集合中不能包含重复的对象，并且最多只允许包含一个 null 元素。</p><p>Set 实现了 Collection 接口，它主要有两个常用的实现类：HashSet 类和 TreeSet类。<br>HashSet 类<br>HashSet 是 Set 接口的典型实现，大多数时候使用 Set 集合时就是使用这个实现类。HashSet 是按照 Hash 算法来存储集合中的元素。因此具有很好的存取和查找性能。</p><p>HashSet 具有以下特点：<br>不能保证元素的排列顺序，顺序可能与添加顺序不同，顺序也有可能发生变化。<br>HashSet 不是同步的，如果多个线程同时访问或修改一个 HashSet，则必须通过代码来保证其同步。<br>集合元素值可以是 null。</p><p>当向 HashSet 集合中存入一个元素时，HashSet 会调用该对象的 hashCode() 方法来得到该对象的 hashCode 值，然后根据该 hashCode 值决定该对象在 HashSet 中的存储位置。如果有两个元素通过 equals() 方法比较返回的结果为 true，但它们的 hashCode 不相等，HashSet 将会把它们存储在不同的位置，依然可以添加成功。</p><p>也就是说，两个对象的 hashCode 值相等且通过 equals() 方法比较返回结果为 true，则 HashSet 集合认为两个元素相等。</p><p>在 HashSet 类中实现了 Collection 接口中的所有方法。HashSet 类的常用构造方法重载形式如下。<br>HashSet()：构造一个新的空的 Set 集合。<br>HashSet(Collection&lt;? extends E&gt;c)：构造一个包含指定 Collection 集合元素的新 Set 集合。其中，“&lt; &gt;”中的 extends 表示 HashSet 的父类，即指明该 Set 集合中存放的集合元素类型。c 表示其中的元素将被存放在此 Set 集合中。</p><p>下面的代码演示了创建两种不同形式的 HashSet 对象。<br>HashSet hs = new HashSet();    // 调用无参的构造函数创建HashSet对象<br>HashSet<String> hss = new HashSet<String>();    // 创建泛型的 HashSet 集合对象</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;在编程时，可以使用数组来保存多个对象，但数组长度不可变化，一旦在初始化数组时指定了数组长度，这个数组长度就是不可变的。如果需要保存数量变化的数据，数组就有点无能为力了。而且数组无法保存具有映射关系的数据，如成绩表为语文——79，数学——80，这种数据看上去像两个数组，但这两
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>java/SpringBoot/Spring-Boot-Starter常用依赖模块</title>
    <link href="http://yoursite.com/2021/07/06/java/SpringBoot/Spring-Boot-Starter%E5%B8%B8%E7%94%A8%E4%BE%9D%E8%B5%96%E6%A8%A1%E5%9D%97/"/>
    <id>http://yoursite.com/2021/07/06/java/SpringBoot/Spring-Boot-Starter%E5%B8%B8%E7%94%A8%E4%BE%9D%E8%B5%96%E6%A8%A1%E5%9D%97/</id>
    <published>2021-07-06T02:52:59.263Z</published>
    <updated>2021-07-06T05:49:06.305Z</updated>
    
    <content type="html"><![CDATA[<p>SpringBoot 提供的依赖模块都约定以 spring-boot-starter- 作为命名的前缀，并且皆位于 org.springframework.boot 包或者命名空间下。</p><p>所有的 spring-boot-starter 都有约定俗成的默认配置，但允许我们调整这些配置以改变默认的配置行为，即“约定优先于配置”。</p><h1 id="应用日志和spring-boot-starter-logging"><a href="#应用日志和spring-boot-starter-logging" class="headerlink" title="应用日志和spring-boot-starter-logging"></a>应用日志和spring-boot-starter-logging</h1><p>Java 的日志系统多种多样，从<code>java.util</code>默认提供的日志支持，到<code>log4j，log4j2，commons logging</code>等，复杂繁多，所以，应用日志系统的配置就会比较特殊，从而<code>spring-boot-starter-logging</code>也比较特殊一些。</p><p>假如 maven 依赖中添加了<code>spring-boot-starter-logging</code>：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span> org.springframework.boot <span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span> spring-boot-starter-logging <span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><p>那么，我们的 SpringBoot 应用将自动使用 logback 作为应用日志框架，SpringBoot 启动的时候，由 org.springframework.boot.logging.Logging-Application-Listener 根据情况初始化并使用。</p><p>SpringBoot 为我们提供了很多默认的日志配置，所以，只要将 spring-boot-starter-logging 作为依赖加入到当前应用的 classpath，则“开箱即用”，不需要做任何多余的配置，但假设我们要对默认 SpringBoot 提供的应用日志设定做调整，则可以通过几种方式进行配置调整：<br>遵循 logback 的约定，在 classpath 中使用自己定制的 logback.xml 配置文件。<br>在文件系统中任何一个位置提供自己的 logback.xml 配置文件，然后通过 logging.config 配置项指向这个配置文件来启用它，比如在 application.properties 中指定如下的配置。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">logging.config&#x3D;&#x2F;&#123;some.path.you.defined&#125;&#x2F;any-logfile-name-I-like.log</span><br></pre></td></tr></table></figure><p>SpringBoot 默认允许我们通过在配置文件或者命令行等方式使用 logging.file 和 logging.path 来自定义日志文件的名称和存放路径，不过，这只是允许我们在 SpringBoot 框架预先定义的默认日志系统设定的基础上做有限的设置，如果我们希望更灵活的配置，最好通过框架特定的配置方式提供相应的配置文件，然后通过 logging.config 来启用。</p><p>如果大家更习惯使用 log4j 或者 log4j2，那么也可以采用类似的方式将它们对应的 spring-boot-starter 依赖模块加到 Maven 依赖中即可：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span> org.springframework.boot <span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span> spring-boot-starter-log4j <span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 或者 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span> org.springframework.boot <span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span> spring-boot-starter-log4j2 <span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><p>但一定不要将这些完成同一目的的 spring-boot-starter 都加到依赖中。</p><h1 id="快速-Web-应用开发与-spring-boot-starter-web"><a href="#快速-Web-应用开发与-spring-boot-starter-web" class="headerlink" title="快速 Web 应用开发与 spring-boot-starter-web"></a>快速 Web 应用开发与 spring-boot-starter-web</h1><p>为了帮我们简化快速搭建并开发一个 Web 项目，SpringBoot 提供了<code>spring-boot-starter-web</code>自动配置模块。</p><p>只要将<code>spring-boot-starter-web</code>加入项目的 maven 依赖：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><p>我们就得到了一个直接可执行的 Web 应用，当前项目下运行<code>mvn spring-boot：run</code>就可以直接启动一个使用了嵌入式 tomcat 服务请求的 Web 应用，只不过，我们还没有提供任何服务 Web 请求的<code>Controller</code>，所以，访问任何路径都会返回一个 SpringBoot 默认提供的错误页面（一般称其为 whitelabel error page），我们可以在当前项目下新建一个服务根路径 Web 请求的 Controller 实现：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">IndexController</span> </span>&#123;</span><br><span class="line">    <span class="meta">@RequestMapping</span>(<span class="string">"/"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">index</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"hello, there"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>重新运行 mvn spring-boot：run 并访问<code>http://localhost：8080</code>，错误页面将被我们的<code>Controller</code>返回的消息所替代，一个简单的 Web 应用就这样完成了。</p><h2 id="项目结构层面的约定"><a href="#项目结构层面的约定" class="headerlink" title="项目结构层面的约定"></a>项目结构层面的约定</h2><p>项目结构层面与传统打包为 war 的 Java Web 应用的差异在于，静态文件和页面模板的存放位置变了，原来是放在<code>src/main/webapp</code>目录下的一系列资源，现在都统一放在<code>src/main/resources</code>相应子目录下，比如：</p><ul><li><code>src/main/resources/static</code>用于存放各类静态资源，比如<code>css，js</code>等。</li><li><code>src/main/resources/templates</code>用于存放模板文件，比如<code>*.vm</code>。</li></ul><p>当然，如果还是希望以 war 包的形式，而不是 SpringBoot 推荐使用的独立 jar 包形式发布 Web 应用，也可以继续原来 Java Web 应用的项目结构约定。</p><h2 id="SpringMVC-框架层面的约定和定制"><a href="#SpringMVC-框架层面的约定和定制" class="headerlink" title="SpringMVC 框架层面的约定和定制"></a>SpringMVC 框架层面的约定和定制</h2><p><code>spring-boot-starter-web</code>默认将为我们自动配置如下一些 SpringMVC 必要组件：</p><ul><li>必要的 ViewResolver，比如 ContentNegotiatingViewResolver 和 Bean-NameViewResolver。</li><li>将必要的 Converter、GenericConverter 和 Formatter 等 bean 注册到 IoC 容器。</li><li>添加一系列的 HttpMessageConverter 以便支持对 Web 请求和相应的类型转换。</li><li>自动配置和注册 MessageCodesResolver。</li><li>其他。</li></ul><p>任何时候，如果我们对默认提供的 SpringMVC 组件设定不满意，都可以在 IoC 容器中注册新的同类型的<code>bean</code>定义来替换，或者直接提供一个基于<code>WebMvcConfigurerAdapter</code>类型的<code>bean</code>定义来定制，甚至直接提供一个标注了<code>@EnableWebMvc</code>的<code>@Configuration</code>配置类完全接管所有 SpringMVC 的相关配置，自己完全重新配置。</p><h1 id="spring-boot-starter-jdbc与数据访问"><a href="#spring-boot-starter-jdbc与数据访问" class="headerlink" title="spring-boot-starter-jdbc与数据访问"></a>spring-boot-starter-jdbc与数据访问</h1><p>若想 SpringBoot 为我们自动配置数据访问的基础设施，那么，我们需要直接或者间接地依赖 spring-jdbc，一旦 spring-jdbc 位于我们 SpringBoot 应用的 classpath，即会触发数据访问相关的自动配置行为，最简单的做法就是把 spring-boot-starter-jdbc 加为应用的依赖。</p><p>默认情况下，如果我们没有配置任何 DataSource，那么，SpringBoot 会为我们自动配置一个基于嵌入式数据库的 DataSource，这种自动配置行为其实很适合于测试场景，但对实际的开发帮助不大，基本上我们会自己配置一个 DataSource 实例，或者通过自动配置模块提供的配置参数对 DataSource 实例进行自定义的配置。</p><p>假设我们的 SpringBoot 应用只依赖一个数据库，那么，使用 DataSource 自动配置模块提供的配置参数是最方便的：<br>spring.datasource.url=jdbc:mysql://{database host}:3306/{databaseName}<br>spring.datasource.username={database username}<br>spring.datasource.password={database password}</p><p>当然，自己配置一个 DataSource 也是可以的，SpringBoot 也会智能地选择我们自己配置的这个 DataSource 实例（只不过必要性真不大）。</p><p>除了 DataSource 会自动配置，SpringBoot 还会自动配置相应的 JdbcTemplate、DataSourceTransactionManager 等关联“设施”，可谓服务周到，我们只要在使用的地方注入就可以了：<br>class SomeDao {<br>    @Autowired<br>    JdbcTemplate jdbcTemplate;<br>    public <T> List<T> queryForList(String sql){<br>        // …<br>    }<br>    // …<br>}<br>不过，spring-boot-starter-jdbc 以及与其相关的自动配置也不总是带来便利，在某些场景下，我们可能会在一个应用中需要依赖和访问多个数据库，这个时候就会出现问题了。</p><p>假设我们在 ApplicationContext 中配置了多个 DataSource 实例指向多个数据库：<br>@Bean<br>public DataSource dataSource1() throws Throwable {<br>    DruidDataSource dataSource = new DruidDataSource();<br>    dataSource.setUrl(…);<br>    dataSource.setUsername(…);<br>    dataSource.setPassword(…);<br>    // TODO other settings if necessary in the future.<br>    return dataSource;<br>}<br>@Bean<br>public DataSource dataSource2() throws Throwable {<br>    DruidDataSource dataSource = new DruidDataSource();<br>    dataSource.setUrl(…);<br>    dataSource.setUsername(…);<br>    dataSource.setPassword(…);<br>    // TODO other settings if necessary in the future.<br>    return dataSource;<br>}<br>那么，不好意思，启动 SpringBoot 应用的时候会抛出类似如下的异常（Exception）：<br>Exception）：No qualifying bean of type [javax.sql.DataSource] is defined: expected single matching bean but found 2 </p><p>为了避免这种情况的发生，我们需要在 SpringBoot 的启动类上做点儿“手脚”：<br>@SpringBootApplication(exclude = { DataSourceAutoConfiguration.class,<br>        DataSourceTransactionManagerAutoConfiguration.class })<br>public class UnveilSpringChapter3Application {<br>    public static void main(String[] args) {<br>        SpringApplication.run(UnveilSpringChapter3Application.class, args);<br>    }<br>}<br>也就是说，我们需要在这种场景下排除掉对 SpringBoot 默认提供的 DataSource 相关的自动配置。但如果我们还是想要享受 SpringBoot 提供的自动配置 DataSource 的机能，也可以通过为其中一个 DataSource 配置添加 org.springframework.context.annotation.Primary 这个 Annotation 的方式以实现两全其美：<br>@Bean<br>@Primary<br>public DataSource dataSource1() throws Throwable {<br>    DruidDataSource dataSource = new DruidDataSource();<br>    dataSource.setUrl(…);<br>    dataSource.setUsername(…);<br>    dataSource.setPassword(…);<br>    // TODO other settings if necessary in the future.<br>    return dataSource;<br>}<br>@Bean<br>public DataSource dataSource2() throws Throwable {<br>    DruidDataSource dataSource = new DruidDataSource();<br>    dataSource.setUrl(…);<br>    dataSource.setUsername(…);<br>    dataSource.setPassword(…);<br>    // TODO other settings if necessary in the future.<br>    return dataSource;<br>    }<br>}<br>另外，SpringBoot 还提供了很多其他数据访问相关的自动配置模块，比如<code>spring-boot-starter-data-jpa、spring-boot-starter-data-mongodb</code>等。</p><p>如果选择了<code>spring-boot-starter-data-jpa</code>等关系数据库相关的数据访问自动配置模块，并且还需要同时依赖访问多个数据库，那么，也需要相应的在 SpringBoot 启动类中排除掉这些自动配置模块中的<code>AutoConfiguration</code>实现类（对应<code>spring-boot-starter-data-jpa</code>是<code>JpaRepositoriesAutoConfiguration</code>），或者标注某个<code>DataSource</code>为<code>@Primary</code>。</p><h1 id="spring-boot-starter-aop"><a href="#spring-boot-starter-aop" class="headerlink" title="spring-boot-starter-aop"></a>spring-boot-starter-aop</h1><p>现在 Spring 框架提供的 AOP 方案倡导了一种各取所长的方案，即使用 SpringAOP 的面向对象的方式来编写和组织织入逻辑，并使用 AspectJ 的 Pointcut 描述语言配合 Annotation 来标注和指明织入点（Jointpoint）。</p><p>SpringBoot 为我们提供了一个 spring-boot-starter-aop 自动配置模块。</p><p>spring-boot-starter-aop 自动配置行为由两部分内容组成：<br>位于 spring-boot-autoconfigure的org.springframework.boot.autoconfigure.aop.AopAutoConfiguration 提供 @Configuration 配置类和相应的配置项。<br>spring-boot-starter-aop 模块自身提供了针对 spring-aop、aspectjrt 和 aspectjweaver 的依赖。</p><p>一般情况下，只要项目依赖中加入了 spring-boot-starter-aop，其实就会自动触发 AOP 的关联行为，包括构建相应的 AutoProxyCreator，将横切关注点织入（Weave）相应的目标对象等，不过 AopAutoConfiguration 依然为我们提供了可怜的两个配置项，用来有限地干预 AOP 相关配置：<br>spring.aop.auto=true<br>spring.aop.proxy-target-class=false</p><p>对我们来说，这两个配置项的最大意义在于：允许我们投反对票，比如可以选择关闭自动的 aop 配置（spring.aop.auto=false），或者启用针对 class 而不是 interface 级别的 aop 代理（aop proxy）。</p><p>AOP 的应用场景很多，我们不妨以当下最热门的 APM（Application Performance Monitoring）为实例场景，尝试使用 spring-boot-starter-aop 的支持打造一个应用性能监控的工具原型。<br>spring-boot-starter-aop 在构建 spring-boot-starter-metrics 自定义模块中的应用<br>对于应用性能监控来说，架构逻辑上其实很简单，基本上就是三步走（如图 1 所示）。</p><p>本节暂时只构建一个 spring-boot-starter-metrics 自定义的自动配置模块用来解决“应用性能数据采集”的问题。<br>应用性能监控关键环节示意图<br>图 1  应用性能监控关键环节示意图</p><p>在此之前，有几个原则我们需要先说明一下：</p><p>虽然说采集应用性能数据可以帮助我们更好地分析和改进应用的性能指标，但这不意味着可以借着 APM 的名义对应用的核心职能形成侵害，加上应用性能数据采集功能一定会对应用的性能本身带来拖累，你拿到的所谓性能数据是分摊了你的数据采集方案带来的负担，所以，一般情况下，最好把应用性能数据采集模块的性能损耗控制在 10% 以内甚至更小。</p><p>SpringAOP 其实提供了多种横切逻辑织入机制（Weaving），性能损耗上也是各有差别，从运行期间的动态代理和字节码增强 Weavng，到类加载期间的 Weaving，甚至高冷的 AspectJ 二次静态编译 Weaving，大家可以根据情况灵活把握。</p><p>针对应用性能数据的采集，最好对应用开发者是透明的，通过配置外部化的形式，可以最大限度地剥离这部分对应用开发者来说非核心的关注点，只在部署和运行之前确定采集点并配置上线即可。</p><p>虽然本节实例采用基于 @Annotation 的方式来标注性能采集点，但不意味着这是最优的方式，更多是基于技术方案（SpringAOP）的现状给出的一种实践方式。</p><p>下面我们正式着手构建 spring-boot-starter-metrics 自定义的自动配置模块的设计和实现方案。</p><p>笔者一向是只在有必要的时候才重新“造轮子”，绝不会为了炫技而去“造轮子”，所以，本次的主角我们选择 Java 中的 Dropwizard Metrics 这个类库作为打造我们 APM 原型的起点。</p><p>Dropwizard Metrics 为我们提供了多种不同类型的应用数据度量方案，且通过相应的数据处理算法在性能和批量状态的管理上做了很优秀的工作，只不过，如果我们直接用它的 API 来对自己的应用代码进行度量的话，那写起来代码太多，而且这些性能代码混杂在应用的核心逻辑执行路径上，一个是界面不友好，另外一个就是不容易维护：<br>public class MockService implements InitializingBean {<br>    @Autowired<br>    MetricRegistry metricRegistry;<br>    private Timer timer;<br>    private Counter counter;<br>    // define more other metrics…<br>    public void doSth() {<br>        counter.inc();<br>        Timer.Context context = timer.time();<br>        try {<br>            System.out.println(“just do something.”);<br>        } finally {<br>            context.stop();<br>        }<br>    }<br>    @Override<br>    public void afterPropertiesSet() throws Exception {<br>        timer = metricRegistry.timer(“timerToProfilingDoSthMethod”);<br>        counter = metricRegistry.counter(“counterForDoSthMethod”);<br>    }<br>}<br>所以，对于这些非功能性的性能度量代码，我们可以使用 AOP 的方式剥离到相应的 Aspect 中单独维护，而为了能够将这些性能度量的 Aspect 挂接到指定的待度量代码上，基于现有的方案选型。</p><p>可以使用 metrics-annotation 提供的一系列 Annotation 来标注织入位置，这样，开发者只要在需要度量的代码位置上标注相应的 Annotation，我们提供的 spring-boot-starter-metrics 自定义的自动配置模块就会自动地收集这些位置上指定的性能度量数据。</p><p>首先，我们通过 <a href="http://start.spring.io/" target="_blank" rel="noopener">http://start.spring.io/</a> 构建一个 SpringBoot 的脚手架项目，选择以 Maven 编译（选择用 Gradle 的同学自行甄别后面的配置如何具体进行），然后在创建好的 SpringBoot 脚手架项目的 pom.xml 中添加如下必要配置：<br><?xml version="1.0" encoding="UTF-8"?><br><project xmlns="http://maven.apache.org/POM/4.0.0"    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0        http://maven.apache.org/xsd/maven-4.0.0.xsd"><br>    <modelVersion>4.0.0</modelVersion><br>    <groupId>com.keevol</groupId><br>    <artifactId>spring-boot-starter-metrics</artifactId><br>    <version>0.0.1-SNAPSHOT</version><br>    <packaging>jar</packaging><br>    <name>spring-boot-starter-metrics</name><br>    <description>auto configuration module for dropwizard metrics</description><br>    <parent><br>        <groupId>org.springframework.boot</groupId><br>        <artifactId>spring-boot-starter-parent</artifactId><br>        <version>1.3.0.RELEASE</version><br>        <relativePath /> <!-- lookup parent from repository --><br>    </parent><br>    <properties><br>        &lt;project.build.sourceEncoding&gt;UTF-8&lt;/project.build.sourceEncoding&gt;<br>        &lt;java.version&gt;1.8&lt;/java.version&gt;<br>        &lt;metrics.version&gt;3.1.2&lt;/metrics.version&gt;<br>    </properties> <!--其他配置 --><br>    <dependencies><br>        <dependency><br>            <groupId>org.springframework.boot</groupId><br>            <artifactId>spring-boot-starter</artifactId><br>        </dependency><br>        <dependency><br>            <groupId>org.springframework.boot</groupId><br>            <artifactId>spring-boot-starter-aop</artifactId><br>        </dependency><br>        <dependency><br>            <groupId>org.springframework.boot</groupId><br>            <artifactId>spring-boot-starter-actuator</artifactId><br>        </dependency><br>        <dependency><br>            <groupId>io.dropwizard.metrics</groupId><br>            <artifactId>metrics-core</artifactId><br>            <version><br>                ${metrics.version}<br>            </version><br>        </dependency><br>        <dependency><br>            <groupId>io.dropwizard.metrics</groupId><br>            <artifactId>metrics-annotation</artifactId><br>            <version>${metrics.version}</version><br>        </dependency><br>        <dependency><br>            <groupId>org.aspectj</groupId><br>            <artifactId>aspectjrt</artifactId><br>            <version>1.8.7</version><br>        </dependency><br>    </dependencies><br></project><br>pom.xml 中有几个关键配置需要关注：<br>继承了 spring-boot-starter-parent，用于加入 springboot 的相关依赖。<br>添加了 spring-boot-starter-aop 依赖。<br>添加了 io.dropwizard.metrics 下相应的依赖，用来引入 dropwizard metrics 类库和必要的 Annotations。<br>添加了 spring-boot-starter-actuator，这个自动配置模块教程后面会跟大家进一步介绍，在这里我们主要是引入它对 dropwizard metrics 和 JMX 的一部分自动配置逻辑，比如针对 MetricRegistry 和 MBeanServer 的自动配置，这样我们就可以直接 @Autowired 来注入使用 MetricRegistry 和 MBeanServer。</p><p>至于 aspectjrt，是使用了最新的版本，原则上spring-boot-starter-aop已经有依赖，这里可以不用明确添加配置。</p><p>如果单单是一个提供必要依赖的自动配置模块，那么到这里其实就可以结束了，但我们的 spring-boot-starter-metrics 需要使用 AOP 提供相应的横切关注点逻辑。</p><p>所以，还需要编写并提供一些必要的代码组件，因此，最少我们先要提供一个 @Configuration 配置类，用于将我们即将提供的这些 AOP 逻辑暴露给使用者：<br>@Configuration<br>@ComponentScan({ “com.keevol.springboot.metrics.lifecycle”,<br>        “com.keevol.springboot.metrics.aop” })<br>@AutoConfigureAfter(AopAutoConfiguration.class)<br>public class DropwizardMetricsMBeansAutoConfiguration {<br>    @Value(“${metrics.mbeans.domain.name:com.keevol.metrics}”)<br>    String metricsMBeansDomainName;<br>    @Autowired<br>    MBeanServer mbeanServer;<br>    @Autowired<br>    MetricRegistry metricRegistry;<br>    @Bean<br>    public JmxReporter jmxReporter() {<br>        JmxReporter reporter = JmxReporte.forRegistry(metricRegistry)<br>                .inDomain(metricsMBeansDomainName).registerWith(mbeanServer)<br>                .build();<br>        return reporter;<br>    }<br>}<br>然后就是将这个配置类添加到 META-INF/spring.factories：</p><p>org.springframework.boot.autoconfigure.EnableAutoConfiguration=\com.keevol.springboot.metrics.autocfg.DropwizardMetricsMBeansAuto-ConfigurationOK，<br>不要认为将 spring-boot-starter-metrics 打包作为类库发布出去就可以了，AOP 相关的代码还没写。</p><p>我们回头来看 DropwizardMetricsMBeansAutoConfiguration 配置类，这个配置类的实现很简单，注入了 MBeanServer 和 MetricRegistry 的实例，并开放了一个 metrics.mbeans.domain.name 配置属性（默认值 com.keevol.metrics）便于使用者指定自定义的 MBean 暴露和访问的命名空间。</p><p>当然，以上给这些其实都不是重点，因为它们都只是为了将我们要采集的性能数据指标以 JMX 的形式暴露出去而服务的，重点在于 DropwizardMetricsMBeansAutoConfiguration 头顶上的那几顶“帽子”：<br>@Configuration 自然不必说了，这是一个 JavaConfig 配置类。<br>@ComponentScan（{“com.keevol.springboot.metrics.lifecycle”，”com.keevol.springboot.metrics.aop”}），为了简便，让 @ComponentScan 把这两个 java package 下的所有组件都加载到 IoC 容器中，这些组件就包括我们要提供的一系列与 AOP 和 Dropwizard Metrics 相关的实现逻辑。<br>@AutoConfigureAfter（AopAutoConfiguration.class）告诉 SpringBoot：“我希望 DropwizardMetricsMBeansAutoConfiguration 在 AopAutoConfiguration 完成之后进行配置”。</p><p>现在，最后的秘密就隐藏在 @ComponentScan 背后的两个 java package 之下了。</p><p>首先是 com.keevol.springboot.metrics.aop，在这个 java package 下面，我们只提供了一个 AutoMetricsAspect，其定义如下：<br>@Component<br>@Aspectpublic<br>class AutoMetricsAspect {<br>    protected ConcurrentMap&lt;String, Meter&gt; meters = new ConcurrentHashMap&lt;&gt;();<br>    protected ConcurrentMap&lt;String, Meter&gt; exceptionMeters = new ConcurrentHashMap&lt;&gt;();<br>    protected ConcurrentMap&lt;String, Timer&gt; timers = new ConcurrentHashMap&lt;&gt;();<br>    protected ConcurrentMap&lt;String, Counter&gt; counters = new ConcurrentHashMap&lt;&gt;();<br>    @Autowired<br>    MetricRegistry metricRegistry;<br>    @Pointcut(value = “execution(public * *(..))”)<br>    public void publicMethods() {<br>    }<br>    @Before(“publicMethods() &amp;&amp; @annotation(countedAnnotation)”)<br>    public void instrumentCounted(JoinPoint jp, Counted countedAnnotation) {<br>        String name = name(jp.getTarget().getClass(), StringUtils.hasLength(countedAnnotation.name()) ? countedAnnotation.name() : jp.getSignature().getName(), “counter”);<br>        Counter counter = counters.computeIfAbsent(name, key -&gt; metricRegistry.counter(key));<br>        counter.inc();<br>    }   </p><pre><code>@Before(&quot;publicMethods() &amp;&amp; @annotation(meteredAnnotation)&quot;)public void instrumentMetered(JoinPoint jp, Metered meteredAnnotation) {    String name = name(jp.getTarget().getClass(), StringUtils.hasLength(meteredAnnotation.name()) ? meteredAnnotation.name() : jp.getSignature().getName(), &quot;meter&quot;);    Meter meter = meters.computeIfAbsent(name, key -&gt; metricRegistry.meter(key));    meter.mark();}   @AfterThrowing(pointcut = &quot;publicMethods() &amp;&amp; @annotation(exMe-teredAnnotation)&quot;, throwing = &quot;ex&quot;)public void instrumentExceptionMetered(JoinPoint jp, Throwable ex, ExceptionMetered exMeteredAnnotation) {    String name = name(jp.getTarget().getClass(), StringUtils.hasLength(exMeteredAnnotation.name()) ? exMeteredAnnotation.name() : jp.getSignature().getName(), &quot;meter&quot;, &quot;exception&quot;);    Meter meter = exceptionMeters.computeIfAbsent(name, meterName -&gt; metricRegistry.meter(meterName));    meter.mark();}   @Around(&quot;publicMethods() &amp;&amp; @annotation(timedAnnotation)&quot;)public Object instrumentTimed(ProceedingJoinPoint pjp, Timed timedAnnotation) throws Throwable {    String name = name(pjp.getTarget().getClass(), StringUtils.hasLength(timedAnnotation.name()) ? timedAnnotation.name() : pjp.getSignature().getName(), &quot;timer&quot;);    Timer timer = timers.computeIfAbsent(name, inputName -&gt; metricRegistry.timer(inputName));    Timer.Context tc = timer.time();    try {        return pjp.proceed();    } finally {        tc.stop();    }}</code></pre><p>}<br>@Aspect+@Component 的目的在于告诉 Spring 框架：“我是一个 AOP 的 Aspect 实现类并且你可以通过 @ComponentScan 把我加入 IoC 容器之中。”当然，这不是重点。</p><p>io.dropwizard.metrics：metrics-annotation 这个依赖包为我们提供了几个有趣的 Annotation：<br>Timed<br>Gauge<br>Counted<br>Metered<br>ExceptionMetered</p><p>这些语义良好的 Annotation 定义可以用来标注相应的 AOP 逻辑扩展点，比如，针对同一个 MockService，我们可以将性能数据的度量和采集简化为只标注一两个 Annotation 就可以了：<br>@Component<br>public class MockService {<br>    @Timed<br>    @Counted<br>    public void doSth() {<br>        System.out.println(“just do something.”);<br>    }<br>}<br>但是，Annotation 注定只是 Annotation，它们只是一些标记信息，要让它们发挥作用，需要有“伯乐”的眷顾，所以，AutoMetricsAspect 在这里就是这些 Dropwizard Metrics Annotation 的“伯乐”。</p><p>通过拦截每一个 public 方法并检查方法上是否存在某个 metrics annotation，我们就可以根据具体的 metrics annotation 的类型，为匹配的方法注入相应性能数据采集代码逻辑，从而完成整个基于 AOP 和 dropwizard metrics 的应用性能数据采集方案的实现。</p><p>受限于 SpringAOP 自身的一些限制，并不是所有 AOP 的 Joinpoint 类型都支持，而且，以上原型代码方向也不见得是性能最优的方案，大家需要结合自己的目标和手上可用的技术手段，根据自己的具体应用场景具体分析和权衡。</p><h1 id="spring-boot-starter-security与应用安全"><a href="#spring-boot-starter-security与应用安全" class="headerlink" title="spring-boot-starter-security与应用安全"></a>spring-boot-starter-security与应用安全</h1><p>spring-boot-starter-security 主要面向 Web 应用安全，配合 spring-boot-starter-web。</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0</span></span></span><br><span class="line"><span class="tag"><span class="string">        http://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.keevol.unveilspring.chapter3<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>web-security-demo<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>0.0.1-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">packaging</span>&gt;</span>jar<span class="tag">&lt;/<span class="name">packaging</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>web-security-demo<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">description</span>&gt;</span>web security demo project for Spring Boot<span class="tag">&lt;/<span class="name">description</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.3.1.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">relativePath</span> /&gt;</span> <span class="comment">&lt;!-- lookup parent from repository --&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">properties</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">project.build.sourceEncoding</span>&gt;</span>UTF-8<span class="tag">&lt;/<span class="name">project.build.sourceEncoding</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">java.version</span>&gt;</span>1.8<span class="tag">&lt;/<span class="name">java.version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">properties</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-security<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span> <span class="comment">&lt;!--其他依赖 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">build</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">plugins</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-maven-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">plugins</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">build</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure><p>在当前项目中只要添加需要的 Controller 实现，一个添加了基本安全防护的 Web 应用就诞生了。spring-boot-starter-security 默认会提供一个基于 HTTP Basic 认证的安全防护策略，默认用户名为 user，访问密码则在当前 Web 应用启动的时候，打印到控制台，类似于：<br>2017-01-01 13:57:00.596 INFO 17966 — [ost-startStop-1] b.a.s.Au-thenticationManagerConfiguration : Using default security password: 560ff91b-0ae7-492c-ad16-603e1adec54c </p><p>如果我们希望对 HTTP Basic 认证的用户名和密码进行定制，可以通过如下配置项进行：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">security.user.name&#x3D;&#123;个人希望设置的用户名&#125;</span><br><span class="line">security.user.password&#x3D;&#123;个人希望使用的访问密码&#125;</span><br></pre></td></tr></table></figure><p>除此之外，spring-boot-starter-security 还会默认启用一些必要的 Web 安全防护，比如针对 XSS、CSRF 等常见针对 Web 应用的攻击，同时，也会将一些常见的静态资源路径排除在安全防护之外。</p><p>但是，说实话，spring-boot-starter-security 提供的默认安全策略相对于真正的生产环境来说，还是太弱了。但也没办法，既要安全，又要便利，spring-boot-starter-security 默认情况下已经尽量做到够好了。</p><p>不过好在 SpringSecurity 扩展性不错，要在其上构建一套真正严谨有效的 Web 应用安全防护体系也并非难事，只不过，需要我们先能够从其架构设计上理解并把握它，然后再在 SpringSecurity 和 SpringBoot 的基础上构建一套符合自身需要的 Web 应用安全方案。</p><h2 id="了解-SpringSecurity-基本设计"><a href="#了解-SpringSecurity-基本设计" class="headerlink" title="了解 SpringSecurity 基本设计"></a>了解 SpringSecurity 基本设计</h2><p>SpringSecurity 框架不但囊括了基本的认证和授权功能，而且还提供了加密解密、统一登录等一系列相关支持。</p><p>我们可以将 Spring Security 的几个核心概念按照图 1 所示勾勒在一起：<br>SpringSecurity核心概念示意图<br>图 1  SpringSecurity 核心概念示意图</p><p>访问者（Accessor）需要访问某个资源（Resources）是这个场景中最原始的需求，但并不是谁都可以访问资源，也不是任何资源都允许任何人来访问，所以，中间我们要加入一些检查和防护。</p><p>在访问资源的所经之路上，可能需要上山、过桥、下海行船，不管怎么样，这些所经之路对于我们要防护的资源来说都是比较好的设置关卡点，对应上图就是 FilterInvocation（对应 Web 应用场景）、MethodInvocation 以及 Joinpoint，这些在 Spring Security 框架中统称 Secured Object（s）。</p><p>我们知道了在哪里设置关卡最合适，下一步就是设置关卡，对应不同的所经之路，我们分别设置类似 FilterSecurityInterceptor、Method-SecurityInterceptor 以及 AspectJSecurityInterceptor 这样的关卡来负责拦截非法资源访问的闯入者们。</p><p>而在 Spring Security 框架的设计中，关卡的概念统一抽象为 AbstractSecurityInterceptor，而 FilterSecurity-Interceptor、MethodSecurityInterceptor 以及 AspectJSecurityInterceptor 都是它的具体实现类。</p><p>现在把门儿的倒是有了，可是他们不知道该拦谁，不该拦谁，所以，我们需要有类似神盾局（S.H.I.E.L.D）这样的机构，由这个机构来决定谁可以放行，谁必须阻截，而在 SpringSecurity 框架中 AccessDecisionManager 就是这个控制机构，AccessDecisionManager 将决定谁可以访问哪些资源。</p><p>现在剩下最后一个问题，这个谁怎么定义？我们总得知道当前这个访问者是谁才能告知 AccessDecisionManager 阻截还是放行，所以，SpringSecurity 框架中的 AuthenticationManager 将解决的是访问者身份认证的问题，只有确定你在册了，才可以给你授权访问（除非匿名访问某些公共资源）。</p><p>AuthenticationManager、AccessDecisionManager 和 AbstractSecurityInterceptor 属于 Spring Security 框架的基础铁三角。AuthenticationManager 和 Access-DecisionManager 负责制定规则，AbstractSecurityInterceptor 负责执行。</p><p>所有针对不同应用场景的安全方案，基本上都是在这个基础核心的基础上衍生出来的，比如，Web 安全。</p><p>Spring Security 的 Web 安全方案基于 Java 的 Servlet API 规范进行构建，所以，像 Play Framework 这种脱离 Servlet 规范的 Web 框架，则无法享受到 SpringSecurity 提供的默认的 Web 安全方案（当然，依然可以基于基本模型实现扩展方案）。</p><p>既然是基于 Servlet API 规范，那么，要实现关卡的“特效”，则非 javax.servlet.Filter 莫属了。在使用 Spring 框架开发 Filter 的时候，为了让 Filter 可以享受到依赖注入的好处，我们一般是实现 GenericFilterBean 并注册到 IoC 容器。</p><p>为了能够启用这些注册到 IoC 容器的 Filter，我们一般要在 web.xml 或者相应的 JavaConfig 的配置中声明一个 org.springframework.web.filter.DelegatingFilterProxy，使其 filter-name 与 IoC 容器中我们希望启用的 Filter 对应“挂钩”，SpringSecurity 的 Web 安全方案的启用也是这个原理。</p><p>SpringSecurity 默认会需要声明一个默认名称为“springSecurityFilterChain”的 org.springframework.web.filter.DelegatingFilterProxy（web.xml 方式或者 JavaConfig 方式），然后指向 IoC 容器中注册的一个 org.springframework.security.web.FilterChainProxy 实例。</p><p>FilterChainProxy 通过扩展 GenericFilterBean 间接实现了 Filter 接口，同时持有一组 SecurityFilterChain，使它可以针对不同的 Web 资源进行特定的防护，这些“角儿”之间的关系大体上如图 2 所示。<br>FilterChainProxy相关组件关系示意图<br>图 2  FilterChainProxy 相关组件关系示意图</p><p>当然，这些还只是“骨架”，真正执行防护任务的其实是一个个 org.springframework.security.web.SecurityFilterChain 中定义的一系列 Filter：<br>public interface SecurityFilterChain {<br>    boolean matches(HttpServletRequest request);<br>    List<Filter> getFilters();<br>}<br>当我们经常看到如下的 xml schema 形式的配置格式的时候：<br><http auto-config='true'><br>    <intercept-url pattern="/login.jsp*" access="IS_AUTHENTICATED_ANONYMOUSLY"/><br>    <intercept-url pattern="/**" access="ROLE_USER" /><br>    <form-login login-page='/login.jsp'/><br></http><br>其实一个个 http 元素背后对应的就是一个个 SecurityFilterChain 实例，而 http 元素的那些子元素，比如 intercept-url，则对应的就是一个个 Filter。</p><p>默认情况下，Spring Security 为 SecurityFilterChain 中的 Filter 序列设定了一个注册框架，以 100 为间隔步长，按照一个合理的顺序来规划和排布常用的 Filter 实现（代码参考FilterComparator）：<br>int order = 100;<br>put(ChannelProcessingFilter.class, order);<br>order += STEP;<br>put(ConcurrentSessionFilter.class, order);<br>order += STEP;<br>put(WebAsyncManagerIntegrationFilter.class, order);<br>order += STEP;<br>put(SecurityContextPersistenceFilter.class, order);<br>order += STEP;<br>put(HeaderWriterFilter.class, order);<br>order += STEP;<br>put(CsrfFilter.class, order);<br>order += STEP;<br>put(LogoutFilter.class, order);<br>order += STEP;<br>put(X509AuthenticationFilter.class, order);<br>order += STEP;<br>put(AbstractPreAuthenticatedProcessingFilter.class, order);<br>order += STEP;<br>filterToOrder.put(“org.springframework.security.cas.web.CasAuthenticationFilter”, order);<br>order += STEP;<br>put(UsernamePasswordAuthenticationFilter.class, order);<br>order += STEP;<br>put(ConcurrentSessionFilter.class, order);<br>order += STEP;<br>filterToOrder.put(“org.springframework.security.openid.OpenIDAuthenticationFilter”, order);<br>order += STEP;<br>put(DefaultLoginPageGeneratingFilter.class, order);<br>order += STEP;<br>put(ConcurrentSessionFilter.class, order);<br>order += STEP;<br>put(DigestAuthenticationFilter.class, order);<br>order += STEP;<br>put(BasicAuthenticationFilter.class, order);<br>order += STEP;<br>put(RequestCacheAwareFilter.class, order);<br>order += STEP;<br>put(SecurityContextHolderAwareRequestFilter.class, order);<br>order += STEP;<br>put(JaasApiIntegrationFilter.class, order);<br>order += STEP;<br>put(RememberMeAuthenticationFilter.class, order);<br>order += STEP;<br>put(AnonymousAuthenticationFilter.class, order);<br>order += STEP;<br>put(SessionManagementFilter.class, order);<br>order += STEP;<br>put(ExceptionTranslationFilter.class, order);<br>order += STEP;<br>put(FilterSecurityInterceptor.class, order);<br>order += STEP;<br>put(SwitchUserFilter.class, order);<br>这些 Filter 虽然很多，但可以简单划分为几类，除个别 Filter 在每个 SecurityFilterChain 都需要，其他可以根据需要选用并添加：<br>可以认为是信道与状态管理，比如 ChannelProcessingFilter 用于处理 http 或者 https 之间的切换，而 SecurityContextPersistenceFilter 用于重建或者销毁必要的 SecurityContext 状态。<br>是常见 Web 安全防护类，比如 CsrfFilter。<br>是认证和授权，比如 BasicAuthenticationFilter、CasAuthen-ticationFilter 等。</p><p>最需要重点关注的是 FilterSecurityInterceptor，还记得我们前面说到的 secured object 吧。FilterSecurityInterceptor 就属于放在 Web 访问路径上的那道“关卡”，现在，它的真实位置和效能终于浮出水面了。</p><p>ExceptionTranslationFilter 属于另一个需要关注的核心类，它负责接待或者送客，如果访客来访，对方没有报上名来，那么，它会让访客去登记认证（去找 AuthenticationManager 做认证），如果对方报上名了，但认证失败，那么不好意思，请重新认证或者走人。当然，它拒绝访客的方式是抛出相应的 Exception，所以名字叫 ExceptionTranslationFilter。</p><p>最后，这个 Filter 序列因为间隔了 100 的步长，所以，我们可以在其中穿插自己的 Filter 实现类，为定制和扩展 SpringSecurity 的防护体系提供了机会。</p><h2 id="进一步定制spring-boot-starter-security"><a href="#进一步定制spring-boot-starter-security" class="headerlink" title="进一步定制spring-boot-starter-security"></a>进一步定制spring-boot-starter-security</h2><p>除了使用 SecurityProperties 暴露的配置项（以 security.* 开头）对 spring-boot-starter-security 进行简单的配置，我们还可以通过给出一个继承了 WebSecurityConfigurerAdapter 的 JavaConfig 配置类对 spring-boot-starter-security 的行为进行更深一级的定制。</p><p>使用 WebSecurityConfigurerAdapter 的好处在于，我们依然可以使用 spring-boot-starter-security 默认约定的一些行为，只需要对必要的行为进行调整，比如：<br>使用其他的 AuthenticationManager 实例。<br>对默认 HttpSecurity 定义的资源访问的规则进行重新定义。<br>对默认提供的 WebSecurity 行为进行调整。</p><p>为了能够让这些调整生效，我们定义的 WebSecurityConfigurerAdapter 实现类一般在顺序上需要先于 spring-boot-starter-security 默认提供的配置，故此，一般配合@Order（SecurityProperties.ACCESS_OVERRIDE_ORDER）进行标注，代码如下所示：<br>@Configuration<br>@Order(SecurityProperties.ACCESS_OVERRIDE_ORDER)<br>public class DemoSecurityConfiguration extends WebSecurity-ConfigurerAdapter {<br>    protected DemoSecurityConfiguration() {<br>        super(true); // 取消默认提供的安全相关Filters配置<br>    }<br>    @Override<br>    public void configure(WebSecurity web) throws Exception {<br>        // …<br>    }<br>    @Override<br>    protected void configure(HttpSecurity http) throws Exception {<br>        // …<br>    }<br>    // 通过Override其他方法实现对web安全的定制<br>}<br>WebSecurityConfigurerAdapter 其实是为我们预先设定了一个框架，并开放了有限的一些扩展点允许我们对 Web 安全相关的设定进行定制，某些场景下还是会感觉“掣肘”，或者，某些有“洁癖”的开发者，往往不想使用在某些场景下显得并非必要的默认设定。</p><p>这个时候，我们可以直接实现并注册一个标注了 @EnableWebSecurity 的 JavaConfig 配置类到 IoC 容器，从而实现一种“颠覆性”的定制，即跟 spring-boot-starter-security 默认提供的 Web 安全相关配置一刀两断，完全自建：<br>@Configuration<br>@EnableWebSecurity<br>public class OverhaulSecurityConfiguration {<br>    @Bean<br>    public AuthenticationManager authenticationManager() {<br>        // …<br>    }<br>    @Bean<br>    public AccessDecisionManager accessDecisionManager() {<br>        // …<br>    }<br>    @Bean<br>    public SecurityFilterChain mySecurityFilterChain() {<br>        // …<br>    }<br>    // 其他web安全相关组件和依赖配置}<br>}</p><h1 id="spring-boot-starter-actuator与应用监控"><a href="#spring-boot-starter-actuator与应用监控" class="headerlink" title="spring-boot-starter-actuator与应用监控"></a>spring-boot-starter-actuator与应用监控</h1><p>所有的应用开发完成之后，其最终目的都是为了上线运行，SpringBoot 应用也不例外，而在应用运行的漫长生命周期内，为了保障其可以持续稳定的服务，我们通常需要对其进行监控，从而可以了解应用的运行状态，并根据情况决定是否需要对其运行状态进行调整。</p><p>顺应需求，SpringBoot 框架提供了 spring-boot-starter-actuator 自动配置模块用于支持 SpringBoot 应用的监控。</p><p>如图 1 所示，形象的描述了 Actuator 是什么。<br>Sensor和Actuator示意图<br>图 1  Sensor 和 Actuator 示意图</p><p>为了能够感知应用的运行状态，我们通常会设置一些监控指标并采集分析，这些监控指标的采集需要在应用内部设置相应的监控点，这类监控点一般只是读取状态数据，我们通常称它们为 Sensor，即中文一般称为“传感器”的东西。</p><p>应用的运行状态数据通过 Sensors 采集上来之后，我们通常会有专门的系统对这些数据进行分析和判断，一旦某个指标数据超出了预定的阈值，这往往意味着应用的运行状态在这个指标上出现了“不健康”的现象，我们希望对这个指标进行调整，而为了能够执行调整，我们需要预先在应用内部设置对应的执行调整逻辑的控制器。</p><p>比如，直接关闭的开关，或者可以执行微调甚至像刹车一样直接快速拉低某个指标值的装置，这些控制器就称为 Actuator。虽然我们日常天天在说“监控，监控”，但实际上“监”跟“控”是两个概念，Sensor 更多服务于“监”的场景，而 Actuator 则服务于“控”的场景。</p><p>spring-boot-starter-actuator 自动配置模块默认提供了很多 endpoint，虽然自动配置模块名为 spring-boot-starter-actuator，但实际上这些 endpoint 可以按照“监”和“控”划分为两类：</p><ol><li><p>Sensor 类 endpoints</p></li><li><p>Actuator 类 endpoints<br>shutdown：用于关闭当前 SpringBoot 应用的 endpoint。<br>dump：用于执行线程的 dump 操作。</p></li></ol><p>默认情况下，除了 shutdown 这个 endpoint（因为比较危险，如果没有安全防护，谁都可以访问它，然后关闭应用），其他 endpoints 都是默认启用的。</p><p>生产环境下，如果没有启用安全防护（比如没有依赖 spring-boot-starter-security），那么，建议遵循 Deny By Default 原则，将所有的 endpoints 都关掉，然后根据具体情况单独启用某些 endpoint：<br>endpoints.enabled=falseendpoints.info.enabled=trueendpoints.health.enabled=true…</p><p>所有配置项以 endpoints. 为前缀，然后根据 endpoint 名称划分具体配置项。大部分 endpoints 都是开箱即用，但依然有些 endpoint 提供给我们进一步扩展的权利，比如健康状态检查相关的 endpoint（health endpoint）。<br>自定义应用的健康状态检查<br>应用的健康状态检查是很普遍的监控需求，SpringBoot 也预先通过 org.springframework.boot.actuate.autoconfigure.HealthIndicatorAutoConfiguration 为我们提供了一些常见服务的监控检查支持，比如：<br>DataSourceHealthIndicator<br>DiskSpaceHealthIndicator<br>RedisHealthIndicator<br>SolrHealthIndicator<br>MongoHealthIndicator</p><p>如果这些默认提供的健康检查支持依然无法满足我们的需要，SpringBoot 还允许我们提供更多的 HealthIndicator 实现，只要将这些 HealthIndicator 实现类注册到 IoC 容器，SpringBoot 会自动发现并使用它们。</p><p>假设需要检查依赖的 dubbo 服务是否处于健康状态，我们可以实现一个 DubboHealthIndicator：<br>import com.alibaba.dubbo.config.spring.ReferenceBean;<br>import com.alibaba.dubbo.rpc.service.EchoService;<br>import org.springframework.boot.actuate.health.AbstractHealthIndicator;<br>import org.springframework.boot.actuate.health.Health;<br>public class DubboHealthIndicator extends AbstractHealthIndicator {<br>    private final ReferenceBean bean;<br>    public DubboHealthIndicator(ReferenceBean bean) {<br>        this.bean = bean;<br>    }<br>    @Override<br>    protected void doHealthCheck(Health.Builder builder) throws Exception {<br>        builder.withDetail(“interface”, bean.getObjectType());<br>        final EchoService service = (EchoService) bean.getObject();<br>        service.$echo(“hi”);<br>        builder.up();<br>    }<br>}<br>要实现一个自定义的 HealthIndicator，一般我们不会直接实现（Implements）HealthIndicator 接口，而是继承 AbstractHealthIndicator：<br>public abstract class AbstractHealthIndicator implements HealthIndicator {<br>    @Override<br>    public final Health health() {<br>        Health.Builder builder = new Health.Builder();<br>        try {<br>            doHealthCheck(builder);<br>        } catch (Exception ex) {<br>            builder.down(ex);<br>        }<br>        return builder.build();<br>    }<br>    protected abstract void doHealthCheck(Health.Builder builder)<br>            throws Exception;<br>}<br>好处就是，我们只需实现 doHealthCheck，在其中实现我们面向的具体服务的健康检查逻辑就可以了，因此，在 DubboHealthIndicator 实现类中，我们通过 dubbo 框架提供的 EchoService 直接检查相应的 dubbo 服务健康状态即可，只要没有任何异常抛出，我们就认为检查的 dubbo 服务是状态健康的，所以，最后会通过 Health.Builder 的 up() 方法标记服务状态为正常运行。</p><p>为了完成对 dubbo 服务的健康检查，只实现一个 DubboHealthIndicator 是不够的，我们还需要将其注册到 IoC 容器中，但是一个一个单独注册太费劲了，而且还要自己提供针对某个 dubbo 服务的 ReferenceBean 依赖实例。</p><p>所以，为了一劳永逸，也为了其他人能够同样方便地使用针对 dubbo 服务的健康检查支持，我们可以在 DubboHealthIndicator 的基础上实现一个 spring-boot-starter-dubbo-health-indicator 自动配置模块，即：<br>@Configuration<br>@ConditionalOnClass(name = { “com.alibaba.dubbo.rpc.Exporter” })<br>public class DubboHealthIndicatorConfiguration {<br>    @Autowired<br>    HealthAggregator healthAggregator;<br>    @Autowired(required = false)<br>    Map&lt;String, ReferenceBean&gt; references;<br>    @Bean<br>    public HealthIndicator dubboHealthIndicator() {<br>        Map&lt;String, HealthIndicator&gt; indicators = new HashMap&lt;&gt;();<br>        for (String key : references.keySet()) {<br>            final ReferenceBean bean = references.get(key);<br>            indicators.put(key.startsWith(“&amp;”) ? key.replaceFirst(“&amp;”, “”)<br>                    : key, new DubboHealthIndicator(bean));<br>        }<br>        return new CompositeHealthIndicator(healthAggregator, indicators);<br>    }<br>}<br>然后我们在 spring-boot-starter-dubbo-health-indicator 的 META-INF/spring.factories 文件中添加如下配置：<br>org.springframework.boot.autoconfigure.EnableAutoConfiguration=\com.keevol…DubboHealthIndicatorConfiguration </p><p>现在，发布 spring-boot-starter-dubbo-health-indicator 并依赖它就可以自动享受到针对当前应用引用的所有 dubbo 服务进行健康检查的服务。</p><p>那么针对 Map&lt;String，ReferenceBean&gt;references 的依赖注入是从哪里来的？</p><p>其实 Spring 框架支持依赖注入 Key 的类型为 String 的 Map，遇到这种类型的 Map 声明（Map），Spring 框架将扫描容器中所有类型为 T 的 bean，然后以该 bean 的 name 作为 Map 的 Key，以 bean 实例作为对应的 Value，从而构建一个 Map 并注入到依赖处。<br>开放的 endpoints 才真正“有用”<br>不管是 spring-boot-starter-actuator 默认提供的 endpoint 实现，还是我们自己给出的 endpoint 实现，如果只是实现了放在 SpringBoot 应用的“身体内部”，那么它们不会发挥任何作用，只有将它们采集的信息暴露开放给外部监控者，或者允许外部监控者访问它们，这些 endpoints 才会真正发挥出它们的最大“功效”。</p><p>首先，spring-boot-starter-actuator 会通过 org.springframework.boot.actuate.autoconfigure.EndpointMBeanExportAutoConfiguration 将所有的 org.springframework.boot.actuate.endpoint.Endpoint 实例以 JMX MBean 的形式开放给外部监控者使用。</p><p>默认情况下，这些 Endpoint 对应的 JMX MBean 会放在 org.springframework.boot 命名空间下面，不过可以通过 endpoints.jmx.domain 配置项进行更改，比如 endpoints.jmx.domain=com.keevol.management。</p><p>EndpointMBeanExportAutoConfiguration 为我们提供了一条很好的应用监控实践之路，既然它会把所有的 org.springframework.boot.actuate.endpoint.Endpoint 实例都作为 JMX Mbean 开放出去，那么，我们就可以提供一批用于某些场景下的自定义 Endpoint 实现类，比如：<br>public class HelloEndpoint extends AbstractEndpoint<String> {<br>    public HelloEndpoint(String id) {<br>        super(id, false);<br>    }<br>    @Override<br>    public String invoke() {<br>        return “Hello, SpringBoot”;<br>    }<br>}</p><p>然后，将像 HelloEndpoint 这样的实现类注册到 SpringBoot 应用的 IoC 容器，就可以扩展 SpringBoot 的 endpoints 功能了。</p><p>Endpoint 其实更适合简单的 Sensor 场景（即用于读取或者提供信息），或者简单功能的 actuator 场景（不需要行为参数），如果需要对 SpringBoot 进行更细粒度的监控，可以考虑直接使用 Spring 框架的 JMX 支持。</p><p>除了可以使用 JMX 将 spring-boot-starter-actuator 提供的（或者我们自己提供的）endpoints 开放访问，如果当前 SpringBoot 应用恰好又是一个 Web 应用。那么，这些 endpoints 还会通过 HTTP 协议开放给外部访问，与一般的 Web 请求处理一样，使用的也是 Web 应用使用的 HTTP 服务器和地址端口。</p><p>因为每个 Endpoint 都有一个 id 作为唯一标识，所以，这些 endpoints 的默认访问路径其实就是它们的 id，比如 info 这个 endpoint 的 HTTP 访问路径就是 /info，而 beans 这个 endpoint 的 HTTP 访问路径则是 /beans，以此类推。</p><p>SpringBoot 允许我们通过 management. 为前缀的配置项对 endpoints 的 HTTP 开放行为进行调整：<br>使用 management.context-path=设置自定义的 endpoints 访问上下文路径，默认直接根路径，即 /info，/beans 等形式。<br>使用 management.address= 配置单独的 HTTP 服务监听地址，比如只允许本地访问。</p><p>management.address=127.0.0.1 使用 management.port=设置单独的监听端口，默认与 web 应用的对外服务端口相同。</p><p>我们可以通过 management.port=8888 将管理接口的 HTTP 对外监听端口设置为 8888，但如果 management.port=-1，则意味着我们将关闭管理接口的 HTTP 对外服务。</p><p>JMX 和 HTTP 是 endpoints 对外开放访问最常用的方式，鉴于 Java 的序列化漏洞以及 JMX 的远程访问防火墙问题，建议用 HTTP 并配合安全防护将 SpringBoot 应用的 endpoints 开放给外部监控者使用。<br>用还是不用，这是个问题<br>endpoints 属于 spring-boot-starter-actuator 提供的主要功能之一，除此之外，spring-boot-starter-actuator 还提供了更多针对应用监控的支持和实现方案。</p><ol><li>CrshAutoConfiguration 与 spring-boot-starter-remote-shell<br>spring-boot-starter-actuator 提供了基于 CRaSH（<a href="http://www.crashub.org/）的远程" target="_blank" rel="noopener">http://www.crashub.org/）的远程</a> Shell（Remote Shell）支持，从笔者角度来看，这是一把双刃剑，不建议在生产环境使用，因为提供给自己便利的同时，也为黑客朋友们提供了便利。如果实在要用，请加强安全认证和防护。</li></ol><p>不过，这里我们还是会为大家分析一下 spring-boot-starter-actuator 是如何提供针对 CRaSH 的支持的。</p><p>spring-boot-starter-actuator 提供了 org.springframework.boot.actuate.autoconfigure.CrshAutoConfiguration 自动配置类，该类会在 org.crsh.plugin.PluginLifeCycle 出现在 classpath 中的时候生效。</p><p>所以，只要将 CRaSH 作为依赖加入应用的 classpath 依赖就可以了，最简单直接的做法是让需要启用 CRaSH 的 SpringBoot 应用依赖 spring-boot-starter-remote-shell 自动配置模块，spring-boot-starter-remote-shell 的主要功效就是提供了针对 CRaSH 的各项依赖。<br>2. SpringBoot 的 Metrics 与 Dropwizard 的 Metrics<br>SpringBoot 提供了一套自己的针对系统指标的度量框架，这个框架的核心设计如图 2 所示。<br>SpringBoot框架的Metrics核心类设计示意图<br>图 2  SpringBoot 框架的 Metrics 核心类设计示意图</p><p>基本上，我们只需关注 org.springframework.boot.actuate.endpoint.PublicMetrics 即可，它可以理解为提供一组 Metric 的集合，我们既可以通过 PublicMetrics 来汇总和管理 Metric，也可以通过 MetricRepository 来存储和管理 Metric。</p><p>一旦使用了 spring-boot-starter-actuator，只要当前 SpringBoot 应用的 ApplicationContext 中存在任何 PublicMetrics 实例，EndpointAutoConfiguration 就会将这些 PublicMetrics 采集汇总到一起，然后通过 MetricsEndpoint 将它们开放出去。</p><p>spring-boot-starter-actuator 提供的 org.springframework.boot.actuate.autoconfigure.PublicMetricsAutoConfiguration 默认会把一个 SystemPublicMetrics 开放出来，用于提供系统各项指标的度量和状态采集，另外一个就是会把当前 SpringBoot 应用的 ApplicationContext 的 org.springframework.boot.actuate.metrics.repository.MetricRepository 实例中的所有 Metric 汇总并开放出去。</p><p>默认如果用户没有给出任何自定义的 MetricRepository，spring-boot-starter-actuator 会提供一个 InMemoryMetricRepository 实现，如果我们将 Dropwizard 的 Metrics 类库作为依赖加入 classpath，那么，Dropwizard Metrics 的 MetricRegistry 中所有的度量指标项也会通过 PublicMetrics 的形式开发暴露出来。</p><p>虽然 SpringBoot 提供的 metrics 框架也能帮助我们完成系统和应用指标的度量，但笔者更倾向于使用 Dropwizard 这种特定场景下比较完善的方案，从 metrics 的类型，到外围系统的集成，Dropwizard metrics 都更加成熟和完备。<br>3. Auditing 与 Trace<br>SpringBoot 的 Auditing 和 Trace 支持都遵循数据/事件+Repository 的设计（如图 3 所示）。<br>SpringBoot框架Audit和Trace功能支持核心类示意图<br>图 3  SpringBoot 框架 Audit 和 Trace 功能支持核心类示意图</p><p>我们可能只是通过打印日志时候的 Logger 名称来区分并记录 Audit 事件，然后通过日志采集通道汇总分析就可以了，而不用非要实现一个 LogFileBasedAuditEventRepository 或者 ElasticSearchBasedAuditEventRepository 之类的实现，否则看起来难免有些“学究”气。对于 Trace 来说也是同样道理，我们可能直接使用完备的 APM 方案而不是单一或者少量 Trace 事件的记录。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;SpringBoot 提供的依赖模块都约定以 spring-boot-starter- 作为命名的前缀，并且皆位于 org.springframework.boot 包或者命名空间下。&lt;/p&gt;
&lt;p&gt;所有的 spring-boot-starter 都有约定俗成的默认配置，但
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>java/SpringBoot/SpringApplication执行流程</title>
    <link href="http://yoursite.com/2021/07/06/java/SpringBoot/SpringApplication%E6%89%A7%E8%A1%8C%E6%B5%81%E7%A8%8B/"/>
    <id>http://yoursite.com/2021/07/06/java/SpringBoot/SpringApplication%E6%89%A7%E8%A1%8C%E6%B5%81%E7%A8%8B/</id>
    <published>2021-07-06T02:51:41.434Z</published>
    <updated>2021-07-06T05:48:45.474Z</updated>
    
    <content type="html"><![CDATA[<p><code>SpringApplication</code>的<code>run</code>方法的主要流程大体可以归纳如下：</p><ol><li>如果我们使用的是<code>SpringApplication</code>的静态<code>run</code>方法，那么，这个方法里面首先需要创建一个<code>SpringApplication</code>对象实例，然后调用这个创建好的<code>SpringApplication</code>的实例<code>run</code>方法。在<code>SpringApplication</code>实例初始化的时候，它会提前做几件事情：</li></ol><ul><li>根据<code>classpath</code>里面是否存在某个特征类（<code>org.springframework.web.context.ConfigurableWebApplicationContext</code>）来决定是否应该创建一个为 Web 应用使用的<code>ApplicationContext</code>类型，还是应该创建一个标准<code>Standalone</code>应用使用的<code>ApplicationContext</code>类型。</li><li>使用<code>SpringFactoriesLoader</code>在应用的<code>classpath</code>中查找并加载所有可用的<code>ApplicationContextInitializer</code>。</li><li>使用<code>SpringFactoriesLoader</code>在应用的<code>classpath</code>中查找并加载所有可用的<code>ApplicationListener</code>。</li><li>推断并设置<code>main</code>方法的定义类。</li></ul><ol start="2"><li><code>SpringApplication</code>实例初始化完成并且完成设置后，就开始执行 run 方法的逻辑了，方法执行伊始，首先遍历执行所有通过 SpringFactoriesLoader 可以查找到并加载的 SpringApplicationRunListener，调用它们的 started() 方法，告诉这些 SpringApplicationRunListener，“嘿，SpringBoot 应用要开始执行咯！”。</li><li>创建并配置当前 SpringBoot 应用将要使用的 Environment（包括配置要使用的 PropertySource 以及 Profile）。</li><li>遍历调用所有 SpringApplicationRunListener 的 environmentPrepared（）的方法，告诉它们：“当前 SpringBoot 应用使用的 Environment 准备好咯！”。</li><li>如果 SpringApplication的showBanner 属性被设置为 true，则打印 banner（SpringBoot 1.3.x版本，这里应该是基于 Banner.Mode 决定 banner 的打印行为）。</li><li>根据用户是否明确设置了applicationContextClass 类型以及初始化阶段的推断结果，决定该为当前 SpringBoot 应用创建什么类型的 ApplicationContext 并创建完成，然后根据条件决定是否添加 ShutdownHook，决定是否使用自定义的 BeanNameGenerator，决定是否使用自定义的 ResourceLoader，当然，最重要的，将之前准备好的 Environment 设置给创建好的 ApplicationContext 使用。</li><li><code>ApplicationContext</code>创建好之后，SpringApplication 会再次借助 Spring-FactoriesLoader，查找并加载 classpath 中所有可用的 ApplicationContext-Initializer，然后遍历调用这些 ApplicationContextInitializer 的 initialize（applicationContext）方法来对已经创建好的 ApplicationContext 进行进一步的处理。</li><li>遍历调用所有 SpringApplicationRunListener 的 contextPrepared（）方法，通知它们：“SpringBoot 应用使用的 ApplicationContext 准备好啦！”</li><li>最核心的一步，将之前通过 @EnableAutoConfiguration 获取的所有配置以及其他形式的 IoC 容器配置加载到已经准备完毕的 ApplicationContext。</li><li>遍历调用所有 SpringApplicationRunListener 的 contextLoaded() 方法，告知所有 SpringApplicationRunListener，ApplicationContext “装填完毕”！</li><li>调用 ApplicationContext 的 refresh() 方法，完成 IoC 容器可用的最后一道工序。</li><li>查找当前 ApplicationContext 中是否注册有 CommandLineRunner，如果有，则遍历执行它们。</li><li>正常情况下，遍历执行 SpringApplicationRunListener 的 finished() 方法，告知它们：“搞定！”。（如果整个过程出现异常，则依然调用所有 SpringApplicationRunListener 的 finished() 方法，只不过这种情况下会将异常信息一并传入处理）。</li></ol><p>至此，一个完整的 SpringBoot 应用启动完毕！</p><p>整个过程看起来冗长无比，但其实很多都是一些事件通知的扩展点，如果我们将这些逻辑暂时忽略，那么，其实整个 SpringBoot 应用启动的逻辑就可以压缩到极其精简的几步，如图 1 所示。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&lt;code&gt;SpringApplication&lt;/code&gt;的&lt;code&gt;run&lt;/code&gt;方法的主要流程大体可以归纳如下：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;如果我们使用的是&lt;code&gt;SpringApplication&lt;/code&gt;的静态&lt;code&gt;run&lt;/code&gt;方法，
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>java/SpringBoot/SpringBoot入门</title>
    <link href="http://yoursite.com/2021/07/06/java/SpringBoot/SpringBoot%E5%85%A5%E9%97%A8/"/>
    <id>http://yoursite.com/2021/07/06/java/SpringBoot/SpringBoot%E5%85%A5%E9%97%A8/</id>
    <published>2021-07-06T02:03:08.509Z</published>
    <updated>2021-07-06T02:51:14.639Z</updated>
    
    <content type="html"><![CDATA[<h1 id="SpringBootApplication"><a href="#SpringBootApplication" class="headerlink" title="@SpringBootApplication"></a>@SpringBootApplication</h1><p><code>@SpringBootApplication</code>是一个“三体”结构，实际上它是一个复合<code>Annotation</code>：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">@Target(ElementType.TYPE)</span><br><span class="line">@Retention(RetentionPolicy.RUNTIME)</span><br><span class="line">@Documented</span><br><span class="line">@Inherited</span><br><span class="line">@Configuration</span><br><span class="line">@EnableAutoConfiguration</span><br><span class="line">@ComponentScanpublic</span><br><span class="line">@interface</span><br><span class="line">SpringBootApplication&#123;...&#125;</span><br></pre></td></tr></table></figure><p>虽然它的定义使用了多个<code>Annotation</code>进行元信息标注，但实际上对于 SpringBoot 应用来说，重要的只有三个<code>Annotation</code>，而“三体”结构实际上指的就是这三个<code>Annotation</code>：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">@Configuration</span><br><span class="line">@EnableAutoConfiguration</span><br><span class="line">@ComponentScan</span><br></pre></td></tr></table></figure><p>所以，如果我们使用如下的 SpringBoot 启动类，整个 SpringBoot 应用依然可以与之前的启动类功能对等：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableAutoConfiguration</span></span><br><span class="line"><span class="meta">@ComponentScanpublic</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DemoApplication</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    SpringApplication.run(DemoApplication<span class="class">.<span class="keyword">class</span>, <span class="title">args</span>)</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但每次都写三个<code>Annotation</code>显然过于繁琐，所以写一个<code>@SpringBootApplication</code>这样的一站式复合<code>Annotation</code>显然更方便些。</p><h2 id="Configuration"><a href="#Configuration" class="headerlink" title="@Configuration"></a>@Configuration</h2><p>这里的<code>@Configuration</code>是 JavaConfig 形式的 Spring IoC 容器的配置类使用的那个<code>@Configuration</code>，既然 SpringBoot 应用骨子里就是一个 Spring 应用，那么，自然也需要加载某个 IoC 容器的配置，而 SpringBoot 社区推荐使用基于 JavaConfig 的配置形式，所以，很明显，这里的启动类标注了<code>@Configuration</code>之后，本身其实也是一个 IoC 容器的配置类！</p><p>如果我们将上面的 SpringBoot 启动类拆分为两个独立的 Java 类，整个形势就明朗了：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableAutoConfiguration</span></span><br><span class="line"><span class="meta">@ComponentScan</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DemoConfiguration</span> </span>&#123;</span><br><span class="line">  <span class="meta">@Bean</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> Controller <span class="title">controller</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> Controller();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DemoApplication</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    SpringApplication.run(DemoConfiguration<span class="class">.<span class="keyword">class</span>, <span class="title">args</span>)</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>所以，启动类<code>DemoApplication</code>其实就是一个标准的 Standalone 类型 Java 程序的 main 函数启动类，没有什么特殊的。而 @Configuration 标注的 DemoConfiguration 定义其实也是一个普通的 JavaConfig 形式的 IoC 容器配置类。</p><h2 id="EnableAutoConfiguration"><a href="#EnableAutoConfiguration" class="headerlink" title="@EnableAutoConfiguration"></a>@EnableAutoConfiguration</h2><p><code>@EnableAutoConfiguration</code>借助<code>@Import</code>，将所有符合自动配置条件的<code>bean</code>定义加载到 IoC 容器。</p><p><code>@EnableAutoConfiguration</code>作为一个复合<code>Annotation</code>，其自身定义关键信息如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Target</span>(ElementType.TYPE)</span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Inherited</span></span><br><span class="line"><span class="meta">@AutoConfigurationPackage</span></span><br><span class="line"><span class="meta">@Import</span>(EnableAutoConfigurationImportSelector<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class"><span class="title">public</span> @<span class="title">interface</span> <span class="title">EnableAutoConfiguration</span> </span>&#123;...&#125;</span><br></pre></td></tr></table></figure><p>其中，最关键的要属<code>@Import（EnableAutoConfigurationImportSelector.class）</code>，借助<code>EnableAutoConfigurationImportSelector</code>，<code>@EnableAutoConfiguration</code>可以帮助 SpringBoot 应用将所有符合条件的<code>@Configuration</code>配置都加载到当前 SpringBoot 创建并使用的 IoC 容器。</p><img src="/2021/07/06/java/SpringBoot/SpringBoot%E5%85%A5%E9%97%A8/1.png" class=""><p>借助于 Spring 框架原有的一个工具类：SpringFactoriesLoader 的支持，@EnableAutoConfiguration 可以“智能”地自动配置功效才得以大功告成！</p><h2 id="SpringFactoriesLoader"><a href="#SpringFactoriesLoader" class="headerlink" title="SpringFactoriesLoader"></a>SpringFactoriesLoader</h2><p>SpringFactoriesLoader 属于 Spring 框架私有的一种扩展方案（类似于 Java 的 SPI 方案 java.util.ServiceLoader），其主要功能就是从指定的配置文件 META-INF/spring.factories 加载配置，spring.factories 是一个典型的 java properties 文件，配置的格式为 Key=Value 形式，只不过 Key 和 Value 都是 Java 类型的完整类名（Fully qualified name），比如：</p><p>example.MyService=example.MyServiceImpl1,example.MyServiceImpl2 然后框架就可以根据某个类型作为 Key 来查找对应的类型名称列表了：<br>public abstract class SpringFactoriesLoader {<br>    // …<br>    public static <T> List<T> loadFactories(Class<T> factoryClass, ClassLoader classLoader) {<br>        …<br>    }<br>    public static List<String> loadFactoryNames(Class&lt;?&gt; factoryClass, ClassLoader classLoader) {<br>        …<br>    }<br>    // …<br>}<br>对于 @EnableAutoConfiguration 来说，SpringFactoriesLoader 的用途稍微不同一些，其本意是为了提供 SPI 扩展的场景，而在 @EnableAutoConfiguration 的场景中，它更多是提供了一种配置查找的功能支持，即根据 @EnableAutoConfiguration 的完整类名 org.springframework.boot.autoconfigure.EnableAutoConfiguration 作为查找的 Key，获取对应的一组 @Configuration 类：<br>org.springframework.boot.autoconfigure.EnableAutoConfiguration=<br>\org.springframework.boot.autoconfigure.admin.SpringApplicationAdmin- JmxAutoConfiguration,<br>\org.springframework.boot.autoconfigure.aop.AopAutoConfiguration,<br>\org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration,<br>\org.springframework.boot.autoconfigure.MessageSourceAutoConfiguration,<br>\org.springframework.boot.autoconfigure.PropertyPlaceholderAuto- Configuration,<br>\org.springframework.boot.autoconfigure.batch.BatchAutoConfiguration,<br>\org.springframework.boot.autoconfigure.cache.CacheAutoConfiguration,<br>\org.springframework.boot.autoconfigure.cassandra.CassandraAuto-Configuration,<br>\org.springframework.boot.autoconfigure.cloud.CloudAutoConfiguration,<br>\org.springframework.boot.autoconfigure.context.ConfigurationProperties-AutoConfiguration,<br>\org.springframework.boot.autoconfigure.dao.PersistenceException-TranslationAutoConfiguration,<br>\org.springframework.boot.autoconfigure.data.cassandra.Cassandra-DataAutoConfiguration,<br>\org.springframework.boot.autoconfigure.data.cassandra.Cassandra-RepositoriesAutoConfiguration,<br>...</p><p>以上是从 SpringBoot 的 autoconfigure 依赖包中的 META-INF/spring.factories 配置文件中摘录的一段内容，可以很好地说明问题。</p><p>所以，@EnableAutoConfiguration 自动配置的魔法其实就变成了：从 classpath 中搜寻所有 META-INF/spring.factories 配置文件，并将其中 org.spring-framework.boot.autoconfigure.EnableAutoConfiguration 对应的配置项通过反射（Java Reflection）实例化为对应的标注了 @Configuration 的 JavaConfig 形式的 IoC 容器配置类，然后汇总为一个并加载到 IoC 容器。</p><h2 id="可有可无的-ComponentScan"><a href="#可有可无的-ComponentScan" class="headerlink" title="可有可无的@ComponentScan"></a>可有可无的@ComponentScan</h2><p>为啥说 @ComponentScan 是可有可无的？</p><p>因为原则上来说，作为 Spring 框架里的“老一辈革命家”，@ComponentScan 的功能其实就是自动扫描并加载符合条件的组件或 bean 定义，最终将这些 bean 定义加载到容器中。加载 bean 定义到 Spring 的 IoC 容器，我们可以手工单个注册，不一定非要通过批量的自动扫描完成，所以说 @ComponentScan 是可有可无的。</p><p>对于 SpringBoot 应用来说，同样如此，比如我们本章的启动类：<br>@Configuration<br>@EnableAutoConfiguration<br>@ComponentScanpublic<br>class DemoApplication {<br>    public static void main(String[] args) {<br>        SpringApplication.run(DemoApplication.class, args);<br>    }<br>}<br>如果我们当前应用没有任何 bean 定义需要通过 @ComponentScan 加载到当前 SpringBoot 应用对应使用的 IoC 容器，那么，除去 @ComponentScan 的声明，当前 SpringBoot 应用依然可以照常运行，功能对等。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;SpringBootApplication&quot;&gt;&lt;a href=&quot;#SpringBootApplication&quot; class=&quot;headerlink&quot; title=&quot;@SpringBootApplication&quot;&gt;&lt;/a&gt;@SpringBootApplication
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>java/SpringMVC/SpringMVC获取参数</title>
    <link href="http://yoursite.com/2021/07/02/java/SpringMVC/SpringMVC%E8%8E%B7%E5%8F%96%E5%8F%82%E6%95%B0/"/>
    <id>http://yoursite.com/2021/07/02/java/SpringMVC/SpringMVC%E8%8E%B7%E5%8F%96%E5%8F%82%E6%95%B0/</id>
    <published>2021-07-02T08:05:33.408Z</published>
    <updated>2021-07-02T08:08:01.445Z</updated>
    
    <content type="html"><![CDATA[<p>Controller 接收请求参数的方式有很多种，有的适合 get 请求方式，有的适合 post 请求方式，有的两者都适合。</p><h1 id="通过实体-Bean-接收请求参数"><a href="#通过实体-Bean-接收请求参数" class="headerlink" title="通过实体 Bean 接收请求参数"></a>通过实体 Bean 接收请求参数</h1><p>通过一个实体 Bean 来接收请求参数，适用于 get 和 post 提交请求方式。需要注意的是，Bean 的属性名称必须与请求参数名称相同。</p><h2 id="1-创建首页面"><a href="#1-创建首页面" class="headerlink" title="1.创建首页面"></a>1.创建首页面</h2><p>在<code>springMVCDemo02</code>应用的<code>WebContent</code>目录下修改<code>index.jsp</code>页面：</p><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">&lt;%@ page language=<span class="string">"java"</span> contentType=<span class="string">"text/html; charset=UTF-8"</span></span><br><span class="line">    pageEncoding=<span class="string">"UTF-8"</span>%&gt;</span><br><span class="line">&lt;!DOCTYPE html PUBLIC <span class="string">"-//W3C//DTD HTML 4.01 Transitional//EN"</span> <span class="string">"http://www.w3.org/TR/html4/loose.dtd"</span>&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">&lt;meta http-equiv=<span class="string">"Content-Type"</span> content=<span class="string">"text/html; charset=UTF-8"</span>&gt;</span><br><span class="line">&lt;title&gt;Insert title here&lt;/title&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">    没注册的用户，请&lt;a href="$&#123;pageContext.request.contextPath &#125;/index/register"&gt; 注册&lt;/a&gt;！</span><br><span class="line">  &lt;br/&gt;</span><br><span class="line">    已注册的用户，去&lt;a href="$&#123;pageContext.request.contextPath &#125;/index/login"&gt; 登录&lt;/a&gt;！</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure><h2 id="2-完善配置文件"><a href="#2-完善配置文件" class="headerlink" title="2.完善配置文件"></a>2.完善配置文件</h2><p>完善配置文件<code>springmvc-servlet.xml</code>：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span> <span class="attr">xmlns:mvc</span>=<span class="string">"http://www.springframework.org/schema/mvc"</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:p</span>=<span class="string">"http://www.springframework.org/schema/p"</span> <span class="attr">xmlns:context</span>=<span class="string">"http://www.springframework.org/schema/context"</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xsi:schemaLocation</span>=<span class="string">"</span></span></span><br><span class="line"><span class="tag"><span class="string">        http://www.springframework.org/schema/beans</span></span></span><br><span class="line"><span class="tag"><span class="string">        http://www.springframework.org/schema/beans/spring-beans.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">        http://www.springframework.org/schema/context</span></span></span><br><span class="line"><span class="tag"><span class="string">        http://www.springframework.org/schema/context/spring-context.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">        http://www.springframework.org/schema/mvc</span></span></span><br><span class="line"><span class="tag"><span class="string">        http://www.springframework.org/schema/mvc/spring-mvc.xsd"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 使用扫描机制扫描控制器类，控制器类都在controller包及其子包下 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"controller"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:annotation-driven</span> /&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- annotation-driven用于简化开发的配置，注解DefaultAnnotationHandlerMapping和AnnotationMethodHandlerAdapter --&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 使用resources过滤掉不需要dispatcherservlet的资源（即静态资源，例如css、js、html、images）。</span></span><br><span class="line"><span class="comment">        在使用resources时必须使用annotation-driven，否则resources元素会阻止任意控制器被调用 --&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 允许css目录下的所有文件可见 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:resources</span> <span class="attr">location</span>=<span class="string">"/css/"</span> <span class="attr">mapping</span>=<span class="string">"/css/**"</span> /&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 允许html目录下的所有文件可见 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:resources</span> <span class="attr">location</span>=<span class="string">"/html/"</span> <span class="attr">mapping</span>=<span class="string">"/html/**"</span> /&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 允许css目录下的所有文件可见 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:resources</span> <span class="attr">location</span>=<span class="string">"/images/"</span> <span class="attr">mapping</span>=<span class="string">"/images/**"</span> /&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 完成视图的对应 --&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 对转向页面的路径解析。prefix：前缀， suffix：后缀 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span></span></span><br><span class="line"><span class="tag">        <span class="attr">class</span>=<span class="string">"org.springframework.web.servlet.view.InternalResourceViewResolver"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"prefix"</span> <span class="attr">value</span>=<span class="string">"/WEB-INF/jsp/"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"suffix"</span> <span class="attr">value</span>=<span class="string">".jsp"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure><p>3）创建 POJO 实体类<br>在 springMVCDemo02 应用的 src 目录下创建 pojo 包，并在该包中创建实体类 UserForm，代码如下：<br>package pojo;<br>public class UserForm {<br>    private String uname; // 与请求参数名称相同<br>    private String upass;<br>    private String reupass;<br>    // 省略getter和setter方法<br>}<br>4）创建控制器类<br>在 springMVCDemo02 应用的 controller 包中创建控制器类 IndexController 和 UserController。</p><p>IndexController 的代码如下：<br>package controller;<br>import org.springframework.stereotype.Controller;<br>import org.springframework.web.bind.annotation.RequestMapping;<br>@Controller<br>@RequestMapping(“/index”)<br>public class IndexController {<br>    @RequestMapping(“/login”)<br>    public String login() {<br>        return “login”; // 跳转到/WEB-INF/jsp下的login.jsp<br>    }<br>    @RequestMapping(“/register”)<br>    public String register() {<br>        return “register”;<br>    }<br>}<br>UserController 的代码如下：<br>package controller;<br>import javax.servlet.http.HttpSession;<br>import org.springframework.stereotype.Controller;<br>import org.springframework.ui.Model;<br>import org.springframework.web.bind.annotation.RequestMapping;<br>import pojo.UserForm;<br>import com.sun.org.apache.commons.logging.Log;<br>import com.sun.org.apache.commons.logging.LogFactory;<br>@Controller<br>@RequestMapping(“/user”)<br>public class UserController {<br>    // 得到一个用来记录日志的对象，这样在打印信息的时候能够标记打印的是哪个类的信息<br>    private static final Log logger = LogFactory.getLog(UserController.class);<br>    /**<br>     * 处理登录 使用UserForm对象(实体Bean) user接收注册页面提交的请求参数<br>     <em>/<br>    @RequestMapping(“/login”)<br>    public String login(UserForm user, HttpSession session, Model model) {<br>        if (“zhangsan”.equals(user.getUname())<br>                &amp;&amp; “123456”.equals(user.getUpass())) {<br>            session.setAttribute(“u”, user);<br>            logger.info(“成功”);<br>            return “main”; // 登录成功，跳转到 main.jsp<br>        } else {<br>            logger.info(“失败”);<br>            model.addAttribute(“messageError”, “用户名或密码错误”);<br>            return “login”;<br>        }<br>    }<br>    /*</em><br>     * 处理注册 使用UserForm对象(实体Bean) user接收注册页面提交的请求参数<br>     */<br>    @RequestMapping(“/register”)<br>    public String register(UserForm user, Model model) {<br>        if (“zhangsan”.equals(user.getUname())<br>                &amp;&amp; “123456”.equals(user.getUpass())) {<br>            logger.info(“成功”);<br>            return “login”; // 注册成功，跳转到 login.jsp<br>        } else {<br>            logger.info(“失败”);<br>            // 在register.jsp页面上可以使用EL表达式取出model的uname值<br>            model.addAttribute(“uname”, user.getUname());<br>            return “register”; // 返回 register.jsp<br>        }<br>    }<br>}<br>5）创建页面视图<br>在 springMVCDemo02 应用的 /WEB-INF/jsp 目录下创建 register.jsp 和 login.jsp。</p><p>register.jsp 的核心代码如下：<br>&lt;%@ page language=”java” contentType=”text/html; charset=UTF-8”<br>    pageEncoding=”UTF-8”%&gt;<br><!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN" "http://www.w3.org/TR/html4/loose.dtd"></p><html><head><meta http-equiv="Content-Type" content="text/html; charset=UTF-8"><title>Insert title here</title>  <meta name="generator" content="hexo-theme-yilia-plus"><link rel="alternate" href="/atom.xml" title="学海无涯" type="application/atom+xml"></head><body>    <form action="${pageContext.request.contextPath }/user/register" method="post" name="registForm">        <table border=1 bgcolor="lightblue" align="center">            <tr>                <td>姓名：</td>                <td>                    <input class="textSize" type="text" name="uname" value="${uname }" />                </td>            </tr>            <tr>                <td>密码：</td>                <td>                    <input class="textSize" type="password" maxlength="20" name="upass" />                </td>            </tr>            <tr>                <td>确认密码：</td>                <td>                    <input class="textSize" type="password" maxlength="20" name="reupass" />                </td>            </tr>            <tr>                <td colspan="2" align="center">                    <input type="button" value="注册" onclick="allIsNull() " />                </td>            </tr>        </tab1e>    </form></body></html>在 register.jsp 的代码中使用了 EL 语句“$1{uname}”取出“model.addAttribute("uname"，user.getUname())”中的值。对于 EL 和 JSTL 的相关知识，读者可参考《JSP教程》。<p>login.jsp 的核心代码如下：<br>&lt;%@ page language=”java” contentType=”text/html; charset=UTF-8”<br>    pageEncoding=”UTF-8”%&gt;<br><!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN" "http://www.w3.org/TR/html4/loose.dtd"></p><html><head><meta http-equiv="Content-Type" content="text/html; charset=UTF-8"><title>Insert title here</title></head><body>    <form action="${pageContext.request.contextPath }/user/login" method="post">        <table>            <tr>                <td colspan="2">                    <img src="${pageContext.request.contextPath }/images/login.gif">                </td>            </tr>            <tr>                <td>姓名：</td>                <td>                    <input type="text" name="uname" class="textSize">                </td>            </tr>            <tr>                <td>密码：</td>                <td>                    <input type="password" name="upass" class="textsize">                </td>            </tr>            <tr>                <td colspan="2">                    <input type="image" src="${pageContext.request.contextPath }/images/ok.gif" onclick="gogo()">                    <input type="image" src="${pageContext.request.contextPath }/images/cancel.gif" onclick="cancel()">                </td>            </tr>        </table>        ${messageError }    </form></body></html>6）测试应用运行 springMVCDemo02 应用的首页面，进行程序测试。通过处理方法的形参接收请求参数通过处理方法的形参接收请求参数也就是直接把表单参数写在控制器类相应方法的形参中，即形参名称与请求参数名称完全相同。该接收参数方式适用于 get 和 post 提交请求方式。用户可以将“通过实体 Bean 接收请求参数”部分中控制器类 UserController 中 register 方法的代码修改如下：@RequestMapping("/register")/*** 通过形参接收请求参数，形参名称与请求参数名称完全相同*/public String register(String uname,String upass,Model model) {    if ("zhangsan".equals(uname)            && "123456".equals(upass)) {        logger.info("成功");        return "login"; // 注册成功，跳转到 login.jsp    } else {        logger.info("失败");        // 在register.jsp页面上可以使用EL表达式取出model的uname值        model.addAttribute("uname", uname);        return "register"; // 返回 register.jsp    }}通过 HttpServletRequest 接收请求参数通过 HttpServletRequest 接收请求参数适用于 get 和 post 提交请求方式，可以将“通过实体 Bean 接收请求参数”部分中控制器类 UserController 中 register 方法的代码修改如下：@RequestMapping("/register")/*** 通过HttpServletRequest接收请求参数*/public String register(HttpServletRequest request,Model model) {    String uname = request.getParameter("uname");    String upass = request.getParameter("upass");    if ("zhangsan".equals(uname)            && "123456".equals(upass)) {        logger.info("成功");        return "login"; // 注册成功，跳转到 login.jsp    } else {        logger.info("失败");        // 在register.jsp页面上可以使用EL表达式取出model的uname值        model.addAttribute("uname", uname);        return "register"; // 返回 register.jsp    }}通过 @PathVariable 接收 URL 中的请求参数通过 @PathVariable 获取 URL 中的参数，控制器类示例代码如下：@Controller@RequestMapping("/user")public class UserController {    @RequestMapping("/user")    // 必须节method属性    /**     * 通过@PathVariable获取URL的参数     */    public String register(@PathVariable String uname,@PathVariable String upass,Model model) {        if ("zhangsan".equals(uname)                && "123456".equals(upass)) {            logger.info("成功");            return "login"; // 注册成功，跳转到 login.jsp        } else {            // 在register.jsp页面上可以使用EL表达式取出model的uname值            model.addAttribute("uname", uname);            return "register"; // 返回 register.jsp        }    }}在访问“http://localhost：8080/springMVCDemo02/user/register/zhangsan/123456”路径时，上述代码自动将 URL 中的模板变量 {uname} 和 {upass} 绑定到通过 @PathVariable 注解的同名参数上，即 uname=zhangsan、upass=123456。通过 @RequestParam 接收请求参数通过 @RequestParam 接收请求参数适用于 get 和 post 提交请求方式，可以将“通过实体 Bean 接收请求参数”部分控制器类 UserController 中 register 方法的代码修改如下：@RequestMapping("/register")/*** 通过@RequestParam接收请求参数*/public String register(@RequestParam String uname,    @RequestParam String upass, Model model) {    if ("zhangsan".equals(uname) && "123456".equals(upass)) {        logger.info("成功");        return "login"; // 注册成功，跳转到 login.jsp    } else {        // 在register.jsp页面上可以使用EL表达式取出model的uname值        model.addAttribute("uname", uname);        return "register"; // 返回 register.jsp    }}通过 @RequestParam 接收请求参数与“通过处理方法的形参接收请求参数”部分的区别如下：当请求参数与接收参数名不一致时，“通过处理方法的形参接收请求参数”不会报 404 错误，而“通过 @RequestParam 接收请求参数”会报 404 错误。通过 @ModelAttribute 接收请求参数当 @ModelAttribute 注解放在处理方法的形参上时，用于将多个请求参数封装到一个实体对象，从而简化数据绑定流程，而且自动暴露为模型数据，在视图页面展示时使用。<p>而“通过实体 Bean 接收请求参数”中只是将多个请求参数封装到一个实体对象，并不能暴露为模型数据（需要使用 model.addAttribute 语句才能暴露为模型数据，数据绑定与模型数据展示后面教程中会讲解）。</p><p>通过 @ModelAttribute 注解接收请求参数适用于 get 和 post 提交请求方式，可以将“通过实体 Bean 接收请求参数”中控制器类 UserController 中 register 方法的代码修改如下：<br>纯文本复制<br>@RequestMapping(“/register”)<br>public String register(@ModelAttribute(“user”) UserForm user) {<br>    if (“zhangsan”.equals(uname) &amp;&amp; “123456”.equals(upass)) {<br>        logger.info(“成功”);<br>        return “login”; // 注册成功，跳转到 login.jsp<br>    } else {<br>        logger.info(“失败”);<br>        // 使用@ModelAttribute(“user”)与model.addAttribute(“user”,user)的功能相同<br>        //register.jsp页面上可以使用EL表达式${user.uname}取出ModelAttribute的uname值<br>        return “register”; // 返回 register.jsp<br>    }<br>}</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;Controller 接收请求参数的方式有很多种，有的适合 get 请求方式，有的适合 post 请求方式，有的两者都适合。&lt;/p&gt;
&lt;h1 id=&quot;通过实体-Bean-接收请求参数&quot;&gt;&lt;a href=&quot;#通过实体-Bean-接收请求参数&quot; class=&quot;headerlin
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>java/SpringMVC/SpringMVC入门</title>
    <link href="http://yoursite.com/2021/07/01/java/SpringMVC/SpringMVC%E5%85%A5%E9%97%A8/"/>
    <id>http://yoursite.com/2021/07/01/java/SpringMVC/SpringMVC%E5%85%A5%E9%97%A8/</id>
    <published>2021-07-01T01:49:55.480Z</published>
    <updated>2021-07-02T08:06:10.969Z</updated>
    
    <content type="html"><![CDATA[<h1 id="MVC设计模式"><a href="#MVC设计模式" class="headerlink" title="MVC设计模式"></a>MVC设计模式</h1><p>MVC 是 Model、View 和 Controller 的缩写，分别代表 Web 应用程序中的 3 种职责。<br>模型：用于存储数据以及处理用户请求的业务逻辑。<br>视图：向控制器提交数据，显示模型中的数据。<br>控制器：根据视图提出的请求判断将请求和数据交给哪个模型处理，将处理后的有关结果交给哪个视图更新显示。</p><p>基于 Servlet 的 MVC 模式的具体实现如下。<br>模型：一个或多个 JavaBean 对象，用于存储数据（实体模型，由 JavaBean 类创建）和处理业务逻辑（业务模型，由一般的 Java 类创建）。<br>视图：一个或多个 JSP 页面，向控制器提交数据和为模型提供数据显示，JSP 页面主要使用 HTML 标记和 JavaBean 标记来显示数据。<br>控制器：一个或多个 Servlet 对象，根据视图提交的请求进行控制，即将请求转发给处理业务逻辑的 JavaBean，并将处理结果存放到实体模型 JavaBean 中，输出给视图显示。</p><p>基于 Servlet 的 MVC 模式的流程如图。</p><img src="/2021/07/01/java/SpringMVC/SpringMVC%E5%85%A5%E9%97%A8/1.gif" class="" title="JSP 中的 MVC 模式"><h1 id="Spring-MVC-工作流程"><a href="#Spring-MVC-工作流程" class="headerlink" title="Spring MVC 工作流程"></a>Spring MVC 工作流程</h1><p>Spring MVC 框架主要由 DispatcherServlet、处理器映射、控制器、视图解析器、视图组成，其工作原理如图。</p><img src="/2021/07/01/java/SpringMVC/SpringMVC%E5%85%A5%E9%97%A8/2.png" class="" title="Spring MVC 工作原理图"><p>从图中可总结出 Spring MVC 的工作流程如下：<br>客户端请求提交到 DispatcherServlet。<br>由 DispatcherServlet 控制器寻找一个或多个 HandlerMapping，找到处理请求的 Controller。<br>DispatcherServlet 将请求提交到 Controller。<br>Controller 调用业务逻辑处理后返回 ModelAndView。<br>DispatcherServlet 寻找一个或多个 ViewResolver 视图解析器，找到 ModelAndView 指定的视图。<br>视图负责将结果显示到客户端。</p><h1 id="Spring-MVC接口"><a href="#Spring-MVC接口" class="headerlink" title="Spring MVC接口"></a>Spring MVC接口</h1><p>在上图中包含 4 个 Spring MVC 接口，即 DispatcherServlet、HandlerMapping、Controller 和 ViewResolver。</p><p>Spring MVC 所有的请求都经过<code>DispatcherServlet</code>来统一分发，在<code>DispatcherServlet</code>将请求分发给<code>Controller</code>之前需要借助 Spring MVC 提供的<code>HandlerMapping</code>定位到具体的<code>Controller</code>。</p><p><code>HandlerMapping</code>接口负责完成客户请求到<code>Controller</code>映射。</p><p><code>Controller</code>接口将处理用户请求，这和 Java Servlet 扮演的角色是一致的。一旦<code>Controller</code>处理完用户请求，将返回<code>ModelAndView</code>对象给<code>DispatcherServlet</code>前端控制器，<code>ModelAndView</code>中包含了模型（<code>Model</code>）和视图（<code>View</code>）。</p><p>从宏观角度考虑，<code>DispatcherServlet</code>是整个 Web 应用的控制器；从微观考虑，<code>Controller</code>是单个 Http 请求处理过程中的控制器，而<code>ModelAndView</code>是 Http 请求过程中返回的模型（<code>Model</code>）和视图（<code>View</code>）。</p><p><code>ViewResolver</code>接口（视图解析器）在 Web 应用中负责查找<code>View</code>对象，从而将相应结果渲染给客户。</p><h1 id="视图解析器"><a href="#视图解析器" class="headerlink" title="视图解析器"></a>视图解析器</h1><p>Spring 视图解析器是 Spring MVC 中的重要组成部分，用户可以在配置文件中定义 Spring MVC 的一个视图解析器（<code>ViewResolver</code>）：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.springframework.web.servlet.view.InternalResourceViewResolver"</span> &gt;</span></span><br><span class="line">  <span class="comment">&lt;!--前缀--&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"prefix"</span> <span class="attr">value</span>=<span class="string">"/WEB-INF/jsp/"</span>/&gt;</span></span><br><span class="line">  <span class="comment">&lt;!--后缀--&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"suffix"</span> <span class="attr">value</span>=<span class="string">".jsp"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure><p>上述视图解析器配置了前缀和后缀两个属性。</p><p><code>InternalResourceViewResolver</code>是<code>URLBasedViewResolver</code>的子类，所以<code>URLBasedViewResolver</code>支持的特性它都支持。</p><p>在实际应用中<code>InternalResourceViewResolver</code>也是使用的最广泛的一个视图解析器。</p><p>单从字面意思来看，我们可以把<code>InternalResourceViewResolver</code>解释为内部资源视图解析器，这就是<code>InternalResourceViewResolver</code>的一个特性。</p><p><code>InternalResourceViewResolver</code>会把返回的视图名称都解析为<code>InternalResourceView</code>对象，<code>InternalResourceView</code>会把<code>Controller</code>处理器方法返回的模型属性都存放到对应的<code>request</code>属性中，然后通过<code>RequestDispatcher</code>在服务器端把请求<code>forword</code>重定向到目标 URL。</p><p>比如在<code>InternalResourceViewResolver</code>中定义了<code>prefix=/WEB-INF/</code>，<code>suffix=.jsp</code>，然后请求的<code>Controller</code>处理器方法返回的视图名称为<code>login</code>，那么这个时候<code>InternalResourceViewResolver</code>就会把<code>login</code>解析为一个<code>InternalResourceView</code>对象，先把返回的模型属性都存放到对应的<code>HttpServletRequest</code>属性中，然后利用<code>RequestDispatcher</code>在服务器端把请求<code>forword</code>到<code>/WEB-INF/test.jsp</code>。</p><p>这就是<code>InternalResourceViewResolver</code>一个非常重要的特性，我们都知道存放在<code>/WEB-INF/</code>下面的内容是不能直接通过<code>request</code>请求的方式请求到的，为了安全性考虑，我们通常会把<code>jsp</code>文件放在<code>WEB-INF</code>目录下，而<code>InternalResourceView</code>在服务器端跳转的方式可以很好的解决这个问题。</p><h1 id="Controller-注解类型"><a href="#Controller-注解类型" class="headerlink" title="Controller 注解类型"></a>Controller 注解类型</h1><p>在 Spring MVC 中使用<code>org.springframework.stereotype.Controller</code>注解类型声明某类的实例是一个控制器。例如，在<code>springMVCDemo02</code>应用的<code>src</code>目录下创建<code>controller</code>包，并在该包中创建<code>Controller</code>注解的控制器类<code>IndexController</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> controller;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Controller;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* “<span class="doctag">@Controller</span>”表示 IndexController 的实例是一个控制器</span></span><br><span class="line"><span class="comment">*</span></span><br><span class="line"><span class="comment">* <span class="doctag">@Controller</span>相当于<span class="doctag">@Controller</span>(<span class="doctag">@Controller</span>) 或<span class="doctag">@Controller</span>(value="<span class="doctag">@Controller</span>")</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">IndexController</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 处理请求的方法</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在 Spring MVC 中使用扫描机制找到应用中所有基于注解的控制器类，所以，为了让控制器类被 Spring MVC 框架扫描到，需要在配置文件中声明<code>spring-context</code>，并使用<code>&lt;context：component-scan/&gt;</code>元素指定控制器类的基本包（请确保所有控制器类都在基本包及其子包下）。</p><p>例如，在<code>springMVCDemo02</code>应用的<code>/WEB-INF/</code>目录下创建配置文件<code>springmvc-servlet.xml</code>：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span> </span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:mvc</span>=<span class="string">"http://www.springframework.org/schema/mvc"</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:p</span>=<span class="string">"http://www.springframework.org/schema/p"</span> </span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:context</span>=<span class="string">"http://www.springframework.org/schema/context"</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xsi:schemaLocation</span>=<span class="string">"</span></span></span><br><span class="line"><span class="tag"><span class="string">      http://www.springframework.org/schema/beans</span></span></span><br><span class="line"><span class="tag"><span class="string">      http://www.springframework.org/schema/beans/spring-beans.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">      http://www.springframework.org/schema/context</span></span></span><br><span class="line"><span class="tag"><span class="string">      http://www.springframework.org/schema/context/spring-context.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">      http://www.springframework.org/schema/mvc</span></span></span><br><span class="line"><span class="tag"><span class="string">      http://www.springframework.org/schema/mvc/spring-mvc.xsd"</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- 使用扫描机制扫描控制器类，控制器类都在controller包及其子包下 --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"controller"</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.springframework.web.servlet.view.InternalResourceViewResolver"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"prefix"</span> <span class="attr">value</span>=<span class="string">"/WEB-INF/jsp/"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"suffix"</span> <span class="attr">value</span>=<span class="string">".jsp"</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure><h1 id="RequestMapping-注解类型"><a href="#RequestMapping-注解类型" class="headerlink" title="RequestMapping 注解类型"></a>RequestMapping 注解类型</h1><p>在基于注解的控制器类中可以为每个请求编写对应的处理方法。那么如何将请求与处理方法一一对应呢？</p><p>需要使用<code>org.springframework.web.bind.annotation.RequestMapping</code>注解类型将请求与处理方法一一对应。</p><h2 id="1-方法级别注解"><a href="#1-方法级别注解" class="headerlink" title="1.方法级别注解"></a>1.方法级别注解</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> controller;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Controller;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RequestMapping;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* “<span class="doctag">@Controller</span>”表示 IndexController 的实例是一个控制器</span></span><br><span class="line"><span class="comment">*</span></span><br><span class="line"><span class="comment">* <span class="doctag">@Controller</span>相当于<span class="doctag">@Controller</span>(<span class="doctag">@Controller</span>) 或<span class="doctag">@Controller</span>(value="<span class="doctag">@Controller</span>")</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">IndexController</span> </span>&#123;</span><br><span class="line">  <span class="meta">@RequestMapping</span>(value = <span class="string">"/index/login"</span>)</span><br><span class="line">  <span class="function"><span class="keyword">public</span> String <span class="title">login</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">      * login代表逻辑视图名称，需要根据Spring MVC配置</span></span><br><span class="line"><span class="comment">      * 文件中internalResourceViewResolver的前缀和后缀找到对应的物理视图</span></span><br><span class="line"><span class="comment">      */</span></span><br><span class="line">    <span class="keyword">return</span> <span class="string">"login"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="meta">@RequestMapping</span>(value = <span class="string">"/index/register"</span>)</span><br><span class="line">  <span class="function"><span class="keyword">public</span> String <span class="title">register</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"register"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上述示例中有两个 RequestMapping 注解语句，它们都作用在处理方法上。注解的 value 属性将请求 URI 映射到方法，value 属性是 RequestMapping 注解的默认属性，如果只有一个 value 属性，则可以省略该属性。</p><p>用户可以使用如下 URL 访问 login 方法（请求处理方法），在访问<code>login</code>方法之前需要事先在<code>/WEB-INF/jsp/</code>目录下创建<code>login.jsp</code>。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">http:&#x2F;&#x2F;localhost:8080&#x2F;springMVCDemo02&#x2F;index&#x2F;login</span><br></pre></td></tr></table></figure><p>2）类级别注解</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> controller;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Controller;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RequestMapping;</span><br><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/index"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">IndexController</span> </span>&#123;</span><br><span class="line">    <span class="meta">@RequestMapping</span>(<span class="string">"/login"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">login</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"login"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@RequestMapping</span>(<span class="string">"/register"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">register</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"register"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在类级别注解的情况下，控制器类中的所有方法都将映射为类级别的请求。用户可以使用如下 URL 访问 login 方法。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">http:&#x2F;&#x2F;localhost:8080&#x2F;springMVCDemo02&#x2F;index&#x2F;login</span><br></pre></td></tr></table></figure><h1 id="编写请求处理方法"><a href="#编写请求处理方法" class="headerlink" title="编写请求处理方法"></a>编写请求处理方法</h1><p>在控制类中每个请求处理方法可以有多个不同类型的参数，以及一个多种类型的返回结果。<br>1）请求处理方法中常出现的参数类型<br>如果需要在请求处理方法中使用 Servlet API 类型，那么可以将这些类型作为请求处理方法的参数类型。Servlet API 参数类型的示例代码如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> controller;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpSession;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Controller;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RequestMapping;</span><br><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/index"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">IndexController</span> </span>&#123;</span><br><span class="line">  <span class="meta">@RequestMapping</span>(<span class="string">"/login"</span>)</span><br><span class="line">  <span class="function"><span class="keyword">public</span> String <span class="title">login</span><span class="params">(HttpSession session,HttpServletRequest request)</span> </span>&#123;</span><br><span class="line">    session.setAttribute(<span class="string">"skey"</span>, <span class="string">"session范围的值"</span>);</span><br><span class="line">    session.setAttribute(<span class="string">"rkey"</span>, <span class="string">"request范围的值"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"login"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>除了 Servlet API 参数类型以外，还有输入输出流、表单实体类、注解类型、与 Spring 框架相关的类型等，这些类型在后续章节中使用时再详细介绍。</p><p>其中特别重要的类型是 org.springframework.ui.Model 类型，该类型是一个包含 Map 的 Spring 框架类型。在每次调用请求处理方法时 Spring MVC 都将创建 org.springframework.ui.Model 对象。Model 参数类型的示例代码如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> controller;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpServletRequest;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpSession;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Controller;</span><br><span class="line"><span class="keyword">import</span> org.springframework.ui.Model;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RequestMapping;</span><br><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/index"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">IndexController</span> </span>&#123;</span><br><span class="line">  <span class="meta">@RequestMapping</span>(<span class="string">"/register"</span>)</span><br><span class="line">  <span class="function"><span class="keyword">public</span> String <span class="title">register</span><span class="params">(Model model)</span> </span>&#123;</span><br><span class="line">    <span class="comment">/*在视图中可以使用EL表达式$&#123;success&#125;取出model中的值*/</span></span><br><span class="line">    model.addAttribute(<span class="string">"success"</span>, <span class="string">"注册成功"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"register"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>2）请求处理方法常见的返回类型<br>最常见的返回类型就是代表逻辑视图名称的 String 类型。除了 String 类型以外，还有 ModelAndView、Model、View 以及其他任意的 Java 类型。</p><h1 id="获取参数的几种常见方式"><a href="#获取参数的几种常见方式" class="headerlink" title="获取参数的几种常见方式"></a>获取参数的几种常见方式</h1>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;MVC设计模式&quot;&gt;&lt;a href=&quot;#MVC设计模式&quot; class=&quot;headerlink&quot; title=&quot;MVC设计模式&quot;&gt;&lt;/a&gt;MVC设计模式&lt;/h1&gt;&lt;p&gt;MVC 是 Model、View 和 Controller 的缩写，分别代表 Web 应用程序中的 
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>java/Maven/Maven 入门</title>
    <link href="http://yoursite.com/2021/06/29/java/Maven/Maven%20%E5%85%A5%E9%97%A8/"/>
    <id>http://yoursite.com/2021/06/29/java/Maven/Maven%20%E5%85%A5%E9%97%A8/</id>
    <published>2021-06-29T03:31:55.682Z</published>
    <updated>2021-06-30T07:22:00.295Z</updated>
    
    <content type="html"><![CDATA[<p>Maven 是一款基于 Java 平台的项目管理和整合工具，它将项目的开发和管理过程抽象成一个项目对象模型（POM）。开发人员只需要做一些简单的配置，Maven 就可以自动完成项目的编译、测试、打包、发布以及部署等工作。</p><p>Maven 是使用 Java 语言编写的，因此它和 Java 一样具有跨平台性，这意味着无论是在 Windows ，还是在 Linux 或者 Mac OS 上，都可以使用相同的命令进行操作。</p><p>Maven 使用标准的目录结构和默认构建生命周期，因此开发者几乎不用花费多少时间就能够自动完成项目的基础构建工作。</p><p>Maven 能够帮助开发者完成以下任务：<br>构建项目<br>生成文档<br>创建报告<br>维护依赖<br>软件配置管理<br>发布<br>部署</p><p>总而言之，Maven 简化并标准化了项目构建过程。它将项目的编译，生成文档，创建报告，发布，部署等任务无缝衔接，构建成一套完整的生命周期。<br>约定优于配置<br>约定优于配置（Convention Over Configuration）是 Maven 最核心的涉及理念之一 ，Maven对项目的目录结构、测试用例命名方式等内容都做了规定，凡是使用 Maven 管理的项目都必须遵守这些规则。</p><p>Maven 项目构建过程中，会自动创建默认项目结构，开发人员仅需要在相应目录结构下放置相应的文件即可。</p><p>例如，下表显示了项目源代码文件，资源文件和其他配置在 Maven 项目中的默认位置。 </p><p>Maven 的特点<br>Maven 具有以下特点：<br>设置简单。<br>所有项目的用法一致。<br>可以管理和自动进行更新依赖。<br>庞大且不断增长的资源库。<br>可扩展，使用 Java 或脚本语言可以轻松的编写插件。<br>几乎无需额外配置，即可立即访问新功能。<br>基于模型的构建：Maven 能够将任意数量的项目构建为预定义的输出类型，例如 JAR，WAR。<br>项目信息采取集中式的元数据管理：使用与构建过程相同的元数据，Maven 能够生成一个网站（site）和一个包含完整文档的 PDF。<br>发布管理和发行发布：Maven 可以与源代码控制系统（例如 Git、SVN）集成并管理项目的发布。<br>向后兼容性：您可以轻松地将项目从旧版本的 Maven 移植到更高版本的 Maven 中。<br>并行构建：它能够分析项目依赖关系，并行构建工作，使用此功能，可以将性能提高 20%-50％。<br>更好的错误和完整性报告：Maven 使用了较为完善的错误报告机制，它提供了指向 Maven Wiki 页面的链接，您将在其中获得有关错误的完整描述。</p><h1 id="在-windows-上安装-Maven"><a href="#在-windows-上安装-Maven" class="headerlink" title="在 windows 上安装 Maven"></a>在 windows 上安装 Maven</h1><p>在安装 Maven 之前要确保安装了 JDK。</p><p>访问 Maven 下载页面：<code>https://maven.apache.org/download.cgi</code>，下载<code>apache-maven-3.6.3-bin.zip</code>。</p><img src="/2021/06/29/java/Maven/Maven%20%E5%85%A5%E9%97%A8/img1.png" class=""><p>将压缩包解压到指定的目录中，比如：<code>C:\path\apache-maven-3.6.3</code>。</p><p>接着需要设置 Maven 的环境变量。</p><img src="/2021/06/29/java/Maven/Maven%20%E5%85%A5%E9%97%A8/img2.png" class=""><img src="/2021/06/29/java/Maven/Maven%20%E5%85%A5%E9%97%A8/img3.png" class=""><p>在 cmd 中输入<code>mvn -v</code>，检查 Maven 是否安装成功。</p><h1 id="POM"><a href="#POM" class="headerlink" title="POM"></a>POM</h1><p>POM（Project Object Model，项目对象模型）是 Maven 的基本组件，它是以 xml 文件的形式存放在项目的根目录下，名称为 pom.xml。</p><p>POM 中定义了项目的基本信息，用于描述项目如何构建、声明项目依赖等等。</p><p>当 Maven 执行一个任务时，它会先查找当前项目的 POM 文件，读取所需的配置信息，然后执行任务。在 POM 中可以设置如下配置：<br>项目依赖<br>插件<br>目标<br>构建时的配置文件<br>版本<br>开发者<br>邮件列表</p><p>在创建 POM 之前，首先要确定工程组（groupId），及其名称（artifactId）和版本，在仓库中这些属性是项目的唯一标识。<br>POM 示例<br><project xmlns="http://maven.apache.org/POM/4.0.0"         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd"><br>    <modelVersion>4.0.0</modelVersion><br>    <groupId>net.biancheng.www</groupId><br>    <artifactId>maven</artifactId><br>    <version>0.0.1-SNAPSHOT</version><br></project></p><p>所有的 Maven 项目都有一个 POM 文件，所有的 POM 文件都必须有 project 元素和 3 个必填字段：groupId、artifactId 以及 version。<br>Super POM<br>无论 POM 文件中是否显示的声明，所有的 POM 均继承自一个父 POM，这个父 POM 被称为 Super POM，它包含了一些可以被继承的默认设置。</p><p>Maven 使用 effective pom (Super POM 的配置加上项目的配置）来执行相关任务，它替开发人员在 pom.xml 中做了一些最基本的配置。当然，开发人员依然可以根据需要重写其中的配置信息。</p><p>执行以下命令 ，就可以查看 Super POM 的默认配置。<br>mvn help:effective-pom<br>示例</p><ol><li><p>在 D 盘创建一个名为 maven 的目录，将 POM 示例中的 pom.xml 文件移动至该文件夹下。</p></li><li><p>打开命令行窗口，跳转到 pom.xml 所在的目录下，执行以下 mvn 命令。<br>mvn help:effective-pom</p></li><li><p>Maven 将会开始执行命令并显示 effective-pom 的内容，执行结果如下。</p></li></ol><p>你可以看到 effective-pom 中包含了 Maven 在执行任务时需要用到的默认目录结构、输出目录、插件、仓库和报表目录等内容。<br>实际开发过程中，Maven 的 pom.xml 文件不需要手工编写，Maven 提供了大量的原型（Archetype）插件来创建项目，包括项目结构和 pom.xml。</p><h1 id="创建Maven项目"><a href="#创建Maven项目" class="headerlink" title="创建Maven项目"></a>创建Maven项目</h1><p>Maven 提供了大量不同类型的 Archetype 模板，通过它们可以帮助用户快速的创建 Java 项目，其中最简单的模板就是 maven-archetype-quickstart，它只需要用户提供项目最基本的信息，就能生成项目的基本结构及 POM 文件。<br>创建 Maven 项目<br>下面我们将通过 maven-archetype-quickstart 原型，在 D:\maven 目录中创建一个基于 Maven 的 Java 项目。</p><p>打开命令行窗口，跳转到 D:\maven 目录，执行以下 mvn 命令。<br>​mvn archetype:generate -DgroupId=net.biancheng.www -DartifactId=helloMaven -DarchetypeArtifactId=maven-archetype-quickstart -DinteractiveMode=false</p><p>参数说明：<br>-DgroupId: 项目组 ID，通常为组织名或公司网址的反写。<br>-DartifactId: 项目名。<br>-DarchetypeArtifactId: 指定 ArchetypeId，maven-archetype-quickstart 用于快速创建一个简单的 Maven 项目。<br>-DinteractiveMode: 是否使用交互模式。</p><p>Maven 开始进行处理，并创建一套完整的 Maven 项目目录结构。</p><p>目录结构<br>进入 D:\maven 目录， 我们看到 Maven 已经创建了一个名为 helloMaven 的 Java 项目（在 artifactId 中指定的），该项目使用一套标准的目录结构，如下图所示。</p><p>目录及文件说明：<br>helloMaven：项目名，包含 src 文件夹和 pom.xml。<br>src/main/java：用于存放项目的 Java 文件。<br>src/main/resources：用于存放项目资源文件。<br>src/test/java：用于存放所有测试 Java 文件，如 JUnit 测试类。<br>src/test/resources ：用于存放测试资源文件。<br>target：项目输出位置，用于存放编译后的文件。<br>pom.xml：Maven 项目核心配置文件。</p><p>Maven 创建项目时，还自动生成了两个Java 文件： App.java 和 AppTest.java。其中 App.java 位于 src/main/java 下 ，代码如下。<br>package net.biancheng.www;<br>/**</p><ul><li>Hello world!</li><li>/<br>public class App {<br>   public static void main(String[] args) {<pre><code>System.out.println(&quot;Hello World!&quot;);</code></pre>   }<br>}</li></ul><p>AppTest.java 位于 src/test/java 下，代码如下。<br>package net.biancheng.www;<br>import junit.framework.Test;<br>import junit.framework.TestCase;<br>import junit.framework.TestSuite;<br>/**</p><ul><li>Unit test for simple App.</li><li>/<br>public class AppTest<pre><code>extends TestCase {</code></pre>   /**<pre><code>* Create the test case** @param testName name of the test case*/</code></pre>   public AppTest(String testName) {<pre><code>super(testName);</code></pre>   }<br>   /**<pre><code>* @return the suite of tests being tested*/</code></pre>   public static Test suite() {<pre><code>return new TestSuite(AppTest.class);</code></pre>   }<br>   /**<pre><code>* Rigourous Test :-)*/</code></pre>   public void testApp() {<pre><code>assertTrue(true);</code></pre>   }<br>}<br>注：本节侧重点在于使用 maven-archetype-quickstart 原型快速创建一个简单的 Maven 项目，为后续的学习做准备。对于 archetype 了解即可，在后面的 Maven Archetype 模板 中会详细讲解。<h1 id="Maven项目的构建与测试"><a href="#Maven项目的构建与测试" class="headerlink" title="Maven项目的构建与测试"></a>Maven项目的构建与测试</h1><h1 id="构建项目"><a href="#构建项目" class="headerlink" title="构建项目"></a>构建项目</h1>查看 helloMaven 项目的 pom.xml 文件，配置如下。<br>&lt;project xmlns=”<a href="http://maven.apache.org/POM/4.0.0&quot;" target="_blank" rel="noopener">http://maven.apache.org/POM/4.0.0&quot;</a> xmlns:xsi=”<a href="http://www.w3.org/2001/XMLSchema-instance&quot;" target="_blank" rel="noopener">http://www.w3.org/2001/XMLSchema-instance&quot;</a><pre><code>xsi:schemaLocation=&quot;http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd&quot;&gt;</code></pre>   <modelVersion>4.0.0</modelVersion><br>   <groupId>net.biancheng.www</groupId><br>   <artifactId>helloMaven</artifactId><br>   <packaging>jar</packaging><br>   <version>1.0-SNAPSHOT</version><br>   <name>helloMaven</name><br>   <url><a href="http://maven.apache.org" target="_blank" rel="noopener">http://maven.apache.org</a></url>   <dependencies>       <dependency>           <groupId>junit</groupId>           <artifactId>junit</artifactId>           <version>3.8.1</version>           <scope>test</scope>       </dependency>   </dependencies></project></li></ul><p>从以上配置可知，Maven 已经添加了 Junit 作为该项目的测试框架，且 Maven 也在项目中自动生成了一个源码文件 App.java 和一个测试文件 AppTest.java 。</p><p>打开命令行窗口，跳转到 D:\maven\helloMaven 目录，执行以下 mvn 命令，对该项目进行构建。<br>mvn clean package</p><p>项目构建完成后，在该项目根目录中生成了一个名为 target 的目录，该目录包含以下内容。</p><p>说明：<br>Maven 命令中包含了两个命令：clean 和 package，其中 clean 负责清理 target 目录，package 负责将项目构建并打包输出为 jar 文件。<br>classes：源代码编译后存放在该目录中。<br>test-classes：测试源代码编译后并存放在该目录中。<br>surefire-reports：Maven 运行测试用例生成的测试报告存放在该目录中。<br>helloMaven-1.0-SNAPSHOT.jar：Maven 对项目进行打包生成的 jar 文件。</p><p>打开命令控制台，跳转到 D:\maven\helloMaven\target\classes 目录，执行 Java 命令<br>java net.biancheng.<a href="http://www.App" target="_blank" rel="noopener">www.App</a></p><p>执行结果如下。<br>Hello World!<br>测试项目<br>下面我们介绍如何在 Maven 项目中添加其他的 Java 文件，并进行测试。</p><p>打开 D:\maven\helloMaven\src\main\java\net\biancheng\www 文件夹，在其中创建一个名为  Util.java 的文件。<br>package net.biancheng.www;<br>/**</p><ul><li>Maven 项目中添加的自定义java类</li><li></li><li>@author 编程帮 <a href="http://www.biancheng.net" target="_blank" rel="noopener">www.biancheng.net</a></li><li>/<br>public class Util {<br>   public static void printMessage(String message) {<pre><code>System.out.println(message);</code></pre>   }<br>}</li></ul><p>更新 App 类，调用 Util 类中的 printMessage() 方法，代码如下。<br>package net.biancheng.www;<br>/**</p><ul><li>@author 编程帮 <a href="http://www.biancheng.net" target="_blank" rel="noopener">www.biancheng.net</a></li><li>/<br>public class App {<br>   public static void main(String[] args) {<pre><code>Util.printMessage(&quot;编程帮欢迎您，网址：www.biancheng.net&quot;);</code></pre>   }<br>}</li></ul><p>打开命令行窗口，跳转到 D:\maven\helloMaven 目录，并执行如下 Maven 命令，对项目进行编译。<br>mvn clean compile                                                                                  </p><p>命令执行结果如下。</p><p>编译成功后，跳转到 D:\maven\helloMaven\target\classes 目录下，执行以下 Java 命令。<br>java  net.biancheng.<a href="http://www.App" target="_blank" rel="noopener">www.App</a>                                                                                    </p><p>执行结果如下。<br>编程帮欢迎您，网址：<a href="http://www.biancheng.net" target="_blank" rel="noopener">www.biancheng.net</a></p><h1 id="Maven坐标"><a href="#Maven坐标" class="headerlink" title="Maven坐标"></a>Maven坐标</h1><p>在 Maven 中，任何一个依赖、插件或者项目构建的输出，都可以称为构件。</p><p>Maven 坐标规定：世界上任何一个构件都可以使用 Maven 坐标并作为其唯一标识，Maven 坐标包括<code>groupId、artifactId、version、packaging</code>等元素，只要用户提供了正确的坐标元素，Maven 就能找到对应的构件。 </p><p>任何一个构件都必须明确定义自己的坐标，这是 Maven 的强制要求，任何构件都不能例外。我们在开发 Maven 项目时，也需要为其定义合适的坐标，只有定义了坐标，其他项目才能引用该项目生成的构件。</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">project</span>&gt;</span> </span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>net.xxx.www<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>helloMaven<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">packaging</span>&gt;</span>jar<span class="tag">&lt;/<span class="name">packaging</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure><p>Maven 坐标主要由以下元素组成：</p><ul><li><code>groupId</code>：项目组 ID，定义当前 Maven 项目隶属的组织或公司，通常是唯一的。它的取值一般是项目所属公司或组织的网址或 URL 的反写。</li><li><code>artifactId</code>：项目 ID，通常是项目的名称。</li><li><code>version</code>：版本。</li><li><code>packaging</code>：项目的打包方式，默认值为<code>jar</code>。</li></ul><p><code>groupId、artifactId</code>和<code>version</code>是必须定义的，packaging 是可选的。</p><h1 id="Maven依赖"><a href="#Maven依赖" class="headerlink" title="Maven依赖"></a>Maven依赖</h1><p>如果一个 Maven 构建所产生的构件（例如 Jar 文件）被其他项目引用，那么该构件就是其他项目的依赖。</p><h2 id="依赖声明"><a href="#依赖声明" class="headerlink" title="依赖声明"></a>依赖声明</h2><p>Maven 坐标是依赖的前提，所有 Maven 项目必须明确定义自己的坐标，只有这样，它们才可能成为其他项目的依赖。</p><p>当 Maven 项目需要声明某一个依赖时，通常只需要在其 POM 中配置该依赖的坐标信息，Maven 会根据坐标自动将依赖下载到项目中。</p><p>例如，某个项目中使用<code>servlet-api</code>作为其依赖，其配置如下。</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span> <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd"</span>&gt;</span></span><br><span class="line">  ...</span><br><span class="line">  <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>javax.servlet<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>servlet-api<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.5<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">scope</span>&gt;</span>provided<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure><p><code>dependencies</code>元素可以包含一个或者多个<code>dependency</code>子元素，用以声明一个或者多个项目依赖，每个依赖都可以包含以下元素：</p><ul><li><code>groupId、artifactId</code>和<code>version</code>：依赖的基本坐标，对于任何一个依赖来说，基本坐标是最重要的，Maven 根据坐标才能找到需要的依赖。</li><li><code>type</code>：依赖的类型，对应于项目坐标定义的<code>packaging</code>。大部分情况下，该元素不必声明，其默认值是<code>jar</code>。</li><li><code>scope</code>：依赖的范围。</li><li><code>optional</code>：标记依赖是否可选。</li><li><code>exclusions</code>：用来排除传递性依赖。</li></ul><h1 id="Maven仓库"><a href="#Maven仓库" class="headerlink" title="Maven仓库"></a>Maven仓库</h1><p>在 Maven 中，任何一个依赖、插件或者项目构建的输出，都可以称为构件。</p><p>Maven 在某个统一的位置存储所有项目的构件，这个统一的位置，我们就称之为仓库。换言之，仓库就是存放依赖和插件的地方。</p><p>任何的构件都有唯一的坐标，该坐标定义了构件在仓库中的唯一存储路径。当 Maven 项目需要某些构件时，只要其 POM 文件中声明了这些构件的坐标，Maven 就会根据这些坐标找自动到仓库中找到并使用它们。</p><p>项目构建完成生成的构件，也可以安装或者部署到仓库中，供其他项目使用。</p><h2 id="仓库的分类"><a href="#仓库的分类" class="headerlink" title="仓库的分类"></a>仓库的分类</h2><p>Maven 仓库可以分为 2 个大类：本地仓库、远程仓库。</p><p>当 Maven 根据坐标寻找构件时，它会首先查看本地仓库，若本地仓库存在此构件，则直接使用；若本地仓库不存在此构件，Maven 就会去远程仓库查找，若发现所需的构件后，则下载到本地仓库使用。如果本地仓库和远程仓库都没有所需的构件，则 Maven 就会报错。</p><p>远程仓库还可以分为 3 个小类：中央仓库、私服、其他公共仓库。</p><p>中央仓库是由 Maven 社区提供的一种特殊的远程仓库，它包含了绝大多数流行的开源构件。在默认情况下，当本地仓库没有 Maven 所需的构件时，会首先尝试从中央仓库下载。</p><p>私服是一种特殊的远程仓库，它通常设立在局域网内，用来代理所有外部的远程仓库。它的好处是可以节省带宽，比外部的远程仓库更加稳定。</p><p>除了中央仓库和私服外，还有很多其他公共仓库，例如 JBoss Maven 库，Java.net Maven 库等等。</p><p>Maven 仓库的分类如下图。</p><img src="/2021/06/29/java/Maven/Maven%20%E5%85%A5%E9%97%A8/4.png" class="" title="Maven 仓库分类"><h2 id="本地仓库"><a href="#本地仓库" class="headerlink" title="本地仓库"></a>本地仓库</h2><p>Maven 本地仓库实际上就是本地计算机上的一个目录（文件夹），它会在第一次执行 Maven 命令时被创建。</p><p>Maven 本地仓库可以储存本地所有项目所需的构件。当 Maven 项目第一次进行构建时，会自动从远程仓库搜索依赖项，并将其下载到本地仓库中。当项目再进行构建时，会直接从本地仓库搜索依赖项并引用，而不会再次向远程仓库获取。</p><p>Maven 本地仓库默认地址为<code>C:\%USER_HOME%\.m2\repository</code>，但出于某些原因（例如 C 盘空间不够），我们通常会重新自定义本地仓库的位置。这时需要修改<code>%MAVEN_HOME%\conf</code>目录下的<code>settings.xml</code>文件，通过<code>localRepository</code>元素定义另一个本地仓库地址，例如：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">settings</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/SETTINGS/1.0.0"</span></span></span><br><span class="line"><span class="tag">          <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">          <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/SETTINGS/1.0.0</span></span></span><br><span class="line"><span class="tag"><span class="string">   http://maven.apache.org/xsd/settings-1.0.0.xsd"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">localRepository</span>&gt;</span>D:/myRepository/repository<span class="tag">&lt;/<span class="name">localRepository</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">settings</span>&gt;</span></span><br></pre></td></tr></table></figure><p>构件只有储存在本地仓库中，才能被其他的 Maven 项目使用。构件想要进入本地仓库，除了从远程仓库下载到本地仓库外，还可以使用命令<code>mvn install</code>将本地项目的输出构件安装到本地仓库中。</p><h2 id="中央仓库"><a href="#中央仓库" class="headerlink" title="中央仓库"></a>中央仓库</h2><p>中央仓库是由 Maven 社区提供的一种特殊的远程仓库，它包含了绝大多数流行的开源构件。在默认情况下，当本地仓库没有 Maven 所需的构件时，会首先尝试从中央仓库下载。</p><p>中央仓库具有如下特点：</p><ul><li>这个仓库由 Maven 社区管理</li><li>不需要配置</li><li>需要通过网络才能访问</li></ul><p>我们可以通过 Maven 社区提供的 URL：<code>http://search.maven.org/#browse</code>，浏览其中的构件。中央仓库包含了绝大多数流行的开源 Java 构件及其源码、作者信息和许可证信息等。一般来说，Maven 项目所依赖的构件都可以从中央仓库下载到。</p><p>虽然中央仓库属于远程仓库的范畴，但由于它的特殊性，一般会把它与其他远程仓库区分开。我们常说的远程仓库，一般不包括中央仓库。</p><h2 id="远程仓库"><a href="#远程仓库" class="headerlink" title="远程仓库"></a>远程仓库</h2><p>如果 Maven 在本地仓库和中央仓库中都找不到依赖的库文件，它就会停止构建过程并输出错误信息到控制台。为避免这种情况的发生，Maven 还提供了远程仓库的概念，它是一种由开发人员自己定制的仓库，其中包含了供其他项目使用的代码库或者构件。</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>net.biancheng.www<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>maven<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>0.0.1-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.bangcheng.common-lib<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>common-lib<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">repositories</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">repository</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">id</span>&gt;</span>bianchengbang.lib1<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">url</span>&gt;</span>http://download.bianchengbang.org/maven2/lib1<span class="tag">&lt;/<span class="name">url</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">repository</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">repository</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">id</span>&gt;</span>bianchengbang.lib2<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">url</span>&gt;</span>http://download.bianchengbang.org/maven2/lib2<span class="tag">&lt;/<span class="name">url</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">repository</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">repositories</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure><h2 id="Maven-依赖搜索顺序"><a href="#Maven-依赖搜索顺序" class="headerlink" title="Maven 依赖搜索顺序"></a>Maven 依赖搜索顺序</h2><p>当通过 Maven 构建项目时，Maven 按照如下顺序查找依赖的构件。</p><ol><li>从本地仓库查找构件，如果没有找到，跳到第 2 步，否则继续执行其他处理。</li><li>从中央仓库查找构件，如果没有找到，并且已经设置其他远程仓库，然后移动到第 4 步；如果找到，那么将构件下载到本地仓库中使用。</li><li>如果没有设置其他远程仓库，Maven 则会停止处理并抛出错误。</li><li>在远程仓库查找构件，如果找到，则会下载到本地仓库并使用，否则 Maven 停止处理并抛出错误。</li></ol><h1 id="Maven生命周期"><a href="#Maven生命周期" class="headerlink" title="Maven生命周期"></a>Maven生命周期</h1><p>这个生命周期将项目的清理、初始化、编译、测试、打包、集成测试、验证、部署和站点生成等几乎所有构建过程进行了抽象和统一。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;Maven 是一款基于 Java 平台的项目管理和整合工具，它将项目的开发和管理过程抽象成一个项目对象模型（POM）。开发人员只需要做一些简单的配置，Maven 就可以自动完成项目的编译、测试、打包、发布以及部署等工作。&lt;/p&gt;
&lt;p&gt;Maven 是使用 Java 语言编写
      
    
    </summary>
    
    
    
  </entry>
  
  <entry>
    <title>java/基础/Java 注解</title>
    <link href="http://yoursite.com/2021/06/29/java/%E5%9F%BA%E7%A1%80/Java%20%E6%B3%A8%E8%A7%A3/"/>
    <id>http://yoursite.com/2021/06/29/java/%E5%9F%BA%E7%A1%80/Java%20%E6%B3%A8%E8%A7%A3/</id>
    <published>2021-06-29T01:42:55.095Z</published>
    <updated>2021-06-29T03:23:39.872Z</updated>
    
    <content type="html"><![CDATA[<p>从 Java 5 版本之后可以在源代码中嵌入一些补充信息，这种补充信息称为注解（·Annotation·）。注解都是<code>@</code>符号开头的。同<code>Class</code>和<code>Interface</code>一样，注解也属于一种类型。</p><p>注解并不能改变程序的运行结果，也不会影响程序运行的性能。有些注解可以在编译时给用户提示或警告，有的注解可以在运行时读写字节码文件信息。</p><p>注解可以用元数据这个词来描述，即一种描述数据的数据。所以可以说注解就是源代码的元数据。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">"C语言中文网Java教程"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面的代码重写了<code>Object</code>类的<code>toString()</code>方法并使用了<code>@Override</code>注解。如果不使用<code>@Override</code>注解标记代码，程序也能够正常执行。那么这么写有什么好处吗？事实上，使用<code>@Override</code>注解就相当于告诉编译器这个方法是一个重写方法，如果父类中不存在该方法，编译器便会报错，提示该方法没有重写父类中的方法。这样可以防止不小心拼写错误造成麻烦。</p><p>例如，在没有使用<code>@Override</code>注解的情况下，将<code>toString()</code>写成了<code>toStrring()</code>，这时程序依然能编译运行，但运行结果会和所期望的结果大不相同。</p><p>注解常见的作用有以下几种：</p><ul><li>生成帮助文档。这是最常见的，也是 Java 最早提供的注解。常用的有 @see、@param 和 @return 等；</li><li>跟踪代码依赖性，实现替代配置文件功能。作用就是减少配置。现在的框架基本都使用了这种配置来减少配置文件的数量；</li><li>在编译时进行格式检查。如把<code>@Override</code>注解放在方法前，如果这个方法并不是重写了父类方法，则编译时就能检查出。</li></ul><p>无论是哪一种注解，本质上都是一种接口类型。到 Java 8 为止 Java SE 提供了 11 个内置注解。其中有 5 个是基本注解，它们来自于·java.lang·包。有 6 个是元注解，它们来自于·java.lang.annotation·包，自定义注解会用到元注解。</p><p>提示：元注解就是负责注解其他的注解。</p><p>基本注解包括：<code>@Override、@Deprecated、@SuppressWarnings、@SafeVarargs</code>和<code>@FunctionalInterface</code>。</p><h1 id="Override注解"><a href="#Override注解" class="headerlink" title="@Override注解"></a>@Override注解</h1><p><code>@Override</code>注解是用来指定方法重写的，只能修饰方法并且只能用于方法重写，不能修饰其它的元素。它可以强制一个子类必须重写父类方法或者实现接口的方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">  <span class="keyword">private</span> String name = <span class="string">""</span>;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">  ...</span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> String <span class="title">t0String</span><span class="params">()</span> </span>&#123; <span class="comment">//toString()</span></span><br><span class="line">    <span class="keyword">return</span> <span class="string">"Person [name="</span> + name + <span class="string">", age="</span> + age + <span class="string">"]"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上述代码第 6 行是重写<code>Object</code>类的<code>toString()</code>方法，该方法使用<code>@Override</code>注解。如果<code>toString()</code>不小心写成了<code>t0String()</code>，那么程序会发生编译错误。</p><p>所以<code>@Override</code>的作用是告诉编译器检查这个方法，保证父类要包含一个被该方法重写的方法，否则就会编译出错。这样可以帮助程序员避免一些低级错误。</p><p>当然如果代码中的方法前面不加<code>@Override</code>注解，即便是方法编辑错误了，编译器也不会有提示。这时<code>Object</code>父类的<code>toString()</code>方法并没有被重写，将会引起程序出现 Bug。</p><h1 id="Deprecated注解"><a href="#Deprecated注解" class="headerlink" title="@Deprecated注解"></a>@Deprecated注解</h1><p><code>@Deprecated</code>可以用来注解类、接口、成员方法和成员变量等，用于表示某个元素（类、方法等）已过时。当其他程序使用已过时的元素时，编译器将会给出警告。</p><p>Java 9 为 @Deprecated 注解增加了以下两个属性：</p><ul><li><code>forRemoval</code>：该<code>boolean</code>类型的属性指定该 API 在将来是否会被删除。</li><li><code>since</code>：该<code>String</code>类型的属性指定该 API 从哪个版本被标记为过时。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">  <span class="comment">// since属性指定从哪个版本开始被标记成过时，forRemoval指定该API将来会被删除</span></span><br><span class="line">  <span class="meta">@Deprecated</span>(since = <span class="string">"9"</span>, forRemoval = <span class="keyword">true</span>)</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"这里是C语言中文网Java教程！"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DeprecatedTest</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 下面使用info()方法时将会被编译器警告</span></span><br><span class="line">    <span class="keyword">new</span> Test().print();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面程序的第 12 行代码使用了<code>Test</code>的<code>print()</code>方法，而<code>Test</code>类中定义<code>print()</code>方法时使用了<code>@Deprecated</code>修饰，表明该方法已过时，所以将会引起编译器警告。</p><p><code>@Deprecated</code>的作用与文档注释中的<code>@deprecated</code>标记的作用基本相同，但它们的用法不同，前者无须放在文档注释语法（/** … */部分）中，而是直接用于修饰程序中的程序单元，如方法、类和接口等。</p><h1 id="SuppressWarnings：抑制编译器警告"><a href="#SuppressWarnings：抑制编译器警告" class="headerlink" title="@SuppressWarnings：抑制编译器警告"></a>@SuppressWarnings：抑制编译器警告</h1><p><code>@SuppressWarnings</code>注解指示被该注解修饰的程序元素（以及该程序元素中的所有子元素）取消显示指定的编译器警告，且会一直作用于该程序元素的所有子元素。例如，使用<code>@SuppressWarnings</code>修饰某个类取消显示某个编译器警告，同时又修饰该类里的某个方法取消显示另一个编译器警告，那么该方法将会同时取消显示这两个编译器警告。</p><p><code>@SuppressWarnings</code>注解主要用在取消一些编译器产生的警告对代码左侧行列的遮挡，有时候这样会挡住我们断点调试时打的断点。</p><p>如果你确认程序中的警告没有问题，可以不用理会。通常情况下，如果程序中使用没有泛型限制的集合将会引起编译器警告，为了避免这种编译器警告，可以使用<code>@SuppressWarnings</code>注解消除这些警告。</p><p>注解的使用有以下三种：</p><ul><li>抑制单类型的警告：<code>@SuppressWarnings(&quot;unchecked&quot;)</code></li><li>抑制多类型的警告：<code>@SuppressWarnings(&quot;unchecked&quot;,&quot;rawtypes&quot;)</code></li><li>抑制所有类型的警告：<code>@SuppressWarnings(&quot;unchecked&quot;)</code></li></ul><p>抑制警告的关键字如下表所示。</p><p>| 关键字 | 用途 |<br>| all | 抑制所有警告 |<br>| boxing | 抑制装箱、拆箱操作时候的警告 |<br>| cast | 抑制映射相关的警告 |<br>| dep-ann | 抑制启用注释的警告 |<br>| deprecation | 抑制过期方法警告 |<br>| fallthrough | 抑制在 switch 中缺失 breaks 的警告 |<br>| finally | 抑制 finally 模块没有返回的警告 |<br>| hiding | 抑制相对于隐藏变量的局部变量的警告 |<br>| incomplete-switch | 忽略不完整的 switch 语句 |<br>| nls | 忽略非 nls 格式的字符 |<br>| null | 忽略对 null 的操作 |<br>| rawtypes | 使用 generics 时忽略没有指定相应的类型 |<br>| restriction | 抑制禁止使用劝阻或禁止引用的警告 |<br>| serial | 忽略在 serializable 类中没有声明 serialVersionUID 变量 |<br>| static-access | 抑制不正确的静态访问方式警告 |<br>| synthetic-access | 抑制子类没有按最优方法访问内部类的警告 |<br>| unchecked | 抑制没有进行类型检查操作的警告 |<br>| unqualified-field-access | 抑制没有权限访问的域的警告 |<br>| unused | 抑制没被使用过的代码的警告 |</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloWorld</span> </span>&#123;</span><br><span class="line">  <span class="meta">@SuppressWarnings</span>(&#123; <span class="string">"deprecation"</span> &#125;)</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    Person p = <span class="keyword">new</span> Person();</span><br><span class="line">    p.setNameAndAge(<span class="string">"C语言中文网"</span>, <span class="number">20</span>);</span><br><span class="line">    p.name = <span class="string">"Java教程"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="SafeVarargs注解"><a href="#SafeVarargs注解" class="headerlink" title="@SafeVarargs注解"></a>@SafeVarargs注解</h1><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloWorld</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 传递可变参数，参数是泛型集合</span></span><br><span class="line">    display(<span class="number">10</span>, <span class="number">20</span>, <span class="number">30</span>);</span><br><span class="line">    <span class="comment">// 传递可变参数，参数是非泛型集合</span></span><br><span class="line">    display(<span class="string">"10"</span>, <span class="number">20</span>, <span class="number">30</span>); <span class="comment">// 会有编译警告</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function"><span class="keyword">void</span> <span class="title">display</span><span class="params">(T... array)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (T arg : array) &#123;</span><br><span class="line">      System.out.println(arg.getClass().getName() + <span class="string">":"</span> + arg);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>代码第 10 行声明了一种可变参数方法<code>display</code>，<code>display</code>方法参数个数可以变化，它可以接受不确定数量的相同类型的参数。可以通过在参数类型名后面加入…的方式来表示这是可变参数。可变参数方法中的参数类型相同，为此声明参数是需要指定泛型。</p><p>但是调用可变参数方法时，应该提供相同类型的参数，代码第 4 行调用时没有警告，而代码第 6 行调用时则会发生警告，这个警告是<code>unchecked</code>（未检查不安全代码），就是因为将非泛型变量赋值给泛型变量所发生的。</p><p>可用<code>@SafeVarargs</code>注解抑制编译器警告。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloWorld</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 传递可变参数，参数是泛型集合</span></span><br><span class="line">    display(<span class="number">10</span>, <span class="number">20</span>, <span class="number">30</span>);</span><br><span class="line">    <span class="comment">// 传递可变参数，参数是非泛型集合</span></span><br><span class="line">    display(<span class="string">"10"</span>, <span class="number">20</span>, <span class="number">30</span>); <span class="comment">// 没有@SafeVarargs会有编译警告</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="meta">@SafeVarargs</span></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function"><span class="keyword">void</span> <span class="title">display</span><span class="params">(T... array)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (T arg : array) &#123;</span><br><span class="line">      System.out.println(arg.getClass().getName() + <span class="string">"："</span> + arg);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上述代码在可变参数<code>display</code>前添加了<code>@SafeVarargs</code>注解，当然也可以使用<code>@SuppressWarnings(&quot;unchecked&quot;)</code>注解，但是两者相比较来说<code>@SafeVarargs</code>注解更适合。</p><p>注意：<code>@SafeVarargs</code>注解不适用于非<code>static</code>或非<code>final</code>声明的方法，对于未声明为<code>static</code>或<code>final</code>的方法，如果要抑制<code>unchecked</code>警告，可以使用<code>@SuppressWarnings</code>注解。</p><h1 id="FunctionalInterface注解"><a href="#FunctionalInterface注解" class="headerlink" title="@FunctionalInterface注解"></a>@FunctionalInterface注解</h1><p>如果接口中只有一个抽象方法（可以包含多个默认方法或多个<code>static</code>方法），那么该接口就是函数式接口。<code>@FunctionalInterface</code>就是用来指定某个接口必须是函数式接口，所以<code>@FunInterface</code>只能修饰接口，不能修饰其它程序元素。</p><p>函数式接口就是为 Java 8 的 Lambda 表达式准备的，Java 8 允许使用 Lambda 表达式创建函数式接口的实例，因此 Java 8 专门增加了<code>@FunctionalInterface</code>。</p><p>如下程序使用<code>@FunctionalInterface</code>修饰了函数式接口。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@FunctionalInterface</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">FunInterface</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"zhangsaan"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">default</span> <span class="keyword">void</span> <span class="title">show</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"lisi"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>; <span class="comment">// 只定义一个抽象方法</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>编译上面程序，可能丝毫看不出程序中的<code>@FunctionalInterface</code>有何作用，因为<code>@FunctionalInterface</code>注解的作用只是告诉编译器检查这个接口，保证该接口只能包含一个抽象方法，否则就会编译出错。</p><p><code>@FunctionalInterface</code>注解主要是帮助程序员避免一些低级错误，例如，在上面的<code>FunInterface</code>接口中再增加一个抽象方法<code>abc()</code>，编译程序时将出现如下错误提示：<br><code>“@FunctionInterface”批注无效；FunInterface不是functional接口</code>。</p><h1 id="元注解作用及使用"><a href="#元注解作用及使用" class="headerlink" title="元注解作用及使用"></a>元注解作用及使用</h1><p>元注解是负责对其它注解进行说明的注解，自定义注解时可以使用元注解。Java 5 定义了 4 个注解，分别是<code>@Documented、@Target、@Retention</code>和<code>@Inherited</code>。Java 8 又增加了<code>@Repeatable</code>和<code>@Native</code>两个注解。这些注解都可以在<code>java.lang.annotation</code>包中找到。</p><h2 id="Documented"><a href="#Documented" class="headerlink" title="@Documented"></a>@Documented</h2><p><code>@Documented</code>是一个标记注解，没有成员变量。用<code>@Documented</code>注解修饰的注解类会被 JavaDoc 工具提取成文档。默认情况下，JavaDoc 是不包括注解的，但如果声明注解时指定了<code>@Documented</code>，就会被 JavaDoc 之类的工具处理，所以注解类型信息就会被包括在生成的帮助文档中。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Target</span>(&#123; ElementType.TYPE, ElementType.METHOD &#125;)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> MyDocumented &#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> String <span class="title">value</span><span class="params">()</span> <span class="keyword">default</span> "这是@Documented注解"</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 测试类：</span></span><br><span class="line"><span class="meta">@MyDocumented</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DocumentedTest</span> </span>&#123;</span><br><span class="line">  <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 测试document</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">  <span class="meta">@MyDocumented</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> String <span class="title">Test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"zhangsan"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>打开 Java 文件所在的目录，分别输入如下两条命令行：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">javac MyDocumented.java DocumentedTest.java</span><br><span class="line">javadoc -d doc MyDocumented.java DocumentedTest.java</span><br></pre></td></tr></table></figure><p>运行成功后，打开生成的帮助文档，可以看到在类和方法上都保留了<code>MyDocument</code>的注解信息。</p><h2 id="Target"><a href="#Target" class="headerlink" title="@Target"></a>@Target</h2><p><code>@Target</code>注解用来指定一个注解的使用范围，即被<code>@Target</code>修饰的注解可以用在什么地方。<code>@Target</code>注解有一个成员变量（<code>value</code>）用来设置适用目标，<code>value</code>是<code>java.lang.annotation.ElementType</code>枚举类型的数组，下表为 ElementType 常用的枚举常量。</p><table><thead><tr><th align="center">名称</th><th align="center">说明</th></tr></thead><tbody><tr><td align="center">CONSTRUCTOR</td><td align="center">用于构造方法</td></tr><tr><td align="center">FIELD</td><td align="center">用于成员变量（包括枚举常量）</td></tr><tr><td align="center">LOCAL_VARIABLE</td><td align="center">用于局部变量</td></tr><tr><td align="center">METHOD</td><td align="center">用于方法</td></tr><tr><td align="center">PACKAGE</td><td align="center">用于包</td></tr><tr><td align="center">PARAMETER</td><td align="center">用于类型参数</td></tr><tr><td align="center">TYPE</td><td align="center">用于类、接口（包括注解类型）或 enum 声明</td></tr></tbody></table><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Target</span>(&#123; ElementType.METHOD &#125;)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> MyTarget &#123;&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">  <span class="meta">@MyTarget</span></span><br><span class="line">  String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如上代码第 6 行会编译错误，错误信息为：<code>The annotation @MyTarget is disallowed for this location</code>。提示此位置不允许使用注解<code>@MyDocumented</code>，<code>@MyTarget</code>不能修饰成员变量，只能修饰方法。</p><h2 id="Retention"><a href="#Retention" class="headerlink" title="@Retention"></a>@Retention</h2><p><code>@Retention</code>用于描述注解的生命周期，也就是该注解被保留的时间长短。<code>@Retention</code>注解中的成员变量（<code>value</code>）用来设置保留策略，<code>value</code>是<code>java.lang.annotation.RetentionPolicy</code>枚举类型，<code>RetentionPolicy</code>有 3 个枚举常量。</p><ul><li><code>SOURCE</code>：在源文件中有效（即源文件保留）</li><li><code>CLASS</code>：在<code>class</code>文件中有效（即 class 保留）</li><li><code>RUNTIME</code>：在运行时有效（即运行时保留）</li></ul><p>生命周期大小排序为<code>SOURCE &lt; CLASS &lt; RUNTIME</code>，前者能使用的地方后者一定也能使用。如果需要在运行时去动态获取注解信息，那只能用<code>RUNTIME</code>注解；如果要在编译时进行一些预处理操作，比如生成一些辅助代码（如<code>ButterKnife</code>），就用<code>CLASS</code>注解；如果只是做一些检查性的操作，比如<code>@Override</code>和<code>@SuppressWarnings</code>，则可选用<code>SOURCE</code>注解。</p><h2 id="Inherited"><a href="#Inherited" class="headerlink" title="@Inherited"></a>@Inherited</h2><p><code>@Inherited</code>是一个标记注解，用来指定该注解可以被继承。使用<code>@Inherited</code>注解的<code>Class</code>类，表示这个注解可以被用于该<code>Class</code>类的子类。就是说如果某个类使用了被<code>@Inherited</code>修饰的注解，则其子类将自动具有该注解。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 创建一个自定义注解</span></span><br><span class="line"><span class="meta">@Target</span>(&#123; ElementType.TYPE &#125;)</span><br><span class="line"><span class="meta">@Inherited</span></span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> MyInherited &#123;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 测试类代码如下：</span></span><br><span class="line"><span class="meta">@MyInherited</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestA</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    System.out.println(TestA<span class="class">.<span class="keyword">class</span>.<span class="title">getAnnotation</span>(<span class="title">MyInherited</span>.<span class="title">class</span>))</span>;</span><br><span class="line">    System.out.println(TestB<span class="class">.<span class="keyword">class</span>.<span class="title">getAnnotation</span>(<span class="title">MyInherited</span>.<span class="title">class</span>))</span>;</span><br><span class="line">    System.out.println(TestC<span class="class">.<span class="keyword">class</span>.<span class="title">getAnnotation</span>(<span class="title">MyInherited</span>.<span class="title">class</span>))</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TestB</span> <span class="keyword">extends</span> <span class="title">TestA</span> </span>&#123;&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TestC</span> <span class="keyword">extends</span> <span class="title">TestB</span> </span>&#123;&#125;</span><br><span class="line"><span class="comment">// 运行结果为：</span></span><br><span class="line"><span class="comment">//@MyInherited()</span></span><br><span class="line"><span class="comment">//@MyInherited()</span></span><br><span class="line"><span class="comment">//@MyInherited()</span></span><br></pre></td></tr></table></figure><h2 id="Repeatable"><a href="#Repeatable" class="headerlink" title="@Repeatable"></a>@Repeatable</h2><p><code>@Repeatable</code>注解是 Java 8 新增加的，它允许在相同的程序元素中重复注解，在需要对同一种注解多次使用时，往往需要借助 @Repeatable 注解。Java 8 版本以前，同一个程序元素前最多只能有一个相同类型的注解，如果需要在同一个元素前使用多个相同类型的注解，则必须使用注解“容器”。</p><p>Java 8 之前的做法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Roles &#123;</span><br><span class="line">  Role[] roles();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Roles &#123;</span><br><span class="line">  Role[] value();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RoleTest</span> </span>&#123;</span><br><span class="line">  <span class="meta">@Roles</span>(roles = &#123;<span class="meta">@Role</span>(roleName = <span class="string">"role1"</span>), <span class="meta">@Role</span>(roleName = <span class="string">"role2"</span>)&#125;)</span><br><span class="line">  <span class="function"><span class="keyword">public</span> String <span class="title">doString</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"这是C语言中国网Java教程"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Java 8 之后增加了重复注解，使用方式如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Roles &#123;</span><br><span class="line">  Role[] value();</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@Repeatable</span>(Roles<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class"><span class="title">public</span> @<span class="title">interface</span> <span class="title">Role</span> </span>&#123;</span><br><span class="line">  <span class="function">String <span class="title">roleName</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RoleTest</span> </span>&#123;</span><br><span class="line">  <span class="meta">@Role</span>(roleName = <span class="string">"role1"</span>)</span><br><span class="line">  <span class="meta">@Role</span>(roleName = <span class="string">"role2"</span>)</span><br><span class="line">  <span class="function"><span class="keyword">public</span> String <span class="title">doString</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"这是C语言中文网Java教程"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>不同的地方是，创建重复注解<code>Role</code>时加上了<code>@Repeatable</code>注解，指向存储注解<code>Roles</code>，这样在使用时就可以直接重复使用<code>Role</code>注解。从上面例子看出，使用<code>@Repeatable</code>注解更符合常规思维，可读性强一点。</p><p>两种方法获得的效果相同。重复注解只是一种简化写法，这种简化写法是一种假象，多个重复注解其实会被作为“容器”注解的<code>value</code>成员的数组元素处理。</p><h2 id="Native"><a href="#Native" class="headerlink" title="@Native"></a>@Native</h2><p>使用<code>@Native</code>注解修饰成员变量，则表示这个变量可以被本地代码引用，常常被代码生成工具使用。<code>@Native</code>注解不常使用。</p><h1 id="自定义注解"><a href="#自定义注解" class="headerlink" title="自定义注解"></a>自定义注解</h1><p>声明自定义注解使用<code>@interface</code>关键字实现。定义注解与定义接口非常像。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义一个简单的注解类型</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Test &#123;&#125;</span><br></pre></td></tr></table></figure><p>上述代码声明了一个<code>Test</code>注解。默认情况下，注解可以在程序的任何地方使用，通常用于修饰类、接口、方法和变量等。</p><p>定义注解和定义类相似，注解前面的访问修饰符和类一样有两种，分别是公有访问权限（<code>public</code>）和默认访问权限（默认不写）。一个源程序文件中可以声明多个注解，但只能有一个是公有访问权限的注解。且源程序文件命名和公有访问权限的注解名一致。</p><p>不包含任何成员变量的注解称为标记注解，例如上面声明的<code>Test</code>注解以及基本注解中的<code>@Override</code>注解都属于标记注解。根据需要，注解中可以定义成员变量，成员变量以无形参的方法形式来声明，其方法名和返回值定义了该成员变量的名字和类型。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> MyTag &#123;</span><br><span class="line">  <span class="comment">// 定义带两个成员变量的注解</span></span><br><span class="line">  <span class="comment">// 注解中的成员变量以方法的形式来定义</span></span><br><span class="line">  <span class="function">String <span class="title">name</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">int</span> <span class="title">age</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>以上代码中声明了一个<code>MyTag</code>注解，定义了两个成员变量，分别是<code>name</code>和<code>age</code>。成员变量也可以有访问权限修饰符，但是只能有公有权限和默认权限。</p><p>如果在注解里定义了成员变量，那么使用该注解时就应该为它的成员变量指定值。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 使用带成员变量的注解时，需要为成员变量赋值</span></span><br><span class="line">  <span class="meta">@MyTag</span>(name=<span class="string">"xx"</span>, age=<span class="number">6</span>)</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">info</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">  &#125;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>注解中的成员变量也可以有默认值，可使用<code>default</code>关键字。如下代码定义了<code>@MyTag</code>注解，该注解里包含了<code>name</code>和<code>age</code>两个成员变量。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> MyTag &#123;</span><br><span class="line">  <span class="comment">// 定义了两个成员变量的注解</span></span><br><span class="line">  <span class="comment">// 使用default为两个成员变量指定初始值</span></span><br><span class="line">  <span class="function">String <span class="title">name</span><span class="params">()</span> <span class="keyword">default</span> "zhangsan"</span>;</span><br><span class="line">  <span class="function"><span class="keyword">int</span> <span class="title">age</span><span class="params">()</span> <span class="keyword">default</span> 7</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果为注解的成员变量指定了默认值，那么使用该注解时就可以不为这些成员变量赋值，而是直接使用默认值。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 使用带成员变量的注解</span></span><br><span class="line">  <span class="comment">// MyTag注释的成员变量有默认值，所以可以不为它的成员变量赋值</span></span><br><span class="line">  <span class="meta">@MyTag</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">info</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">  &#125;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当然也可以在使用<code>MyTag</code>注解时为成员变量指定值，如果为<code>MyTag</code>的成员变量指定了值，则默认值不会起作用。</p><p>根据注解是否包含成员变量，可以分为如下两类。</p><ul><li>标记注解：没有定义成员变量的注解类型被称为标记注解。这种注解仅利用自身的存在与否来提供信息，<code>@Override、@Test</code>等都是标记注解。</li><li>元数据注解：包含成员变量的注解，因为它们可以接受更多的元数据，所以也被称为元数据注解。</li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;从 Java 5 版本之后可以在源代码中嵌入一些补充信息，这种补充信息称为注解（·Annotation·）。注解都是&lt;code&gt;@&lt;/code&gt;符号开头的。同&lt;code&gt;Class&lt;/code&gt;和&lt;code&gt;Interface&lt;/code&gt;一样，注解也属于一种类型。&lt;/p&gt;
      
    
    </summary>
    
    
    
  </entry>
  
</feed>
